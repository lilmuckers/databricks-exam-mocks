{
  "meta": {
    "id": "aws-mle-exam-06",
    "certification": "aws-ml-engineer-associate",
    "certificationName": "AWS Certified Machine Learning Engineer – Associate",
    "title": "AWS-MLE-6: Advanced Practice Scenarios",
    "version": "1.0",
    "timeLimit": 130,
    "passingScore": 72,
    "totalQuestions": 65,
    "domains": [
      {
        "id": "preparing-data",
        "name": "Data Ingestion and Preparation",
        "weight": 17
      },
      {
        "id": "model-lifecycle",
        "name": "ML Model Development",
        "weight": 17
      },
      {
        "id": "ml-workflows",
        "name": "MLOps and Automation",
        "weight": 17
      },
      {
        "id": "model-deployment",
        "name": "Model Deployment",
        "weight": 17
      },
      {
        "id": "solution-monitoring",
        "name": "Monitoring and Observability",
        "weight": 17
      },
      {
        "id": "feature-store",
        "name": "Feature Engineering",
        "weight": 15
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "Revenue Pipeline Scenario: **SageMaker Processing** must handle `checkpoint retries` while preserving `metric comparison`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for revenue pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "B",
          "text": "Use SageMaker Processing for revenue pipeline scenario to handle `checkpoint retries`."
        },
        {
          "id": "C",
          "text": "Use visual tile for revenue pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "D",
          "text": "Use admin grant for revenue pipeline scenario instead of configuring SageMaker Processing."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because revenue pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\n**B** is correct because revenue pipeline scenario needs SageMaker Processing specifically for `checkpoint retries`. The documentation describes that product behavior, and `metric comparison` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because revenue pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\n**D** is incorrect because revenue pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\nSee [SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html)"
    },
    {
      "id": "q02",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Fraud Pipeline Scenario: **AWS Glue job bookmarks** must handle `duplicate key ranking` while preserving `labeled quality monitoring`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use AWS Glue job bookmarks for fraud pipeline scenario to handle `duplicate key ranking`."
        },
        {
          "id": "B",
          "text": "Use visual tile for fraud pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "C",
          "text": "Use admin grant for fraud pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "D",
          "text": "Use local notebook for fraud pipeline scenario instead of configuring AWS Glue job bookmarks."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because fraud pipeline scenario needs AWS Glue job bookmarks specifically for `duplicate key ranking`. The documentation describes that product behavior, and `labeled quality monitoring` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because fraud pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\n**C** is incorrect because fraud pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\n**D** is incorrect because fraud pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\nSee [AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html) for the vendor documentation that supports the correct choice.",
      "reference": "[AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html)"
    },
    {
      "id": "q03",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "Telemetry Pipeline Scenario: **SageMaker Data Wrangler** must handle `nested array expansion` while preserving `dual store features`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for telemetry pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "B",
          "text": "Use local notebook for telemetry pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "C",
          "text": "Use driver file for telemetry pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "D",
          "text": "Use SageMaker Data Wrangler for telemetry pipeline scenario to handle `nested array expansion`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because telemetry pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**B** is incorrect because telemetry pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**C** is incorrect because telemetry pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**D** is correct because telemetry pipeline scenario needs SageMaker Data Wrangler specifically for `nested array expansion`. The documentation describes that product behavior, and `dual store features` keeps the workflow inside the managed platform capability.\n\nSee [SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html)"
    },
    {
      "id": "q04",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Partner Pipeline Scenario: **SageMaker Experiments** must handle `cache invalidation` while preserving `Pipeline condition step`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for partner pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "B",
          "text": "Use driver file for partner pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "C",
          "text": "Use SageMaker Experiments for partner pipeline scenario to handle `cache invalidation`."
        },
        {
          "id": "D",
          "text": "Use report table for partner pipeline scenario instead of configuring SageMaker Experiments."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because partner pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\n**B** is incorrect because partner pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\n**C** is correct because partner pipeline scenario needs SageMaker Experiments specifically for `cache invalidation`. The documentation describes that product behavior, and `Pipeline condition step` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because partner pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\nSee [SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html)"
    },
    {
      "id": "q05",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "Amazon SageMaker built-in algorithms include options such as XGBoost, Linear Learner, K-Means, DeepAR, and BlazingText for common training tasks.\n\nA practitioner must choose the documented **Built-in algorithms** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for clinical pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "B",
          "text": "Use Built-in algorithms because the documentation supports this exact behavior: Amazon SageMaker built-in algorithms include options such as XGBoost, Linear Learner, K-Means, DeepAR, and BlazingText for common training tasks."
        },
        {
          "id": "C",
          "text": "Use report table for clinical pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "D",
          "text": "Use shell cron for clinical pipeline scenario instead of configuring Built-in algorithms."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Built-in algorithms behavior described here: Amazon SageMaker built-in algorithms include options such as XGBoost, Linear Learner, K-Means, DeepAR, and BlazingText for common training tasks.\n\n**B** is correct because Amazon SageMaker built-in algorithms include options such as XGBoost, Linear Learner, K-Means, DeepAR, and BlazingText for common training tasks. This directly matches the scenario and is supported by the referenced Built-in algorithms documentation.\n\n**C** is incorrect because it does not provide the documented Built-in algorithms behavior described here: Amazon SageMaker built-in algorithms include options such as XGBoost, Linear Learner, K-Means, DeepAR, and BlazingText for common training tasks.\n\n**D** is incorrect because it does not provide the documented Built-in algorithms behavior described here: Amazon SageMaker built-in algorithms include options such as XGBoost, Linear Learner, K-Means, DeepAR, and BlazingText for common training tasks.\n\nSee [Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html)"
    },
    {
      "id": "q06",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Finance Reconciliation Over Late Arriving Invoices With Approval Cutoffs And Immutable Audit Columns: **Script mode** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Script mode for loan pipeline scenario to handle `metric comparison`."
        },
        {
          "id": "B",
          "text": "Use report table for loan pipeline scenario instead of configuring Script mode."
        },
        {
          "id": "C",
          "text": "Use shell cron for loan pipeline scenario instead of configuring Script mode."
        },
        {
          "id": "D",
          "text": "Use email approval for loan pipeline scenario instead of configuring Script mode."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because loan pipeline scenario needs Script mode specifically for `metric comparison`. The documentation describes that product behavior, and `recursive hierarchy traversal` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because loan pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\n**C** is incorrect because loan pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\n**D** is incorrect because loan pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\nSee [Script mode](https://docs.aws.amazon.com/sagemaker/latest/dg/script-mode.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Script mode](https://docs.aws.amazon.com/sagemaker/latest/dg/script-mode.html)"
    },
    {
      "id": "q07",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Slotting Pipeline Scenario: **SageMaker Pipelines** must handle `point in time joins` while preserving `materialized view maintenance`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for slotting pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "B",
          "text": "Use email approval for slotting pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for slotting pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "D",
          "text": "Use SageMaker Pipelines for slotting pipeline scenario to handle `point in time joins`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because slotting pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**B** is incorrect because slotting pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**C** is incorrect because slotting pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**D** is correct because slotting pipeline scenario needs SageMaker Pipelines specifically for `point in time joins`. The documentation describes that product behavior, and `materialized view maintenance` keeps the workflow inside the managed platform capability.\n\nSee [SageMaker Pipelines](https://docs.aws.amazon.com/sagemaker/latest/dg/pipelines.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Pipelines](https://docs.aws.amazon.com/sagemaker/latest/dg/pipelines.html)"
    },
    {
      "id": "q08",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "hard",
      "stem": "Consent Pipeline Scenario: **Pipeline steps** must handle `offline object scoring` while preserving `Search service refresh`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for consent pipeline scenario instead of configuring Pipeline steps."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for consent pipeline scenario instead of configuring Pipeline steps."
        },
        {
          "id": "C",
          "text": "Use Pipeline steps for consent pipeline scenario to handle `offline object scoring`."
        },
        {
          "id": "D",
          "text": "Use visual tile for consent pipeline scenario instead of configuring Pipeline steps."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because consent pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\n**B** is incorrect because consent pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\n**C** is correct because consent pipeline scenario needs Pipeline steps specifically for `offline object scoring`. The documentation describes that product behavior, and `Search service refresh` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because consent pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\nSee [Pipeline steps](https://docs.aws.amazon.com/sagemaker/latest/dg/build-and-manage-steps.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Pipeline steps](https://docs.aws.amazon.com/sagemaker/latest/dg/build-and-manage-steps.html)"
    },
    {
      "id": "q09",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "Campaign Pipeline Scenario: **Model Registry** must handle `scheduled drift statistics` while preserving `registry approval state`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for campaign pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "B",
          "text": "Use Model Registry for campaign pipeline scenario to handle `scheduled drift statistics`."
        },
        {
          "id": "C",
          "text": "Use visual tile for campaign pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "D",
          "text": "Use admin grant for campaign pipeline scenario instead of configuring Model Registry."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because campaign pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\n**B** is correct because campaign pipeline scenario needs Model Registry specifically for `scheduled drift statistics`. The documentation describes that product behavior, and `registry approval state` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because campaign pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\n**D** is incorrect because campaign pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\nSee [Model Registry](https://docs.aws.amazon.com/sagemaker/latest/dg/model-registry.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Registry](https://docs.aws.amazon.com/sagemaker/latest/dg/model-registry.html)"
    },
    {
      "id": "q10",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Intent Pipeline Scenario: **Real-time endpoints** must handle `metric gated registration` while preserving `bursty join sizing`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Real-time endpoints for intent pipeline scenario to handle `metric gated registration`."
        },
        {
          "id": "B",
          "text": "Use visual tile for intent pipeline scenario instead of configuring Real-time endpoints."
        },
        {
          "id": "C",
          "text": "Use admin grant for intent pipeline scenario instead of configuring Real-time endpoints."
        },
        {
          "id": "D",
          "text": "Use local notebook for intent pipeline scenario instead of configuring Real-time endpoints."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because intent pipeline scenario needs Real-time endpoints specifically for `metric gated registration`. The documentation describes that product behavior, and `bursty join sizing` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because intent pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\n**C** is incorrect because intent pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\n**D** is incorrect because intent pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\nSee [Real-time endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/realtime-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Real-time endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/realtime-endpoints.html)"
    },
    {
      "id": "q11",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Insurance Reserve Triangle Review With Actuarial Cohorts And Locked Reporting Periods: **Serverless inference** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for claims pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "B",
          "text": "Use local notebook for claims pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "C",
          "text": "Use driver file for claims pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "D",
          "text": "Use Serverless inference for claims pipeline scenario to handle `idle serverless serving`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because claims pipeline scenario would still lack Serverless inference; the suggested workaround cannot satisfy `idle serverless serving` or preserve `Hyperopt parallel search` in the supported workflow.\n\n**B** is incorrect because claims pipeline scenario would still lack Serverless inference; the suggested workaround cannot satisfy `idle serverless serving` or preserve `Hyperopt parallel search` in the supported workflow.\n\n**C** is incorrect because claims pipeline scenario would still lack Serverless inference; the suggested workaround cannot satisfy `idle serverless serving` or preserve `Hyperopt parallel search` in the supported workflow.\n\n**D** is correct because claims pipeline scenario needs Serverless inference specifically for `idle serverless serving`. The documentation describes that product behavior, and `Hyperopt parallel search` keeps the workflow inside the managed platform capability.\n\nSee [Serverless inference](https://docs.aws.amazon.com/sagemaker/latest/dg/serverless-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Serverless inference](https://docs.aws.amazon.com/sagemaker/latest/dg/serverless-endpoints.html)"
    },
    {
      "id": "q12",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Inventory Pipeline Scenario: **Asynchronous inference** must handle `registry approval state` while preserving `OBJECT CONSTRUCT payload`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for inventory pipeline scenario instead of configuring Asynchronous inference."
        },
        {
          "id": "B",
          "text": "Use driver file for inventory pipeline scenario instead of configuring Asynchronous inference."
        },
        {
          "id": "C",
          "text": "Use Asynchronous inference for inventory pipeline scenario to handle `registry approval state`."
        },
        {
          "id": "D",
          "text": "Use report table for inventory pipeline scenario instead of configuring Asynchronous inference."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because inventory pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\n**B** is incorrect because inventory pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\n**C** is correct because inventory pipeline scenario needs Asynchronous inference specifically for `registry approval state`. The documentation describes that product behavior, and `OBJECT CONSTRUCT payload` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because inventory pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\nSee [Asynchronous inference](https://docs.aws.amazon.com/sagemaker/latest/dg/async-inference.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Asynchronous inference](https://docs.aws.amazon.com/sagemaker/latest/dg/async-inference.html)"
    },
    {
      "id": "q13",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "Payment Pipeline Scenario: **Batch Transform** must handle `profile spill diagnosis` while preserving `SQL language model call`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for payment pipeline scenario instead of configuring Batch Transform."
        },
        {
          "id": "B",
          "text": "Use Batch Transform for payment pipeline scenario to handle `profile spill diagnosis`."
        },
        {
          "id": "C",
          "text": "Use report table for payment pipeline scenario instead of configuring Batch Transform."
        },
        {
          "id": "D",
          "text": "Use shell cron for payment pipeline scenario instead of configuring Batch Transform."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because payment pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\n**B** is correct because payment pipeline scenario needs Batch Transform specifically for `profile spill diagnosis`. The documentation describes that product behavior, and `SQL language model call` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because payment pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\n**D** is incorrect because payment pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\nSee [Batch Transform](https://docs.aws.amazon.com/sagemaker/latest/dg/batch-transform.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Batch Transform](https://docs.aws.amazon.com/sagemaker/latest/dg/batch-transform.html)"
    },
    {
      "id": "q14",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Recommendation Pipeline Scenario: **Multi-model endpoints** must handle `unpivoted measure rows` while preserving `dependency lineage graph`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Multi-model endpoints for recommendation pipeline scenario to handle `unpivoted measure rows`."
        },
        {
          "id": "B",
          "text": "Use report table for recommendation pipeline scenario instead of configuring Multi-model endpoints."
        },
        {
          "id": "C",
          "text": "Use shell cron for recommendation pipeline scenario instead of configuring Multi-model endpoints."
        },
        {
          "id": "D",
          "text": "Use email approval for recommendation pipeline scenario instead of configuring Multi-model endpoints."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because recommendation pipeline scenario needs Multi-model endpoints specifically for `unpivoted measure rows`. The documentation describes that product behavior, and `dependency lineage graph` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because recommendation pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `unpivoted measure rows` or preserve `dependency lineage graph` in the supported workflow.\n\n**C** is incorrect because recommendation pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `unpivoted measure rows` or preserve `dependency lineage graph` in the supported workflow.\n\n**D** is incorrect because recommendation pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `unpivoted measure rows` or preserve `dependency lineage graph` in the supported workflow.\n\nSee [Multi-model endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/multi-model-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Multi-model endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/multi-model-endpoints.html)"
    },
    {
      "id": "q15",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "Contract Pipeline Scenario: **Model Monitor** must handle `SQL language model call` while preserving `Clarify bias baseline`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for contract pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "B",
          "text": "Use email approval for contract pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for contract pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "D",
          "text": "Use Model Monitor for contract pipeline scenario to handle `SQL language model call`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because contract pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**B** is incorrect because contract pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**C** is incorrect because contract pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**D** is correct because contract pipeline scenario needs Model Monitor specifically for `SQL language model call`. The documentation describes that product behavior, and `Clarify bias baseline` keeps the workflow inside the managed platform capability.\n\nSee [Model Monitor](https://docs.aws.amazon.com/sagemaker/latest/dg/model-monitor.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Monitor](https://docs.aws.amazon.com/sagemaker/latest/dg/model-monitor.html)"
    },
    {
      "id": "q16",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Customer Contact Center Transcript Mining With Language Tags And Escalation Categories: **Clarify** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for energy pipeline scenario instead of configuring Clarify."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for energy pipeline scenario instead of configuring Clarify."
        },
        {
          "id": "C",
          "text": "Use Clarify for energy pipeline scenario to handle `traffic split rollout`."
        },
        {
          "id": "D",
          "text": "Use visual tile for energy pipeline scenario instead of configuring Clarify."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because energy pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\n**B** is incorrect because energy pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\n**C** is correct because energy pipeline scenario needs Clarify specifically for `traffic split rollout`. The documentation describes that product behavior, and `checkpoint retries` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because energy pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\nSee [Clarify](https://docs.aws.amazon.com/sagemaker/latest/dg/clarify-fairness-and-explainability.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Clarify](https://docs.aws.amazon.com/sagemaker/latest/dg/clarify-fairness-and-explainability.html)"
    },
    {
      "id": "q17",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "Supplier Pipeline Scenario: **Feature Store** must handle `post load aggregate refresh` while preserving `optional JSON extraction`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for supplier pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "B",
          "text": "Use Feature Store for supplier pipeline scenario to handle `post load aggregate refresh`."
        },
        {
          "id": "C",
          "text": "Use visual tile for supplier pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "D",
          "text": "Use admin grant for supplier pipeline scenario instead of configuring Feature Store."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because supplier pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\n**B** is correct because supplier pipeline scenario needs Feature Store specifically for `post load aggregate refresh`. The documentation describes that product behavior, and `optional JSON extraction` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because supplier pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\n**D** is incorrect because supplier pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\nSee [Feature Store](https://docs.aws.amazon.com/sagemaker/latest/dg/feature-store.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature Store](https://docs.aws.amazon.com/sagemaker/latest/dg/feature-store.html)"
    },
    {
      "id": "q18",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Dispatch Pipeline Scenario: **CloudWatch SageMaker** must handle `optional JSON extraction` while preserving `email masking rule`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use CloudWatch SageMaker for dispatch pipeline scenario to handle `optional JSON extraction`."
        },
        {
          "id": "B",
          "text": "Use visual tile for dispatch pipeline scenario instead of configuring CloudWatch SageMaker."
        },
        {
          "id": "C",
          "text": "Use admin grant for dispatch pipeline scenario instead of configuring CloudWatch SageMaker."
        },
        {
          "id": "D",
          "text": "Use local notebook for dispatch pipeline scenario instead of configuring CloudWatch SageMaker."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because dispatch pipeline scenario needs CloudWatch SageMaker specifically for `optional JSON extraction`. The documentation describes that product behavior, and `email masking rule` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because dispatch pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\n**C** is incorrect because dispatch pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\n**D** is incorrect because dispatch pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\nSee [CloudWatch SageMaker](https://docs.aws.amazon.com/sagemaker/latest/dg/monitoring-cloudwatch.html) for the vendor documentation that supports the correct choice.",
      "reference": "[CloudWatch SageMaker](https://docs.aws.amazon.com/sagemaker/latest/dg/monitoring-cloudwatch.html)"
    },
    {
      "id": "q19",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Session Pipeline Scenario: **EventBridge scheduling** must handle `sparse tabular algorithm` while preserving `Model Monitor schedule`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for session pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "B",
          "text": "Use local notebook for session pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "C",
          "text": "Use driver file for session pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "D",
          "text": "Use EventBridge scheduling for session pipeline scenario to handle `sparse tabular algorithm`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because session pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**B** is incorrect because session pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**C** is incorrect because session pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**D** is correct because session pipeline scenario needs EventBridge scheduling specifically for `sparse tabular algorithm`. The documentation describes that product behavior, and `Model Monitor schedule` keeps the workflow inside the managed platform capability.\n\nSee [EventBridge scheduling](https://docs.aws.amazon.com/sagemaker/latest/dg/pipeline-eventbridge.html) for the vendor documentation that supports the correct choice.",
      "reference": "[EventBridge scheduling](https://docs.aws.amazon.com/sagemaker/latest/dg/pipeline-eventbridge.html)"
    },
    {
      "id": "q20",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "hard",
      "stem": "Identity Pipeline Scenario: **IAM least privilege** must handle `script dependency packaging` while preserving `cache invalidation`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for identity pipeline scenario instead of configuring IAM least privilege."
        },
        {
          "id": "B",
          "text": "Use driver file for identity pipeline scenario instead of configuring IAM least privilege."
        },
        {
          "id": "C",
          "text": "Use IAM least privilege for identity pipeline scenario to handle `script dependency packaging`."
        },
        {
          "id": "D",
          "text": "Use report table for identity pipeline scenario instead of configuring IAM least privilege."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because identity pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `script dependency packaging` or preserve `cache invalidation` in the supported workflow.\n\n**B** is incorrect because identity pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `script dependency packaging` or preserve `cache invalidation` in the supported workflow.\n\n**C** is correct because identity pipeline scenario needs IAM least privilege specifically for `script dependency packaging`. The documentation describes that product behavior, and `cache invalidation` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because identity pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `script dependency packaging` or preserve `cache invalidation` in the supported workflow.\n\nSee [IAM least privilege](https://docs.aws.amazon.com/sagemaker/latest/dg/security_iam_id-based-policy-examples.html) for the vendor documentation that supports the correct choice.",
      "reference": "[IAM least privilege](https://docs.aws.amazon.com/sagemaker/latest/dg/security_iam_id-based-policy-examples.html)"
    },
    {
      "id": "q21",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "Label Pipeline Scenario: **SageMaker Processing** must handle `semantic retrieval setup` while preserving `semantic retrieval setup`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for label pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "B",
          "text": "Use SageMaker Processing for label pipeline scenario to handle `semantic retrieval setup`."
        },
        {
          "id": "C",
          "text": "Use report table for label pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "D",
          "text": "Use shell cron for label pipeline scenario instead of configuring SageMaker Processing."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because label pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\n**B** is correct because label pipeline scenario needs SageMaker Processing specifically for `semantic retrieval setup`. The documentation describes that product behavior, and `semantic retrieval setup` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because label pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\n**D** is incorrect because label pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\nSee [SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html)"
    },
    {
      "id": "q22",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Champion Pipeline Scenario: **AWS Glue job bookmarks** must handle `sensitivity tag policy` while preserving `event scheduled pipeline`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use AWS Glue job bookmarks for champion pipeline scenario to handle `sensitivity tag policy`."
        },
        {
          "id": "B",
          "text": "Use report table for champion pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "C",
          "text": "Use shell cron for champion pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "D",
          "text": "Use email approval for champion pipeline scenario instead of configuring AWS Glue job bookmarks."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because champion pipeline scenario needs AWS Glue job bookmarks specifically for `sensitivity tag policy`. The documentation describes that product behavior, and `event scheduled pipeline` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because champion pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `sensitivity tag policy` or preserve `event scheduled pipeline` in the supported workflow.\n\n**C** is incorrect because champion pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `sensitivity tag policy` or preserve `event scheduled pipeline` in the supported workflow.\n\n**D** is incorrect because champion pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `sensitivity tag policy` or preserve `event scheduled pipeline` in the supported workflow.\n\nSee [AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html) for the vendor documentation that supports the correct choice.",
      "reference": "[AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html)"
    },
    {
      "id": "q23",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Support Pipeline Scenario: **SageMaker Data Wrangler** must handle `labeled quality monitoring` while preserving `Data Wrangler export`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for support pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "B",
          "text": "Use email approval for support pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for support pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "D",
          "text": "Use SageMaker Data Wrangler for support pipeline scenario to handle `labeled quality monitoring`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because support pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `labeled quality monitoring` or preserve `Data Wrangler export` in the supported workflow.\n\n**B** is incorrect because support pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `labeled quality monitoring` or preserve `Data Wrangler export` in the supported workflow.\n\n**C** is incorrect because support pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `labeled quality monitoring` or preserve `Data Wrangler export` in the supported workflow.\n\n**D** is correct because support pipeline scenario needs SageMaker Data Wrangler specifically for `labeled quality monitoring`. The documentation describes that product behavior, and `Data Wrangler export` keeps the workflow inside the managed platform capability.\n\nSee [SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html)"
    },
    {
      "id": "q24",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Quota Pipeline Scenario: **SageMaker Experiments** must handle `feature lineage capture` while preserving `point in time joins`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for quota pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for quota pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "C",
          "text": "Use SageMaker Experiments for quota pipeline scenario to handle `feature lineage capture`."
        },
        {
          "id": "D",
          "text": "Use visual tile for quota pipeline scenario instead of configuring SageMaker Experiments."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because quota pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\n**B** is incorrect because quota pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\n**C** is correct because quota pipeline scenario needs SageMaker Experiments specifically for `feature lineage capture`. The documentation describes that product behavior, and `point in time joins` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because quota pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\nSee [SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html)"
    },
    {
      "id": "q25",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "SageMaker built-in algorithms provide prebuilt training containers and algorithm choices for supervised and unsupervised workloads without custom training code.\n\nA practitioner must choose the documented **Built-in algorithms** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for access pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "B",
          "text": "Use Built-in algorithms because the documentation supports this exact behavior: SageMaker built-in algorithms provide prebuilt training containers and algorithm choices for supervised and unsupervised workloads without custom training code."
        },
        {
          "id": "C",
          "text": "Use visual tile for access pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "D",
          "text": "Use admin grant for access pipeline scenario instead of configuring Built-in algorithms."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Built-in algorithms behavior described here: SageMaker built-in algorithms provide prebuilt training containers and algorithm choices for supervised and unsupervised workloads without custom training code.\n\n**B** is correct because SageMaker built-in algorithms provide prebuilt training containers and algorithm choices for supervised and unsupervised workloads without custom training code. This directly matches the scenario and is supported by the referenced Built-in algorithms documentation.\n\n**C** is incorrect because it does not provide the documented Built-in algorithms behavior described here: SageMaker built-in algorithms provide prebuilt training containers and algorithm choices for supervised and unsupervised workloads without custom training code.\n\n**D** is incorrect because it does not provide the documented Built-in algorithms behavior described here: SageMaker built-in algorithms provide prebuilt training containers and algorithm choices for supervised and unsupervised workloads without custom training code.\n\nSee [Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html)"
    },
    {
      "id": "q26",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Maintenance Telemetry Rollup For Turbine Sensors With Calibration Batches And Technician Exception Queues: **Script mode** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Script mode for document pipeline scenario to handle `recursive hierarchy traversal`."
        },
        {
          "id": "B",
          "text": "Use visual tile for document pipeline scenario instead of configuring Script mode."
        },
        {
          "id": "C",
          "text": "Use admin grant for document pipeline scenario instead of configuring Script mode."
        },
        {
          "id": "D",
          "text": "Use local notebook for document pipeline scenario instead of configuring Script mode."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because document pipeline scenario needs Script mode specifically for `recursive hierarchy traversal`. The documentation describes that product behavior, and `warehouse scaling policy` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because document pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `recursive hierarchy traversal` or preserve `warehouse scaling policy` in the supported workflow.\n\n**C** is incorrect because document pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `recursive hierarchy traversal` or preserve `warehouse scaling policy` in the supported workflow.\n\n**D** is incorrect because document pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `recursive hierarchy traversal` or preserve `warehouse scaling policy` in the supported workflow.\n\nSee [Script mode](https://docs.aws.amazon.com/sagemaker/latest/dg/script-mode.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Script mode](https://docs.aws.amazon.com/sagemaker/latest/dg/script-mode.html)"
    },
    {
      "id": "q27",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "Defect Pipeline Scenario: **SageMaker Pipelines** must handle `managed preprocessing container` while preserving `multi model endpoint load`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for defect pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "B",
          "text": "Use local notebook for defect pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "C",
          "text": "Use driver file for defect pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "D",
          "text": "Use SageMaker Pipelines for defect pipeline scenario to handle `managed preprocessing container`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because defect pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**B** is incorrect because defect pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**C** is incorrect because defect pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**D** is correct because defect pipeline scenario needs SageMaker Pipelines specifically for `managed preprocessing container`. The documentation describes that product behavior, and `multi model endpoint load` keeps the workflow inside the managed platform capability.\n\nSee [SageMaker Pipelines](https://docs.aws.amazon.com/sagemaker/latest/dg/pipelines.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Pipelines](https://docs.aws.amazon.com/sagemaker/latest/dg/pipelines.html)"
    },
    {
      "id": "q28",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Residual Pipeline Scenario: **Pipeline steps** must handle `registry notification hook` while preserving `metric gated registration`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for residual pipeline scenario instead of configuring Pipeline steps."
        },
        {
          "id": "B",
          "text": "Use driver file for residual pipeline scenario instead of configuring Pipeline steps."
        },
        {
          "id": "C",
          "text": "Use Pipeline steps for residual pipeline scenario to handle `registry notification hook`."
        },
        {
          "id": "D",
          "text": "Use report table for residual pipeline scenario instead of configuring Pipeline steps."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because residual pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\n**B** is incorrect because residual pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\n**C** is correct because residual pipeline scenario needs Pipeline steps specifically for `registry notification hook`. The documentation describes that product behavior, and `metric gated registration` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because residual pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\nSee [Pipeline steps](https://docs.aws.amazon.com/sagemaker/latest/dg/build-and-manage-steps.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Pipeline steps](https://docs.aws.amazon.com/sagemaker/latest/dg/build-and-manage-steps.html)"
    },
    {
      "id": "q29",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "SageMaker Model Registry organizes model package groups, model packages, approval status, and versioned metadata for promotion workflows.\n\nA practitioner must choose the documented **Model Registry** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for approval pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "B",
          "text": "Use Model Registry because the documentation supports this exact behavior: SageMaker Model Registry organizes model package groups, model packages, approval status, and versioned metadata for promotion workflows."
        },
        {
          "id": "C",
          "text": "Use report table for approval pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "D",
          "text": "Use shell cron for approval pipeline scenario instead of configuring Model Registry."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Model Registry behavior described here: SageMaker Model Registry organizes model package groups, model packages, approval status, and versioned metadata for promotion workflows.\n\n**B** is correct because SageMaker Model Registry organizes model package groups, model packages, approval status, and versioned metadata for promotion workflows. This directly matches the scenario and is supported by the referenced Model Registry documentation.\n\n**C** is incorrect because it does not provide the documented Model Registry behavior described here: SageMaker Model Registry organizes model package groups, model packages, approval status, and versioned metadata for promotion workflows.\n\n**D** is incorrect because it does not provide the documented Model Registry behavior described here: SageMaker Model Registry organizes model package groups, model packages, approval status, and versioned metadata for promotion workflows.\n\nSee [Model Registry](https://docs.aws.amazon.com/sagemaker/latest/dg/model-registry.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Registry](https://docs.aws.amazon.com/sagemaker/latest/dg/model-registry.html)"
    },
    {
      "id": "q30",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Scoring Pipeline Scenario: **Real-time endpoints** must handle `online feature serving` while preserving `Cortex analyst semantic model`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Real-time endpoints for scoring pipeline scenario to handle `online feature serving`."
        },
        {
          "id": "B",
          "text": "Use report table for scoring pipeline scenario instead of configuring Real-time endpoints."
        },
        {
          "id": "C",
          "text": "Use shell cron for scoring pipeline scenario instead of configuring Real-time endpoints."
        },
        {
          "id": "D",
          "text": "Use email approval for scoring pipeline scenario instead of configuring Real-time endpoints."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because scoring pipeline scenario needs Real-time endpoints specifically for `online feature serving`. The documentation describes that product behavior, and `Cortex analyst semantic model` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because scoring pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\n**C** is incorrect because scoring pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\n**D** is incorrect because scoring pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\nSee [Real-time endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/realtime-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Real-time endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/realtime-endpoints.html)"
    },
    {
      "id": "q31",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "SageMaker Serverless Inference provisions a serverless endpoint that automatically scales with traffic and removes the need to choose instance counts.\n\nA practitioner must choose the documented **Serverless inference** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for alert pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "B",
          "text": "Use email approval for alert pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for alert pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "D",
          "text": "Use Serverless inference because the documentation supports this exact behavior: SageMaker Serverless Inference provisions a serverless endpoint that automatically scales with traffic and removes the need to choose instance counts."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Serverless inference behavior described here: SageMaker Serverless Inference provisions a serverless endpoint that automatically scales with traffic and removes the need to choose instance counts.\n\n**B** is incorrect because it does not provide the documented Serverless inference behavior described here: SageMaker Serverless Inference provisions a serverless endpoint that automatically scales with traffic and removes the need to choose instance counts.\n\n**C** is incorrect because it does not provide the documented Serverless inference behavior described here: SageMaker Serverless Inference provisions a serverless endpoint that automatically scales with traffic and removes the need to choose instance counts.\n\n**D** is correct because SageMaker Serverless Inference provisions a serverless endpoint that automatically scales with traffic and removes the need to choose instance counts. This directly matches the scenario and is supported by the referenced Serverless inference documentation.\n\nSee [Serverless inference](https://docs.aws.amazon.com/sagemaker/latest/dg/serverless-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Serverless inference](https://docs.aws.amazon.com/sagemaker/latest/dg/serverless-endpoints.html)"
    },
    {
      "id": "q32",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "hard",
      "stem": "Lifetime Pipeline Scenario: **Asynchronous inference** must handle `dependency lineage graph` while preserving `profile spill diagnosis`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for lifetime pipeline scenario instead of configuring Asynchronous inference."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for lifetime pipeline scenario instead of configuring Asynchronous inference."
        },
        {
          "id": "C",
          "text": "Use Asynchronous inference for lifetime pipeline scenario to handle `dependency lineage graph`."
        },
        {
          "id": "D",
          "text": "Use visual tile for lifetime pipeline scenario instead of configuring Asynchronous inference."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because lifetime pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\n**B** is incorrect because lifetime pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\n**C** is correct because lifetime pipeline scenario needs Asynchronous inference specifically for `dependency lineage graph`. The documentation describes that product behavior, and `profile spill diagnosis` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because lifetime pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\nSee [Asynchronous inference](https://docs.aws.amazon.com/sagemaker/latest/dg/async-inference.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Asynchronous inference](https://docs.aws.amazon.com/sagemaker/latest/dg/async-inference.html)"
    },
    {
      "id": "q33",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "Invoice Pipeline Scenario: **Batch Transform** must handle `top ranked customer filter` while preserving `online feature serving`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for invoice pipeline scenario instead of configuring Batch Transform."
        },
        {
          "id": "B",
          "text": "Use Batch Transform for invoice pipeline scenario to handle `top ranked customer filter`."
        },
        {
          "id": "C",
          "text": "Use visual tile for invoice pipeline scenario instead of configuring Batch Transform."
        },
        {
          "id": "D",
          "text": "Use admin grant for invoice pipeline scenario instead of configuring Batch Transform."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because invoice pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\n**B** is correct because invoice pipeline scenario needs Batch Transform specifically for `top ranked customer filter`. The documentation describes that product behavior, and `online feature serving` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because invoice pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\n**D** is incorrect because invoice pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\nSee [Batch Transform](https://docs.aws.amazon.com/sagemaker/latest/dg/batch-transform.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Batch Transform](https://docs.aws.amazon.com/sagemaker/latest/dg/batch-transform.html)"
    },
    {
      "id": "q34",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Summarizer Pipeline Scenario: **Multi-model endpoints** must handle `outer flatten rows` while preserving `MLflow autolog capture`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Multi-model endpoints for summarizer pipeline scenario to handle `outer flatten rows`."
        },
        {
          "id": "B",
          "text": "Use visual tile for summarizer pipeline scenario instead of configuring Multi-model endpoints."
        },
        {
          "id": "C",
          "text": "Use admin grant for summarizer pipeline scenario instead of configuring Multi-model endpoints."
        },
        {
          "id": "D",
          "text": "Use local notebook for summarizer pipeline scenario instead of configuring Multi-model endpoints."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because summarizer pipeline scenario needs Multi-model endpoints specifically for `outer flatten rows`. The documentation describes that product behavior, and `MLflow autolog capture` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because summarizer pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `outer flatten rows` or preserve `MLflow autolog capture` in the supported workflow.\n\n**C** is incorrect because summarizer pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `outer flatten rows` or preserve `MLflow autolog capture` in the supported workflow.\n\n**D** is incorrect because summarizer pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `outer flatten rows` or preserve `MLflow autolog capture` in the supported workflow.\n\nSee [Multi-model endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/multi-model-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Multi-model endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/multi-model-endpoints.html)"
    },
    {
      "id": "q35",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Heatmap Pipeline Scenario: **Model Monitor** must handle `email masking rule` while preserving `Access History trace`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for heatmap pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "B",
          "text": "Use local notebook for heatmap pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "C",
          "text": "Use driver file for heatmap pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "D",
          "text": "Use Model Monitor for heatmap pipeline scenario to handle `email masking rule`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because heatmap pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**B** is incorrect because heatmap pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**C** is incorrect because heatmap pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**D** is correct because heatmap pipeline scenario needs Model Monitor specifically for `email masking rule`. The documentation describes that product behavior, and `Access History trace` keeps the workflow inside the managed platform capability.\n\nSee [Model Monitor](https://docs.aws.amazon.com/sagemaker/latest/dg/model-monitor.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Monitor](https://docs.aws.amazon.com/sagemaker/latest/dg/model-monitor.html)"
    },
    {
      "id": "q36",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Geospatial Delivery Promise Forecast With Depot Zones And Holiday Calendars: **Clarify** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for clause pipeline scenario instead of configuring Clarify."
        },
        {
          "id": "B",
          "text": "Use driver file for clause pipeline scenario instead of configuring Clarify."
        },
        {
          "id": "C",
          "text": "Use Clarify for clause pipeline scenario to handle `future grant pattern`."
        },
        {
          "id": "D",
          "text": "Use report table for clause pipeline scenario instead of configuring Clarify."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because clause pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `future grant pattern` or preserve `traffic split rollout` in the supported workflow.\n\n**B** is incorrect because clause pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `future grant pattern` or preserve `traffic split rollout` in the supported workflow.\n\n**C** is correct because clause pipeline scenario needs Clarify specifically for `future grant pattern`. The documentation describes that product behavior, and `traffic split rollout` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because clause pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `future grant pattern` or preserve `traffic split rollout` in the supported workflow.\n\nSee [Clarify](https://docs.aws.amazon.com/sagemaker/latest/dg/clarify-fairness-and-explainability.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Clarify](https://docs.aws.amazon.com/sagemaker/latest/dg/clarify-fairness-and-explainability.html)"
    },
    {
      "id": "q37",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "Drift Pipeline Scenario: **Feature Store** must handle `async payload routing` while preserving `top ranked customer filter`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for drift pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "B",
          "text": "Use Feature Store for drift pipeline scenario to handle `async payload routing`."
        },
        {
          "id": "C",
          "text": "Use report table for drift pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "D",
          "text": "Use shell cron for drift pipeline scenario instead of configuring Feature Store."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because drift pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\n**B** is correct because drift pipeline scenario needs Feature Store specifically for `async payload routing`. The documentation describes that product behavior, and `top ranked customer filter` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because drift pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\n**D** is incorrect because drift pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\nSee [Feature Store](https://docs.aws.amazon.com/sagemaker/latest/dg/feature-store.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature Store](https://docs.aws.amazon.com/sagemaker/latest/dg/feature-store.html)"
    },
    {
      "id": "q38",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Latency Pipeline Scenario: **CloudWatch SageMaker** must handle `event scheduled pipeline` while preserving `Glue bookmark state`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use CloudWatch SageMaker for latency pipeline scenario to handle `event scheduled pipeline`."
        },
        {
          "id": "B",
          "text": "Use report table for latency pipeline scenario instead of configuring CloudWatch SageMaker."
        },
        {
          "id": "C",
          "text": "Use shell cron for latency pipeline scenario instead of configuring CloudWatch SageMaker."
        },
        {
          "id": "D",
          "text": "Use email approval for latency pipeline scenario instead of configuring CloudWatch SageMaker."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because latency pipeline scenario needs CloudWatch SageMaker specifically for `event scheduled pipeline`. The documentation describes that product behavior, and `Glue bookmark state` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because latency pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\n**C** is incorrect because latency pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\n**D** is incorrect because latency pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\nSee [CloudWatch SageMaker](https://docs.aws.amazon.com/sagemaker/latest/dg/monitoring-cloudwatch.html) for the vendor documentation that supports the correct choice.",
      "reference": "[CloudWatch SageMaker](https://docs.aws.amazon.com/sagemaker/latest/dg/monitoring-cloudwatch.html)"
    },
    {
      "id": "q39",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "Comparison Pipeline Scenario: **EventBridge scheduling** must handle `training baseline capture` while preserving `duplicate key ranking`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for comparison pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "B",
          "text": "Use email approval for comparison pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for comparison pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "D",
          "text": "Use EventBridge scheduling for comparison pipeline scenario to handle `training baseline capture`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because comparison pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**B** is incorrect because comparison pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**C** is incorrect because comparison pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**D** is correct because comparison pipeline scenario needs EventBridge scheduling specifically for `training baseline capture`. The documentation describes that product behavior, and `duplicate key ranking` keeps the workflow inside the managed platform capability.\n\nSee [EventBridge scheduling](https://docs.aws.amazon.com/sagemaker/latest/dg/pipeline-eventbridge.html) for the vendor documentation that supports the correct choice.",
      "reference": "[EventBridge scheduling](https://docs.aws.amazon.com/sagemaker/latest/dg/pipeline-eventbridge.html)"
    },
    {
      "id": "q40",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Entitlement Pipeline Scenario: **IAM least privilege** must handle `dual store features` while preserving `sparse tabular algorithm`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for entitlement pipeline scenario instead of configuring IAM least privilege."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for entitlement pipeline scenario instead of configuring IAM least privilege."
        },
        {
          "id": "C",
          "text": "Use IAM least privilege for entitlement pipeline scenario to handle `dual store features`."
        },
        {
          "id": "D",
          "text": "Use visual tile for entitlement pipeline scenario instead of configuring IAM least privilege."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because entitlement pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\n**B** is incorrect because entitlement pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\n**C** is correct because entitlement pipeline scenario needs IAM least privilege specifically for `dual store features`. The documentation describes that product behavior, and `sparse tabular algorithm` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because entitlement pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\nSee [IAM least privilege](https://docs.aws.amazon.com/sagemaker/latest/dg/security_iam_id-based-policy-examples.html) for the vendor documentation that supports the correct choice.",
      "reference": "[IAM least privilege](https://docs.aws.amazon.com/sagemaker/latest/dg/security_iam_id-based-policy-examples.html)"
    },
    {
      "id": "q41",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "Analyst Pipeline Scenario: **SageMaker Processing** must handle `warehouse scaling policy` while preserving `future grant pattern`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for analyst pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "B",
          "text": "Use SageMaker Processing for analyst pipeline scenario to handle `warehouse scaling policy`."
        },
        {
          "id": "C",
          "text": "Use visual tile for analyst pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "D",
          "text": "Use admin grant for analyst pipeline scenario instead of configuring SageMaker Processing."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because analyst pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\n**B** is correct because analyst pipeline scenario needs SageMaker Processing specifically for `warehouse scaling policy`. The documentation describes that product behavior, and `future grant pattern` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because analyst pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\n**D** is incorrect because analyst pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\nSee [SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html)"
    },
    {
      "id": "q42",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Aggregate Pipeline Scenario: **AWS Glue job bookmarks** must handle `row access predicate` while preserving `Feature Store online lookup`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use AWS Glue job bookmarks for aggregate pipeline scenario to handle `row access predicate`."
        },
        {
          "id": "B",
          "text": "Use visual tile for aggregate pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "C",
          "text": "Use admin grant for aggregate pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "D",
          "text": "Use local notebook for aggregate pipeline scenario instead of configuring AWS Glue job bookmarks."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because aggregate pipeline scenario needs AWS Glue job bookmarks specifically for `row access predicate`. The documentation describes that product behavior, and `Feature Store online lookup` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because aggregate pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\n**C** is incorrect because aggregate pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\n**D** is incorrect because aggregate pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\nSee [AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html) for the vendor documentation that supports the correct choice.",
      "reference": "[AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html)"
    },
    {
      "id": "q43",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Hierarchy Pipeline Scenario: **SageMaker Data Wrangler** must handle `materialized view maintenance` while preserving `role visibility`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for hierarchy pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "B",
          "text": "Use local notebook for hierarchy pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "C",
          "text": "Use driver file for hierarchy pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "D",
          "text": "Use SageMaker Data Wrangler for hierarchy pipeline scenario to handle `materialized view maintenance`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because hierarchy pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `materialized view maintenance` or preserve `role visibility` in the supported workflow.\n\n**B** is incorrect because hierarchy pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `materialized view maintenance` or preserve `role visibility` in the supported workflow.\n\n**C** is incorrect because hierarchy pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `materialized view maintenance` or preserve `role visibility` in the supported workflow.\n\n**D** is correct because hierarchy pipeline scenario needs SageMaker Data Wrangler specifically for `materialized view maintenance`. The documentation describes that product behavior, and `role visibility` keeps the workflow inside the managed platform capability.\n\nSee [SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html)"
    },
    {
      "id": "q44",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "hard",
      "stem": "Payload Pipeline Scenario: **SageMaker Experiments** must handle `Cortex analyst semantic model` while preserving `sensitivity tag policy`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for payload pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "B",
          "text": "Use driver file for payload pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "C",
          "text": "Use SageMaker Experiments for payload pipeline scenario to handle `Cortex analyst semantic model`."
        },
        {
          "id": "D",
          "text": "Use report table for payload pipeline scenario instead of configuring SageMaker Experiments."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because payload pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\n**B** is incorrect because payload pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\n**C** is correct because payload pipeline scenario needs SageMaker Experiments specifically for `Cortex analyst semantic model`. The documentation describes that product behavior, and `sensitivity tag policy` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because payload pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\nSee [SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html)"
    },
    {
      "id": "q45",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "Corpus Pipeline Scenario: **Built-in algorithms** must handle `Snowpark dataframe transform` while preserving `training baseline capture`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for corpus pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "B",
          "text": "Use Built-in algorithms for corpus pipeline scenario to handle `Snowpark dataframe transform`."
        },
        {
          "id": "C",
          "text": "Use report table for corpus pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "D",
          "text": "Use shell cron for corpus pipeline scenario instead of configuring Built-in algorithms."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because corpus pipeline scenario would still lack Built-in algorithms; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\n**B** is correct because corpus pipeline scenario needs Built-in algorithms specifically for `Snowpark dataframe transform`. The documentation describes that product behavior, and `training baseline capture` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because corpus pipeline scenario would still lack Built-in algorithms; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\n**D** is incorrect because corpus pipeline scenario would still lack Built-in algorithms; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\nSee [Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html)"
    },
    {
      "id": "q46",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Endpoint Pipeline Scenario: **Script mode** must handle `Hyperopt parallel search` while preserving `Processing job output`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Script mode for endpoint pipeline scenario to handle `Hyperopt parallel search`."
        },
        {
          "id": "B",
          "text": "Use report table for endpoint pipeline scenario instead of configuring Script mode."
        },
        {
          "id": "C",
          "text": "Use shell cron for endpoint pipeline scenario instead of configuring Script mode."
        },
        {
          "id": "D",
          "text": "Use email approval for endpoint pipeline scenario instead of configuring Script mode."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because endpoint pipeline scenario needs Script mode specifically for `Hyperopt parallel search`. The documentation describes that product behavior, and `Processing job output` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because endpoint pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `Hyperopt parallel search` or preserve `Processing job output` in the supported workflow.\n\n**C** is incorrect because endpoint pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `Hyperopt parallel search` or preserve `Processing job output` in the supported workflow.\n\n**D** is incorrect because endpoint pipeline scenario would still lack Script mode; the suggested workaround cannot satisfy `Hyperopt parallel search` or preserve `Processing job output` in the supported workflow.\n\nSee [Script mode](https://docs.aws.amazon.com/sagemaker/latest/dg/script-mode.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Script mode](https://docs.aws.amazon.com/sagemaker/latest/dg/script-mode.html)"
    },
    {
      "id": "q47",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Condition Pipeline Scenario: **SageMaker Pipelines** must handle `MLflow autolog capture` while preserving `offline object scoring`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for condition pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "B",
          "text": "Use email approval for condition pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for condition pipeline scenario instead of configuring SageMaker Pipelines."
        },
        {
          "id": "D",
          "text": "Use SageMaker Pipelines for condition pipeline scenario to handle `MLflow autolog capture`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because condition pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**B** is incorrect because condition pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**C** is incorrect because condition pipeline scenario would still lack SageMaker Pipelines; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**D** is correct because condition pipeline scenario needs SageMaker Pipelines specifically for `MLflow autolog capture`. The documentation describes that product behavior, and `offline object scoring` keeps the workflow inside the managed platform capability.\n\nSee [SageMaker Pipelines](https://docs.aws.amazon.com/sagemaker/latest/dg/pipelines.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Pipelines](https://docs.aws.amazon.com/sagemaker/latest/dg/pipelines.html)"
    },
    {
      "id": "q48",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Queue Pipeline Scenario: **Pipeline steps** must handle `pyfunc wrapper loading` while preserving `BI precomputation`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for queue pipeline scenario instead of configuring Pipeline steps."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for queue pipeline scenario instead of configuring Pipeline steps."
        },
        {
          "id": "C",
          "text": "Use Pipeline steps for queue pipeline scenario to handle `pyfunc wrapper loading`."
        },
        {
          "id": "D",
          "text": "Use visual tile for queue pipeline scenario instead of configuring Pipeline steps."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because queue pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\n**B** is incorrect because queue pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\n**C** is correct because queue pipeline scenario needs Pipeline steps specifically for `pyfunc wrapper loading`. The documentation describes that product behavior, and `BI precomputation` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because queue pipeline scenario would still lack Pipeline steps; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\nSee [Pipeline steps](https://docs.aws.amazon.com/sagemaker/latest/dg/build-and-manage-steps.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Pipeline steps](https://docs.aws.amazon.com/sagemaker/latest/dg/build-and-manage-steps.html)"
    },
    {
      "id": "q49",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "SageMaker Model Registry tracks registered model versions and approval states so teams can govern which model package is ready for deployment.\n\nA practitioner must choose the documented **Model Registry** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for bookmark pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "B",
          "text": "Use Model Registry because the documentation supports this exact behavior: SageMaker Model Registry tracks registered model versions and approval states so teams can govern which model package is ready for deployment."
        },
        {
          "id": "C",
          "text": "Use visual tile for bookmark pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "D",
          "text": "Use admin grant for bookmark pipeline scenario instead of configuring Model Registry."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Model Registry behavior described here: SageMaker Model Registry tracks registered model versions and approval states so teams can govern which model package is ready for deployment.\n\n**B** is correct because SageMaker Model Registry tracks registered model versions and approval states so teams can govern which model package is ready for deployment. This directly matches the scenario and is supported by the referenced Model Registry documentation.\n\n**C** is incorrect because it does not provide the documented Model Registry behavior described here: SageMaker Model Registry tracks registered model versions and approval states so teams can govern which model package is ready for deployment.\n\n**D** is incorrect because it does not provide the documented Model Registry behavior described here: SageMaker Model Registry tracks registered model versions and approval states so teams can govern which model package is ready for deployment.\n\nSee [Model Registry](https://docs.aws.amazon.com/sagemaker/latest/dg/model-registry.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Registry](https://docs.aws.amazon.com/sagemaker/latest/dg/model-registry.html)"
    },
    {
      "id": "q50",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Flow Pipeline Scenario: **Real-time endpoints** must handle `Glue bookmark state` while preserving `CloudWatch endpoint alarm`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Real-time endpoints for flow pipeline scenario to handle `Glue bookmark state`."
        },
        {
          "id": "B",
          "text": "Use visual tile for flow pipeline scenario instead of configuring Real-time endpoints."
        },
        {
          "id": "C",
          "text": "Use admin grant for flow pipeline scenario instead of configuring Real-time endpoints."
        },
        {
          "id": "D",
          "text": "Use local notebook for flow pipeline scenario instead of configuring Real-time endpoints."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because flow pipeline scenario needs Real-time endpoints specifically for `Glue bookmark state`. The documentation describes that product behavior, and `CloudWatch endpoint alarm` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because flow pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\n**C** is incorrect because flow pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\n**D** is incorrect because flow pipeline scenario would still lack Real-time endpoints; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\nSee [Real-time endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/realtime-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Real-time endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/realtime-endpoints.html)"
    },
    {
      "id": "q51",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "Product Taxonomy Enrichment With Synonym Dictionaries And Merchandiser Stewardship: **Serverless inference** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for bias pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "B",
          "text": "Use local notebook for bias pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "C",
          "text": "Use driver file for bias pipeline scenario instead of configuring Serverless inference."
        },
        {
          "id": "D",
          "text": "Use Serverless inference for bias pipeline scenario to handle `Batch Transform manifest`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because bias pipeline scenario would still lack Serverless inference; the suggested workaround cannot satisfy `Batch Transform manifest` or preserve `idle serverless serving` in the supported workflow.\n\n**B** is incorrect because bias pipeline scenario would still lack Serverless inference; the suggested workaround cannot satisfy `Batch Transform manifest` or preserve `idle serverless serving` in the supported workflow.\n\n**C** is incorrect because bias pipeline scenario would still lack Serverless inference; the suggested workaround cannot satisfy `Batch Transform manifest` or preserve `idle serverless serving` in the supported workflow.\n\n**D** is correct because bias pipeline scenario needs Serverless inference specifically for `Batch Transform manifest`. The documentation describes that product behavior, and `idle serverless serving` keeps the workflow inside the managed platform capability.\n\nSee [Serverless inference](https://docs.aws.amazon.com/sagemaker/latest/dg/serverless-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Serverless inference](https://docs.aws.amazon.com/sagemaker/latest/dg/serverless-endpoints.html)"
    },
    {
      "id": "q52",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Retention Pipeline Scenario: **Asynchronous inference** must handle `Model Monitor schedule` while preserving `registry notification hook`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for retention pipeline scenario instead of configuring Asynchronous inference."
        },
        {
          "id": "B",
          "text": "Use driver file for retention pipeline scenario instead of configuring Asynchronous inference."
        },
        {
          "id": "C",
          "text": "Use Asynchronous inference for retention pipeline scenario to handle `Model Monitor schedule`."
        },
        {
          "id": "D",
          "text": "Use report table for retention pipeline scenario instead of configuring Asynchronous inference."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because retention pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\n**B** is incorrect because retention pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\n**C** is correct because retention pipeline scenario needs Asynchronous inference specifically for `Model Monitor schedule`. The documentation describes that product behavior, and `registry notification hook` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because retention pipeline scenario would still lack Asynchronous inference; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\nSee [Asynchronous inference](https://docs.aws.amazon.com/sagemaker/latest/dg/async-inference.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Asynchronous inference](https://docs.aws.amazon.com/sagemaker/latest/dg/async-inference.html)"
    },
    {
      "id": "q53",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "Ranking Pipeline Scenario: **Batch Transform** must handle `Feature Store online lookup` while preserving `Snowpark dataframe transform`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for ranking pipeline scenario instead of configuring Batch Transform."
        },
        {
          "id": "B",
          "text": "Use Batch Transform for ranking pipeline scenario to handle `Feature Store online lookup`."
        },
        {
          "id": "C",
          "text": "Use report table for ranking pipeline scenario instead of configuring Batch Transform."
        },
        {
          "id": "D",
          "text": "Use shell cron for ranking pipeline scenario instead of configuring Batch Transform."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because ranking pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\n**B** is correct because ranking pipeline scenario needs Batch Transform specifically for `Feature Store online lookup`. The documentation describes that product behavior, and `Snowpark dataframe transform` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because ranking pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\n**D** is incorrect because ranking pipeline scenario would still lack Batch Transform; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\nSee [Batch Transform](https://docs.aws.amazon.com/sagemaker/latest/dg/batch-transform.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Batch Transform](https://docs.aws.amazon.com/sagemaker/latest/dg/batch-transform.html)"
    },
    {
      "id": "q54",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Arrays Pipeline Scenario: **Multi-model endpoints** must handle `Autopilot candidate review` while preserving `GROUPING SETS subtotal`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Multi-model endpoints for arrays pipeline scenario to handle `Autopilot candidate review`."
        },
        {
          "id": "B",
          "text": "Use report table for arrays pipeline scenario instead of configuring Multi-model endpoints."
        },
        {
          "id": "C",
          "text": "Use shell cron for arrays pipeline scenario instead of configuring Multi-model endpoints."
        },
        {
          "id": "D",
          "text": "Use email approval for arrays pipeline scenario instead of configuring Multi-model endpoints."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because arrays pipeline scenario needs Multi-model endpoints specifically for `Autopilot candidate review`. The documentation describes that product behavior, and `GROUPING SETS subtotal` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because arrays pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `Autopilot candidate review` or preserve `GROUPING SETS subtotal` in the supported workflow.\n\n**C** is incorrect because arrays pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `Autopilot candidate review` or preserve `GROUPING SETS subtotal` in the supported workflow.\n\n**D** is incorrect because arrays pipeline scenario would still lack Multi-model endpoints; the suggested workaround cannot satisfy `Autopilot candidate review` or preserve `GROUPING SETS subtotal` in the supported workflow.\n\nSee [Multi-model endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/multi-model-endpoints.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Multi-model endpoints](https://docs.aws.amazon.com/sagemaker/latest/dg/multi-model-endpoints.html)"
    },
    {
      "id": "q55",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Cache Pipeline Scenario: **Model Monitor** must handle `Data Wrangler export` while preserving `unpivoted measure rows`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for cache pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "B",
          "text": "Use email approval for cache pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for cache pipeline scenario instead of configuring Model Monitor."
        },
        {
          "id": "D",
          "text": "Use Model Monitor for cache pipeline scenario to handle `Data Wrangler export`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because cache pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**B** is incorrect because cache pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**C** is incorrect because cache pipeline scenario would still lack Model Monitor; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**D** is correct because cache pipeline scenario needs Model Monitor specifically for `Data Wrangler export`. The documentation describes that product behavior, and `unpivoted measure rows` keeps the workflow inside the managed platform capability.\n\nSee [Model Monitor](https://docs.aws.amazon.com/sagemaker/latest/dg/model-monitor.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Monitor](https://docs.aws.amazon.com/sagemaker/latest/dg/model-monitor.html)"
    },
    {
      "id": "q56",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "hard",
      "stem": "Visibility Pipeline Scenario: **Clarify** must handle `Processing job output` while preserving `recursive termination`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for visibility pipeline scenario instead of configuring Clarify."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for visibility pipeline scenario instead of configuring Clarify."
        },
        {
          "id": "C",
          "text": "Use Clarify for visibility pipeline scenario to handle `Processing job output`."
        },
        {
          "id": "D",
          "text": "Use visual tile for visibility pipeline scenario instead of configuring Clarify."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because visibility pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\n**B** is incorrect because visibility pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\n**C** is correct because visibility pipeline scenario needs Clarify specifically for `Processing job output`. The documentation describes that product behavior, and `recursive termination` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because visibility pipeline scenario would still lack Clarify; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\nSee [Clarify](https://docs.aws.amazon.com/sagemaker/latest/dg/clarify-fairness-and-explainability.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Clarify](https://docs.aws.amazon.com/sagemaker/latest/dg/clarify-fairness-and-explainability.html)"
    },
    {
      "id": "q57",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "Metrics Pipeline Scenario: **Feature Store** must handle `Pipeline condition step` while preserving `pyfunc wrapper loading`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for metrics pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "B",
          "text": "Use Feature Store for metrics pipeline scenario to handle `Pipeline condition step`."
        },
        {
          "id": "C",
          "text": "Use visual tile for metrics pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "D",
          "text": "Use admin grant for metrics pipeline scenario instead of configuring Feature Store."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because metrics pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\n**B** is correct because metrics pipeline scenario needs Feature Store specifically for `Pipeline condition step`. The documentation describes that product behavior, and `pyfunc wrapper loading` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because metrics pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\n**D** is incorrect because metrics pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\nSee [Feature Store](https://docs.aws.amazon.com/sagemaker/latest/dg/feature-store.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature Store](https://docs.aws.amazon.com/sagemaker/latest/dg/feature-store.html)"
    },
    {
      "id": "q58",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Leakage Pipeline Scenario: **CloudWatch SageMaker** must handle `multi model endpoint load` while preserving `Streamlit governed app`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use CloudWatch SageMaker for leakage pipeline scenario to handle `multi model endpoint load`."
        },
        {
          "id": "B",
          "text": "Use visual tile for leakage pipeline scenario instead of configuring CloudWatch SageMaker."
        },
        {
          "id": "C",
          "text": "Use admin grant for leakage pipeline scenario instead of configuring CloudWatch SageMaker."
        },
        {
          "id": "D",
          "text": "Use local notebook for leakage pipeline scenario instead of configuring CloudWatch SageMaker."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because leakage pipeline scenario needs CloudWatch SageMaker specifically for `multi model endpoint load`. The documentation describes that product behavior, and `Streamlit governed app` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because leakage pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\n**C** is incorrect because leakage pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\n**D** is incorrect because leakage pipeline scenario would still lack CloudWatch SageMaker; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\nSee [CloudWatch SageMaker](https://docs.aws.amazon.com/sagemaker/latest/dg/monitoring-cloudwatch.html) for the vendor documentation that supports the correct choice.",
      "reference": "[CloudWatch SageMaker](https://docs.aws.amazon.com/sagemaker/latest/dg/monitoring-cloudwatch.html)"
    },
    {
      "id": "q59",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Storage Pipeline Scenario: **EventBridge scheduling** must handle `CloudWatch endpoint alarm` while preserving `post load aggregate refresh`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for storage pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "B",
          "text": "Use local notebook for storage pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "C",
          "text": "Use driver file for storage pipeline scenario instead of configuring EventBridge scheduling."
        },
        {
          "id": "D",
          "text": "Use EventBridge scheduling for storage pipeline scenario to handle `CloudWatch endpoint alarm`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because storage pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**B** is incorrect because storage pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**C** is incorrect because storage pipeline scenario would still lack EventBridge scheduling; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**D** is correct because storage pipeline scenario needs EventBridge scheduling specifically for `CloudWatch endpoint alarm`. The documentation describes that product behavior, and `post load aggregate refresh` keeps the workflow inside the managed platform capability.\n\nSee [EventBridge scheduling](https://docs.aws.amazon.com/sagemaker/latest/dg/pipeline-eventbridge.html) for the vendor documentation that supports the correct choice.",
      "reference": "[EventBridge scheduling](https://docs.aws.amazon.com/sagemaker/latest/dg/pipeline-eventbridge.html)"
    },
    {
      "id": "q60",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Morning Pipeline Scenario: **IAM least privilege** must handle `Search service refresh` while preserving `outer flatten rows`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for morning pipeline scenario instead of configuring IAM least privilege."
        },
        {
          "id": "B",
          "text": "Use driver file for morning pipeline scenario instead of configuring IAM least privilege."
        },
        {
          "id": "C",
          "text": "Use IAM least privilege for morning pipeline scenario to handle `Search service refresh`."
        },
        {
          "id": "D",
          "text": "Use report table for morning pipeline scenario instead of configuring IAM least privilege."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because morning pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\n**B** is incorrect because morning pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\n**C** is correct because morning pipeline scenario needs IAM least privilege specifically for `Search service refresh`. The documentation describes that product behavior, and `outer flatten rows` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because morning pipeline scenario would still lack IAM least privilege; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\nSee [IAM least privilege](https://docs.aws.amazon.com/sagemaker/latest/dg/security_iam_id-based-policy-examples.html) for the vendor documentation that supports the correct choice.",
      "reference": "[IAM least privilege](https://docs.aws.amazon.com/sagemaker/latest/dg/security_iam_id-based-policy-examples.html)"
    },
    {
      "id": "q61",
      "domain": "preparing-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "Validation Pipeline Scenario: **SageMaker Processing** must handle `QUALIFY window filter` while preserving `Batch Transform manifest`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for validation pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "B",
          "text": "Use SageMaker Processing for validation pipeline scenario to handle `QUALIFY window filter`."
        },
        {
          "id": "C",
          "text": "Use report table for validation pipeline scenario instead of configuring SageMaker Processing."
        },
        {
          "id": "D",
          "text": "Use shell cron for validation pipeline scenario instead of configuring SageMaker Processing."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because validation pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `QUALIFY window filter` or preserve `Batch Transform manifest` in the supported workflow.\n\n**B** is correct because validation pipeline scenario needs SageMaker Processing specifically for `QUALIFY window filter`. The documentation describes that product behavior, and `Batch Transform manifest` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because validation pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `QUALIFY window filter` or preserve `Batch Transform manifest` in the supported workflow.\n\n**D** is incorrect because validation pipeline scenario would still lack SageMaker Processing; the suggested workaround cannot satisfy `QUALIFY window filter` or preserve `Batch Transform manifest` in the supported workflow.\n\nSee [SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Processing](https://docs.aws.amazon.com/sagemaker/latest/dg/processing-job.html)"
    },
    {
      "id": "q62",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Idle Pipeline Scenario: **AWS Glue job bookmarks** must handle `GROUPING SETS subtotal` while preserving `nested array expansion`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use AWS Glue job bookmarks for idle pipeline scenario to handle `GROUPING SETS subtotal`."
        },
        {
          "id": "B",
          "text": "Use report table for idle pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "C",
          "text": "Use shell cron for idle pipeline scenario instead of configuring AWS Glue job bookmarks."
        },
        {
          "id": "D",
          "text": "Use email approval for idle pipeline scenario instead of configuring AWS Glue job bookmarks."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because idle pipeline scenario needs AWS Glue job bookmarks specifically for `GROUPING SETS subtotal`. The documentation describes that product behavior, and `nested array expansion` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because idle pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `GROUPING SETS subtotal` or preserve `nested array expansion` in the supported workflow.\n\n**C** is incorrect because idle pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `GROUPING SETS subtotal` or preserve `nested array expansion` in the supported workflow.\n\n**D** is incorrect because idle pipeline scenario would still lack AWS Glue job bookmarks; the suggested workaround cannot satisfy `GROUPING SETS subtotal` or preserve `nested array expansion` in the supported workflow.\n\nSee [AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html) for the vendor documentation that supports the correct choice.",
      "reference": "[AWS Glue job bookmarks](https://docs.aws.amazon.com/glue/latest/dg/monitor-continuations.html)"
    },
    {
      "id": "q63",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "Status Pipeline Scenario: **SageMaker Data Wrangler** must handle `OBJECT CONSTRUCT payload` while preserving `script dependency packaging`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for status pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "B",
          "text": "Use email approval for status pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for status pipeline scenario instead of configuring SageMaker Data Wrangler."
        },
        {
          "id": "D",
          "text": "Use SageMaker Data Wrangler for status pipeline scenario to handle `OBJECT CONSTRUCT payload`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because status pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `OBJECT CONSTRUCT payload` or preserve `script dependency packaging` in the supported workflow.\n\n**B** is incorrect because status pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `OBJECT CONSTRUCT payload` or preserve `script dependency packaging` in the supported workflow.\n\n**C** is incorrect because status pipeline scenario would still lack SageMaker Data Wrangler; the suggested workaround cannot satisfy `OBJECT CONSTRUCT payload` or preserve `script dependency packaging` in the supported workflow.\n\n**D** is correct because status pipeline scenario needs SageMaker Data Wrangler specifically for `OBJECT CONSTRUCT payload`. The documentation describes that product behavior, and `script dependency packaging` keeps the workflow inside the managed platform capability.\n\nSee [SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Data Wrangler](https://docs.aws.amazon.com/sagemaker/latest/dg/data-wrangler.html)"
    },
    {
      "id": "q64",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Timeline Pipeline Scenario: **SageMaker Experiments** must handle `Access History trace` while preserving `async payload routing`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for timeline pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for timeline pipeline scenario instead of configuring SageMaker Experiments."
        },
        {
          "id": "C",
          "text": "Use SageMaker Experiments for timeline pipeline scenario to handle `Access History trace`."
        },
        {
          "id": "D",
          "text": "Use visual tile for timeline pipeline scenario instead of configuring SageMaker Experiments."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because timeline pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `Access History trace` or preserve `async payload routing` in the supported workflow.\n\n**B** is incorrect because timeline pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `Access History trace` or preserve `async payload routing` in the supported workflow.\n\n**C** is correct because timeline pipeline scenario needs SageMaker Experiments specifically for `Access History trace`. The documentation describes that product behavior, and `async payload routing` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because timeline pipeline scenario would still lack SageMaker Experiments; the suggested workaround cannot satisfy `Access History trace` or preserve `async payload routing` in the supported workflow.\n\nSee [SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html) for the vendor documentation that supports the correct choice.",
      "reference": "[SageMaker Experiments](https://docs.aws.amazon.com/sagemaker/latest/dg/experiments.html)"
    },
    {
      "id": "q65",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "SageMaker built-in algorithms are selected by problem type, input format, and training objective, such as XGBoost for tabular classification or regression.\n\nA practitioner must choose the documented **Built-in algorithms** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for columns pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "B",
          "text": "Use Built-in algorithms because the documentation supports this exact behavior: SageMaker built-in algorithms are selected by problem type, input format, and training objective, such as XGBoost for tabular classification or regression."
        },
        {
          "id": "C",
          "text": "Use visual tile for columns pipeline scenario instead of configuring Built-in algorithms."
        },
        {
          "id": "D",
          "text": "Use admin grant for columns pipeline scenario instead of configuring Built-in algorithms."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Built-in algorithms behavior described here: SageMaker built-in algorithms are selected by problem type, input format, and training objective, such as XGBoost for tabular classification or regression.\n\n**B** is correct because SageMaker built-in algorithms are selected by problem type, input format, and training objective, such as XGBoost for tabular classification or regression. This directly matches the scenario and is supported by the referenced Built-in algorithms documentation.\n\n**C** is incorrect because it does not provide the documented Built-in algorithms behavior described here: SageMaker built-in algorithms are selected by problem type, input format, and training objective, such as XGBoost for tabular classification or regression.\n\n**D** is incorrect because it does not provide the documented Built-in algorithms behavior described here: SageMaker built-in algorithms are selected by problem type, input format, and training objective, such as XGBoost for tabular classification or regression.\n\nSee [Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Built-in algorithms](https://docs.aws.amazon.com/sagemaker/latest/dg/algos.html)"
    }
  ]
}
