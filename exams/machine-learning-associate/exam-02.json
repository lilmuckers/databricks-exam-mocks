{
  "meta": {
    "id": "mla-exam-02",
    "certification": "machine-learning-associate",
    "certificationName": "Databricks Certified Machine Learning Associate",
    "title": "DBX-MLA-2: Spark ML, AutoML and Hyperparameter Tuning",
    "version": "1.0",
    "timeLimit": 90,
    "passingScore": 70,
    "totalQuestions": 45,
    "domains": [
      {
        "id": "databricks-ml",
        "name": "Databricks ML",
        "weight": 32
      },
      {
        "id": "ml-workflows",
        "name": "ML Workflows",
        "weight": 29
      },
      {
        "id": "spark-ml",
        "name": "Spark ML",
        "weight": 23
      },
      {
        "id": "scaling-ml",
        "name": "Scaling ML Models",
        "weight": 16
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist wants to start an MLflow tracking run and log a hyperparameter called `learning_rate` with value 0.01. Which code is correct?",
      "options": [
        {
          "id": "A",
          "text": "with mlflow.start_run():\n    mlflow.log_metric('learning_rate', 0.01)"
        },
        {
          "id": "B",
          "text": "with mlflow.start_run():\n    mlflow.log_param('learning_rate', 0.01)"
        },
        {
          "id": "C",
          "text": "mlflow.start_run(params={'learning_rate': 0.01})"
        },
        {
          "id": "D",
          "text": "mlflow.log_param('learning_rate', 0.01)"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**B** is correct because `mlflow.log_param()` is the correct function for logging a single key-value hyperparameter within an active run context opened by `mlflow.start_run()`.\n\n**A** is wrong because `mlflow.log_metric()` is for numeric evaluation metrics that can be tracked over time with steps (e.g., validation loss per epoch), not for hyperparameters.\n\n**C** is wrong because `mlflow.start_run()` does not accept a `params` keyword argument; hyperparameters must be logged separately with `mlflow.log_param()`.\n\n**D** is wrong because `mlflow.log_param()` requires an active run; calling it without first entering a `with mlflow.start_run():` context will raise a `MlflowException`.\n\n[MLflow Tracking — Logging Functions](https://mlflow.org/docs/latest/tracking.html)",
      "reference": "[MLflow Tracking — Logging Functions](https://mlflow.org/docs/latest/tracking.html)"
    },
    {
      "id": "q02",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Databricks AutoML function should a data scientist use to build a binary classification model predicting customer churn?",
      "options": [
        {
          "id": "A",
          "text": "databricks.automl.regress()"
        },
        {
          "id": "B",
          "text": "databricks.automl.classify()"
        },
        {
          "id": "C",
          "text": "databricks.automl.forecast()"
        },
        {
          "id": "D",
          "text": "databricks.automl.cluster()"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**B** is correct because `databricks.automl.classify()` is the AutoML function for building classification models, including binary classification tasks like churn prediction.\n\n**A** is wrong because `databricks.automl.regress()` is for regression tasks where the target is a continuous numeric value.\n\n**C** is wrong because `databricks.automl.forecast()` is for time-series forecasting tasks.\n\n**D** is wrong because `databricks.automl.cluster()` is not a valid Databricks AutoML function; AutoML does not support unsupervised clustering.\n\n[Databricks AutoML](https://docs.databricks.com/machine-learning/automl/index.html)",
      "reference": "[Databricks AutoML](https://docs.databricks.com/machine-learning/automl/index.html)"
    },
    {
      "id": "q03",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer wants to register an existing MLflow run's model in the Model Registry. The run ID is `abc123` and the model was logged to the path `model`. Which code achieves this?",
      "options": [
        {
          "id": "A",
          "text": "mlflow.register_model('runs:/abc123/model', 'my_model')"
        },
        {
          "id": "B",
          "text": "mlflow.log_model(run_id='abc123', registered_model_name='my_model')"
        },
        {
          "id": "C",
          "text": "mlflow.Model.register(run_id='abc123', path='model')"
        },
        {
          "id": "D",
          "text": "mlflow.tracking.MlflowClient().create_model_version(run_id='abc123', artifact_path='model')"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because `mlflow.register_model()` takes a model URI in the form `'runs:/<run_id>/<artifact_path>'` and a registered model name, creating a new version in the Model Registry linked to that run.\n\n**B** is wrong because `mlflow.log_model()` is used during training to log a model object; it does not register an already-logged model from a past run.\n\n**C** is wrong because `mlflow.Model.register()` is not a valid MLflow function.\n\n**D** is wrong because `MlflowClient().create_model_version()` requires both the model name and a `source` URI parameter pointing to the artifact path — the signature shown is incorrect and would raise a `TypeError`.\n\n[MLflow Model Registry](https://mlflow.org/docs/latest/model-registry.html)",
      "reference": "[MLflow Model Registry](https://mlflow.org/docs/latest/model-registry.html)"
    },
    {
      "id": "q04",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist uses `mlflow.log_metric('val_loss', loss_value, step=epoch)` inside a training loop. What is the purpose of the `step` parameter?",
      "options": [
        {
          "id": "A",
          "text": "It sets the learning rate step size for gradient descent"
        },
        {
          "id": "B",
          "text": "It specifies how many decimal places to round the metric value to"
        },
        {
          "id": "C",
          "text": "It associates the metric value with a specific training step or epoch, enabling the MLflow UI to display the metric as a time-series plot"
        },
        {
          "id": "D",
          "text": "It is required for all mlflow.log_metric() calls; omitting it raises an error"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because the optional `step` parameter in `mlflow.log_metric()` is an integer that allows the same metric key to be logged multiple times at different steps (e.g., epochs), creating a time-series that can be visualized as a chart in the MLflow UI.\n\n**A** is wrong because the `step` parameter has no relationship to gradient descent learning rate; it is purely an index for tracking metric history.\n\n**B** is wrong because MLflow does not round metric values based on a step parameter; MLflow stores metric values at full float precision.\n\n**D** is wrong because the `step` parameter is optional; omitting it defaults to `step=0` and the metric is still logged without error.\n\n[MLflow Tracking — Logging Metrics](https://mlflow.org/docs/latest/tracking.html)",
      "reference": "[MLflow Tracking — Logging Metrics](https://mlflow.org/docs/latest/tracking.html)"
    },
    {
      "id": "q05",
      "domain": "databricks-ml",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "An ML team wants to use MLflow's Model Registry to manage model lifecycle. Select TWO valid operations the Model Registry supports. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Transitioning a model version from Staging to Production"
        },
        {
          "id": "B",
          "text": "Comparing metrics across different versions of a registered model"
        },
        {
          "id": "C",
          "text": "Automatically retraining a model when new data arrives"
        },
        {
          "id": "D",
          "text": "Serving the model as a REST API with zero configuration"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "**A** is correct because one of the core Model Registry features is the ability to transition model versions through lifecycle stages (None → Staging → Production → Archived) to manage deployment promotion.\n\n**B** is correct because the MLflow UI and API allow users to view and compare metrics, parameters, and artifacts logged across different versions of a registered model.\n\n**C** is wrong because the Model Registry does not trigger or manage automated retraining; that requires external orchestration (e.g., Databricks Workflows).\n\n**D** is wrong because while Databricks Model Serving can deploy registered models, the Model Registry itself does not automatically expose a REST API with zero configuration.\n\n[MLflow Model Registry](https://mlflow.org/docs/latest/model-registry.html)",
      "reference": "[MLflow Model Registry](https://mlflow.org/docs/latest/model-registry.html)"
    },
    {
      "id": "q06",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data scientist sets up autologging with `mlflow.autolog()` before training a scikit-learn Pipeline that includes a `StandardScaler` and a `LogisticRegression`. Which of the following is TRUE about what gets logged?",
      "options": [
        {
          "id": "A",
          "text": "Only the final estimator's parameters (LogisticRegression) are logged; preprocessor parameters are ignored"
        },
        {
          "id": "B",
          "text": "mlflow.autolog() does not work with sklearn Pipelines; each stage must be logged manually"
        },
        {
          "id": "C",
          "text": "Only metrics are logged; all parameters must be logged with mlflow.log_param()"
        },
        {
          "id": "D",
          "text": "All pipeline stage parameters (including StandardScaler's with_mean, LogisticRegression's C and max_iter) and training metrics are logged automatically"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because sklearn autologging is pipeline-aware: it logs parameters from all stages in the pipeline (using the `stage_name__param_name` convention, e.g., `logisticregression__C`), not just the final estimator. Training metrics such as training accuracy are also captured automatically.\n\n**A** is wrong because sklearn autologging extracts and logs parameters from all named steps in the pipeline, including preprocessors like `StandardScaler`.\n\n**B** is wrong because sklearn autologging fully supports `Pipeline` objects; it is one of the primary use cases.\n\n**C** is wrong because autologging captures both parameters and metrics automatically without requiring manual `log_param()` calls.\n\n[MLflow Autologging](https://mlflow.org/docs/latest/tracking.html#automatic-logging)",
      "reference": "[MLflow Autologging](https://mlflow.org/docs/latest/tracking.html#automatic-logging)"
    },
    {
      "id": "q07",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "An ML engineer needs to configure a Databricks cluster for training a large PyTorch model that requires GPU acceleration. Which cluster configuration is MOST appropriate?",
      "options": [
        {
          "id": "A",
          "text": "Use the standard Databricks ML Runtime on a CPU cluster with the maximum number of cores"
        },
        {
          "id": "B",
          "text": "Use the Databricks Runtime (without ML) and install PyTorch manually via pip on a GPU instance"
        },
        {
          "id": "C",
          "text": "Use the Databricks GPU ML Runtime on a cluster with GPU instance types (e.g., p3.2xlarge on AWS)"
        },
        {
          "id": "D",
          "text": "Use the standard ML Runtime on a high-memory CPU instance; GPUs are not supported in Databricks"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because the Databricks GPU ML Runtime includes CUDA libraries and GPU-optimized builds of PyTorch, and must be paired with GPU-enabled instance types (such as p3 on AWS or NC-series on Azure) for GPU-accelerated training.\n\n**A** is wrong because a CPU cluster cannot perform GPU training regardless of the ML Runtime used.\n\n**B** is wrong because manually installing PyTorch on the standard Runtime (without ML) is possible but is not best practice; CUDA compatibility issues are common, and the GPU ML Runtime is purpose-built for this use case.\n\n**D** is wrong because Databricks fully supports GPU instances; the GPU ML Runtime is specifically designed for this scenario.\n\n[Databricks Machine Learning on GPU Clusters](https://docs.databricks.com/machine-learning/index.html)",
      "reference": "[Databricks Machine Learning on GPU Clusters](https://docs.databricks.com/machine-learning/index.html)"
    },
    {
      "id": "q08",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist uses Databricks AutoML to run a classification experiment. After the experiment completes, where can they find the best model?",
      "options": [
        {
          "id": "A",
          "text": "In a dedicated AutoML dashboard separate from MLflow"
        },
        {
          "id": "B",
          "text": "In the Databricks File System (DBFS) as a pickle file at /databricks/automl/"
        },
        {
          "id": "C",
          "text": "In the MLflow experiment linked to the AutoML run, where the best trial is identified by the lowest validation metric"
        },
        {
          "id": "D",
          "text": "The best model must be manually identified by inspecting all trial logs"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because Databricks AutoML logs all trials to an MLflow experiment and clearly identifies the best run based on the primary metric. The result object returned by the AutoML call also exposes `.best_trial` for programmatic access.\n\n**A** is wrong because AutoML uses the MLflow experiment UI; there is no separate AutoML dashboard.\n\n**B** is wrong because AutoML does not save models as raw pickle files on DBFS; models are logged to MLflow as model artifacts.\n\n**D** is wrong because AutoML automatically identifies and surfaces the best trial; manual inspection is not required.\n\n[Databricks AutoML](https://docs.databricks.com/machine-learning/automl/index.html)",
      "reference": "[Databricks AutoML](https://docs.databricks.com/machine-learning/automl/index.html)"
    },
    {
      "id": "q09",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A model version in the MLflow Model Registry is currently in 'Production'. An engineer wants to retire it because a newer version will replace it. Which stage should they transition it to?",
      "options": [
        {
          "id": "A",
          "text": "None"
        },
        {
          "id": "B",
          "text": "Archived"
        },
        {
          "id": "C",
          "text": "Deprecated"
        },
        {
          "id": "D",
          "text": "Retired"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**B** is correct because 'Archived' is the designated MLflow Model Registry stage for model versions that are no longer active but should be retained for audit and reproducibility purposes. Archiving a Production model is the standard step when promoting a newer version.\n\n**A** is wrong because 'None' is the initial stage for a newly registered model version; transitioning a Production model back to None is not the conventional retirement path and is not recommended practice.\n\n**C** is wrong because 'Deprecated' is not a valid MLflow Model Registry stage.\n\n**D** is wrong because 'Retired' is not a valid MLflow Model Registry stage.\n\n[MLflow Model Registry — Stage Transitions](https://mlflow.org/docs/latest/model-registry.html)",
      "reference": "[MLflow Model Registry — Stage Transitions](https://mlflow.org/docs/latest/model-registry.html)"
    },
    {
      "id": "q10",
      "domain": "databricks-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist is comparing Databricks AutoML to manually building a model. Select TWO TRUE statements about Databricks AutoML. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "AutoML explores multiple algorithms and hyperparameter combinations, logging each as a separate MLflow run"
        },
        {
          "id": "B",
          "text": "AutoML generates a source notebook for the best run that can be cloned and customized"
        },
        {
          "id": "C",
          "text": "AutoML supports custom neural network architectures defined in PyTorch"
        },
        {
          "id": "D",
          "text": "AutoML automatically handles feature engineering for all data types including unstructured text and images"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "**A** is correct because Databricks AutoML systematically explores a configurable set of algorithms (e.g., LightGBM, scikit-learn models, XGBoost) and hyperparameter combinations, logging each trial as a separate MLflow run for full observability.\n\n**B** is correct because at the end of an AutoML experiment, Databricks generates an editable Python notebook containing the full code for the best run, which data scientists can clone, inspect, and customize to meet their needs.\n\n**C** is wrong because AutoML does not support custom neural network architectures; it uses a fixed set of supported classical ML algorithms.\n\n**D** is wrong because AutoML is designed for structured tabular data and does not perform feature engineering on unstructured data such as raw images or free-form text.\n\n[Databricks AutoML](https://docs.databricks.com/machine-learning/automl/index.html)",
      "reference": "[Databricks AutoML](https://docs.databricks.com/machine-learning/automl/index.html)"
    },
    {
      "id": "q11",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist needs to scale numeric features to have zero mean and unit variance before training a Support Vector Machine. Which sklearn transformer should they use?",
      "options": [
        {
          "id": "A",
          "text": "MinMaxScaler"
        },
        {
          "id": "B",
          "text": "LabelEncoder"
        },
        {
          "id": "C",
          "text": "Normalizer"
        },
        {
          "id": "D",
          "text": "StandardScaler"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because `StandardScaler` standardizes features by subtracting the mean and dividing by the standard deviation, resulting in features with zero mean and unit variance — the canonical preprocessing for SVMs and other distance-based algorithms.\n\n**A** is wrong because `MinMaxScaler` scales features to a fixed range (default [0, 1]) but does not produce zero mean and unit variance.\n\n**B** is wrong because `LabelEncoder` converts class labels to integer indices; it is used for target encoding, not feature scaling.\n\n**C** is wrong because `Normalizer` scales each sample (row) to unit norm, not each feature (column) to zero mean and unit variance.\n\n[scikit-learn StandardScaler](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.StandardScaler.html)",
      "reference": "[scikit-learn StandardScaler](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.StandardScaler.html)"
    },
    {
      "id": "q12",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist is building a time-series model to predict daily sales. They need to create train and test sets. Why should they NOT use random shuffling when splitting this dataset?",
      "options": [
        {
          "id": "A",
          "text": "Random shuffling causes class imbalance in time-series data"
        },
        {
          "id": "B",
          "text": "sklearn's train_test_split does not support time-series data and raises an error"
        },
        {
          "id": "C",
          "text": "Random shuffling reduces the size of the training set"
        },
        {
          "id": "D",
          "text": "Random shuffling breaks the temporal order, causing future data to leak into the training set, which inflates performance estimates"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because time-series data has a temporal dependency structure: future values are correlated with past values. Random shuffling mixes future observations into the training set, allowing the model to learn from data it would not have access to in production. The correct approach is to use a chronological split (all training data before a cutoff date, test data after).\n\n**A** is wrong because class imbalance is a separate issue unrelated to the temporal ordering problem in time-series splitting.\n\n**B** is wrong because sklearn's `train_test_split` works with any array; however, it must be called with `shuffle=False` for time-series data to preserve temporal order.\n\n**C** is wrong because the size of the train/test split is controlled by the `test_size` parameter, not by whether shuffling is used.\n\n[scikit-learn Cross-validation: Time Series](https://scikit-learn.org/stable/modules/cross_validation.html#time-series-split)",
      "reference": "[scikit-learn Cross-validation: Time Series](https://scikit-learn.org/stable/modules/cross_validation.html#time-series-split)"
    },
    {
      "id": "q13",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist trains a model with 5-fold cross-validation using `cross_val_score`. The scores are [0.82, 0.85, 0.79, 0.88, 0.84]. What is the best interpretation of these results?",
      "options": [
        {
          "id": "A",
          "text": "The model will achieve exactly 83.6% accuracy on new data"
        },
        {
          "id": "B",
          "text": "The model has an estimated generalization accuracy of approximately 0.836 ± some variance, providing a more robust estimate than a single train/test split"
        },
        {
          "id": "C",
          "text": "The best fold score of 0.88 should be used as the model's performance estimate"
        },
        {
          "id": "D",
          "text": "The model fails cross-validation because the scores vary across folds"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**B** is correct because k-fold cross-validation trains and evaluates the model k times on different data subsets, producing k performance estimates whose mean (here ≈ 0.836) and standard deviation provide a more statistically robust estimate of generalization performance than a single split. The variation across folds is normal and informative.\n\n**A** is wrong because cross-validation provides an estimate with uncertainty; exact performance on unseen data cannot be guaranteed.\n\n**C** is wrong because cherry-picking the best fold overstates performance; the mean across all folds is the appropriate estimate.\n\n**D** is wrong because variation across folds is expected and normal, not a sign of failure.\n\n[scikit-learn Cross-validation](https://scikit-learn.org/stable/modules/cross_validation.html)",
      "reference": "[scikit-learn Cross-validation](https://scikit-learn.org/stable/modules/cross_validation.html)"
    },
    {
      "id": "q14",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data scientist is selecting between two models for a medical screening test: Model A has precision=0.90, recall=0.60, and Model B has precision=0.70, recall=0.90. The goal is to identify as many true positive cases as possible while minimizing patients who need additional testing. Which model should they choose and why?",
      "options": [
        {
          "id": "A",
          "text": "Model A, because higher precision (0.90) means fewer patients are unnecessarily referred for additional testing"
        },
        {
          "id": "B",
          "text": "Both models are equivalent because their F1 scores are the same"
        },
        {
          "id": "C",
          "text": "Model A, because precision is always more important than recall in medical applications"
        },
        {
          "id": "D",
          "text": "Model B, because higher recall (0.90) ensures fewer true positives are missed, which is critical in medical screening where missing a case is more costly than a false alarm"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because in medical screening, the primary concern is minimizing false negatives (missing a true positive case). Recall measures the fraction of actual positives correctly identified. Model B's recall of 0.90 means it catches 90% of true cases, compared to only 60% for Model A. The tradeoff is more false alarms (lower precision), but for a screening test the cost of a missed diagnosis typically far exceeds the cost of follow-up testing.\n\n**A** is wrong because while Model A has fewer false alarms, missing 40% of true cases is clinically unacceptable in a screening context.\n\n**B** is wrong because Model A F1 = 2*(0.90*0.60)/(0.90+0.60) ≈ 0.72 and Model B F1 = 2*(0.70*0.90)/(0.70+0.90) ≈ 0.79; they are not equal.\n\n**C** is wrong because precision vs. recall tradeoffs are domain-specific; in screening contexts, recall is typically prioritized to avoid missed diagnoses.\n\n[scikit-learn Precision, Recall and F1](https://scikit-learn.org/stable/modules/model_evaluation.html#precision-recall-and-f-measures)",
      "reference": "[scikit-learn Precision, Recall and F1](https://scikit-learn.org/stable/modules/model_evaluation.html#precision-recall-and-f-measures)"
    },
    {
      "id": "q15",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist uses a sklearn Pipeline and wants to search over both the imputation strategy and the number of estimators in a RandomForest. Which `param_grid` syntax is correct for `GridSearchCV`?",
      "options": [
        {
          "id": "A",
          "text": "{'strategy': ['mean', 'median'], 'n_estimators': [100, 200]}"
        },
        {
          "id": "B",
          "text": "{'imputer__strategy': ['mean', 'median'], 'rf__n_estimators': [100, 200]}"
        },
        {
          "id": "C",
          "text": "{'SimpleImputer.strategy': ['mean', 'median'], 'RandomForest.n_estimators': [100, 200]}"
        },
        {
          "id": "D",
          "text": "{'imputer.strategy': ['mean', 'median'], 'rf.n_estimators': [100, 200]}"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**B** is correct because when tuning parameters inside a sklearn Pipeline with `GridSearchCV`, parameter names follow the convention `stepname__paramname` (double underscore), where `stepname` is the name given to the stage when constructing the Pipeline (e.g., `Pipeline([('imputer', SimpleImputer()), ('rf', RandomForestClassifier())])`).\n\n**A** is wrong because bare parameter names without the step prefix are ambiguous and would raise an error with Pipeline — GridSearchCV cannot determine which stage the parameter belongs to.\n\n**C** is wrong because sklearn uses double underscores (`__`) as the separator, not a dot (`.`).\n\n**D** is wrong because sklearn pipeline parameter references use double underscores (`__`), not single dots (`.`).\n\n[scikit-learn Pipeline and GridSearchCV](https://scikit-learn.org/stable/modules/compose.html#nested-parameters)",
      "reference": "[scikit-learn Pipeline and GridSearchCV](https://scikit-learn.org/stable/modules/compose.html#nested-parameters)"
    },
    {
      "id": "q16",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist has a pandas DataFrame and wants to compute the mean of the `revenue` column grouped by `region`. Which pandas code is correct?",
      "options": [
        {
          "id": "A",
          "text": "df.mean('revenue').groupby('region')"
        },
        {
          "id": "B",
          "text": "df.aggregate('revenue', by='region', func='mean')"
        },
        {
          "id": "C",
          "text": "df.groupby('region')['revenue'].mean()"
        },
        {
          "id": "D",
          "text": "df.pivot('region', 'revenue', aggfunc='mean')"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because `df.groupby('region')['revenue'].mean()` groups the DataFrame by the 'region' column, selects the 'revenue' column, and computes the mean within each group — the standard pandas groupby aggregation pattern.\n\n**A** is wrong because `df.mean()` is called on the whole DataFrame first and does not accept a column name as a positional argument; this would fail.\n\n**B** is wrong because `df.aggregate()` does not accept a `by` parameter in this form; the correct usage is `df.groupby().agg()`.\n\n**D** is wrong because `df.pivot()` reshapes a DataFrame from long to wide format; it does not perform groupby aggregation in the described manner.\n\n[pandas GroupBy](https://pandas.pydata.org/docs/user_guide/groupby.html)",
      "reference": "[pandas GroupBy](https://pandas.pydata.org/docs/user_guide/groupby.html)"
    },
    {
      "id": "q17",
      "domain": "ml-workflows",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist is selecting features for a gradient-boosted tree model. Select TWO valid methods for assessing feature importance with tree-based models. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Examining feature_importances_ attribute of the fitted model (e.g., RandomForestClassifier().fit().feature_importances_)"
        },
        {
          "id": "B",
          "text": "Calculating SHAP values to obtain per-sample directional feature contributions"
        },
        {
          "id": "C",
          "text": "Running a chi-squared test on all features against the target for any data type"
        },
        {
          "id": "D",
          "text": "Computing the Pearson correlation coefficient between each feature and the target as the primary importance measure for tree models"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "**A** is correct because sklearn tree-based models expose a `feature_importances_` array after fitting, which ranks features by their contribution to reducing impurity (e.g., Gini impurity) across all trees, providing a fast global importance measure.\n\n**B** is correct because SHAP (SHapley Additive exPlanations) provides theoretically grounded per-sample importance values with directional information, and has efficient implementations (TreeSHAP) specifically for gradient-boosted and random forest models.\n\n**C** is wrong because the chi-squared test is only valid for non-negative categorical features against categorical targets; it is not a general-purpose feature importance method for tree models with mixed data types.\n\n**D** is wrong because Pearson correlation measures linear relationships and is not appropriate as a primary importance measure for non-linear tree models; it would miss important non-linear interactions.\n\n[scikit-learn Feature Importances](https://scikit-learn.org/stable/modules/ensemble.html#feature-importance-evaluation)",
      "reference": "[scikit-learn Feature Importances](https://scikit-learn.org/stable/modules/ensemble.html#feature-importance-evaluation)"
    },
    {
      "id": "q18",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A regression model is being evaluated and shows RMSE = 50, MAE = 30. What can be inferred from the fact that RMSE is significantly larger than MAE?",
      "options": [
        {
          "id": "A",
          "text": "The model has some large prediction errors (outliers), since RMSE penalizes large errors more heavily than MAE"
        },
        {
          "id": "B",
          "text": "The model is overfitting the training data"
        },
        {
          "id": "C",
          "text": "RMSE and MAE should always be equal for a well-calibrated model"
        },
        {
          "id": "D",
          "text": "The target variable has high variance that is unrelated to model quality"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because RMSE squares individual errors before averaging and then takes the square root, making it disproportionately sensitive to large errors. When RMSE is significantly larger than MAE, it is a sign that the model makes some very large prediction errors (outliers in the residuals), even if its typical error (captured by MAE) is moderate.\n\n**B** is wrong because the RMSE-MAE gap indicates the presence of large errors, not necessarily overfitting (which would be identified by comparing train vs. test metrics).\n\n**C** is wrong because RMSE and MAE are equal only when all errors are identical in magnitude (no variance in errors), which is not a general requirement for a well-calibrated model.\n\n**D** is wrong because the target variable's variance affects metrics differently but does not explain the RMSE-MAE gap on its own; the gap specifically reflects large individual residuals.\n\n[scikit-learn Regression Metrics](https://scikit-learn.org/stable/modules/model_evaluation.html#regression-metrics)",
      "reference": "[scikit-learn Regression Metrics](https://scikit-learn.org/stable/modules/model_evaluation.html#regression-metrics)"
    },
    {
      "id": "q19",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist has a feature column with 15% missing values. They decide to fill missing values with the median of that column. Which sklearn class should they use?",
      "options": [
        {
          "id": "A",
          "text": "KNNImputer(strategy='median')"
        },
        {
          "id": "B",
          "text": "StandardScaler(with_mean=False)"
        },
        {
          "id": "C",
          "text": "IterativeImputer(strategy='median')"
        },
        {
          "id": "D",
          "text": "SimpleImputer(strategy='median')"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because sklearn's `SimpleImputer` with `strategy='median'` replaces missing values with the column median computed from the training set. It is the simplest and most commonly used imputation strategy for numeric features.\n\n**A** is wrong because `KNNImputer` does not accept a `strategy` parameter; it uses k-nearest neighbor distances to impute missing values, which is a different, more complex approach.\n\n**B** is wrong because `StandardScaler` is a feature scaling transformer, not an imputer; it does not handle missing values and will raise an error if `NaN` values are present.\n\n**C** is wrong because `IterativeImputer` does not accept a `strategy='median'` parameter; it uses iterative model-based imputation and is configured differently.\n\n[scikit-learn SimpleImputer](https://scikit-learn.org/stable/modules/generated/sklearn.impute.SimpleImputer.html)",
      "reference": "[scikit-learn SimpleImputer](https://scikit-learn.org/stable/modules/generated/sklearn.impute.SimpleImputer.html)"
    },
    {
      "id": "q20",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist calls `pipeline.fit(train_df)` on a Spark ML Pipeline. What object is returned?",
      "options": [
        {
          "id": "A",
          "text": "A new Pipeline object with updated parameters"
        },
        {
          "id": "B",
          "text": "A Spark DataFrame with predictions added as a new column"
        },
        {
          "id": "C",
          "text": "A list of individual fitted models, one per stage"
        },
        {
          "id": "D",
          "text": "A PipelineModel that contains fitted versions of all Estimator stages"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because calling `.fit()` on a Spark ML `Pipeline` returns a `PipelineModel`, which is itself a `Transformer`. The `PipelineModel` contains the fitted (transformed) versions of all `Estimator` stages, and calling `.transform()` on it applies each stage in sequence.\n\n**A** is wrong because `.fit()` does not return a new `Pipeline`; it returns a `PipelineModel`.\n\n**B** is wrong because `.fit()` learns from the data but does not produce predictions; `.transform()` is used to generate predictions/transformations on data.\n\n**C** is wrong because `.fit()` returns a single unified `PipelineModel`, not a list of separate models.\n\n[Spark ML Pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html)",
      "reference": "[Spark ML Pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html)"
    },
    {
      "id": "q21",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer needs to encode the categorical column `city` (with values 'NYC', 'LA', 'Chicago') for use in a Spark ML gradient-boosted tree model. What is the correct sequence of transformers?",
      "options": [
        {
          "id": "A",
          "text": "OneHotEncoder directly on the string column"
        },
        {
          "id": "B",
          "text": "VectorAssembler to combine the string column with numeric columns"
        },
        {
          "id": "C",
          "text": "StringIndexer to convert strings to numeric indices, then OneHotEncoder to create binary vector columns"
        },
        {
          "id": "D",
          "text": "Bucketizer to split city names into numeric buckets"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because Spark ML's `OneHotEncoder` requires numeric index input, not raw strings. The correct pipeline is: (1) `StringIndexer` converts the string column to a numeric index (e.g., NYC→0, LA→1, Chicago→2), then (2) `OneHotEncoder` converts those indices to binary vectors, avoiding the false ordinal relationship implied by raw integer indices.\n\n**A** is wrong because Spark ML's `OneHotEncoder` does not accept string columns directly; it requires the output of `StringIndexer`.\n\n**B** is wrong because `VectorAssembler` assembles numeric columns into a feature vector but cannot process raw string columns.\n\n**D** is wrong because `Bucketizer` works on continuous numeric columns and cannot process string values.\n\n[Spark ML Feature Transformers](https://spark.apache.org/docs/latest/ml-features.html#stringindexer)",
      "reference": "[Spark ML Feature Transformers](https://spark.apache.org/docs/latest/ml-features.html#stringindexer)"
    },
    {
      "id": "q22",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer trained a Spark ML `RandomForestClassifier` and wants to evaluate it on a test set. The target has three classes. Which evaluator and metric is MOST appropriate?",
      "options": [
        {
          "id": "A",
          "text": "BinaryClassificationEvaluator with metricName='areaUnderROC'"
        },
        {
          "id": "B",
          "text": "RegressionEvaluator with metricName='rmse'"
        },
        {
          "id": "C",
          "text": "MulticlassClassificationEvaluator with metricName='f1'"
        },
        {
          "id": "D",
          "text": "BinaryClassificationEvaluator with metricName='areaUnderPR'"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because `MulticlassClassificationEvaluator` is designed for multi-class classification problems (more than two classes), and `metricName='f1'` computes the weighted F1 score across all classes, making it appropriate for a 3-class problem.\n\n**A** is wrong because `BinaryClassificationEvaluator` is specifically for binary (two-class) classification and computes ROC-AUC for a single positive class; it is not valid for 3 classes.\n\n**B** is wrong because `RegressionEvaluator` computes metrics like RMSE for continuous targets, not class labels.\n\n**D** is wrong because `BinaryClassificationEvaluator` does not support multi-class problems regardless of the metric name.\n\n[Spark ML Classification and Regression](https://spark.apache.org/docs/latest/ml-classification-regression.html)",
      "reference": "[Spark ML Classification and Regression](https://spark.apache.org/docs/latest/ml-classification-regression.html)"
    },
    {
      "id": "q23",
      "domain": "spark-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist is building a Spark ML pipeline for a regression task and needs to tune hyperparameters. Select TWO tools in Spark ML that support hyperparameter tuning. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "CrossValidator with ParamGridBuilder"
        },
        {
          "id": "B",
          "text": "TrainValidationSplit with ParamGridBuilder"
        },
        {
          "id": "C",
          "text": "GridSearchCV from sklearn"
        },
        {
          "id": "D",
          "text": "PipelineModel.tune() method"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "**A** is correct because Spark ML's `CrossValidator` performs k-fold cross-validation over a parameter grid built with `ParamGridBuilder`, evaluating each parameter combination across all folds to find the best set of hyperparameters.\n\n**B** is correct because `TrainValidationSplit` is a Spark ML tuning class that uses a single train/validation split (faster than k-fold CV) combined with a `ParamGridBuilder` grid to select the best hyperparameters.\n\n**C** is wrong because sklearn's `GridSearchCV` operates on single-node scikit-learn estimators, not on Spark ML Pipelines or Spark DataFrames.\n\n**D** is wrong because `PipelineModel.tune()` is not a valid Spark ML method.\n\n[Spark ML Tuning](https://spark.apache.org/docs/latest/ml-tuning.html)",
      "reference": "[Spark ML Tuning](https://spark.apache.org/docs/latest/ml-tuning.html)"
    },
    {
      "id": "q24",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "An ML engineer uses `TrainValidationSplit` in Spark ML instead of `CrossValidator`. What is the main tradeoff compared to `CrossValidator`?",
      "options": [
        {
          "id": "A",
          "text": "TrainValidationSplit performs more model training runs and is slower than CrossValidator"
        },
        {
          "id": "B",
          "text": "TrainValidationSplit supports more algorithms than CrossValidator"
        },
        {
          "id": "C",
          "text": "CrossValidator requires a larger ParamGrid while TrainValidationSplit works with any size grid"
        },
        {
          "id": "D",
          "text": "TrainValidationSplit is faster (evaluates each param combo once) but produces a less reliable estimate than CrossValidator's k-fold averaging"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because `TrainValidationSplit` splits the data once into train and validation sets and evaluates each parameter combination a single time, which is computationally cheaper than `CrossValidator`. However, `CrossValidator`'s k-fold approach averages performance over k different data partitions, giving a more robust and reliable estimate of true generalization performance. The tradeoff is speed (`TrainValidationSplit`) vs. statistical reliability (`CrossValidator`).\n\n**A** is wrong because `TrainValidationSplit` performs fewer training runs (one per param combo) than `CrossValidator` (k runs per param combo).\n\n**B** is wrong because both classes support the same set of Spark ML estimators.\n\n**C** is wrong because both classes use `ParamGridBuilder` with no size restrictions.\n\n[Spark ML Tuning](https://spark.apache.org/docs/latest/ml-tuning.html)",
      "reference": "[Spark ML Tuning](https://spark.apache.org/docs/latest/ml-tuning.html)"
    },
    {
      "id": "q25",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist uses the Spark ML `SQLTransformer`. What does this transformer do?",
      "options": [
        {
          "id": "A",
          "text": "It applies a SQL SELECT statement to the input DataFrame, enabling SQL-based feature engineering within a Spark ML Pipeline"
        },
        {
          "id": "B",
          "text": "It converts a Spark SQL table into a feature vector for ML algorithms"
        },
        {
          "id": "C",
          "text": "It runs a SQL query against an external database and loads the results"
        },
        {
          "id": "D",
          "text": "It validates that a DataFrame conforms to a predefined SQL schema"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because `SQLTransformer` takes a SQL statement (referencing the input DataFrame as `__THIS__`) and applies it as a transformation, making it easy to add derived features, filter rows, or rename columns using SQL syntax within a Spark ML Pipeline. For example: `SQLTransformer(statement='SELECT *, age * income AS age_income FROM __THIS__')`.\n\n**B** is wrong because converting columns to a feature vector is the job of `VectorAssembler`, not `SQLTransformer`.\n\n**C** is wrong because `SQLTransformer` operates on an in-memory Spark DataFrame; it does not connect to external databases.\n\n**D** is wrong because `SQLTransformer` applies a transformation, it does not validate schema conformance.\n\n[Spark ML SQLTransformer](https://spark.apache.org/docs/latest/ml-features.html#sqltransformer)",
      "reference": "[Spark ML SQLTransformer](https://spark.apache.org/docs/latest/ml-features.html#sqltransformer)"
    },
    {
      "id": "q26",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Spark ML algorithm is appropriate for a binary classification problem where the features are numeric and the engineer wants a probabilistic output?",
      "options": [
        {
          "id": "A",
          "text": "LinearRegression"
        },
        {
          "id": "B",
          "text": "KMeans"
        },
        {
          "id": "C",
          "text": "LogisticRegression"
        },
        {
          "id": "D",
          "text": "PCA"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because Spark ML's `LogisticRegression` is a classification algorithm that outputs class probabilities (via the `probability` column) in addition to predicted class labels, making it appropriate for binary classification with probabilistic output.\n\n**A** is wrong because `LinearRegression` is for continuous target prediction (regression), not binary classification.\n\n**B** is wrong because `KMeans` is an unsupervised clustering algorithm that groups data into k clusters, not a supervised binary classifier.\n\n**D** is wrong because `PCA` (Principal Component Analysis) is a dimensionality reduction technique, not a classification algorithm.\n\n[Spark ML Classification and Regression](https://spark.apache.org/docs/latest/ml-classification-regression.html)",
      "reference": "[Spark ML Classification and Regression](https://spark.apache.org/docs/latest/ml-classification-regression.html)"
    },
    {
      "id": "q27",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer uses `mlflow.pyfunc.spark_udf()` to apply a registered model for batch inference on a Spark DataFrame. Which code correctly applies the UDF to create a predictions column?",
      "options": [
        {
          "id": "A",
          "text": "predict = mlflow.pyfunc.spark_udf(spark, model_uri)\nresult_df = df.withColumn('predictions', predict(struct(*df.columns)))"
        },
        {
          "id": "B",
          "text": "result_df = mlflow.pyfunc.predict(df, model_uri)"
        },
        {
          "id": "C",
          "text": "result_df = df.apply(mlflow.pyfunc.load_model(model_uri).predict)"
        },
        {
          "id": "D",
          "text": "predict = mlflow.pyfunc.spark_udf(spark, model_uri)\nresult_df = df.select(predict('*').alias('predictions'))"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because `mlflow.pyfunc.spark_udf()` returns a Spark UDF that accepts a struct of feature columns. The standard usage is to wrap all feature columns in a `struct()` call and apply the UDF via `withColumn()`.\n\n**B** is wrong because `mlflow.pyfunc.predict()` is not a valid function; the `pyfunc` module does not have a standalone `predict()` that accepts a Spark DataFrame.\n\n**C** is wrong because `df.apply()` is a pandas method and does not work on Spark DataFrames; it would also fail to distribute inference.\n\n**D** is wrong because `predict('*')` is not valid syntax for a Spark UDF; the UDF requires an explicit struct of the input columns.\n\n[MLflow pyfunc spark_udf](https://mlflow.org/docs/latest/python_api/mlflow.pyfunc.html)",
      "reference": "[MLflow pyfunc spark_udf](https://mlflow.org/docs/latest/python_api/mlflow.pyfunc.html)"
    },
    {
      "id": "q28",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist is working in a Databricks notebook and imports `pyspark.pandas`. What is the legacy package name that `pyspark.pandas` replaced?",
      "options": [
        {
          "id": "A",
          "text": "pyspark.pandas_udf"
        },
        {
          "id": "B",
          "text": "databricks.pandas"
        },
        {
          "id": "C",
          "text": "pyspark.dataframe.pandas"
        },
        {
          "id": "D",
          "text": "databricks.koalas"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because the Pandas API on Spark (`pyspark.pandas`) was introduced in Spark 3.2 as the official integration, replacing the Koalas project (`databricks.koalas`) that Databricks had previously maintained. Existing Koalas code can generally be migrated by changing the import from `databricks.koalas` to `pyspark.pandas`.\n\n**A** is wrong because `pyspark.pandas_udf` is not a package; `pandas_udf` is a decorator in `pyspark.sql.functions` for creating vectorized UDFs.\n\n**B** is wrong because `databricks.pandas` is not a real package.\n\n**C** is wrong because `pyspark.dataframe.pandas` is not a real package.\n\n[Pandas API on Spark (Koalas migration)](https://spark.apache.org/docs/latest/api/python/migration_guide/koalas_to_pyspark.html)",
      "reference": "[Pandas API on Spark (Koalas migration)](https://spark.apache.org/docs/latest/api/python/migration_guide/koalas_to_pyspark.html)"
    },
    {
      "id": "q29",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer wants to use HorovodRunner to train a PyTorch model across 4 worker nodes in a Databricks cluster. What parallelism strategy does Horovod use?",
      "options": [
        {
          "id": "A",
          "text": "Model parallelism: different layers of the model are placed on different workers"
        },
        {
          "id": "B",
          "text": "Ensemble parallelism: each worker trains a completely different model and results are averaged"
        },
        {
          "id": "C",
          "text": "Pipeline parallelism: the forward pass runs on some workers and the backward pass on others"
        },
        {
          "id": "D",
          "text": "Data parallelism: each worker trains on a different shard of the data using the same model, and gradients are synchronized across workers"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because Horovod implements data-parallel distributed training: the full model is replicated on each worker, each worker processes a different shard of the training data, and gradients are synchronized across workers using an all-reduce operation (ring-allreduce) before updating model weights. This allows training to scale with the number of workers.\n\n**A** is wrong because model parallelism places different parts of the model on different devices, which is a different strategy and is not what Horovod implements by default.\n\n**B** is wrong because ensemble parallelism would train independent models, not the same model collaboratively, which is not the Horovod approach.\n\n**C** is wrong because pipeline parallelism is a specific optimization technique for very large models, not the general Horovod strategy.\n\n[Databricks Distributed Training with Horovod](https://docs.databricks.com/machine-learning/train-model/distributed-training/horovod-runner.html)",
      "reference": "[Databricks Distributed Training with Horovod](https://docs.databricks.com/machine-learning/train-model/distributed-training/horovod-runner.html)"
    },
    {
      "id": "q30",
      "domain": "scaling-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist is choosing between Pandas API on Spark and native PySpark for data preprocessing. Select TWO scenarios where native PySpark is PREFERABLE to Pandas API on Spark. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Processing a 500 GB dataset with complex window functions and custom aggregations"
        },
        {
          "id": "B",
          "text": "When the team needs full control over Spark execution plans and partition-level optimizations"
        },
        {
          "id": "C",
          "text": "When the team is comfortable with pandas syntax and the dataset fits within Spark's in-memory processing without pandas-specific edge cases"
        },
        {
          "id": "D",
          "text": "When rapid prototyping with familiar pandas code on a small sample of data"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "**A** is correct because native PySpark is better suited for very large datasets with complex aggregations and window functions, where Catalyst optimizer pushdowns and explicit partition control yield significantly better performance than the Pandas API on Spark wrapper.\n\n**B** is correct because native PySpark gives engineers direct access to the Catalyst execution plan (via `.explain()`), partition management (`repartition`, `coalesce`), and fine-grained optimization that is abstracted away in the Pandas API on Spark.\n\n**C** is wrong because if the team is comfortable with pandas syntax and the processing does not require native PySpark-specific optimizations, the Pandas API on Spark is a reasonable and productive choice.\n\n**D** is wrong because rapid prototyping with familiar pandas code is a key use case and strength of the Pandas API on Spark; native PySpark is not preferable here.\n\n[Pandas API on Spark](https://spark.apache.org/docs/latest/api/python/reference/pyspark.pandas/index.html)",
      "reference": "[Pandas API on Spark](https://spark.apache.org/docs/latest/api/python/reference/pyspark.pandas/index.html)"
    },
    {
      "id": "q31",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "An ML engineer wants to permanently delete an MLflow experiment and all of its runs. Which function call correctly soft-deletes the experiment (moves it to the 'Deleted' lifecycle stage)?",
      "options": [
        {
          "id": "A",
          "text": "mlflow.remove_experiment(experiment_id)"
        },
        {
          "id": "B",
          "text": "mlflow.tracking.MlflowClient().archive_experiment(experiment_id)"
        },
        {
          "id": "C",
          "text": "mlflow.delete_experiment(experiment_id)"
        },
        {
          "id": "D",
          "text": "mlflow.set_experiment_status(experiment_id, 'DELETED')"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because `mlflow.delete_experiment(experiment_id)` soft-deletes the experiment by moving it to the 'Deleted' lifecycle stage. The experiment and its runs are no longer visible in the active experiments list but are retained in storage and can be restored programmatically.\n\n**A** is wrong because `mlflow.remove_experiment()` is not a valid MLflow function.\n\n**B** is wrong because `MlflowClient().archive_experiment()` is not a valid method; the correct client method is `delete_experiment()`.\n\n**D** is wrong because `mlflow.set_experiment_status()` is not a valid MLflow function.\n\n[MLflow Tracking — Experiment Management](https://mlflow.org/docs/latest/tracking.html)",
      "reference": "[MLflow Tracking — Experiment Management](https://mlflow.org/docs/latest/tracking.html)"
    },
    {
      "id": "q32",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist uses `mlflow.search_experiments(filter_string=\"name LIKE 'churn%'\")`. What does this call return?",
      "options": [
        {
          "id": "A",
          "text": "A list of Experiment objects whose names begin with 'churn', with each object containing the experiment ID, name, artifact location, and lifecycle stage"
        },
        {
          "id": "B",
          "text": "A pandas DataFrame of all runs in experiments whose names begin with 'churn'"
        },
        {
          "id": "C",
          "text": "The single Experiment object with the exact name 'churn'"
        },
        {
          "id": "D",
          "text": "A list of run IDs from experiments matching the name pattern"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because `mlflow.search_experiments()` returns a list of `mlflow.entities.Experiment` objects that match the filter_string. Each object contains metadata such as `experiment_id`, `name`, `artifact_location`, and `lifecycle_stage`. The `LIKE` operator with `%` is a SQL-style wildcard for pattern matching on experiment names.\n\n**B** is wrong because `mlflow.search_experiments()` returns experiment-level objects, not runs or a pandas DataFrame; use `mlflow.search_runs()` to query runs.\n\n**C** is wrong because the `LIKE 'churn%'` pattern can match multiple experiments, not just one with the exact name 'churn'.\n\n**D** is wrong because the function returns `Experiment` objects, not run IDs.\n\n[MLflow Tracking — Search Experiments](https://mlflow.org/docs/latest/tracking.html)",
      "reference": "[MLflow Tracking — Search Experiments](https://mlflow.org/docs/latest/tracking.html)"
    },
    {
      "id": "q33",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer is adding a GPU node type to a Databricks cluster for deep learning. On AWS, which instance family is commonly used for GPU-based Databricks clusters?",
      "options": [
        {
          "id": "A",
          "text": "m5 instance family (e.g., m5.4xlarge), which includes multi-core CPUs optimized for memory-intensive workloads"
        },
        {
          "id": "B",
          "text": "r6i instance family (e.g., r6i.8xlarge), which provides high memory bandwidth for GPU training"
        },
        {
          "id": "C",
          "text": "c5 instance family (e.g., c5.9xlarge), which offers high CPU clock speeds ideal for GPU acceleration"
        },
        {
          "id": "D",
          "text": "p3 or p4 instance families (e.g., p3.2xlarge), which include NVIDIA V100 or A100 GPUs"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because AWS p3 instances (e.g., p3.2xlarge, p3.8xlarge) come with NVIDIA V100 GPUs and are the standard choice for GPU-accelerated deep learning on Databricks. Newer p4 instances include A100 GPUs for even higher performance. The Databricks GPU ML Runtime is configured to work with these instance types.\n\n**A** is wrong because m5 instances are general-purpose CPU instances with no GPUs; they are not suitable for GPU-accelerated training.\n\n**B** is wrong because r6i instances are memory-optimized CPU instances without GPUs; high memory bandwidth refers to RAM, not GPU memory.\n\n**C** is wrong because c5 instances are compute-optimized CPU instances and do not include GPUs.\n\n[Databricks Machine Learning — GPU Clusters](https://docs.databricks.com/machine-learning/index.html)",
      "reference": "[Databricks Machine Learning — GPU Clusters](https://docs.databricks.com/machine-learning/index.html)"
    },
    {
      "id": "q34",
      "domain": "databricks-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "An ML team accesses the MLflow tracking server programmatically. Select TWO operations that can be performed using `mlflow.tracking.MlflowClient()`. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Transitioning a registered model version's stage using client.transition_model_version_stage()"
        },
        {
          "id": "B",
          "text": "Searching for runs within an experiment using client.search_runs()"
        },
        {
          "id": "C",
          "text": "Training a new model and automatically logging it to MLflow"
        },
        {
          "id": "D",
          "text": "Deploying a registered model to a REST endpoint directly"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "**A** is correct because `MlflowClient.transition_model_version_stage(name, version, stage)` is a valid client method that changes a registered model version's lifecycle stage (e.g., from Staging to Production).\n\n**B** is correct because `MlflowClient.search_runs(experiment_ids, filter_string)` is a valid method for querying runs within specified experiments, returning a list of `Run` objects.\n\n**C** is wrong because `MlflowClient` is a tracking and registry management client; model training and automatic logging are handled by `mlflow.autolog()` or explicit `log_*` calls, not by `MlflowClient`.\n\n**D** is wrong because deploying a model to a REST endpoint is handled by Databricks Model Serving, not by `MlflowClient`.\n\n[MLflow MlflowClient](https://mlflow.org/docs/latest/python_api/mlflow.client.html)",
      "reference": "[MLflow MlflowClient](https://mlflow.org/docs/latest/python_api/mlflow.client.html)"
    },
    {
      "id": "q35",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data scientist wants to use the MLflow UI to compare hyperparameters and metrics across 20 runs in an experiment. Which MLflow feature allows them to select multiple runs and view them in a parallel coordinates plot?",
      "options": [
        {
          "id": "A",
          "text": "mlflow.compare_runs(), a Python function that generates an interactive comparison plot"
        },
        {
          "id": "B",
          "text": "The MLflow Model Registry's version comparison page"
        },
        {
          "id": "C",
          "text": "A dedicated Databricks notebook cell magic (%mlflow_compare) for rendering comparison charts"
        },
        {
          "id": "D",
          "text": "The MLflow Experiment UI's 'Compare' view, which renders parallel coordinates and scatter matrix plots for selected runs"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because the MLflow Experiment UI allows users to select multiple runs and click 'Compare' to open a comparison view that includes parallel coordinates plots (showing how hyperparameters relate to metric values across runs), scatter matrix plots, and contour plots. This is a built-in UI feature requiring no additional code.\n\n**A** is wrong because `mlflow.compare_runs()` is not a valid MLflow Python function.\n\n**B** is wrong because the Model Registry's version page shows metadata about registered model versions, not the multi-run comparison UI for experiment runs.\n\n**C** is wrong because `%mlflow_compare` is not a valid Databricks notebook magic command.\n\n[MLflow Experiment Tracking UI](https://mlflow.org/docs/latest/tracking.html)",
      "reference": "[MLflow Experiment Tracking UI](https://mlflow.org/docs/latest/tracking.html)"
    },
    {
      "id": "q36",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist applies `LabelEncoder` to a multi-class target column with values ['low', 'medium', 'high']. What is a known pitfall when using `LabelEncoder` on ordinal features passed as model input features (not the target column)?",
      "options": [
        {
          "id": "A",
          "text": "LabelEncoder can only encode binary (two-class) columns; it raises an error for more than 2 categories"
        },
        {
          "id": "B",
          "text": "LabelEncoder is equivalent to OneHotEncoder and produces the same output"
        },
        {
          "id": "C",
          "text": "LabelEncoder automatically infers the correct ordinal order from the string values alphabetically"
        },
        {
          "id": "D",
          "text": "LabelEncoder assigns arbitrary integer codes that may imply false ordinal relationships between categories, misleading linear models and distance-based algorithms"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because `LabelEncoder` maps categories to integers (e.g., 'high'→0, 'low'→1, 'medium'→2, sorted alphabetically) without respecting the true ordinal order. When used as a feature (not the target), linear models and SVMs will interpret the integers as having a quantitative relationship (e.g., 'medium' is twice 'high'), which is incorrect for nominal categories. For ordinal encoding with a meaningful order, `OrdinalEncoder` with explicit categories should be used.\n\n**A** is wrong because `LabelEncoder` handles any number of unique values, not just binary.\n\n**B** is wrong because `LabelEncoder` produces a single integer column while `OneHotEncoder` produces multiple binary columns; they are fundamentally different.\n\n**C** is wrong because `LabelEncoder` sorts alphabetically, which rarely corresponds to the correct semantic order (e.g., 'high' < 'low' < 'medium' alphabetically, which is wrong for the intended order).\n\n[scikit-learn LabelEncoder](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.LabelEncoder.html)",
      "reference": "[scikit-learn LabelEncoder](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.LabelEncoder.html)"
    },
    {
      "id": "q37",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist interprets a confusion matrix for a binary classifier:\n\n|  | Predicted Positive | Predicted Negative |\n|---|---|---|\n| Actual Positive | 80 | 20 |\n| Actual Negative | 10 | 890 |\n\nWhat is the precision and recall of this model?",
      "options": [
        {
          "id": "A",
          "text": "Precision = 80/(80+20) = 0.800; Recall = 80/(80+10) = 0.889"
        },
        {
          "id": "B",
          "text": "Precision = 890/(890+20) = 0.978; Recall = 80/(80+10) = 0.889"
        },
        {
          "id": "C",
          "text": "Precision = 80/1000 = 0.080; Recall = 80/100 = 0.800"
        },
        {
          "id": "D",
          "text": "Precision = 80/(80+10) = 0.889; Recall = 80/(80+20) = 0.800"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because Precision = TP/(TP+FP) = 80/(80+10) = 0.889, where TP=80 (correctly predicted positive) and FP=10 (actual negative incorrectly predicted as positive). Recall = TP/(TP+FN) = 80/(80+20) = 0.800, where FN=20 (actual positive incorrectly predicted as negative).\n\n**A** is wrong because it swaps the precision and recall formulas: Precision = TP/(TP+FP), not TP/(TP+FN).\n\n**B** is wrong because it incorrectly uses true negatives in the precision calculation; precision is not about negative predictions.\n\n**C** is wrong because precision is not computed as TP divided by total samples; it is TP/(TP+FP).\n\n[scikit-learn Precision and Recall](https://scikit-learn.org/stable/modules/model_evaluation.html#precision-recall-and-f-measures)",
      "reference": "[scikit-learn Precision and Recall](https://scikit-learn.org/stable/modules/model_evaluation.html#precision-recall-and-f-measures)"
    },
    {
      "id": "q38",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist applies `StratifiedKFold` vs `KFold` to a dataset with 5 classes where one class has only 3 samples. Which is the critical difference in behavior?",
      "options": [
        {
          "id": "A",
          "text": "StratifiedKFold is faster than KFold for large datasets"
        },
        {
          "id": "B",
          "text": "KFold preserves class distributions while StratifiedKFold randomizes them"
        },
        {
          "id": "C",
          "text": "StratifiedKFold and KFold produce identical folds when the dataset is large enough"
        },
        {
          "id": "D",
          "text": "StratifiedKFold ensures each fold has at least one sample from the rare class; KFold may create folds where the rare class is completely absent, causing errors during training"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because `StratifiedKFold` ensures that the proportion of samples for each class is approximately the same in each fold. For a class with only 3 samples and 5 folds, `StratifiedKFold` will distribute those 3 samples across folds as evenly as possible. `KFold` randomly partitions data without regard to class, so a rare class with 3 samples may end up entirely in one fold while other folds have none, causing issues with metrics that require at least one positive sample per fold.\n\n**A** is wrong because both use similar computational complexity; speed is not the key difference.\n\n**B** is wrong because `KFold` does not preserve class distributions; it is `StratifiedKFold` that does.\n\n**C** is wrong because even for large datasets, when rare classes exist, `StratifiedKFold` consistently outperforms `KFold` in maintaining class balance per fold.\n\n[scikit-learn StratifiedKFold](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.StratifiedKFold.html)",
      "reference": "[scikit-learn StratifiedKFold](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.StratifiedKFold.html)"
    },
    {
      "id": "q39",
      "domain": "ml-workflows",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist is building a Pipeline with `SelectKBest` for feature selection followed by a `RandomForestClassifier`. Select TWO correct statements about using `SelectKBest` inside a Pipeline during cross-validation. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "When placed inside a Pipeline, SelectKBest is fit only on the training fold data in each CV iteration, correctly preventing the selected features from being influenced by validation fold data"
        },
        {
          "id": "B",
          "text": "SelectKBest must be applied before the Pipeline to avoid compatibility issues"
        },
        {
          "id": "C",
          "text": "The number of features k can be tuned as a Pipeline hyperparameter using GridSearchCV with {'selector__k': [5, 10, 20]}"
        },
        {
          "id": "D",
          "text": "SelectKBest inside a Pipeline always produces worse results than applying it outside because the Pipeline restricts which features can be selected"
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "**A** is correct because placing `SelectKBest` inside a Pipeline means that during each cross-validation fold, `SelectKBest.fit()` is called only on the training fold, and then `transform()` applies the learned feature selection to the validation fold. This prevents any information from validation data from influencing which features are selected.\n\n**B** is wrong because `SelectKBest` is fully compatible inside a sklearn Pipeline and is the recommended pattern to avoid feature selection leakage.\n\n**C** is correct because when the `SelectKBest` step in a Pipeline is named (e.g., 'selector'), the double-underscore convention allows its `k` parameter to be included in the `GridSearchCV` `param_grid` as `'selector__k'`, enabling `k` to be tuned alongside other hyperparameters.\n\n**D** is wrong because using `SelectKBest` inside a Pipeline correctly gives better (more honest) results than applying it outside because it avoids feature selection leakage.\n\n[scikit-learn Pipeline with Feature Selection](https://scikit-learn.org/stable/modules/feature_selection.html#feature-selection-as-part-of-a-pipeline)",
      "reference": "[scikit-learn Pipeline with Feature Selection](https://scikit-learn.org/stable/modules/feature_selection.html#feature-selection-as-part-of-a-pipeline)"
    },
    {
      "id": "q40",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data scientist has a balanced 4-class classification problem and computes `precision_score(y_true, y_pred, average='weighted')`. Class 0 has 250 samples, class 1 has 250, class 2 has 250, class 3 has 250. If the per-class precisions are [0.80, 0.70, 0.90, 0.60], what is the weighted precision?",
      "options": [
        {
          "id": "A",
          "text": "0.90 (the maximum per-class precision)"
        },
        {
          "id": "B",
          "text": "0.60 (the minimum per-class precision, as weighted average penalizes poor classes)"
        },
        {
          "id": "C",
          "text": "0.75 (equal weights since all classes have the same support: (0.80+0.70+0.90+0.60)/4)"
        },
        {
          "id": "D",
          "text": "1.00 (weighted precision is always 1.0 for balanced datasets)"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because when all classes have exactly the same support (250 samples each), weighted average and macro average produce identical results. The weighted precision is the sum of (support_i / total_samples) * precision_i = (250/1000)*0.80 + (250/1000)*0.70 + (250/1000)*0.90 + (250/1000)*0.60 = 0.25*(0.80+0.70+0.90+0.60) = 0.25*3.00 = 0.75.\n\n**A** is wrong because 0.90 is only the best individual class precision, not the weighted average.\n\n**B** is wrong because weighted average is not a minimum; it is a weighted mean across all classes.\n\n**D** is wrong because weighted precision equals 1.0 only when every class has perfect precision, not merely because the dataset is balanced.\n\n[scikit-learn precision_score](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.precision_score.html)",
      "reference": "[scikit-learn precision_score](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.precision_score.html)"
    },
    {
      "id": "q41",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Spark ML transformer would a data scientist use to select the top K features based on chi-squared statistics between features and a categorical label?",
      "options": [
        {
          "id": "A",
          "text": "SelectKBest with score_func=chi2 and k=K"
        },
        {
          "id": "B",
          "text": "VectorIndexer with maxCategories=K"
        },
        {
          "id": "C",
          "text": "ChiSqSelector with numTopFeatures=K"
        },
        {
          "id": "D",
          "text": "PCA with k=K components"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**C** is correct because Spark ML provides `ChiSqSelector`, which computes chi-squared statistics between each feature and the label column to rank features by their dependence on the target, then selects the top `numTopFeatures` (or a percentage of features, or features above a p-value threshold) and outputs a reduced feature vector.\n\n**A** is wrong because `SelectKBest` with `chi2` is a scikit-learn function, not a Spark ML transformer; it operates on pandas DataFrames and single-node data, not Spark DataFrames.\n\n**B** is wrong because `VectorIndexer` identifies and categorizes categorical features by their number of unique values (`maxCategories`), not by chi-squared statistics.\n\n**D** is wrong because `PCA` reduces dimensionality using principal components based on variance, not chi-squared statistics between features and the label.\n\n[Spark ML ChiSqSelector](https://spark.apache.org/docs/latest/ml-features.html#chisqselector)",
      "reference": "[Spark ML ChiSqSelector](https://spark.apache.org/docs/latest/ml-features.html#chisqselector)"
    },
    {
      "id": "q42",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer trains a Spark ML `Word2Vec` model on a corpus of product reviews. What does `Word2Vec` produce as output, and what is the `vectorSize` parameter?",
      "options": [
        {
          "id": "A",
          "text": "Word2Vec produces a sparse TF-IDF vector for each document; vectorSize specifies the number of unique terms in the vocabulary"
        },
        {
          "id": "B",
          "text": "Word2Vec tokenizes sentences into word arrays; vectorSize specifies the maximum number of tokens per sentence"
        },
        {
          "id": "C",
          "text": "Word2Vec computes the average document length in tokens; vectorSize specifies the averaging window"
        },
        {
          "id": "D",
          "text": "Word2Vec maps each word to a dense embedding vector; vectorSize specifies the dimensionality of those embedding vectors"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because Spark ML's `Word2Vec` learns dense embedding vectors for each word in the vocabulary using the Word2Vec algorithm (skip-gram or CBOW). After training, it transforms documents by averaging the embedding vectors of all tokens in each document, producing a single dense vector per document. The `vectorSize` parameter controls the dimensionality of these embedding vectors (e.g., `vectorSize=100` produces 100-dimensional embeddings).\n\n**A** is wrong because TF-IDF vectors are sparse and produced by `HashingTF`/`IDF`, not `Word2Vec`; `Word2Vec` produces dense embeddings.\n\n**B** is wrong because tokenization is handled by `Tokenizer` or `RegexTokenizer`; `Word2Vec` takes token arrays as input and produces embeddings.\n\n**C** is wrong because `Word2Vec` does not compute document length; it produces learned semantic embeddings.\n\n[Spark ML Word2Vec](https://spark.apache.org/docs/latest/ml-features.html#word2vec)",
      "reference": "[Spark ML Word2Vec](https://spark.apache.org/docs/latest/ml-features.html#word2vec)"
    },
    {
      "id": "q43",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer needs to evaluate a regression model in Spark ML and wants to compute the Mean Absolute Error (MAE). Which evaluator and metric name should they use?",
      "options": [
        {
          "id": "A",
          "text": "RegressionEvaluator with metricName='meanAbsoluteError'"
        },
        {
          "id": "B",
          "text": "BinaryClassificationEvaluator with metricName='mae'"
        },
        {
          "id": "C",
          "text": "MulticlassClassificationEvaluator with metricName='mae'"
        },
        {
          "id": "D",
          "text": "RegressionEvaluator with metricName='mae'"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because Spark ML's `RegressionEvaluator` supports the following `metricName` values: `'rmse'` (root mean squared error), `'mse'` (mean squared error), `'mae'` (mean absolute error), and `'r2'` (R-squared). Using `metricName='mae'` computes the mean absolute error between predictions and actual values.\n\n**A** is wrong because `'meanAbsoluteError'` is not a valid `metricName` string; the correct string is the abbreviated `'mae'`.\n\n**B** is wrong because `BinaryClassificationEvaluator` is for binary classification tasks and does not support `'mae'`.\n\n**C** is wrong because `MulticlassClassificationEvaluator` is for multi-class classification and does not compute regression metrics like MAE.\n\n[Spark ML RegressionEvaluator](https://spark.apache.org/docs/latest/ml-tuning.html#example-model-selection-via-train-validation-split)",
      "reference": "[Spark ML RegressionEvaluator](https://spark.apache.org/docs/latest/ml-tuning.html#example-model-selection-via-train-validation-split)"
    },
    {
      "id": "q44",
      "domain": "spark-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "An ML engineer is comparing `TrainValidationSplit` and `CrossValidator` in Spark ML for hyperparameter tuning. Select TWO TRUE statements about `TrainValidationSplit`. (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "TrainValidationSplit evaluates each parameter combination exactly once using a single train/validation split defined by the trainRatio parameter"
        },
        {
          "id": "B",
          "text": "TrainValidationSplit requires fewer total model fits than CrossValidator with k folds for the same parameter grid"
        },
        {
          "id": "C",
          "text": "TrainValidationSplit automatically selects a better model than CrossValidator because it uses more data for training"
        },
        {
          "id": "D",
          "text": "TrainValidationSplit and CrossValidator both produce a CrossValidatorModel as their fitted output type"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "**A** is correct because `TrainValidationSplit` uses a single train/validation split with the proportion controlled by `trainRatio` (e.g., `trainRatio=0.8` uses 80% for training and 20% for validation). Each parameter combination is evaluated exactly once on this split.\n\n**B** is correct because for a grid of N parameter combinations and `CrossValidator` with k folds, `CrossValidator` requires N*k model fits, while `TrainValidationSplit` requires only N fits (one per combination). This makes `TrainValidationSplit` computationally less expensive.\n\n**C** is wrong because having more training data does not guarantee a better model; `CrossValidator`'s k-fold averaging typically provides a more reliable model selection because it evaluates performance across multiple data partitions.\n\n**D** is wrong because `CrossValidator` produces a `CrossValidatorModel` while `TrainValidationSplit` produces a `TrainValidationSplitModel`; they are different output types.\n\n[Spark ML Tuning](https://spark.apache.org/docs/latest/ml-tuning.html)",
      "reference": "[Spark ML Tuning](https://spark.apache.org/docs/latest/ml-tuning.html)"
    },
    {
      "id": "q45",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer uses `mapInPandas` on a Spark DataFrame to apply a batch inference function. What is the key difference between `mapInPandas` and `applyInPandas`?",
      "options": [
        {
          "id": "A",
          "text": "mapInPandas requires a groupBy; applyInPandas works on arbitrary partitions"
        },
        {
          "id": "B",
          "text": "mapInPandas can only be used for batch inference; applyInPandas can only be used for training"
        },
        {
          "id": "C",
          "text": "mapInPandas and applyInPandas are interchangeable; the names differ only for historical reasons"
        },
        {
          "id": "D",
          "text": "mapInPandas processes arbitrary partitions as pandas DataFrames and does not require a groupBy; applyInPandas requires a groupBy and passes each group as a pandas DataFrame"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**D** is correct because `mapInPandas` is called directly on a DataFrame and maps a function over each partition, receiving an iterator of pandas DataFrames (one per partition) without any prior grouping. This makes it ideal for batch inference where the full model is applied to each partition independently. `applyInPandas` is called on a `GroupedData` object (after `groupBy`) and passes each group's data as a single pandas DataFrame to the function, making it appropriate for per-group operations like per-group model training.\n\n**A** is wrong because it reverses the descriptions.\n\n**B** is wrong because both functions can be used for either training or inference depending on what the user-defined function does; the distinction is about grouping semantics, not the use case type.\n\n**C** is wrong because `mapInPandas` and `applyInPandas` have fundamentally different grouping semantics and are not interchangeable.\n\n[PySpark mapInPandas and applyInPandas](https://spark.apache.org/docs/latest/api/python/reference/pyspark.pandas/index.html)",
      "reference": "[PySpark mapInPandas and applyInPandas](https://spark.apache.org/docs/latest/api/python/reference/pyspark.pandas/index.html)"
    }
  ]
}
