{
  "meta": {
    "id": "mla-exam-01",
    "certification": "machine-learning-associate",
    "certificationName": "Databricks Certified Machine Learning Associate",
    "title": "Mock Exam 1",
    "version": "1.0",
    "timeLimit": 90,
    "passingScore": 70,
    "totalQuestions": 45,
    "domains": [
      {
        "id": "databricks-ml",
        "name": "Databricks ML",
        "weight": 32
      },
      {
        "id": "ml-workflows",
        "name": "ML Workflows",
        "weight": 29
      },
      {
        "id": "spark-ml",
        "name": "Spark ML",
        "weight": 23
      },
      {
        "id": "scaling-ml",
        "name": "Scaling ML Models",
        "weight": 16
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which of the following machine learning libraries comes pre-installed in the Databricks ML Runtime?",
      "options": [
        {
          "id": "A",
          "text": "scikit-learn, MLflow, and XGBoost"
        },
        {
          "id": "B",
          "text": "scikit-learn, MLflow, and Apache Kafka"
        },
        {
          "id": "C",
          "text": "MLflow, Apache Flink, and PyTorch"
        },
        {
          "id": "D",
          "text": "TensorFlow, Apache Hudi, and scikit-learn"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the Databricks ML Runtime pre-installs a curated set of ML libraries including scikit-learn, MLflow, XGBoost, LightGBM, TensorFlow, and PyTorch. B is wrong because Apache Kafka is a streaming platform, not an ML library, and is not bundled in the ML Runtime. C is wrong because Apache Flink is a separate stream-processing engine not included in the Databricks ML Runtime. D is wrong because Apache Hudi is a data lake storage format, not an ML library, and is not part of the ML Runtime bundle.",
      "reference": "Databricks ML Runtime"
    },
    {
      "id": "q02",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist wants to track the hyperparameters and evaluation metrics for a model training run using MLflow. Which two function calls should they use?",
      "options": [
        {
          "id": "A",
          "text": "mlflow.log_param() for hyperparameters and mlflow.log_metric() for metrics"
        },
        {
          "id": "B",
          "text": "mlflow.record_param() for hyperparameters and mlflow.record_metric() for metrics"
        },
        {
          "id": "C",
          "text": "mlflow.log_param() for both hyperparameters and metrics"
        },
        {
          "id": "D",
          "text": "mlflow.set_tag() for hyperparameters and mlflow.log_metric() for metrics"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because MLflow provides distinct functions: mlflow.log_param() is used for logging scalar hyperparameters (e.g., learning rate, max depth) and mlflow.log_metric() is used for logging numeric evaluation metrics (e.g., accuracy, RMSE) that can be tracked over time with optional steps. B is wrong because mlflow.record_param() and mlflow.record_metric() are not valid MLflow functions. C is wrong because mlflow.log_param() is only for parameters, not metrics. D is wrong because mlflow.set_tag() stores string key-value metadata tags, not numeric hyperparameter values.",
      "reference": "MLflow Tracking API"
    },
    {
      "id": "q03",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist runs the following code:\n\n```python\nimport mlflow\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.datasets import load_iris\nfrom sklearn.model_selection import train_test_split\n\nX, y = load_iris(return_X_y=True)\nX_train, X_test, y_train, y_test = train_test_split(X, y, random_state=42)\n\nmlflow.sklearn.autolog()\n\nwith mlflow.start_run():\n    clf = RandomForestClassifier(n_estimators=100, max_depth=5)\n    clf.fit(X_train, y_train)\n```\n\nWhich items are automatically logged by MLflow autologging for sklearn?",
      "options": [
        {
          "id": "A",
          "text": "Model parameters (n_estimators, max_depth), training metrics, and the fitted model as an artifact"
        },
        {
          "id": "B",
          "text": "Model parameters only; metrics and artifacts must be logged manually"
        },
        {
          "id": "C",
          "text": "The fitted model artifact only; parameters and metrics must be logged manually"
        },
        {
          "id": "D",
          "text": "Model parameters, training metrics, the fitted model, and the raw training dataset"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because mlflow.sklearn.autolog() automatically captures estimator parameters (such as n_estimators and max_depth), training metrics (such as training accuracy), and saves the fitted model as a logged artifact. B is wrong because autolog captures more than just parameters. C is wrong because autolog captures parameters and metrics, not just the model. D is wrong because autolog does NOT automatically log the raw training dataset; that must be done manually with mlflow.log_artifact() or mlflow datasets API.",
      "reference": "MLflow Autologging for scikit-learn"
    },
    {
      "id": "q04",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer uses Databricks AutoML to build a classification model. Which of the following statements correctly describes what AutoML produces upon completion?",
      "options": [
        {
          "id": "A",
          "text": "A summary of trial runs, the best model registered in MLflow Model Registry, and an editable notebook with the best run's code"
        },
        {
          "id": "B",
          "text": "A single production-ready model deployed behind a REST endpoint with no further action needed"
        },
        {
          "id": "C",
          "text": "A summary of trial runs and the best model artifact; no notebook is generated"
        },
        {
          "id": "D",
          "text": "A set of feature engineering recommendations but no trained models"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Databricks AutoML runs multiple trials, logs each run to MLflow, registers the best model in the MLflow Model Registry, and generates a source notebook containing the full pipeline code for the winning run so the data scientist can inspect and customize it. B is wrong because AutoML does not automatically deploy a REST endpoint; deployment is a separate step. C is wrong because AutoML does generate an editable notebook for the best run. D is wrong because AutoML actually trains models, it does not only make recommendations.",
      "reference": "Databricks AutoML"
    },
    {
      "id": "q05",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "Which of the following correctly describes the MLflow Model Registry stage lifecycle?",
      "options": [
        {
          "id": "A",
          "text": "None → Staging → Production → Archived"
        },
        {
          "id": "B",
          "text": "Draft → Testing → Production → Deprecated"
        },
        {
          "id": "C",
          "text": "Registered → Validated → Deployed → Retired"
        },
        {
          "id": "D",
          "text": "None → Development → Staging → Production"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the MLflow Model Registry defines four stages for model versions: None (newly registered, no stage), Staging (candidate for production, used for validation), Production (actively serving traffic), and Archived (retired version). B is wrong because Draft, Testing, and Deprecated are not valid MLflow Registry stages. C is wrong because Registered, Validated, Deployed, and Retired are not the correct stage names in MLflow. D is wrong because Development is not a valid MLflow Registry stage.",
      "reference": "MLflow Model Registry Stages"
    },
    {
      "id": "q06",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data scientist logs a file called `confusion_matrix.png` during a training run. Which MLflow call is correct?",
      "options": [
        {
          "id": "A",
          "text": "mlflow.log_artifact('confusion_matrix.png')"
        },
        {
          "id": "B",
          "text": "mlflow.log_metric('confusion_matrix.png')"
        },
        {
          "id": "C",
          "text": "mlflow.log_param('confusion_matrix', 'confusion_matrix.png')"
        },
        {
          "id": "D",
          "text": "mlflow.log_model('confusion_matrix.png', 'image')"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because mlflow.log_artifact() is used to save local files (images, plots, CSVs, etc.) to the run's artifact store. B is wrong because mlflow.log_metric() is for numeric scalar values, not file paths. C is wrong because mlflow.log_param() stores string key-value pairs describing hyperparameters, not files. D is wrong because mlflow.log_model() is for serializing and logging a trained ML model object, not an image file.",
      "reference": "MLflow Artifacts"
    },
    {
      "id": "q07",
      "domain": "databricks-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A team wants to use Databricks AutoML for a regression task. Select TWO limitations that apply to Databricks AutoML.",
      "options": [
        {
          "id": "A",
          "text": "AutoML cannot perform time-series forecasting"
        },
        {
          "id": "B",
          "text": "AutoML does not support custom loss functions"
        },
        {
          "id": "C",
          "text": "AutoML only supports structured (tabular) data, not unstructured data like raw text or images"
        },
        {
          "id": "D",
          "text": "AutoML cannot log runs to MLflow"
        }
      ],
      "correct": [
        "B",
        "C"
      ],
      "explanation": "B is correct because Databricks AutoML uses a fixed set of algorithms and evaluation metrics; users cannot inject custom loss functions into AutoML trials. C is correct because AutoML is designed for structured tabular data and does not natively handle unstructured inputs such as raw image pixels or free-form text without prior feature extraction. A is wrong because Databricks AutoML does support time-series forecasting via databricks.automl.forecast(). D is wrong because AutoML automatically logs all trial runs to MLflow and registers the best model.",
      "reference": "Databricks AutoML Limitations"
    },
    {
      "id": "q08",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "What is the primary difference between the Databricks ML Runtime and the Databricks GPU ML Runtime?",
      "options": [
        {
          "id": "A",
          "text": "The GPU ML Runtime includes CUDA libraries and GPU-enabled builds of deep learning frameworks like TensorFlow and PyTorch"
        },
        {
          "id": "B",
          "text": "The GPU ML Runtime includes additional SQL analytics libraries not present in the standard ML Runtime"
        },
        {
          "id": "C",
          "text": "The GPU ML Runtime removes MLflow and replaces it with TensorBoard"
        },
        {
          "id": "D",
          "text": "The GPU ML Runtime is identical to the standard ML Runtime but runs on a faster CPU"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the Databricks GPU ML Runtime is configured with NVIDIA CUDA, cuDNN, and GPU-optimized builds of deep learning frameworks (TensorFlow, PyTorch), enabling GPU-accelerated training. B is wrong because the GPU ML Runtime is optimized for deep learning, not SQL analytics. C is wrong because MLflow is present in both runtimes; TensorBoard does not replace it. D is wrong because the GPU ML Runtime uses actual GPU instances, not faster CPUs.",
      "reference": "Databricks ML Runtime vs GPU ML Runtime"
    },
    {
      "id": "q09",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer registers a newly trained model in the MLflow Model Registry with the following code:\n\n```python\nmlflow.sklearn.log_model(\n    sk_model=model,\n    artifact_path='model',\n    registered_model_name='churn_predictor'\n)\n```\n\nWhat stage will this model version be in immediately after registration?",
      "options": [
        {
          "id": "A",
          "text": "None"
        },
        {
          "id": "B",
          "text": "Staging"
        },
        {
          "id": "C",
          "text": "Production"
        },
        {
          "id": "D",
          "text": "Development"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because when a model is first registered in the MLflow Model Registry (whether via log_model with registered_model_name or mlflow.register_model()), its initial stage is 'None'. The engineer must explicitly transition it to Staging or Production. B is wrong because Staging must be explicitly set after registration. C is wrong because Production must be explicitly set and is not assigned automatically. D is wrong because Development is not a valid MLflow Model Registry stage.",
      "reference": "MLflow Model Registry"
    },
    {
      "id": "q10",
      "domain": "databricks-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist enables autologging with mlflow.autolog() before training a scikit-learn model. Select TWO items that are NOT automatically logged by sklearn autologging.",
      "options": [
        {
          "id": "A",
          "text": "Model hyperparameters (e.g., C, max_iter)"
        },
        {
          "id": "B",
          "text": "Custom business metrics computed after prediction (e.g., revenue impact)"
        },
        {
          "id": "C",
          "text": "Training dataset schema and raw data"
        },
        {
          "id": "D",
          "text": "Test set evaluation metrics"
        }
      ],
      "correct": [
        "B",
        "C"
      ],
      "explanation": "B is correct because custom business metrics calculated outside the model training loop (such as revenue impact from predictions) are not captured by autologging; they require explicit mlflow.log_metric() calls. C is correct because sklearn autologging does not log the raw training dataset or its schema; only model parameters and built-in training metrics are captured automatically. A is wrong because model hyperparameters are automatically captured by sklearn autologging. D is wrong because test set metrics are logged when the user calls model.score() or cross_val_score() inside an active autolog context.",
      "reference": "MLflow Autologging for scikit-learn"
    },
    {
      "id": "q11",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist has a dataset with a highly imbalanced binary target (95% negative, 5% positive). Which evaluation metric is MOST appropriate for assessing model performance on the positive class?",
      "options": [
        {
          "id": "A",
          "text": "F1 score"
        },
        {
          "id": "B",
          "text": "Accuracy"
        },
        {
          "id": "C",
          "text": "Mean Squared Error"
        },
        {
          "id": "D",
          "text": "R-squared"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because F1 score is the harmonic mean of precision and recall, making it sensitive to performance on the minority positive class. It is well-suited for imbalanced datasets where simply predicting the majority class would yield misleadingly high accuracy. B is wrong because a model that predicts all negatives would achieve 95% accuracy without ever correctly identifying a positive case. C is wrong because Mean Squared Error is a regression metric, not a classification metric. D is wrong because R-squared is also a regression metric.",
      "reference": "Evaluation Metrics for Imbalanced Classification"
    },
    {
      "id": "q12",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist needs to create a train/test split while ensuring that the proportion of each class label is preserved in both sets. Which sklearn function and parameter achieves this?",
      "options": [
        {
          "id": "A",
          "text": "train_test_split(X, y, stratify=y)"
        },
        {
          "id": "B",
          "text": "train_test_split(X, y, shuffle=False)"
        },
        {
          "id": "C",
          "text": "train_test_split(X, y, random_state=42)"
        },
        {
          "id": "D",
          "text": "train_test_split(X, y, test_size=0.2, balanced=True)"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the stratify parameter in train_test_split accepts the target array y and ensures that each split preserves the same class distribution as the original dataset, which is critical for imbalanced problems. B is wrong because shuffle=False disables shuffling before splitting, which is useful for time series but does not control class proportions. C is wrong because random_state only controls reproducibility of the random split, not class distribution. D is wrong because balanced=True is not a valid parameter for train_test_split.",
      "reference": "sklearn train_test_split stratify"
    },
    {
      "id": "q13",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist is using GridSearchCV on a pipeline with StandardScaler and LogisticRegression. The data has 10,000 rows and cv=5. Why is it important to include StandardScaler inside the pipeline rather than fitting it on the full dataset before GridSearchCV?",
      "options": [
        {
          "id": "A",
          "text": "Fitting StandardScaler before cross-validation causes data leakage because the scaler learns statistics from validation fold data"
        },
        {
          "id": "B",
          "text": "StandardScaler cannot be used with GridSearchCV"
        },
        {
          "id": "C",
          "text": "Including StandardScaler in the pipeline makes cross-validation run faster"
        },
        {
          "id": "D",
          "text": "StandardScaler must be fit separately before being passed to a pipeline"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because if StandardScaler is fit on the entire dataset before cross-validation, the scaler learns mean and variance from validation fold data that the model should not have seen during training. This constitutes data leakage, leading to overly optimistic performance estimates. By placing StandardScaler inside the pipeline, GridSearchCV ensures the scaler is fit only on training folds during each CV iteration. B is wrong because StandardScaler works perfectly within sklearn pipelines and GridSearchCV. C is wrong because the pipeline does not speed up cross-validation. D is wrong because StandardScaler is designed to be used inside pipelines.",
      "reference": "Avoiding Data Leakage in Cross-Validation"
    },
    {
      "id": "q14",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist is building a churn prediction model and wants to encode a high-cardinality categorical feature (e.g., zip code with 1,000 unique values). Which encoding strategy is most appropriate?",
      "options": [
        {
          "id": "A",
          "text": "Target encoding or hashing, to avoid creating 1,000 binary columns"
        },
        {
          "id": "B",
          "text": "OneHotEncoder, to create one binary column per unique value"
        },
        {
          "id": "C",
          "text": "OrdinalEncoder assigning integers 0–999 and treating as numeric"
        },
        {
          "id": "D",
          "text": "No encoding is needed; tree models handle categorical strings natively in sklearn"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because target encoding summarizes a categorical feature with target-related statistics (e.g., mean of the target per category), and hashing encodes values into a fixed-size vector, both of which handle high-cardinality features without dimensional explosion. B is wrong because OneHotEncoding 1,000 unique zip codes would create 1,000 binary columns, causing the curse of dimensionality and memory issues. C is wrong because assigning arbitrary ordinal integers to nominal categories like zip codes implies a false ordering that tree models and distance-based models may misinterpret. D is wrong because scikit-learn estimators do not natively handle string categoricals; encoding is required.",
      "reference": "Encoding High-Cardinality Categorical Features"
    },
    {
      "id": "q15",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "Which of the following code snippets correctly uses RandomizedSearchCV to tune a RandomForestClassifier?",
      "options": [
        {
          "id": "A",
          "text": "RandomizedSearchCV(RandomForestClassifier(), param_distributions={'n_estimators': [100, 200], 'max_depth': [3, 5, None]}, n_iter=6, cv=5)"
        },
        {
          "id": "B",
          "text": "RandomizedSearchCV(RandomForestClassifier(), param_grid={'n_estimators': [100, 200], 'max_depth': [3, 5, None]}, n_iter=6, cv=5)"
        },
        {
          "id": "C",
          "text": "GridSearchCV(RandomForestClassifier(), param_distributions={'n_estimators': [100, 200]}, n_iter=6, cv=5)"
        },
        {
          "id": "D",
          "text": "RandomizedSearchCV(RandomForestClassifier(), params={'n_estimators': [100, 200]}, n_samples=6, cv=5)"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because RandomizedSearchCV uses the param_distributions parameter (not param_grid) to define the search space, and n_iter to control how many random combinations to sample. B is wrong because RandomizedSearchCV uses param_distributions, not param_grid (that is the GridSearchCV parameter). C is wrong because GridSearchCV does not have a param_distributions or n_iter parameter; those belong to RandomizedSearchCV. D is wrong because RandomizedSearchCV uses param_distributions and n_iter, not params and n_samples.",
      "reference": "sklearn RandomizedSearchCV"
    },
    {
      "id": "q16",
      "domain": "ml-workflows",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist is dealing with an imbalanced classification dataset (10% positive class). Select TWO valid strategies to address the class imbalance.",
      "options": [
        {
          "id": "A",
          "text": "Use SMOTE (Synthetic Minority Over-sampling Technique) to generate synthetic positive-class samples"
        },
        {
          "id": "B",
          "text": "Set class_weight='balanced' in the classifier to penalize misclassifying the minority class"
        },
        {
          "id": "C",
          "text": "Remove all negative-class samples until classes are equal"
        },
        {
          "id": "D",
          "text": "Increase the number of trees in a RandomForestClassifier to fix class imbalance"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A is correct because SMOTE creates synthetic samples of the minority class by interpolating between existing minority-class examples, effectively rebalancing the training set without pure duplication. B is correct because setting class_weight='balanced' in classifiers like LogisticRegression or RandomForestClassifier automatically adjusts the loss function to weight minority class errors more heavily, improving recall for the positive class. C is wrong because removing all negative-class samples (down to 10%) would destroy the vast majority of the training data and introduce severe underfitting. D is wrong because adding more trees to a RandomForestClassifier does not address the underlying class imbalance; the model will still be biased toward the majority class.",
      "reference": "Handling Class Imbalance"
    },
    {
      "id": "q17",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A regression model produces these results: RMSE = 12.4, MAE = 8.1, R² = 0.87. What does the R² value of 0.87 indicate?",
      "options": [
        {
          "id": "A",
          "text": "The model explains 87% of the variance in the target variable"
        },
        {
          "id": "B",
          "text": "The model is correct 87% of the time"
        },
        {
          "id": "C",
          "text": "The model's predictions are within 87 units of the true values on average"
        },
        {
          "id": "D",
          "text": "The model has a 13% error rate"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because R² (coefficient of determination) measures the proportion of variance in the target variable explained by the model. An R² of 0.87 means 87% of the variance is explained, with 13% unexplained. B is wrong because R² does not measure classification accuracy; it is a regression metric related to variance. C is wrong because average prediction error is measured by MAE (8.1 units in this case), not R². D is wrong because R² does not correspond to a percentage error rate.",
      "reference": "R-squared Regression Metric"
    },
    {
      "id": "q18",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data scientist wants to use feature importance from a trained RandomForestClassifier to understand which features drive predictions. They plan to use SHAP values. What is a key advantage of SHAP values over built-in tree feature importances?",
      "options": [
        {
          "id": "A",
          "text": "SHAP values provide per-sample explanations and can show the direction (positive or negative) of each feature's impact"
        },
        {
          "id": "B",
          "text": "SHAP values are faster to compute than tree-based feature importances"
        },
        {
          "id": "C",
          "text": "SHAP values are only available for linear models, not tree models"
        },
        {
          "id": "D",
          "text": "SHAP values automatically retrain the model with fewer features"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because SHAP (SHapley Additive exPlanations) provides local (per-sample) explanations showing how much each feature contributed to a specific prediction, including the direction of impact (positive pushing the prediction higher, negative pushing it lower). Built-in tree feature importances only provide global rankings and do not indicate directionality or per-sample contributions. B is wrong because SHAP values are typically more computationally expensive than built-in tree importances. C is wrong because SHAP is model-agnostic and has efficient TreeSHAP implementations specifically for tree-based models. D is wrong because SHAP values explain a model's predictions but do not retrain or modify the model.",
      "reference": "SHAP Model Interpretability"
    },
    {
      "id": "q19",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist's DataFrame has missing values in a numeric column. Which pandas operation fills all missing values with the column mean?",
      "options": [
        {
          "id": "A",
          "text": "df['col'].fillna(df['col'].mean())"
        },
        {
          "id": "B",
          "text": "df['col'].dropna(df['col'].mean())"
        },
        {
          "id": "C",
          "text": "df['col'].replace(None, df['col'].mean())"
        },
        {
          "id": "D",
          "text": "df['col'].impute(strategy='mean')"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because fillna() replaces NaN values with a specified value, and df['col'].mean() computes the column mean, making this the standard pandas idiom for mean imputation. B is wrong because dropna() removes rows with missing values; it does not accept a fill value argument. C is wrong because replace() is intended for replacing specific known values, not NaN; using None may not reliably match numpy NaN values. D is wrong because impute() is not a pandas DataFrame method; mean imputation is available via sklearn's SimpleImputer or fillna().",
      "reference": "pandas fillna, mean imputation"
    },
    {
      "id": "q20",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "In Spark ML, which component is responsible for combining multiple processing steps (e.g., StringIndexer, VectorAssembler, LogisticRegression) into a single reusable object?",
      "options": [
        {
          "id": "A",
          "text": "Pipeline"
        },
        {
          "id": "B",
          "text": "Transformer"
        },
        {
          "id": "C",
          "text": "Estimator"
        },
        {
          "id": "D",
          "text": "ParamGridBuilder"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the Spark ML Pipeline class chains a sequence of stages (Transformers and Estimators) and exposes a unified .fit() method that fits all Estimator stages in order and produces a PipelineModel. B is wrong because a Transformer is a single stage that applies a transformation (e.g., a fitted model producing predictions); it is a component inside a Pipeline, not the container itself. C is wrong because an Estimator is a single stage that is fit on data (e.g., LogisticRegression); it is also a component inside a Pipeline. D is wrong because ParamGridBuilder is used to construct hyperparameter search grids for CrossValidator, not to chain pipeline stages.",
      "reference": "Spark ML Pipeline API"
    },
    {
      "id": "q21",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist needs to convert a DataFrame with three numeric feature columns (`age`, `income`, `score`) into a single feature vector column required by Spark ML algorithms. Which transformer should they use?",
      "options": [
        {
          "id": "A",
          "text": "VectorAssembler with inputCols=['age', 'income', 'score'] and outputCol='features'"
        },
        {
          "id": "B",
          "text": "StringIndexer with inputCols=['age', 'income', 'score'] and outputCol='features'"
        },
        {
          "id": "C",
          "text": "OneHotEncoder with inputCols=['age', 'income', 'score'] and outputCol='features'"
        },
        {
          "id": "D",
          "text": "Bucketizer with inputCol='age' and outputCol='features'"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because VectorAssembler is specifically designed to merge multiple numeric (and encoded) columns into a single dense or sparse vector column, which is the required input format for Spark ML algorithms. B is wrong because StringIndexer converts string categorical columns to numeric indices; it is not used to assemble numeric features into a vector. C is wrong because OneHotEncoder converts indexed categorical columns to binary vectors; it does not assemble numeric columns. D is wrong because Bucketizer converts a continuous numeric column into discrete bucket indices; it works on a single column and does not assemble a feature vector.",
      "reference": "Spark ML VectorAssembler"
    },
    {
      "id": "q22",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "What is the key distinction between an Estimator and a Transformer in Spark ML?",
      "options": [
        {
          "id": "A",
          "text": "An Estimator has a fit() method that learns from data and returns a Transformer; a Transformer has a transform() method that applies a mapping to data without additional learning"
        },
        {
          "id": "B",
          "text": "An Estimator applies transformations; a Transformer trains models"
        },
        {
          "id": "C",
          "text": "Estimators and Transformers are interchangeable terms in Spark ML"
        },
        {
          "id": "D",
          "text": "An Estimator can only be used with regression; a Transformer can only be used with classification"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because in Spark ML, an Estimator implements fit(dataset) which learns parameters from the data and returns a fitted Model (which is a Transformer). A Transformer implements transform(dataset) to apply a learned or rule-based mapping without further training. For example, LogisticRegression is an Estimator; calling .fit() on it returns a LogisticRegressionModel, which is a Transformer. B is wrong because it reverses the roles: Estimators train/fit, Transformers apply. C is wrong because the two have distinct roles and are not interchangeable. D is wrong because both Estimators and Transformers are used across regression, classification, and feature engineering tasks.",
      "reference": "Spark ML Estimators vs Transformers"
    },
    {
      "id": "q23",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer trained a Spark ML PipelineModel and wants to save it for later use. Which code correctly saves and reloads the model?",
      "options": [
        {
          "id": "A",
          "text": "model.save('/mnt/models/pipeline') and PipelineModel.load('/mnt/models/pipeline')"
        },
        {
          "id": "B",
          "text": "pickle.dump(model, open('model.pkl', 'wb')) and pickle.load(open('model.pkl', 'rb'))"
        },
        {
          "id": "C",
          "text": "mlflow.spark.log_model(model) is the only way to save a Spark ML model"
        },
        {
          "id": "D",
          "text": "model.export('/mnt/models/pipeline') and Pipeline.load('/mnt/models/pipeline')"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Spark ML models implement the save(path) method to persist the model to a distributed filesystem path, and PipelineModel.load(path) reconstructs the full pipeline from that path. B is wrong because Python's pickle module does not work correctly with Spark ML models, which contain JVM objects and distributed state. C is wrong because while mlflow.spark.log_model() is a valid way to log Spark ML models, it is not the only way; direct save/load is also supported. D is wrong because model.export() is not a Spark ML method, and Pipeline.load() should be PipelineModel.load() for a fitted pipeline.",
      "reference": "Saving and Loading Spark ML Models"
    },
    {
      "id": "q24",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "An ML engineer uses CrossValidator in Spark ML with a ParamGrid. The code is:\n\n```python\nfrom pyspark.ml.tuning import CrossValidator, ParamGridBuilder\nfrom pyspark.ml.evaluation import BinaryClassificationEvaluator\n\nparamGrid = ParamGridBuilder() \\\n    .addGrid(lr.regParam, [0.01, 0.1]) \\\n    .addGrid(lr.maxIter, [10, 100]) \\\n    .build()\n\ncv = CrossValidator(estimator=pipeline,\n                    estimatorParamMaps=paramGrid,\n                    evaluator=BinaryClassificationEvaluator(),\n                    numFolds=3)\n```\n\nHow many total model training runs will CrossValidator perform?",
      "options": [
        {
          "id": "A",
          "text": "12 (4 parameter combinations × 3 folds)"
        },
        {
          "id": "B",
          "text": "4 (2 regParam values × 2 maxIter values)"
        },
        {
          "id": "C",
          "text": "6 (2 parameters × 3 folds)"
        },
        {
          "id": "D",
          "text": "3 (one per fold)"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the ParamGrid produces the Cartesian product of all parameter values: 2 regParam values × 2 maxIter values = 4 combinations. CrossValidator then trains each combination on each of the 3 folds (training on 2 folds, evaluating on 1), resulting in 4 × 3 = 12 total training runs. B is wrong because 4 would be the number of parameter combinations only, ignoring the 3 cross-validation folds. C is wrong because 6 would only count 2 parameters × 3 folds, which misunderstands how the Cartesian product grid works. D is wrong because 3 would be the number of folds if only one parameter combination were being tested.",
      "reference": "Spark ML CrossValidator and ParamGridBuilder"
    },
    {
      "id": "q25",
      "domain": "spark-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist needs to evaluate a multi-class classification model in Spark ML. Select TWO valid approaches.",
      "options": [
        {
          "id": "A",
          "text": "Use MulticlassClassificationEvaluator with metricName='accuracy'"
        },
        {
          "id": "B",
          "text": "Use MulticlassClassificationEvaluator with metricName='f1'"
        },
        {
          "id": "C",
          "text": "Use BinaryClassificationEvaluator with metricName='areaUnderROC'"
        },
        {
          "id": "D",
          "text": "Use RegressionEvaluator with metricName='rmse' on class label predictions"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A is correct because MulticlassClassificationEvaluator supports metricName='accuracy' to compute overall accuracy for multi-class problems. B is correct because MulticlassClassificationEvaluator also supports metricName='f1' to compute the weighted F1 score across all classes. C is wrong because BinaryClassificationEvaluator is designed for binary classification problems and computes ROC-AUC or area under precision-recall curve; it is not appropriate for multi-class classification. D is wrong because RegressionEvaluator computes RMSE and similar regression metrics; applying it to class labels is statistically meaningless.",
      "reference": "Spark ML MulticlassClassificationEvaluator"
    },
    {
      "id": "q26",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Spark ML transformer converts a string categorical column (e.g., 'Red', 'Blue', 'Green') into a numeric index column before it can be used with OneHotEncoder?",
      "options": [
        {
          "id": "A",
          "text": "StringIndexer"
        },
        {
          "id": "B",
          "text": "VectorAssembler"
        },
        {
          "id": "C",
          "text": "Tokenizer"
        },
        {
          "id": "D",
          "text": "Bucketizer"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because StringIndexer encodes a string column as a numeric index column, assigning indices based on label frequency (most frequent label gets index 0). The resulting index column can then be passed to OneHotEncoder. B is wrong because VectorAssembler combines multiple numeric columns into a single feature vector; it does not convert string labels to indices. C is wrong because Tokenizer splits a string into an array of tokens; it is used in NLP, not categorical encoding. D is wrong because Bucketizer maps continuous numeric values into discrete buckets; it operates on numeric columns, not string categoricals.",
      "reference": "Spark ML StringIndexer"
    },
    {
      "id": "q27",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "An ML engineer has a trained scikit-learn model stored in MLflow and wants to apply it to a large Spark DataFrame for batch inference. Which MLflow function creates a Spark UDF from the model?",
      "options": [
        {
          "id": "A",
          "text": "mlflow.pyfunc.spark_udf(spark, model_uri, result_type='double')"
        },
        {
          "id": "B",
          "text": "mlflow.sklearn.spark_predict(spark, model_uri)"
        },
        {
          "id": "C",
          "text": "spark.udf.register('predict', mlflow.load_model(model_uri))"
        },
        {
          "id": "D",
          "text": "mlflow.spark.apply_model(spark, model_uri)"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because mlflow.pyfunc.spark_udf() wraps any MLflow-logged model (regardless of flavor) as a Spark UDF that can be applied to Spark DataFrame columns using df.withColumn('predictions', predict_udf(struct(...))). B is wrong because mlflow.sklearn.spark_predict() is not a valid MLflow function. C is wrong because spark.udf.register() registers a Python function as a Spark UDF, but it cannot directly accept an MLflow model object; the function would need to be wrapped manually. D is wrong because mlflow.spark.apply_model() is not a valid MLflow function.",
      "reference": "mlflow.pyfunc.spark_udf"
    },
    {
      "id": "q28",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist wants to use Pandas API on Spark (pyspark.pandas) to preprocess a 100 GB dataset stored in Delta Lake. Which statement about Pandas API on Spark is TRUE?",
      "options": [
        {
          "id": "A",
          "text": "It allows pandas-style code to run on a distributed Spark cluster, but some pandas operations are not supported or may behave differently"
        },
        {
          "id": "B",
          "text": "It converts the entire dataset to a single-node pandas DataFrame before processing"
        },
        {
          "id": "C",
          "text": "It is identical to pandas in every way, including all edge cases and performance characteristics"
        },
        {
          "id": "D",
          "text": "It can only be used with CSV files, not Delta Lake tables"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Pandas API on Spark (pyspark.pandas, formerly Koalas) enables familiar pandas syntax to be executed in a distributed manner on a Spark cluster. However, not all pandas operations are supported, and some behave differently due to the distributed nature of Spark (e.g., operations requiring a global sort, index semantics). B is wrong because Pandas API on Spark does not collect data to a single node; it distributes computation across the cluster. C is wrong because there are known differences in behavior and supported operations compared to standard pandas. D is wrong because Pandas API on Spark works with Delta Lake tables and many other data sources, not just CSV.",
      "reference": "Pandas API on Spark (pyspark.pandas)"
    },
    {
      "id": "q29",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer wants to distribute deep learning training across multiple GPU workers in Databricks. Which framework integration does Databricks provide for this use case?",
      "options": [
        {
          "id": "A",
          "text": "HorovodRunner, which enables data-parallel distributed deep learning training across worker nodes"
        },
        {
          "id": "B",
          "text": "Databricks AutoML, which automatically distributes deep learning training across GPUs"
        },
        {
          "id": "C",
          "text": "Spark MLlib's GBTClassifier, which natively distributes deep learning"
        },
        {
          "id": "D",
          "text": "PySpark's mapPartitions, which must manually shard the model weights across workers"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Databricks provides HorovodRunner, which integrates with Horovod to enable data-parallel distributed training of deep learning models (TensorFlow, PyTorch, Keras) across multiple GPU worker nodes in a Databricks cluster. B is wrong because Databricks AutoML is focused on tabular ML tasks (classification, regression, forecasting) and does not distribute deep learning training. C is wrong because Spark MLlib's GBTClassifier is a gradient-boosted tree algorithm, not a deep learning framework. D is wrong because using mapPartitions to manually shard model weights is not the standard or recommended approach for distributed deep learning in Databricks.",
      "reference": "Databricks HorovodRunner"
    },
    {
      "id": "q30",
      "domain": "scaling-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist is deciding whether to use distributed training for a new model. Select TWO scenarios where distributed training provides a CLEAR benefit.",
      "options": [
        {
          "id": "A",
          "text": "Training a deep learning model on a dataset with 50 million image records that cannot fit in memory on a single GPU"
        },
        {
          "id": "B",
          "text": "Training a logistic regression model on a 500-row tabular dataset"
        },
        {
          "id": "C",
          "text": "Training a gradient-boosted tree model on a dataset with 200 million rows that takes hours on a single node"
        },
        {
          "id": "D",
          "text": "Performing hyperparameter tuning with 2 trials on a dataset of 1,000 rows"
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A is correct because a 50-million-image dataset that does not fit in single-GPU memory is a clear use case for distributed training: the data can be sharded across multiple GPU workers, with each processing a partition and gradients synchronized across workers. C is correct because a 200-million-row dataset that takes hours on a single node is exactly the kind of large-scale training task where distributing computation across multiple workers reduces wall-clock time significantly. B is wrong because a 500-row dataset is trivially small; distributed training would add communication overhead with no benefit and would likely run slower. D is wrong because only 2 hyperparameter trials on 1,000 rows is a minimal workload where the overhead of setting up and coordinating distributed training far outweighs any potential speedup.",
      "reference": "When to Use Distributed ML Training"
    },
    {
      "id": "q31",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist wants to retrieve an MLflow experiment object by its name rather than its numeric ID. Which function call is correct?",
      "options": [
        {
          "id": "A",
          "text": "mlflow.get_experiment_by_name('my_experiment')"
        },
        {
          "id": "B",
          "text": "mlflow.get_experiment('my_experiment')"
        },
        {
          "id": "C",
          "text": "mlflow.load_experiment(name='my_experiment')"
        },
        {
          "id": "D",
          "text": "mlflow.tracking.MlflowClient().get_experiment_by_id('my_experiment')"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because mlflow.get_experiment_by_name() accepts an experiment name string and returns an mlflow.entities.Experiment object containing the experiment's ID, artifact location, and lifecycle stage. B is wrong because mlflow.get_experiment() accepts a numeric experiment ID, not a name string. C is wrong because mlflow.load_experiment() is not a valid MLflow function. D is wrong because MlflowClient().get_experiment_by_id() also accepts a numeric ID, not a name.",
      "reference": "mlflow.get_experiment_by_name"
    },
    {
      "id": "q32",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist uses `mlflow.search_runs(experiment_ids=['42'], filter_string=\"params.model_type = 'xgboost' and metrics.val_auc > 0.90\")`. What does this call return?",
      "options": [
        {
          "id": "A",
          "text": "A pandas DataFrame where each row represents a run matching the filter, with columns for run metadata, parameters, and metrics"
        },
        {
          "id": "B",
          "text": "A list of run IDs that match the filter"
        },
        {
          "id": "C",
          "text": "The single best run object matching the filter"
        },
        {
          "id": "D",
          "text": "A Spark DataFrame of all runs in the experiment, ignoring the filter_string"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because mlflow.search_runs() returns a pandas DataFrame where each row is a run that satisfies the filter, with columns like run_id, status, start_time, params.*, metrics.*, and tags.*. This makes it easy to sort or filter further in pandas. B is wrong because the return type is a pandas DataFrame, not a plain list of IDs. C is wrong because search_runs() returns all matching runs, not just the single best one; ranking and selection must be done by the caller. D is wrong because search_runs() applies the filter_string and operates on the specified experiment IDs, not the full tracking server.",
      "reference": "mlflow.search_runs"
    },
    {
      "id": "q33",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer installs a Python package on a Databricks cluster using `%pip install lightgbm==3.3.5` in a notebook cell. Which statement best describes the scope and persistence of this installation?",
      "options": [
        {
          "id": "A",
          "text": "The package is installed on the current cluster for the lifetime of that cluster session; it must be reinstalled if the cluster restarts or a new cluster is used"
        },
        {
          "id": "B",
          "text": "The package is permanently installed on the cluster and persists across all restarts without reinstallation"
        },
        {
          "id": "C",
          "text": "The package is installed only in the current notebook cell's execution context and is unavailable in other cells"
        },
        {
          "id": "D",
          "text": "%pip install is not supported in Databricks notebooks; packages must be installed via the Cluster Libraries UI only"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because %pip install in a Databricks notebook installs the package into the current cluster's Python environment. The installation persists for the lifetime of that cluster but is lost when the cluster terminates or restarts. For persistent installation across restarts, cluster-scoped init scripts or cluster libraries should be used. B is wrong because %pip installations are not persisted across cluster restarts. C is wrong because %pip install makes the package available notebook-wide (and cluster-wide), not just in a single cell. D is wrong because Databricks notebooks fully support %pip install as a magic command for notebook-scoped package management.",
      "reference": "Databricks %pip install"
    },
    {
      "id": "q34",
      "domain": "databricks-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "An ML engineer logs a scikit-learn model using `mlflow.sklearn.log_model(model, 'sklearn_model')`. What does the MLflow `sklearn` flavor provide compared to the generic `pyfunc` flavor?",
      "options": [
        {
          "id": "A",
          "text": "The sklearn flavor allows loading the model as a native sklearn estimator (with access to .predict(), .predict_proba(), etc.), while pyfunc loads it as a generic wrapper with only a .predict() interface"
        },
        {
          "id": "B",
          "text": "The sklearn flavor is faster at inference than pyfunc because it bypasses serialization"
        },
        {
          "id": "C",
          "text": "pyfunc cannot load a model saved with the sklearn flavor; they are incompatible"
        },
        {
          "id": "D",
          "text": "The sklearn flavor stores models as plain Python dictionaries; pyfunc stores them as pickle files"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because a model logged with mlflow.sklearn.log_model() can be loaded either via mlflow.sklearn.load_model() (returning the native sklearn estimator with the full sklearn API) or via mlflow.pyfunc.load_model() (returning a pyfunc wrapper with only a standardized .predict() method). The pyfunc flavor is the universal interface that enables models from any framework to be served consistently. B is wrong because the inference speed difference is negligible; the distinction is about the API surface available after loading, not raw speed. C is wrong because pyfunc can load any MLflow-logged model, including those logged with the sklearn flavor. D is wrong because both flavors persist the underlying model as a pickle file (for sklearn); the difference is in the loading interface, not the storage format.",
      "reference": "MLflow Model Flavors: sklearn vs pyfunc"
    },
    {
      "id": "q35",
      "domain": "databricks-ml",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "An ML team wants to install libraries on a Databricks cluster. Select TWO valid methods for making a library available to all notebooks on a cluster.",
      "options": [
        {
          "id": "A",
          "text": "Adding the library via the Cluster Libraries UI (specifying PyPI package name, DBFS path, or Maven coordinates)"
        },
        {
          "id": "B",
          "text": "Using an init script that runs pip install commands every time the cluster starts"
        },
        {
          "id": "C",
          "text": "Running %pip install in a notebook cell, which makes the package available globally to all clusters in the workspace"
        },
        {
          "id": "D",
          "text": "Uploading a wheel file to DBFS and then importing it with a standard Python import statement"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A is correct because the Cluster Libraries UI allows attaching libraries (from PyPI, DBFS, Maven, or CRAN) at the cluster level; they are installed when the cluster starts and available to all attached notebooks. B is correct because cluster-scoped init scripts execute shell commands (including pip install) each time the cluster initializes, making libraries available to all notebooks without any per-notebook %pip commands. C is wrong because %pip install in a notebook only installs the library on the current cluster for the current session; it does not propagate to other clusters across the workspace. D is wrong because simply uploading a wheel file to DBFS and using a standard import statement would fail; the wheel must first be installed (e.g., via %pip install /dbfs/... or via the Libraries UI) before it can be imported.",
      "reference": "Databricks Cluster Libraries"
    },
    {
      "id": "q36",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data scientist has a multi-class classification problem with 5 classes and wants to ensure each fold in cross-validation preserves the class distribution. Which sklearn class should they use?",
      "options": [
        {
          "id": "A",
          "text": "StratifiedKFold"
        },
        {
          "id": "B",
          "text": "KFold"
        },
        {
          "id": "C",
          "text": "GroupKFold"
        },
        {
          "id": "D",
          "text": "TimeSeriesSplit"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because StratifiedKFold ensures that each fold contains approximately the same proportion of samples from each class as the original dataset, which is critical for multi-class problems where one or more classes may be rare. B is wrong because standard KFold splits data randomly without regard to class distribution, which can result in folds where some classes are severely underrepresented. C is wrong because GroupKFold ensures that samples from the same group are not split across train and test folds; it is used to prevent data leakage from grouped data, not to preserve class balance. D is wrong because TimeSeriesSplit is for sequential time-ordered data splits, not for stratified class-balanced splits.",
      "reference": "sklearn StratifiedKFold"
    },
    {
      "id": "q37",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist uses `ColumnTransformer` to apply `StandardScaler` to numeric columns and `OneHotEncoder` to categorical columns simultaneously. What is the primary benefit of `ColumnTransformer`?",
      "options": [
        {
          "id": "A",
          "text": "It applies different preprocessing transformations to different column subsets in a single pipeline-compatible step, preventing leakage when used inside a Pipeline"
        },
        {
          "id": "B",
          "text": "It automatically detects column types and selects the best transformer without any configuration"
        },
        {
          "id": "C",
          "text": "It can only be used with numeric columns; categorical columns must be preprocessed separately"
        },
        {
          "id": "D",
          "text": "It replaces the need for a Pipeline by handling both feature engineering and model training"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because ColumnTransformer allows different transformers to be applied to different column subsets simultaneously and returns a concatenated feature matrix. When placed inside a sklearn Pipeline, it correctly participates in cross-validation and hyperparameter search without data leakage, since it fits only on training data during each CV fold. B is wrong because ColumnTransformer requires explicit configuration of which transformer to apply to which columns; it does not auto-detect types. C is wrong because ColumnTransformer is specifically designed to handle both numeric and categorical columns using different transformers for each. D is wrong because ColumnTransformer handles only feature transformation; a separate estimator step is still needed for model training.",
      "reference": "sklearn ColumnTransformer"
    },
    {
      "id": "q38",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist computes `precision_score(y_true, y_pred, average='macro')` and `precision_score(y_true, y_pred, average='weighted')` for a 3-class problem. Class sizes are: class A=1000, class B=100, class C=10. How do these two averages differ?",
      "options": [
        {
          "id": "A",
          "text": "'macro' gives equal weight to all three classes regardless of size; 'weighted' weights each class by its support (number of true instances), so large classes dominate"
        },
        {
          "id": "B",
          "text": "'macro' weights classes by their support; 'weighted' gives equal weight to all classes"
        },
        {
          "id": "C",
          "text": "Both averages produce identical results for any dataset"
        },
        {
          "id": "D",
          "text": "'macro' only works for binary classification; 'weighted' is required for multi-class"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because average='macro' computes the metric independently for each class and then takes the unweighted mean, giving equal importance to class C (10 samples) as to class A (1000 samples). average='weighted' computes the metric per class but weights by the number of true instances (support), so class A's precision dominates. For imbalanced problems, macro highlights rare-class performance while weighted reflects overall dataset-level performance. B is wrong because it reverses the definitions. C is wrong because the two averages only agree when class sizes are equal. D is wrong because both average options work for multi-class problems; 'macro' is fully supported for multi-class scenarios.",
      "reference": "sklearn precision_score averaging strategies"
    },
    {
      "id": "q39",
      "domain": "ml-workflows",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data scientist evaluates a 3-class classifier with `roc_auc_score(y_true, y_score, multi_class='ovr', average='macro')`. Select TWO TRUE statements about this call.",
      "options": [
        {
          "id": "A",
          "text": "multi_class='ovr' computes ROC-AUC for each class treated as the positive class against all others, then averages the scores"
        },
        {
          "id": "B",
          "text": "y_score must be a 2D array of shape (n_samples, n_classes) containing predicted probabilities for each class"
        },
        {
          "id": "C",
          "text": "multi_class='ovr' is only valid for binary classification; it raises an error for 3 classes"
        },
        {
          "id": "D",
          "text": "y_score must be a 1D array of binary predictions (0 or 1) for this call to work"
        }
      ],
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A is correct because the One-vs-Rest (OVR) strategy trains a separate binary classifier for each class (treating that class as positive and all others as negative), computes the ROC-AUC for each, and then averages them. B is correct because roc_auc_score with multi_class='ovr' requires y_score to be a probability matrix of shape (n_samples, n_classes), such as the output of clf.predict_proba(), not a 1D array of class labels. C is wrong because multi_class='ovr' is explicitly designed for multi-class problems and is the recommended strategy when n_classes > 2. D is wrong because y_score must contain probability scores (floats between 0 and 1), not binary predictions; binary predictions would make the AUC computation meaningless.",
      "reference": "sklearn roc_auc_score multi_class"
    },
    {
      "id": "q40",
      "domain": "ml-workflows",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data scientist uses `SelectKBest(score_func=mutual_info_classif, k=10)` for feature selection before training a classifier. What does `mutual_info_classif` measure, and why is it preferred over a correlation-based filter for non-linear relationships?",
      "options": [
        {
          "id": "A",
          "text": "It measures the mutual information between each feature and the target, capturing both linear and non-linear dependencies; correlation only detects linear relationships"
        },
        {
          "id": "B",
          "text": "It measures the Pearson correlation between each feature and the target, which is equivalent to mutual information for classification"
        },
        {
          "id": "C",
          "text": "It measures the chi-squared statistic, which requires all features to be non-negative integers"
        },
        {
          "id": "D",
          "text": "It selects features by ranking their variance, independent of the target variable"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because mutual_info_classif computes the mutual information I(X;Y) between each feature X and the target Y, which quantifies how much knowing X reduces uncertainty about Y. Mutual information captures non-linear statistical dependencies, unlike Pearson correlation which only measures linear association. This makes it more appropriate for complex feature-target relationships. B is wrong because Pearson correlation and mutual information are fundamentally different measures; they are not equivalent, and correlation can be zero while mutual information is positive (for non-linear relationships). C is wrong because chi-squared (chi2) is a different score function in sklearn that requires non-negative features; mutual_info_classif does not have this restriction. D is wrong because variance-based selection (VarianceThreshold) selects by feature variance without reference to the target; it is entirely different from mutual information.",
      "reference": "sklearn SelectKBest mutual_info_classif"
    },
    {
      "id": "q41",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "easy",
      "stem": "After applying StringIndexer to encode a label column, an ML engineer wants to convert predicted numeric indices back to the original string labels. Which Spark ML transformer achieves this?",
      "options": [
        {
          "id": "A",
          "text": "IndexToString, which maps numeric indices back to the original label strings using the metadata from StringIndexer"
        },
        {
          "id": "B",
          "text": "StringIndexer with a reverse=True parameter"
        },
        {
          "id": "C",
          "text": "OneHotEncoder applied in reverse"
        },
        {
          "id": "D",
          "text": "VectorAssembler with inverse=True"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Spark ML provides IndexToString specifically to invert the StringIndexer transformation. It reads the label-to-index mapping stored in the column's metadata by StringIndexer and converts predicted indices back to human-readable label strings. IndexToString is commonly placed as the final stage of a PipelineModel to produce readable predictions. B is wrong because StringIndexer does not have a reverse=True parameter; it only encodes strings to indices. C is wrong because OneHotEncoder does not perform reverse index-to-string mapping. D is wrong because VectorAssembler does not have an inverse=True parameter and is not related to label decoding.",
      "reference": "Spark ML IndexToString"
    },
    {
      "id": "q42",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "An ML engineer is processing text data in Spark ML. They want to convert raw text sentences into numeric feature vectors using TF-IDF. Which sequence of transformers is correct?",
      "options": [
        {
          "id": "A",
          "text": "Tokenizer → HashingTF → IDF, producing a sparse TF-IDF feature vector"
        },
        {
          "id": "B",
          "text": "StringIndexer → OneHotEncoder → VectorAssembler, for text encoding"
        },
        {
          "id": "C",
          "text": "Word2Vec → Tokenizer → IDF, for TF-IDF computation"
        },
        {
          "id": "D",
          "text": "IDF → HashingTF → Tokenizer, applied in reverse order"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the standard Spark ML TF-IDF pipeline for text is: (1) Tokenizer splits sentences into arrays of word tokens, (2) HashingTF converts token arrays to fixed-size sparse term-frequency vectors using the hashing trick, and (3) IDF scales the term frequencies by inverse document frequency to downweight common words. This produces a sparse TF-IDF vector suitable as input to ML algorithms. B is wrong because StringIndexer, OneHotEncoder, and VectorAssembler are for categorical and numeric features, not raw text. C is wrong because Word2Vec produces dense word embeddings, not TF-IDF vectors, and must follow Tokenizer, not precede it; also IDF requires term frequencies as input. D is wrong because the pipeline must be applied in forward order: Tokenizer then HashingTF then IDF.",
      "reference": "Spark ML TF-IDF Pipeline"
    },
    {
      "id": "q43",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist uses `BinaryClassificationEvaluator` in Spark ML for cross-validation. What is the default metric computed by this evaluator, and what is its valid `metricName` string?",
      "options": [
        {
          "id": "A",
          "text": "Area under the ROC curve; metricName='areaUnderROC'"
        },
        {
          "id": "B",
          "text": "F1 score; metricName='f1'"
        },
        {
          "id": "C",
          "text": "Accuracy; metricName='accuracy'"
        },
        {
          "id": "D",
          "text": "Root Mean Square Error; metricName='rmse'"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Spark ML's BinaryClassificationEvaluator defaults to computing the area under the ROC curve (AUC-ROC), and the corresponding metricName string is 'areaUnderROC'. The other valid metricName is 'areaUnderPR' for area under the precision-recall curve. B is wrong because F1 score is a metric of MulticlassClassificationEvaluator, not BinaryClassificationEvaluator, and 'f1' is not a valid metricName for the binary evaluator. C is wrong because 'accuracy' is a metric of MulticlassClassificationEvaluator, not BinaryClassificationEvaluator. D is wrong because 'rmse' is a metric of RegressionEvaluator, not BinaryClassificationEvaluator.",
      "reference": "Spark ML BinaryClassificationEvaluator"
    },
    {
      "id": "q44",
      "domain": "spark-ml",
      "type": "single",
      "difficulty": "hard",
      "stem": "An ML engineer fits a `CrossValidatorModel` in Spark ML and wants to save the best model with all tuning metadata intact so they can reload it later. Which approach is correct?",
      "options": [
        {
          "id": "A",
          "text": "cvModel.save('/mnt/models/cv_model') and CrossValidatorModel.load('/mnt/models/cv_model'), which restores the full CrossValidatorModel including bestModel and avgMetrics"
        },
        {
          "id": "B",
          "text": "cvModel.bestModel.save('/mnt/models/best') is the only valid save approach; CrossValidatorModel cannot be saved directly"
        },
        {
          "id": "C",
          "text": "pickle.dump(cvModel, open('cv.pkl', 'wb')) to persist the CrossValidatorModel"
        },
        {
          "id": "D",
          "text": "cvModel.export('/mnt/models/cv_model') and PipelineModel.load('/mnt/models/cv_model')"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because CrossValidatorModel implements the MLWritable interface, so it can be saved with .save(path) and reloaded with CrossValidatorModel.load(path). The reloaded object contains bestModel (the fitted model from the best parameter combination) and avgMetrics (average cross-validation scores per parameter combination). B is wrong because CrossValidatorModel can be saved directly; saving only bestModel is possible but discards the tuning metadata. C is wrong because pickle does not work with Spark ML models containing JVM objects. D is wrong because cvModel.export() is not a valid Spark ML method.",
      "reference": "Spark ML CrossValidatorModel save/load"
    },
    {
      "id": "q45",
      "domain": "scaling-ml",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data scientist has a Spark DataFrame partitioned by `store_id` and wants to train a separate scikit-learn model for each store using all rows for that store. Which PySpark pattern is most appropriate?",
      "options": [
        {
          "id": "A",
          "text": "Use applyInPandas on a grouped DataFrame, where each group's pandas DataFrame is passed to a function that trains and returns predictions or model metadata"
        },
        {
          "id": "B",
          "text": "Use mapInPandas on the full DataFrame with a function that trains one global model across all partitions"
        },
        {
          "id": "C",
          "text": "Use mlflow.pyfunc.spark_udf() to train a separate model per row"
        },
        {
          "id": "D",
          "text": "Use a Spark ML Pipeline with a GroupBy parameter to train per-group models automatically"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because applyInPandas (called on df.groupBy('store_id')) passes each group's data as a pandas DataFrame to a user-defined function. This function can train a scikit-learn model on that group's data and return predictions or a results DataFrame. This is the idiomatic pattern for distributed per-group model training in PySpark. B is wrong because mapInPandas operates on arbitrary partitions, not on semantically grouped data; it cannot guarantee all rows for a given store_id end up in the same pandas batch without a prior groupBy. C is wrong because mlflow.pyfunc.spark_udf() is for batch inference with a pre-trained model, not for training per-row or per-group models. D is wrong because Spark ML Pipeline does not have a GroupBy parameter for per-group training; this capability requires the applyInPandas pattern.",
      "reference": "PySpark applyInPandas for per-group model training"
    }
  ]
}
