{
  "meta": {
    "id": "sfde-exam-02",
    "certification": "snowpro-advanced-data-engineer",
    "certificationName": "SnowPro Advanced: Data Engineer",
    "title": "SF-SPADE-2: Dynamic Tables, Snowpark and Transformations",
    "version": "1.0",
    "timeLimit": 115,
    "passingScore": 75,
    "totalQuestions": 65,
    "domains": [
      {
        "id": "data-pipeline-design",
        "name": "Data Pipeline Design",
        "weight": 20
      },
      {
        "id": "streams-tasks",
        "name": "Streams & Tasks",
        "weight": 25
      },
      {
        "id": "dynamic-tables",
        "name": "Dynamic Tables",
        "weight": 20
      },
      {
        "id": "snowpark",
        "name": "Snowpark",
        "weight": 20
      },
      {
        "id": "data-transformation",
        "name": "Data Transformation",
        "weight": 10
      },
      {
        "id": "performance-tuning",
        "name": "Performance Tuning",
        "weight": 5
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer creates the following stream:\n\n```sql\nCREATE STREAM product_changes ON TABLE products APPEND_ONLY = TRUE;\n```\n\nA colleague then runs `UPDATE products SET price = price * 1.1 WHERE category = 'Electronics'`. What records, if any, will appear in `product_changes` after this UPDATE?",
      "options": [
        {
          "id": "q01a1",
          "text": "Both a DELETE row and an INSERT row for each updated product (the standard CDC representation)",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q01a2",
          "text": "Only the new (after) version of each updated product as an INSERT row",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q01a3",
          "text": "No records \u2014 append-only streams do not capture UPDATE or DELETE operations",
          "correct": true,
          "explanation": "Append-only streams track only INSERT operations. UPDATE and DELETE DML on the underlying table is not captured in an append-only stream. This is by design \u2014 append-only streams are optimized for sources where rows are only ever added, and the efficiency gain comes precisely from not tracking row modifications. The UPDATE operation still succeeds on the base table (no error is raised, so **D** is wrong). Standard streams (not append-only) would capture the UPDATE as a DELETE+INSERT pair (A). option **B** describes a hypothetical partial-capture that does not exist."
        },
        {
          "id": "q01a4",
          "text": "An error is raised because UPDATE operations are not permitted on tables with append-only streams",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q02",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "easy",
      "stem": "What type of Snowflake stream must be used when tracking changes on an External Table?",
      "options": [
        {
          "id": "q02a1",
          "text": "Standard stream",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q02a2",
          "text": "Append-only stream",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q02a3",
          "text": "Insert-only stream",
          "correct": true,
          "explanation": "External tables expose data from files in external stages (S3, GCS, Azure Blob) and can only detect new file arrivals \u2014 they cannot track updates or deletes within files. Therefore, only insert-only streams are supported on external tables. Standard streams (A) require row-level change tracking not available for external tables. Append-only streams (B) are for internal tables where only inserts happen. External tables do support streams, but only the insert-only type (**D** is wrong)."
        },
        {
          "id": "q02a4",
          "text": "External tables cannot have streams",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q03",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer builds a task DAG with a `FINALIZE` task that should run after all parallel branches complete. They define:\n\n```sql\nCREATE TASK finalize_load\n  WAREHOUSE = 'LOAD_WH'\n  AFTER branch_a_task, branch_b_task\nAS\n  CALL update_pipeline_status('COMPLETE');\n```\n\nWhen does `finalize_load` execute?",
      "options": [
        {
          "id": "q03a1",
          "text": "After `branch_a_task` completes, regardless of `branch_b_task` status",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q03a2",
          "text": "After BOTH `branch_a_task` AND `branch_b_task` have completed successfully",
          "correct": true,
          "explanation": "When a task lists multiple tasks in its `AFTER` clause, it acts as a join node in the DAG and will only execute after ALL listed predecessor tasks have completed successfully. This enables fan-in patterns where parallel branches are synchronized before a final step. If either predecessor fails, `finalize_load` does not run (the DAG short-circuits). Options A, C, and D all misrepresent the fan-in behavior."
        },
        {
          "id": "q03a3",
          "text": "Simultaneously with `branch_a_task` and `branch_b_task` as a parallel branch",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q03a4",
          "text": "Only when `branch_a_task` succeeds and `branch_b_task` fails",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q04",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer notices that a task that processes a stream is running every 5 minutes but consistently produces no output rows. The stream has data. After investigation, they discover the task is defined with:\n\n```sql\nWHEN SYSTEM$STREAM_HAS_DATA('orders_stream')\n```\n\nbut the stream is actually named `order_stream` (no 's'). What happens at runtime?",
      "options": [
        {
          "id": "q04a1",
          "text": "Snowflake raises a stream-not-found error and suspends the task after the configured failure threshold",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q04a2",
          "text": "`SYSTEM$STREAM_HAS_DATA()` returns FALSE for a non-existent stream, so the task body is skipped every run",
          "correct": true,
          "explanation": "`SYSTEM$STREAM_HAS_DATA()` returns FALSE (not an error) when the specified stream does not exist or the name is incorrect. This causes the task's `WHEN` condition to evaluate to FALSE, and the task body is skipped silently every run \u2014 which explains why the task runs but produces no output and no errors. This is a subtle but common bug. option **A** (error + suspend) does not happen. option **C** (NULL treated as TRUE) is wrong. option **D** (auto-correct) does not exist."
        },
        {
          "id": "q04a3",
          "text": "`SYSTEM$STREAM_HAS_DATA()` returns NULL for a non-existent stream, which is treated as TRUE, so the task runs but queries an empty stream",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q04a4",
          "text": "Snowflake auto-corrects the stream name to the nearest matching stream at compile time",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q05",
      "domain": "streams-tasks",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "A data engineer creates a stream on a secure view. Select TWO constraints that apply to streams on views. (Select TWO)",
      "options": [
        {
          "id": "q05a1",
          "text": "The view must be a secure view; standard views do not support streams",
          "correct": true,
          "explanation": "Snowflake streams on views have two key constraints: (1) the view must be a SECURE view \u2014 streams cannot be created on standard (non-secure) views (**A** is correct); (2) change tracking must be enabled on all base tables referenced by the view, which Snowflake uses to propagate changes through the view (**B** is correct). The view must reference tables in the same database for stream support (**C** is wrong \u2014 cross-database view streams have limitations). Streams on views support standard stream types, not just insert-only (**D** is wrong)."
        },
        {
          "id": "q05a2",
          "text": "All tables referenced by the view must have change tracking enabled",
          "correct": true,
          "explanation": "Snowflake streams on views have two key constraints: (1) the view must be a SECURE view \u2014 streams cannot be created on standard (non-secure) views (**A** is correct); (2) change tracking must be enabled on all base tables referenced by the view, which Snowflake uses to propagate changes through the view (**B** is correct). The view must reference tables in the same database for stream support (**C** is wrong \u2014 cross-database view streams have limitations). Streams on views support standard stream types, not just insert-only (**D** is wrong)."
        },
        {
          "id": "q05a3",
          "text": "The view can reference tables from different databases within the same account",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q05a4",
          "text": "Streams on views automatically become insert-only, regardless of the view's underlying table type",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Working with Secure Views](https://docs.snowflake.com/en/user-guide/views-secure)"
    },
    {
      "id": "q06",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "hard",
      "stem": "A near-real-time inventory pipeline uses a task scheduled every 1 minute to merge stream data into a Silver table. The warehouse is an X-Small. During peak hours, the merge takes 90 seconds to complete. What is the behavior of the scheduled task runs during this overlap?",
      "options": [
        {
          "id": "q06a1",
          "text": "Snowflake queues a new run while the current run is executing, and starts it immediately after the current run finishes",
          "correct": false,
          "explanation": "Snowflake does not queue overlapping runs; the skipped interval is simply dropped."
        },
        {
          "id": "q06a2",
          "text": "Both runs execute concurrently, which could cause duplicate rows in the Silver table",
          "correct": false,
          "explanation": "Concurrent task execution does not happen; Snowflake explicitly prevents overlapping runs of the same task."
        },
        {
          "id": "q06a3",
          "text": "The task automatically upgrades to a larger warehouse to reduce execution time below the schedule interval",
          "correct": false,
          "explanation": "Warehouse-backed tasks do not auto-scale; only serverless tasks have Snowflake-managed compute sizing."
        },
        {
          "id": "q06a4",
          "text": "Snowflake skips any scheduled run that overlaps with a currently executing run; the next non-overlapping scheduled interval attempts to run",
          "correct": true,
          "explanation": "Snowflake Tasks do not queue concurrent runs. If a task is still running when the next scheduled interval fires, that scheduled run is skipped. The task will attempt its next run at the subsequent scheduled interval. This prevents concurrent execution (which could cause data conflicts) but means that if a task consistently runs longer than its schedule interval, it will run less frequently than scheduled."
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q07",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer wants to create a task using serverless compute that processes a stream. They write:\n\n```sql\nCREATE TASK process_events\n  USER_TASK_MANAGED_INITIAL_WAREHOUSE_SIZE = 'MEDIUM'\n  SCHEDULE = 'USING CRON 0 * * * * UTC'\n  WHEN SYSTEM$STREAM_HAS_DATA('event_stream')\nAS\n  INSERT INTO silver_events SELECT * FROM event_stream WHERE METADATA$ACTION = 'INSERT';\n```\n\nWhat does `USER_TASK_MANAGED_INITIAL_WAREHOUSE_SIZE = 'MEDIUM'` control?",
      "options": [
        {
          "id": "q07a1",
          "text": "It sets the minimum warehouse size that Snowflake will scale down to during the task run",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q07a2",
          "text": "It provides Snowflake a hint for the initial compute size to allocate; Snowflake may automatically adjust based on task history",
          "correct": true,
          "explanation": "`USER_TASK_MANAGED_INITIAL_WAREHOUSE_SIZE` is a hint to Snowflake's serverless compute engine about what size to start with for the first run. Over time, Snowflake learns from execution history and may automatically scale the compute allocation up or down \u2014 the parameter is an initial suggestion, not a fixed constraint. It does not create a named warehouse (C), is not a minimum scale-down size (A), and is not a hard cap (D). Snowflake manages the actual compute provisioning dynamically in serverless mode."
        },
        {
          "id": "q07a3",
          "text": "It creates a dedicated MEDIUM warehouse named after the task for exclusive use",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q07a4",
          "text": "It sets a hard cap on the compute size that Snowflake will allocate for this task",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q08",
      "domain": "streams-tasks",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data engineer is auditing their task graph and runs `SELECT * FROM TABLE(INFORMATION_SCHEMA.TASK_HISTORY(SCHEDULED_TIME_RANGE_START => DATEADD('hours', -24, CURRENT_TIMESTAMP())))`. Select TWO columns in the result that are most useful for diagnosing why a task run failed. (Select TWO)",
      "options": [
        {
          "id": "q08a1",
          "text": "`ERROR_CODE` and `ERROR_MESSAGE` columns that contain the failure reason",
          "correct": true,
          "explanation": "`ERROR_CODE` and `ERROR_MESSAGE` directly describe what went wrong when a task fails (**A** is correct). `STATE` tells the engineer the outcome of each run and helps filter to only FAILED runs (**C** is correct). `RETURN_VALUE` is populated by stored procedures that explicitly return a value \u2014 it does not contain a stack trace for general task failures (**B** is wrong). `WAREHOUSE_EXTERNAL_SIZE` is not a column in `TASK_HISTORY` output (**D** is wrong)."
        },
        {
          "id": "q08a2",
          "text": "`RETURN_VALUE` column that contains the exception stack trace from the task body",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q08a3",
          "text": "`STATE` column that shows SUCCEEDED, FAILED, SKIPPED, or CANCELLED",
          "correct": true,
          "explanation": "`ERROR_CODE` and `ERROR_MESSAGE` directly describe what went wrong when a task fails (**A** is correct). `STATE` tells the engineer the outcome of each run and helps filter to only FAILED runs (**C** is correct). `RETURN_VALUE` is populated by stored procedures that explicitly return a value \u2014 it does not contain a stack trace for general task failures (**B** is wrong). `WAREHOUSE_EXTERNAL_SIZE` is not a column in `TASK_HISTORY` output (**D** is wrong)."
        },
        {
          "id": "q08a4",
          "text": "`WAREHOUSE_EXTERNAL_SIZE` column that shows the compute used by the failed task",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q09",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer creates a Dynamic Table with `TARGET LAG = '10 minutes'`. The base table has not changed in 2 hours. How many refreshes will Snowflake execute during that 2-hour period?",
      "options": [
        {
          "id": "q09a1",
          "text": "12 refreshes \u2014 one every 10 minutes regardless of whether base data changed",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q09a2",
          "text": "0 refreshes \u2014 Snowflake skips refreshes when base data has not changed to save compute",
          "correct": true,
          "explanation": "Snowflake's Dynamic Table refresh engine is change-aware. If the base tables have not changed, Snowflake can determine there is no new data to process and will skip the refresh entirely, saving compute costs. This is one of the advantages of Dynamic Tables over a naive scheduled task that runs regardless of data changes. option **A** (always refreshing) is incorrect and would waste compute. option **C** (exactly one) is wrong \u2014 Snowflake may run a lightweight check but skips the full computation. option **D** incorrectly suggests mode-dependent skip behavior."
        },
        {
          "id": "q09a3",
          "text": "1 refresh \u2014 Snowflake runs exactly one refresh to confirm no changes and then waits",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q09a4",
          "text": "It depends on the refresh mode: FULL mode skips, INCREMENTAL mode always runs",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q10",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer creates the following Dynamic Table:\n\n```sql\nCREATE OR REPLACE DYNAMIC TABLE customer_summary\n  TARGET LAG = '30 minutes'\n  WAREHOUSE = 'DT_WH'\nAS\nSELECT\n  customer_id,\n  COUNT(*) AS order_count,\n  SUM(amount) AS total_spend,\n  MAX(order_date) AS last_order_date\nFROM orders\nGROUP BY customer_id;\n```\n\nSnowflake chooses INCREMENTAL refresh for this table. A batch of 500 new orders for 200 distinct customers arrives. What does the incremental refresh process?",
      "options": [
        {
          "id": "q10a1",
          "text": "Recomputes `customer_summary` for all customers from scratch by re-executing the full SELECT",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q10a2",
          "text": "Computes the delta for only the 200 affected `customer_id` groups and merges those changes into `customer_summary`",
          "correct": true,
          "explanation": "Incremental refresh for Dynamic Tables processes only the rows/groups affected by the base table changes. For a GROUP BY aggregation, Snowflake identifies the affected `customer_id` keys (200 in this case), recomputes aggregates only for those groups, and updates (merges) those rows in the dynamic table. The remaining ~N-200 unchanged customers are untouched. This is far more efficient than a full refresh (A and D). option **C** (appending raw rows) would break the aggregation semantic."
        },
        {
          "id": "q10a3",
          "text": "Appends the 500 new order rows directly to `customer_summary` without aggregation",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q10a4",
          "text": "Detects the 200 changed customers but still re-aggregates all customers to ensure correctness",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q11",
      "domain": "dynamic-tables",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "A data engineer runs `ALTER DYNAMIC TABLE my_dt SUSPEND`. Select TWO things that are true while `my_dt` is suspended. (Select TWO)",
      "options": [
        {
          "id": "q11a1",
          "text": "The dynamic table is no longer refreshed automatically",
          "correct": true,
          "explanation": "When a Dynamic Table is suspended, automatic refreshes stop \u2014 no new data is written (**A** is correct). However, the existing data in the table at the time of suspension remains available for queries, it just becomes increasingly stale (**B** is correct). Snowflake does NOT automatically cascade the suspension to downstream dynamic tables (**C** is wrong \u2014 downstream tables may eventually become stale but are not automatically suspended). Suspension does not delete the table definition; `ALTER DYNAMIC TABLE RESUME` simply re-enables refreshes (**D** is wrong)."
        },
        {
          "id": "q11a2",
          "text": "The data in `my_dt` at the time of suspension remains queryable",
          "correct": true,
          "explanation": "When a Dynamic Table is suspended, automatic refreshes stop \u2014 no new data is written (**A** is correct). However, the existing data in the table at the time of suspension remains available for queries, it just becomes increasingly stale (**B** is correct). Snowflake does NOT automatically cascade the suspension to downstream dynamic tables (**C** is wrong \u2014 downstream tables may eventually become stale but are not automatically suspended). Suspension does not delete the table definition; `ALTER DYNAMIC TABLE RESUME` simply re-enables refreshes (**D** is wrong)."
        },
        {
          "id": "q11a3",
          "text": "Downstream dynamic tables that reference `my_dt` are also automatically suspended",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q11a4",
          "text": "The dynamic table's definition is deleted and must be recreated with `ALTER DYNAMIC TABLE RESUME`",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q12",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer defines a Dynamic Table using a non-deterministic function:\n\n```sql\nCREATE OR REPLACE DYNAMIC TABLE daily_snapshot\n  TARGET LAG = '1 hour'\n  WAREHOUSE = 'DT_WH'\nAS\nSELECT order_id, amount, CURRENT_TIMESTAMP() AS snapshot_time\nFROM orders;\n```\n\nWhat refresh behavior should the engineer expect?",
      "options": [
        {
          "id": "q12a1",
          "text": "Snowflake will use INCREMENTAL refresh and call `CURRENT_TIMESTAMP()` once per refresh to get a consistent timestamp for all new rows",
          "correct": false,
          "explanation": "Incremental refresh cannot be used with non-deterministic functions; each full evaluation produces different values that the engine cannot diff reliably."
        },
        {
          "id": "q12a2",
          "text": "The Dynamic Table will fail to create because non-deterministic functions are not allowed in Dynamic Table definitions",
          "correct": false,
          "explanation": "The table CAN be created; Snowflake allows non-deterministic functions in dynamic table definitions but forces FULL refresh mode."
        },
        {
          "id": "q12a3",
          "text": "The Dynamic Table will be forced to use FULL refresh mode because `CURRENT_TIMESTAMP()` is non-deterministic and incremental refresh cannot reliably compute deltas containing it",
          "correct": true,
          "explanation": "Non-deterministic functions (like `CURRENT_TIMESTAMP()`, `RANDOM()`, `SEQ4()`) produce different results each time they are evaluated, which breaks the incremental refresh engine's ability to compute reliable change deltas. Snowflake falls back to FULL refresh mode for queries containing non-deterministic functions."
        },
        {
          "id": "q12a4",
          "text": "Snowflake will replace `CURRENT_TIMESTAMP()` with the table's last refresh timestamp automatically",
          "correct": false,
          "explanation": "Snowflake does not modify the query semantics or substitute any timestamp on behalf of the user."
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q13",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer wants to manually trigger an immediate refresh of a Dynamic Table that normally has `TARGET LAG = '6 hours'`, without changing the lag setting. Which command achieves this?",
      "options": [
        {
          "id": "q13a1",
          "text": "`ALTER DYNAMIC TABLE my_dt SET TARGET LAG = '0 seconds'`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q13a2",
          "text": "`ALTER DYNAMIC TABLE my_dt REFRESH`",
          "correct": true,
          "explanation": "`ALTER DYNAMIC TABLE <name> REFRESH` triggers an immediate, on-demand refresh of the dynamic table without modifying its `TARGET LAG` setting. This is useful for forcing fresh data after a critical upstream change. option **A** would change the lag setting, which affects ongoing scheduling. option **C** just queries the table \u2014 it does not trigger a refresh. Option **D** is not a real Snowflake system call."
        },
        {
          "id": "q13a3",
          "text": "`EXECUTE IMMEDIATE (SELECT * FROM my_dt)`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q13a4",
          "text": "`CALL SYSTEM$REFRESH_DYNAMIC_TABLE('my_dt')`",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q14",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "medium",
      "stem": "How does a Dynamic Table differ from a Materialized View in Snowflake, specifically regarding supported SQL?",
      "options": [
        {
          "id": "q14a1",
          "text": "Materialized Views support JOINs across multiple tables; Dynamic Tables do not",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q14a2",
          "text": "Dynamic Tables support JOINs, aggregations across multiple tables, and self-referential patterns; Materialized Views are restricted to single-table queries with limited aggregations",
          "correct": true,
          "explanation": "Dynamic Tables support a much richer SQL surface than Materialized Views. Dynamic Tables can JOIN multiple tables (including across schemas), perform complex aggregations, reference other Dynamic Tables as sources (chaining), and support GROUP BY, window functions, and more. Snowflake Materialized Views are more limited: they essentially support single-table queries with a restricted set of aggregations (no JOINs in Snowflake MVs). option **A** has it backwards \u2014 it's MVs that are limited. Option **C** is wrong about parity. Option **D** is completely wrong about Dynamic Tables."
        },
        {
          "id": "q14a3",
          "text": "Both support identical SQL; the only difference is that Dynamic Tables support lag-based scheduling",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q14a4",
          "text": "Dynamic Tables only support SELECT * without any transformations; Materialized Views support full SQL",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q15",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer registers a Snowpark UDTF (User-Defined Table Function) using the `@udtf` decorator. What is the key difference between a UDTF and a scalar UDF in Snowpark?",
      "options": [
        {
          "id": "q15a1",
          "text": "A UDTF processes each row in parallel using pandas; a scalar UDF processes rows sequentially",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q15a2",
          "text": "A UDTF can return zero or more rows per input row; a scalar UDF returns exactly one scalar value per input row",
          "correct": true,
          "explanation": "The fundamental difference: a scalar UDF returns exactly one value per input row (one-to-one mapping). A UDTF returns a table (zero or more rows) per input row (one-to-many mapping), making them useful for operations like parsing a JSON array into multiple rows, generating date ranges, or applying a model that produces multiple predictions per input. Both UDFs and UDTFs run inside Snowflake (**D** is wrong). The pandas/sequential distinction (A) describes vectorized vs. scalar UDFs, not UDTFs. Permanence is not a UDTF-specific restriction (C)."
        },
        {
          "id": "q15a3",
          "text": "A UDTF is always permanent and cannot be temporary; scalar UDFs can be temporary",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q15a4",
          "text": "A UDTF runs on the client machine; a scalar UDF runs inside Snowflake",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q16",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer writes the following Snowpark code:\n\n```python\ndf = session.table('sales')\ndf_enriched = df.join(\n    session.table('customers'),\n    df['customer_id'] == session.table('customers')['customer_id'],\n    join_type='left'\n)\ndf_enriched.write.mode('overwrite').save_as_table('enriched_sales')\n```\n\nSnowflake executes this as a single SQL statement. What SQL operation does this most closely translate to?",
      "options": [
        {
          "id": "q16a1",
          "text": "`CREATE OR REPLACE TABLE enriched_sales AS SELECT * FROM sales s LEFT JOIN customers c ON s.customer_id = c.customer_id`",
          "correct": true,
          "explanation": "Snowpark's `df.write.mode('overwrite').save_as_table('enriched_sales')` compiles to a `CREATE OR REPLACE TABLE ... AS SELECT ...` (CTAS) statement with the full query plan (left join in this case) pushed down to Snowflake SQL. `mode('overwrite')` maps to `CREATE OR REPLACE`, which is equivalent to truncating and replacing. option **B** uses INNER JOIN (wrong join type) and INSERT (not overwrite). Option **C** is a MERGE, which is not what `save_as_table` produces. option **D** uses `COPY INTO` which is for stage-to-table loading, not DataFrame writes."
        },
        {
          "id": "q16a2",
          "text": "`INSERT INTO enriched_sales SELECT * FROM sales s INNER JOIN customers c ON s.customer_id = c.customer_id`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q16a3",
          "text": "`MERGE INTO enriched_sales USING (SELECT ...) ON (customer_id) WHEN MATCHED THEN UPDATE`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q16a4",
          "text": "`COPY INTO enriched_sales FROM (SELECT * FROM sales s LEFT JOIN customers c ON s.customer_id = c.customer_id)`",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q17",
      "domain": "snowpark",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "A data engineer wants to use third-party Python packages (e.g., `scikit-learn`, `numpy`) inside a Snowpark Python UDF. Select TWO requirements that must be met. (Select TWO)",
      "options": [
        {
          "id": "q17a1",
          "text": "The packages must be available in the Snowflake Anaconda channel (or uploaded to a stage)",
          "correct": true,
          "explanation": "Snowflake UDFs run in a sandboxed environment on warehouse nodes. To use third-party packages, they must either be available in Snowflake's curated Anaconda channel (or uploaded to a stage as a ZIP), and they must be declared at registration time via the `packages=['scikit-learn', 'numpy']` parameter so Snowflake can make them available in the sandbox (**A** and **B** are correct). You cannot `pip install` directly onto Snowflake warehouse nodes (**C** is wrong \u2014 the compute is managed and ephemeral). option **D** (ZIP upload) is valid but is the alternative for packages NOT in Anaconda \u2014 it's not a universal requirement when the package IS in Anaconda."
        },
        {
          "id": "q17a2",
          "text": "The packages must be specified in the UDF registration using the `packages` parameter",
          "correct": true,
          "explanation": "Snowflake UDFs run in a sandboxed environment on warehouse nodes. To use third-party packages, they must either be available in Snowflake's curated Anaconda channel (or uploaded to a stage as a ZIP), and they must be declared at registration time via the `packages=['scikit-learn', 'numpy']` parameter so Snowflake can make them available in the sandbox (**A** and **B** are correct). You cannot `pip install` directly onto Snowflake warehouse nodes (**C** is wrong \u2014 the compute is managed and ephemeral). option **D** (ZIP upload) is valid but is the alternative for packages NOT in Anaconda \u2014 it's not a universal requirement when the package IS in Anaconda."
        },
        {
          "id": "q17a3",
          "text": "The packages must be installed on the Snowflake warehouse nodes using pip before the UDF runs",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q17a4",
          "text": "The packages must be bundled in a ZIP file and uploaded to a Snowflake internal stage",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q18",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer writes a Snowpark stored procedure to perform a complex multi-step transformation and registers it with `EXECUTE AS CALLER`. A user with the `ANALYST` role calls the stored procedure. The procedure attempts to INSERT into a table that `ANALYST` does not have INSERT privilege on. What happens?",
      "options": [
        {
          "id": "q18a1",
          "text": "The INSERT succeeds because stored procedures always have elevated privileges",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q18a2",
          "text": "The INSERT fails with a permission error because `EXECUTE AS CALLER` runs the procedure with the CALLER's privileges (ANALYST role)",
          "correct": true,
          "explanation": "`EXECUTE AS CALLER` is the rights model where all SQL within the stored procedure runs with the calling user's current role's privileges. Since `ANALYST` lacks INSERT on the target table, the INSERT fails. This contrasts with `EXECUTE AS OWNER`, where the procedure's owner role privileges apply instead. Option **A** is wrong \u2014 stored procedures do not auto-elevate privileges. option **C** does not reflect Snowflake's runtime behavior. option **D** describes `EXECUTE AS OWNER` behavior, not `CALLER`."
        },
        {
          "id": "q18a3",
          "text": "Snowflake prompts the ANALYST user to grant themselves INSERT privilege at runtime",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q18a4",
          "text": "The INSERT succeeds only if the stored procedure owner has INSERT privilege on the table",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q19",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer builds a Snowpark UDTF that parses a JSON string column into multiple event rows. The `process()` method yields individual rows. They also implement `end_partition()`. When is `end_partition()` called?",
      "options": [
        {
          "id": "q19a1",
          "text": "After every row is processed by `process()`, as a row-level post-processing hook",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q19a2",
          "text": "Once, after all rows in a partition have been processed by `process()`, allowing partition-level aggregation or finalization",
          "correct": true,
          "explanation": "In Snowpark UDTFs, `process()` is called once per input row and can yield rows immediately. `end_partition()` is called once per partition after ALL rows in that partition have been processed by `process()`. This makes it ideal for finalizing partition-level state \u2014 for example, emitting a summary row, flushing accumulated state, or computing an aggregate across all rows in the partition before yielding the final output. It is not per-row (A), not an error handler (C), and not called before `process()` (D \u2014 that's what `__init__` is for)."
        },
        {
          "id": "q19a3",
          "text": "Only when `process()` raises an exception, as an error handler",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q19a4",
          "text": "Before `process()` is called, to initialize partition-level state",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q20",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer writes:\n\n```python\ndf = session.sql('SELECT * FROM raw_events WHERE event_date = CURRENT_DATE()')\ndf.write.mode('append').save_as_table('daily_events')\n```\n\nIs this idiomatic Snowpark? What concern does this raise?",
      "options": [
        {
          "id": "q20a1",
          "text": "It works but bypasses Snowpark's type-safe column API and column pushdown optimization; `session.table().filter()` would be preferred",
          "correct": true,
          "explanation": "`session.sql()` returns a Snowpark DataFrame, so chaining `.write.save_as_table()` is technically valid. However, using raw SQL strings is less idiomatic than the DataFrame API (`session.table().filter(col('event_date') == current_date())`) because raw SQL bypasses Snowpark's type-safe column introspection and Python-level validation."
        },
        {
          "id": "q20a2",
          "text": "It is fully idiomatic; mixing `session.sql()` and DataFrame writes is the recommended pattern",
          "correct": false,
          "explanation": "While this pattern works, it is not the recommended best practice; the DataFrame API is preferred for code that benefits from type safety and refactoring."
        },
        {
          "id": "q20a3",
          "text": "It will fail because `session.sql()` returns a ResultSet, not a DataFrame, and `write` is not available on ResultSets",
          "correct": false,
          "explanation": "`session.sql()` returns a Snowpark DataFrame, not a ResultSet; `.write.save_as_table()` is fully available on it."
        },
        {
          "id": "q20a4",
          "text": "It will fail because `CURRENT_DATE()` is a SQL function and cannot be evaluated inside a Snowpark DataFrame context",
          "correct": false,
          "explanation": "`CURRENT_DATE()` is a standard SQL function that Snowflake evaluates server-side; it works perfectly in SQL strings passed to `session.sql()`."
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q21",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer uses a Snowpark stored procedure to orchestrate a pipeline that creates a temp table, populates it, and then reads it back. The procedure is registered as a permanent stored procedure. Another session calls the same procedure simultaneously. What happens to the temp tables created inside the procedure?",
      "options": [
        {
          "id": "q21a1",
          "text": "Both sessions share the same temp table, causing data contamination between runs",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q21a2",
          "text": "Each session call gets its own session-scoped temp table, isolating the two executions",
          "correct": true,
          "explanation": "In Snowflake, session-scoped temporary tables are isolated per session. When two sessions call the same stored procedure simultaneously, each session creates its own instance of any temp tables defined inside the procedure. They do not share temp table state, which ensures isolation and correctness. option **A** (shared state) would be a serious data integrity problem and is not the case. Option **C** is wrong \u2014 multiple concurrent calls are fully supported. option **D** misrepresents how temp tables work."
        },
        {
          "id": "q21a3",
          "text": "The second call fails because temp tables inside stored procedures are exclusive to the first caller",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q21a4",
          "text": "Temp tables inside stored procedures are actually permanent tables with a temp-like TTL of 1 hour",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q22",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer is designing an ELT pipeline where raw JSON data is loaded into a Snowflake internal stage and then into a Bronze table. Which approach represents the ELT pattern?",
      "options": [
        {
          "id": "q22a1",
          "text": "Parse and normalize the JSON in a Spark job before writing to Snowflake",
          "correct": false,
          "explanation": "Parsing and normalizing in a Spark job before writing to Snowflake is ETL (transform before load), not ELT."
        },
        {
          "id": "q22a2",
          "text": "Load the raw JSON into Snowflake as a VARIANT column and perform all transformations using Snowflake SQL after landing",
          "correct": false,
          "explanation": "Transforming JSON into Parquet using AWS Lambda before staging is also ETL; the transformation happens outside Snowflake before the data lands."
        },
        {
          "id": "q22a3",
          "text": "Load the raw JSON into Snowflake as a VARIANT column and perform all transformations using Snowflake SQL after landing",
          "correct": true,
          "explanation": "ELT (Extract, Load, Transform) means loading data first into the target system and performing all transformations inside the target platform. Snowflake's native VARIANT type stores raw semi-structured JSON, and Snowflake SQL (FLATTEN, PARSE_JSON, lateral views) is used for in-place transformation after landing."
        },
        {
          "id": "q22a4",
          "text": "Use a custom connector to transform JSON to CSV during the Snowflake load process",
          "correct": false,
          "explanation": "Transforming JSON to CSV during the connector load process is still ETL; ELT requires transformation to happen inside Snowflake after the raw data has landed."
        }
      ],
      "reference": "[Semi-structured data types](https://docs.snowflake.com/en/sql-reference/data-types-semistructured)"
    },
    {
      "id": "q23",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineering team uses a Bronze/Silver/Gold pattern with Streams and Tasks. `BRONZE_STREAM` on the `BRONZE_ORDERS` table feeds `SILVER_LOAD_TASK`. The Silver table then has its own stream `SILVER_STREAM` that feeds `GOLD_LOAD_TASK`. The engineer wants the Gold table to be updated only after the Silver table is fully updated. How should the task dependencies be structured?",
      "options": [
        {
          "id": "q23a1",
          "text": "`GOLD_LOAD_TASK` should run on its own schedule (every 10 minutes) independent of `SILVER_LOAD_TASK`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q23a2",
          "text": "`GOLD_LOAD_TASK` should be defined as `AFTER SILVER_LOAD_TASK` in the same task DAG, so Gold processing only starts after Silver completes",
          "correct": true,
          "explanation": "The correct approach is to chain the tasks in a single DAG: `SILVER_LOAD_TASK` as the root task (consuming BRONZE_STREAM), and `GOLD_LOAD_TASK` as `AFTER SILVER_LOAD_TASK` with `WHEN SYSTEM$STREAM_HAS_DATA('SILVER_STREAM')`. This ensures Gold processing only begins after Silver has been fully updated. Independent scheduling (A and D) creates a race condition where Gold might read Silver before it's updated. Sharing the Bronze stream's `WHEN` clause (C) would mean Gold runs on Bronze stream data, not Silver stream data."
        },
        {
          "id": "q23a3",
          "text": "Both tasks should share the same `WHEN` clause, pointing to `BRONZE_STREAM`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q23a4",
          "text": "The `GOLD_LOAD_TASK` should poll `SILVER_STREAM` using `SYSTEM$STREAM_HAS_DATA()` on its own independent 1-minute schedule",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q24",
      "domain": "data-pipeline-design",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A team is implementing a dead-letter pattern for a Snowflake ELT pipeline where some records fail data quality checks during the Silver load. Select TWO characteristics of a well-designed dead-letter pattern in Snowflake. (Select TWO)",
      "options": [
        {
          "id": "q24a1",
          "text": "Failed records are routed to a separate `DEAD_LETTER` table with the error reason and original record payload preserved",
          "correct": true,
          "explanation": "A dead-letter pattern's purpose is exactly to isolate bad records while allowing the good-record pipeline to continue. Routing failed records to a dedicated table with their error context (A) enables investigation and reprocessing without blocking the main pipeline. The main pipeline should continue processing valid records (B), which is the whole point of a dead-letter queue. `ON_ERROR = ABORT_STATEMENT` (C) is the opposite of a dead-letter pattern \u2014 it halts the entire load on any error. Dead-letter records should be retained for investigation and possible reprocessing, not auto-deleted (D)."
        },
        {
          "id": "q24a2",
          "text": "The main pipeline continues processing valid records even when some records fail validation",
          "correct": true,
          "explanation": "A dead-letter pattern's purpose is exactly to isolate bad records while allowing the good-record pipeline to continue. Routing failed records to a dedicated table with their error context (A) enables investigation and reprocessing without blocking the main pipeline. The main pipeline should continue processing valid records (B), which is the whole point of a dead-letter queue. `ON_ERROR = ABORT_STATEMENT` (C) is the opposite of a dead-letter pattern \u2014 it halts the entire load on any error. Dead-letter records should be retained for investigation and possible reprocessing, not auto-deleted (D)."
        },
        {
          "id": "q24a3",
          "text": "The task is configured with `ON_ERROR = ABORT_STATEMENT` so all records are rejected if any fail",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q24a4",
          "text": "Dead-letter records are permanently deleted after 24 hours to prevent the table from growing",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowflake key concepts and architecture](https://docs.snowflake.com/en/user-guide/intro-key-concepts)"
    },
    {
      "id": "q25",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer is designing a pipeline where Snowflake is the downstream consumer of a Debezium CDC feed from a PostgreSQL database. Debezium publishes full row images with operation type (`c` for create, `u` for update, `d` for delete) to Kafka, which is then landed in a Snowflake Bronze table. Which Snowflake DML best propagates these CDC events from Bronze to a Silver target table?",
      "options": [
        {
          "id": "q25a1",
          "text": "Use a standard Snowflake stream on the Bronze table \u2014 Snowflake will automatically interpret Debezium's `op` field as stream metadata",
          "correct": false,
          "explanation": "Snowflake streams are agnostic to Debezium's payload; a stream on Bronze would show Debezium messages as INSERTs (rows landing in Bronze), not as the CDC operations they represent."
        },
        {
          "id": "q25a2",
          "text": "Use `INSERT INTO silver SELECT * FROM bronze_stream` and rely on Snowflake to detect duplicates automatically",
          "correct": false,
          "explanation": "A simple `INSERT INTO` ignores delete and update events and does not perform deduplication; Snowflake has no automatic CDC-aware duplicate detection."
        },
        {
          "id": "q25a3",
          "text": "Use `MERGE INTO` on the Silver table, joining on primary key, routing `c`/`u` operations to `WHEN MATCHED THEN UPDATE` or `WHEN NOT MATCHED THEN INSERT`, and `d` operations to `WHEN MATCHED THEN DELETE`",
          "correct": true,
          "explanation": "When Debezium sends full-row images with an explicit operation field (`op`), the Bronze table contains rows representing CDC events. A `MERGE INTO` on the Silver table that reads the `op` field and routes `c`/`u` to INSERT/UPDATE and `d` to DELETE is the correct approach to propagate these changes into a maintained Silver table."
        },
        {
          "id": "q25a4",
          "text": "Use the Snowflake stream on the Bronze table and filter by `METADATA$ACTION = 'DELETE'` for Debezium delete events",
          "correct": false,
          "explanation": "`METADATA$ACTION` is a Snowflake stream metadata column reflecting DML changes to the Bronze table itself, not a reference to Debezium's `op` field in the payload."
        }
      ],
      "reference": "[Snowflake key concepts and architecture](https://docs.snowflake.com/en/user-guide/intro-key-concepts)"
    },
    {
      "id": "q26",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineering team is evaluating Snowflake Tasks versus an external orchestrator (Apache Airflow) for a new pipeline. Which of the following is the BEST reason to use Snowflake Tasks instead of Airflow for this pipeline?",
      "options": [
        {
          "id": "q26a1",
          "text": "The pipeline needs to trigger a Salesforce API call after each successful Snowflake load",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q26a2",
          "text": "The pipeline is entirely within Snowflake (stream \u2192 transformation \u2192 Silver table) and the team wants to minimize external dependencies and operational overhead",
          "correct": true,
          "explanation": "Snowflake Tasks are the best choice when the entire pipeline is within Snowflake and the team wants no external infrastructure to maintain. Tasks eliminate the need for an Airflow deployment, Airflow workers, and the associated ops overhead. Scenarios A (Salesforce API), C (multi-cloud branching), and D (PagerDuty integration) all require external system integration \u2014 exactly where Airflow excels but Snowflake Tasks cannot natively reach."
        },
        {
          "id": "q26a3",
          "text": "The pipeline has complex branching logic that depends on data from multiple cloud systems",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q26a4",
          "text": "The pipeline needs SLA alerting via PagerDuty when tasks exceed their expected duration",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowflake key concepts and architecture](https://docs.snowflake.com/en/user-guide/intro-key-concepts)"
    },
    {
      "id": "q27",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer uses `COPY INTO` with an inline transformation:\n\n```sql\nCOPY INTO raw.orders (order_id, customer_id, amount_usd, loaded_at)\nFROM (\n  SELECT\n    $1::INTEGER,\n    $2::VARCHAR(50),\n    $3::FLOAT * 1.08,\n    CURRENT_TIMESTAMP()\n  FROM @orders_stage/orders_20240601.csv.gz\n)\nFILE_FORMAT = (TYPE = CSV SKIP_HEADER = 1);\n```\n\nWhat does the `$1`, `$2`, `$3` notation refer to in this context?",
      "options": [
        {
          "id": "q27a1",
          "text": "Named bind parameters that must be declared before the COPY INTO statement",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q27a2",
          "text": "Column position references in the source file \u2014 `$1` is the first column, `$2` the second, etc.",
          "correct": true,
          "explanation": "In a `COPY INTO` SELECT transformation, `$1`, `$2`, `$3`, etc. are positional column references to the columns of the source file being loaded. `$1` refers to the first column in the CSV, `$2` to the second, and so on. This allows you to reorder columns, cast types, or transform values from the file during load. They are not bind parameters (A), session variables (C), or references to the target table's columns (D)."
        },
        {
          "id": "q27a3",
          "text": "References to Snowflake session variables that hold the column values",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q27a4",
          "text": "References to columns in the target `raw.orders` table by position",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[COPY INTO *<table>*](https://docs.snowflake.com/en/sql-reference/sql/copy-into-table)"
    },
    {
      "id": "q28",
      "domain": "data-transformation",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data engineer needs to implement a multi-table routing pattern where rows from a single staging table are routed to different target tables based on a `record_type` column. Select TWO Snowflake SQL approaches that support conditional multi-table inserts. (Select TWO)",
      "options": [
        {
          "id": "q28a1",
          "text": "`INSERT ALL` with `WHEN condition THEN INTO table_name` clauses for each target table",
          "correct": true,
          "explanation": "Snowflake supports two conditional multi-table INSERT forms: `INSERT ALL` evaluates ALL `WHEN` conditions for each row, so a row matching multiple conditions inserts into multiple target tables (A). `INSERT FIRST` evaluates conditions in order and inserts the row into only the FIRST matching target table (B). `MERGE INTO` (C) targets a single table per statement \u2014 you cannot target different tables in one MERGE. `COPY INTO` (D) has no such routing parameter."
        },
        {
          "id": "q28a2",
          "text": "`INSERT FIRST` with `WHEN condition THEN INTO table_name`, stopping at the first matching condition per row",
          "correct": true,
          "explanation": "Snowflake supports two conditional multi-table INSERT forms: `INSERT ALL` evaluates ALL `WHEN` conditions for each row, so a row matching multiple conditions inserts into multiple target tables (A). `INSERT FIRST` evaluates conditions in order and inserts the row into only the FIRST matching target table (B). `MERGE INTO` (C) targets a single table per statement \u2014 you cannot target different tables in one MERGE. `COPY INTO` (D) has no such routing parameter."
        },
        {
          "id": "q28a3",
          "text": "`MERGE INTO` with multiple `WHEN MATCHED` clauses targeting different tables in a single statement",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q28a4",
          "text": "`COPY INTO` with a `ROUTING` parameter to send rows to named target tables",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[COPY INTO *<table>*](https://docs.snowflake.com/en/sql-reference/sql/copy-into-table)"
    },
    {
      "id": "q29",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer needs to perform a full sync of a `PRODUCTS` dimension table from a staging area. Products that exist in staging but not in the target should be inserted. Products that exist in both should be updated. Products that exist in the target but NOT in staging should be deleted. Which MERGE statement skeleton achieves this in Snowflake?",
      "options": [
        {
          "id": "q29a1",
          "text": "`MERGE INTO products USING staging ON (products.id = staging.id) WHEN MATCHED THEN UPDATE ... WHEN NOT MATCHED THEN INSERT ...`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q29a2",
          "text": "`MERGE INTO products USING staging ON (products.id = staging.id) WHEN MATCHED THEN UPDATE ... WHEN NOT MATCHED THEN INSERT ... WHEN NOT MATCHED BY SOURCE THEN DELETE`",
          "correct": true,
          "explanation": "A full-sync MERGE requires all three clauses: `WHEN MATCHED` (row in both source and target \u2192 UPDATE), `WHEN NOT MATCHED` (row in source but not in target \u2192 INSERT), and `WHEN NOT MATCHED BY SOURCE` (row in target but not in source \u2192 DELETE). option **A** misses the delete for orphaned target rows. option **C** deletes matching rows instead of updating them. option **D** uses `NOT MATCHED BY TARGET` for DELETE, which is semantically backwards \u2014 `NOT MATCHED BY TARGET` means 'in source but not target' \u2192 that's for INSERTs."
        },
        {
          "id": "q29a3",
          "text": "`MERGE INTO products USING staging ON (products.id = staging.id) WHEN MATCHED THEN DELETE WHEN NOT MATCHED THEN INSERT ...`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q29a4",
          "text": "`MERGE INTO products USING staging ON (products.id = staging.id) WHEN NOT MATCHED BY TARGET THEN DELETE WHEN NOT MATCHED THEN INSERT ...`",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q30",
      "domain": "performance-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer runs a heavy ELT query that joins a 10TB fact table with a 1GB dimension table and applies several aggregations. The query is run nightly on an X-Large warehouse. The engineer notices that roughly 5% of runs take 10x longer than typical runs, seemingly at random. Which Snowflake feature is MOST appropriate to address these outlier query performance spikes?",
      "options": [
        {
          "id": "q30a1",
          "text": "Enable result caching on the warehouse so repeated queries are served from cache",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q30a2",
          "text": "Enable the Query Acceleration Service on the warehouse to automatically scale compute for outlier queries",
          "correct": true,
          "explanation": "The Query Acceleration Service (QAS) is specifically designed for outlier query situations \u2014 queries that occasionally take much longer than typical due to data skew, large scan partitions, or other variability. QAS automatically provides additional elastic compute for these outlier queries without requiring the entire warehouse to be upsized. Result caching (A) only helps if the exact same query with the same data is re-run. A permanently larger warehouse (C) costs more 100% of the time to fix a 5% problem. Multi-Cluster Warehouses (D) address concurrent user/query load, not single-query performance outliers."
        },
        {
          "id": "q30a3",
          "text": "Switch to a 2X-Large warehouse to ensure consistent performance",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q30a4",
          "text": "Use a Multi-Cluster Warehouse to handle concurrent query load",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Multi-cluster warehouses](https://docs.snowflake.com/en/user-guide/warehouses-multicluster)"
    },
    {
      "id": "q31",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer sets up a Snowpipe to auto-ingest files from an S3 bucket. After creating the Snowpipe, how does Snowflake receive notification that new files have arrived in S3?",
      "options": [
        {
          "id": "q31a1",
          "text": "Snowflake polls the S3 bucket on a configurable interval (default: 1 minute)",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q31a2",
          "text": "S3 sends an event notification to an SQS queue, and Snowflake's Snowpipe service reads from that SQS queue to trigger ingestion",
          "correct": true,
          "explanation": "Snowpipe event-driven ingestion from S3 works via S3 Event Notifications \u2192 SQS \u2192 Snowflake. When a new object arrives in the configured S3 bucket/prefix, S3 publishes an event to an SQS queue. Snowflake's Snowpipe service continuously reads from that SQS queue and triggers file loading within seconds to minutes. Snowflake does not poll S3 directly (A). Manual `REFRESH` (C) is the fallback for ad-hoc loads, not the auto-ingest mechanism. There is no polling task (D)."
        },
        {
          "id": "q31a3",
          "text": "The data engineer must call `ALTER PIPE ... REFRESH` each time new files arrive",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q31a4",
          "text": "A Snowflake Task polls `SYSTEM$PIPE_STATUS()` and triggers ingestion when new files are detected",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpipe](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-intro)"
    },
    {
      "id": "q32",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer wants to check the credit consumption of a Snowpipe pipe over the last 7 days, broken down by hour. Which function provides this billing/usage data?",
      "options": [
        {
          "id": "q32a1",
          "text": "`COPY_HISTORY(TABLE_NAME => 'target_table', START_TIME => DATEADD('day', -7, CURRENT_TIMESTAMP()))`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q32a2",
          "text": "`PIPE_USAGE_HISTORY(DATE_RANGE_START => DATEADD('day', -7, CURRENT_TIMESTAMP()))`",
          "correct": true,
          "explanation": "`PIPE_USAGE_HISTORY()` is the table function that returns credit usage for Snowpipe over a specified time range, broken down by pipe and time interval. It shows credits consumed for auto-ingest compute. `COPY_HISTORY()` (A) shows file-level load status, not credit consumption. `SHOW PIPES` (C) shows configuration, not usage metrics. `INFORMATION_SCHEMA.SNOWPIPE_HISTORY` (D) is not a real Snowflake view."
        },
        {
          "id": "q32a3",
          "text": "`SHOW PIPES` with the `CREDITS_USED` column",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q32a4",
          "text": "`INFORMATION_SCHEMA.SNOWPIPE_HISTORY` view",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpipe](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-intro)"
    },
    {
      "id": "q33",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer is building a pipeline that calls Snowflake's REST SQL API from a microservice. The microservice must authenticate using key-pair (JWT) authentication. After generating the JWT, which HTTP header carries the token in REST API calls to Snowflake?",
      "options": [
        {
          "id": "q33a1",
          "text": "`X-Snowflake-Token: Bearer <jwt>`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q33a2",
          "text": "`Authorization: Snowflake Token=\"<jwt>\"`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q33a3",
          "text": "`Authorization: Bearer <jwt>`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q33a4",
          "text": "`X-Snowflake-Authorization-Token-Type: KEYPAIR_JWT` with the JWT in the `Authorization: Bearer` header",
          "correct": true,
          "explanation": "Snowflake's REST SQL API with key-pair JWT authentication requires TWO headers: `Authorization: Bearer <jwt>` containing the signed JWT, AND `X-Snowflake-Authorization-Token-Type: KEYPAIR_JWT` which tells Snowflake to validate the token as a key-pair JWT (not an OAuth token). Without the token-type header, Snowflake cannot distinguish JWT from OAuth Bearer tokens. option **C** alone is incomplete \u2014 it's missing the token type header. Options A and B use non-standard or incorrect header formats."
        }
      ],
      "reference": "[Snowflake key concepts and architecture](https://docs.snowflake.com/en/user-guide/intro-key-concepts)"
    },
    {
      "id": "q34",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer uses the Snowpipe Streaming API (`insertRows()`) to push data into Snowflake. The SDK client buffers rows into a channel before flushing to Snowflake. What is a Snowpipe Streaming 'channel' in this context?",
      "options": [
        {
          "id": "q34a1",
          "text": "A named Kafka consumer group that reads from a Kafka topic and writes to Snowflake",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q34a2",
          "text": "A logical, ordered sequence of rows for a specific table partition, managed by the SDK client, that provides exactly-once delivery guarantees via offset tracking",
          "correct": true,
          "explanation": "In Snowpipe Streaming, a 'channel' is a logical unit of ordered data flow for a specific target table. Each channel tracks an offset (similar to a Kafka consumer offset) to provide exactly-once semantics \u2014 if the client fails and restarts, it can resume from the last committed offset without duplicating rows. Multiple channels can write to the same table in parallel. Channels are not Kafka consumer groups (A), not internal stages (C), and not transformation functions (D)."
        },
        {
          "id": "q34a3",
          "text": "A Snowflake internal stage that temporarily holds rows before they are committed to the target table",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q34a4",
          "text": "A serverless function that transforms rows before inserting them into the target table",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpipe](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-intro)"
    },
    {
      "id": "q35",
      "domain": "data-pipeline-design",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data engineer is reviewing the observability setup for a Snowflake ELT pipeline. Select TWO Snowflake-native mechanisms that provide task run history including execution duration and error messages. (Select TWO)",
      "options": [
        {
          "id": "q35a1",
          "text": "`INFORMATION_SCHEMA.TASK_HISTORY()` table function with a time range filter",
          "correct": true,
          "explanation": "Two valid Snowflake-native mechanisms: (A) `INFORMATION_SCHEMA.TASK_HISTORY()` is a table function available with low latency (within minutes) and is the primary operational tool for querying recent task runs; (B) `SNOWFLAKE.ACCOUNT_USAGE.TASK_HISTORY` is a long-retention view (up to 365 days of history) with up to 45-minute latency, suitable for trend analysis. option **C** (`SHOW TASK RUNS`) is not a valid Snowflake command. option **D** (`SYSTEM$TASK_HISTORY()`) is not the correct function name \u2014 the correct function is in `INFORMATION_SCHEMA`."
        },
        {
          "id": "q35a2",
          "text": "`SNOWFLAKE.ACCOUNT_USAGE.TASK_HISTORY` view (with up to 45 minutes of latency)",
          "correct": true,
          "explanation": "Two valid Snowflake-native mechanisms: (A) `INFORMATION_SCHEMA.TASK_HISTORY()` is a table function available with low latency (within minutes) and is the primary operational tool for querying recent task runs; (B) `SNOWFLAKE.ACCOUNT_USAGE.TASK_HISTORY` is a long-retention view (up to 365 days of history) with up to 45-minute latency, suitable for trend analysis. option **C** (`SHOW TASK RUNS`) is not a valid Snowflake command. option **D** (`SYSTEM$TASK_HISTORY()`) is not the correct function name \u2014 the correct function is in `INFORMATION_SCHEMA`."
        },
        {
          "id": "q35a3",
          "text": "`SHOW TASK RUNS` command for real-time task run status",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q35a4",
          "text": "`SYSTEM$TASK_HISTORY()` function filtered by task name",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowflake key concepts and architecture](https://docs.snowflake.com/en/user-guide/intro-key-concepts)"
    },
    {
      "id": "q36",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer runs `ALTER TASK my_task SUSPEND` and then `ALTER TASK my_task RESUME`. What is the net effect on the task's schedule?",
      "options": [
        {
          "id": "q36a1",
          "text": "The task resumes its configured schedule as if it had never been suspended \u2014 no schedule changes occur",
          "correct": true,
          "explanation": "`ALTER TASK ... SUSPEND` stops the task from being scheduled. `ALTER TASK ... RESUME` re-activates the task on its configured schedule from that point forward. The schedule definition (CRON or INTERVAL) is preserved during suspension and resumes exactly as defined \u2014 no changes occur to the schedule itself."
        },
        {
          "id": "q36a2",
          "text": "The task is permanently disabled until a `CREATE OR REPLACE TASK` is issued",
          "correct": false,
          "explanation": "SUSPEND does not permanently disable the task or delete its definition; RESUME restores it without any `CREATE OR REPLACE TASK`."
        },
        {
          "id": "q36a3",
          "text": "The task's schedule is reset to run immediately on resume, then continues the configured interval",
          "correct": false,
          "explanation": "RESUME does not trigger an immediate one-off run; the task simply re-enters its normal scheduling cycle at the next interval."
        },
        {
          "id": "q36a4",
          "text": "Suspending and then resuming a task doubles the schedule interval as a backoff penalty",
          "correct": false,
          "explanation": "Snowflake does not implement any backoff penalty mechanism for suspended tasks."
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q37",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer has a multi-task DAG: ROOT \u2192 [CHILD_A, CHILD_B] \u2192 FINALIZE. The team needs to add a new task CHILD_C that must run after ROOT and before FINALIZE. What is the CORRECT order of DDL operations to modify a live DAG safely?",
      "options": [
        {
          "id": "q37a1",
          "text": "1. Create CHILD_C AFTER ROOT, 2. Alter FINALIZE to add CHILD_C to its AFTER clause, 3. Resume CHILD_C",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q37a2",
          "text": "1. Suspend ROOT, 2. Create CHILD_C AFTER ROOT, 3. Alter FINALIZE to add CHILD_C to its AFTER clause, 4. Resume CHILD_C, 5. Resume ROOT",
          "correct": true,
          "explanation": "Modifying a Snowflake Task DAG requires suspending the root task first to prevent the DAG from running during structural changes. The correct sequence for adding CHILD_C is: (1) Suspend ROOT to freeze the DAG, (2) Create CHILD_C with `AFTER ROOT`, (3) Alter FINALIZE's AFTER clause to include CHILD_C (so FINALIZE waits for A, B, and C), (4) Resume CHILD_C, (5) Resume ROOT to re-activate the DAG. option **A** skips the root suspension, risking a DAG run during modification. Options **C** and **D** are either overly destructive or structurally incorrect."
        },
        {
          "id": "q37a3",
          "text": "1. Drop and recreate the entire DAG with CHILD_C included",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q37a4",
          "text": "1. Suspend FINALIZE, 2. Create CHILD_C, 3. Resume FINALIZE, 4. Resume ROOT",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q38",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "medium",
      "stem": "A task runs a stored procedure that must succeed or fail as a unit. The stored procedure uses a Snowflake scripting block:\n\n```sql\nBEGIN\n  INSERT INTO silver.orders SELECT ... FROM bronze_stream WHERE METADATA$ACTION = 'INSERT';\n  DELETE FROM silver.orders WHERE order_id IN (SELECT order_id FROM bronze_stream WHERE METADATA$ACTION = 'DELETE');\nCOMMIT;\n```\n\nIf the DELETE statement fails, what happens to the INSERT that already ran?",
      "options": [
        {
          "id": "q38a1",
          "text": "The INSERT is committed and the DELETE failure is logged but the task continues",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q38a2",
          "text": "The entire block is rolled back because the transaction is not committed until the COMMIT statement at the end",
          "correct": true,
          "explanation": "In Snowflake scripting blocks, statements between `BEGIN` and `COMMIT` are part of a single transaction. If any statement fails before `COMMIT`, Snowflake automatically rolls back all preceding statements in the block \u2014 the INSERT is rolled back along with the failed DELETE. This ensures atomicity. option **A** (partial commit) violates transactional semantics. option **C** (auto-commit per statement) is not the behavior inside explicit transaction blocks \u2014 auto-commit only applies outside explicit `BEGIN`/`COMMIT` blocks. option **D** (partial retry) is not a Snowflake feature."
        },
        {
          "id": "q38a3",
          "text": "The INSERT is committed because Snowflake auto-commits each DML statement by default inside stored procedures",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q38a4",
          "text": "The task fails and Snowflake automatically retries only the DELETE statement",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q39",
      "domain": "streams-tasks",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data engineer configures a task with `ERROR_INTEGRATION = my_notification_integration`. Select TWO things the error integration can notify when the task fails. (Select TWO)",
      "options": [
        {
          "id": "q39a1",
          "text": "The task name and schema that failed",
          "correct": true,
          "explanation": "When a task fails with an `ERROR_INTEGRATION` configured, Snowflake sends a JSON payload to the configured notification endpoint (SNS, Azure Event Grid, etc.) containing metadata about the failure including: the task name, schema/database, scheduled time, error code, and error message. Options **A** and **B** are included in the notification payload. option **C** (specific SQL line within the task body) is generally not included in the notification payload \u2014 error messages describe the overall failure, not precise line numbers within multi-statement bodies. option **D** (warehouse size recommendation) is not part of Snowflake's notification schema."
        },
        {
          "id": "q39a2",
          "text": "The full ERROR_MESSAGE and ERROR_CODE from the failed run",
          "correct": true,
          "explanation": "When a task fails with an `ERROR_INTEGRATION` configured, Snowflake sends a JSON payload to the configured notification endpoint (SNS, Azure Event Grid, etc.) containing metadata about the failure including: the task name, schema/database, scheduled time, error code, and error message. Options **A** and **B** are included in the notification payload. option **C** (specific SQL line within the task body) is generally not included in the notification payload \u2014 error messages describe the overall failure, not precise line numbers within multi-statement bodies. option **D** (warehouse size recommendation) is not part of Snowflake's notification schema."
        },
        {
          "id": "q39a3",
          "text": "The specific SQL statement within the task body that caused the failure",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q39a4",
          "text": "A recommendation for which warehouse size to use to avoid the failure",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q40",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer's task DAG has CHILD_A and CHILD_B running in parallel after ROOT. CHILD_A succeeds. CHILD_B fails. What happens to FINALIZE (which has `AFTER CHILD_A, CHILD_B`)?",
      "options": [
        {
          "id": "q40a1",
          "text": "FINALIZE runs after CHILD_A succeeds, ignoring CHILD_B's failure",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q40a2",
          "text": "FINALIZE does not run because one of its predecessors (CHILD_B) failed",
          "correct": true,
          "explanation": "In a Snowflake Task DAG, a task with multiple predecessors in its `AFTER` clause (fan-in) only runs if ALL predecessors have completed SUCCESSFULLY. If any predecessor fails (CHILD_B in this case), the downstream task (FINALIZE) is not triggered. The DAG short-circuits at the failure point. FINALIZE is not automatically suspended (D) \u2014 it simply doesn't run for this DAG execution. It will attempt to run again in the next root task cycle. Options A and C misrepresent Snowflake's fan-in behavior."
        },
        {
          "id": "q40a3",
          "text": "FINALIZE runs but receives the failure status from CHILD_B as an input parameter",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q40a4",
          "text": "FINALIZE is suspended automatically when a predecessor fails",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q41",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "medium",
      "stem": "A task uses a `WHEN` clause with a complex expression including `SYSTEM$STREAM_HAS_DATA()`. A data engineer wants to see how often this condition evaluates to FALSE (causing SKIPPED runs) vs TRUE (causing execution) over the past week. Which approach retrieves this information?",
      "options": [
        {
          "id": "q41a1",
          "text": "Query `INFORMATION_SCHEMA.TASK_HISTORY()` and count rows where `STATE = 'SKIPPED'` vs `STATE = 'SUCCEEDED'` or `'FAILED'`",
          "correct": true,
          "explanation": "`INFORMATION_SCHEMA.TASK_HISTORY()` records every scheduled task attempt with its `STATE` \u2014 SKIPPED, SUCCEEDED, or FAILED. Querying and counting by STATE over the past 7 days is the correct way to analyze WHEN clause evaluation frequency. `SYSTEM$STREAM_HAS_DATA()` (B) is a runtime function that returns current state, not a historical log. `SHOW STREAMS` (C) shows stream configuration, not evaluation history. option **D** uses a non-existent `TASK_EXECUTION_LOGGING` property."
        },
        {
          "id": "q41a2",
          "text": "Query `SYSTEM$STREAM_HAS_DATA()` for the past week's historical values",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q41a3",
          "text": "Query `SHOW STREAMS` to see how many times the stream was read vs skipped",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q41a4",
          "text": "Enable `TASK_EXECUTION_LOGGING = TRUE` on the task to write WHEN clause evaluations to a log table",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q42",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer creates two separate streams on the same table for two different downstream consumers:\n\n```sql\nCREATE STREAM orders_stream_silver ON TABLE raw_orders;\nCREATE STREAM orders_stream_audit ON TABLE raw_orders;\n```\n\nA task consumes `orders_stream_silver` and commits successfully. What is the state of `orders_stream_audit` afterward?",
      "options": [
        {
          "id": "q42a1",
          "text": "`orders_stream_audit` is also advanced to the same offset as `orders_stream_silver`",
          "correct": false,
          "explanation": "Stream offsets are independent; consuming one stream never advances another stream's offset."
        },
        {
          "id": "q42a2",
          "text": "`orders_stream_audit` is automatically emptied as a side effect of the other stream's consumption",
          "correct": false,
          "explanation": "One stream's consumption does not empty or affect any other stream on the same table."
        },
        {
          "id": "q42a3",
          "text": "`orders_stream_audit` raises a stale error because the underlying data was consumed by the other stream",
          "correct": false,
          "explanation": "Stale errors occur when a stream's offset exceeds the table's data retention window, not when another stream on the same table is consumed."
        },
        {
          "id": "q42a4",
          "text": "`orders_stream_audit` still contains the unconsumed records because each stream maintains its own independent offset",
          "correct": true,
          "explanation": "Each Snowflake stream on the same table maintains its own independent offset pointer into the table's change-tracking metadata. Consuming (advancing) one stream does not affect the offset of other streams on the same table. `orders_stream_audit` retains all unconsumed records even after `orders_stream_silver` has been consumed and committed. This enables fan-out patterns where multiple consumers independently process the same change data."
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q43",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer wants to determine whether a Dynamic Table `ORDERS_GOLD` is currently set to automatically refresh or has been manually suspended. Which command provides this status most directly?",
      "options": [
        {
          "id": "q43a1",
          "text": "`SELECT * FROM DYNAMIC_TABLE_REFRESH_HISTORY(NAME => 'ORDERS_GOLD') LIMIT 1`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q43a2",
          "text": "`SHOW DYNAMIC TABLES LIKE 'ORDERS_GOLD'` \u2014 check the `SCHEDULING_STATE` column",
          "correct": true,
          "explanation": "`SHOW DYNAMIC TABLES LIKE 'ORDERS_GOLD'` returns a row per matching dynamic table with a `SCHEDULING_STATE` column that shows `ACTIVE` (automatic refresh is enabled) or `SUSPENDED` (refresh is paused). This is the most direct way to check the operational state of a dynamic table. `DYNAMIC_TABLE_REFRESH_HISTORY()` (A) shows past refreshes but not current scheduling state. `DESCRIBE DYNAMIC TABLE` (C) shows the definition but not the live scheduling state. option **D** references a non-existent `IS_SUSPENDED` column in that view."
        },
        {
          "id": "q43a3",
          "text": "`DESCRIBE DYNAMIC TABLE ORDERS_GOLD` \u2014 check the `STATE` field",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q43a4",
          "text": "`SELECT IS_SUSPENDED FROM INFORMATION_SCHEMA.DYNAMIC_TABLES WHERE TABLE_NAME = 'ORDERS_GOLD'`",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q44",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "medium",
      "stem": "A dynamic table `ORDERS_SILVER` has UDFs referenced in its query definition:\n\n```sql\nCREATE DYNAMIC TABLE orders_silver\n  TARGET LAG = '10 minutes'\n  WAREHOUSE = 'DT_WH'\nAS\nSELECT order_id, clean_phone(phone_number) AS phone, amount\nFROM raw_orders;\n```\n\nWhere `clean_phone` is a Python UDF. Is this supported in a dynamic table?",
      "options": [
        {
          "id": "q44a1",
          "text": "Yes, dynamic tables support UDFs in their query definition, but UDFs cause the dynamic table to use FULL refresh mode",
          "correct": true,
          "explanation": "Dynamic Tables do support UDFs (including Python UDFs) in their query definitions. However, UDFs are considered non-deterministic from the perspective of Snowflake's incremental refresh engine \u2014 the engine cannot track which UDF invocations produce changed outputs without re-running the UDF. As a result, dynamic tables that reference UDFs are forced into FULL refresh mode. They still work correctly \u2014 just not incrementally. Option **B** is wrong (UDFs are supported). Option **C** is wrong (incremental refresh is not available with UDFs). Option **D** is wrong (Python UDFs are allowed, but cause full refresh)."
        },
        {
          "id": "q44a2",
          "text": "No, dynamic tables do not support UDFs of any kind in their query definition",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q44a3",
          "text": "Yes, dynamic tables support both SQL and Python UDFs, and the UDF is executed on each refresh (incremental or full)",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q44a4",
          "text": "Yes, but only SQL UDFs are supported; Python UDFs are not allowed in dynamic table definitions",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q45",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "hard",
      "stem": "A dynamic table chain has three levels: DT_A \u2192 DT_B \u2192 DT_C. DT_C has `TARGET LAG = '1 hour'`. DT_B has `TARGET LAG = DOWNSTREAM`. DT_A has a fixed `TARGET LAG = '6 hours'`. A user queries DT_C. The data in DT_A was last refreshed 5 hours ago, DT_B was refreshed 50 minutes ago. Is DT_C within its lag target?",
      "options": [
        {
          "id": "q45a1",
          "text": "No \u2014 DT_C's data is 5 hours stale because it depends on DT_A which is 5 hours old",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q45a2",
          "text": "Yes \u2014 DT_C was refreshed 50 minutes ago via DT_B, so it is within its 1-hour lag target",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q45a3",
          "text": "It depends \u2014 if DT_A had no new data in 5 hours, then DT_C is current; if DT_A had new data, DT_C may be stale",
          "correct": true,
          "explanation": "The key insight is whether DT_A actually had new data since its last refresh. If DT_A's source (raw tables) had no changes in 5 hours, then DT_A is fully current despite not having refreshed recently \u2014 there was nothing new to refresh. DT_B would have refreshed and found no changes from DT_A, and DT_C at 50 minutes is within its 1-hour target. However, if DT_A's source had changes that haven't been incorporated yet (because DT_A won't refresh for another hour due to its 6-hour lag), then DT_C does not reflect those latest changes and is technically stale relative to the source. The answer depends on whether upstream data changed."
        },
        {
          "id": "q45a4",
          "text": "Indeterminate \u2014 DT_C's lag target conflicts with DT_A's 6-hour fixed lag",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q46",
      "domain": "dynamic-tables",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data engineer reviews `DYNAMIC_TABLE_REFRESH_HISTORY()` and finds a row with `STATE = 'FAILED'` and `ERROR_MESSAGE` indicating an out-of-memory error during a FULL refresh. Select TWO remediation strategies. (Select TWO)",
      "options": [
        {
          "id": "q46a1",
          "text": "Increase the warehouse size assigned to the dynamic table's `WAREHOUSE` parameter",
          "correct": true,
          "explanation": "Two valid remediations for OOM during FULL refresh: (A) Use a larger warehouse \u2014 more memory per node directly addresses OOM. (B) Eliminate query constructs (LIMIT, non-deterministic functions, unsupported UDFs) that force FULL refresh mode; enabling incremental refresh processes smaller deltas, using far less memory. option **C** (`MAX_DATA_RETENTION_TIME_IN_DAYS = 0`) is a retention setting, not a memory fix \u2014 and it would not help with refresh OOM. option **D** (switching to Streams+Tasks) is a valid architectural alternative but is a more disruptive change and not the most direct remediation."
        },
        {
          "id": "q46a2",
          "text": "Rewrite the dynamic table query to eliminate features that prevent incremental refresh (such as LIMIT or non-deterministic functions) so incremental refresh can be used instead",
          "correct": true,
          "explanation": "Two valid remediations for OOM during FULL refresh: (A) Use a larger warehouse \u2014 more memory per node directly addresses OOM. (B) Eliminate query constructs (LIMIT, non-deterministic functions, unsupported UDFs) that force FULL refresh mode; enabling incremental refresh processes smaller deltas, using far less memory. option **C** (`MAX_DATA_RETENTION_TIME_IN_DAYS = 0`) is a retention setting, not a memory fix \u2014 and it would not help with refresh OOM. option **D** (switching to Streams+Tasks) is a valid architectural alternative but is a more disruptive change and not the most direct remediation."
        },
        {
          "id": "q46a3",
          "text": "Add a `MAX_DATA_RETENTION_TIME_IN_DAYS = 0` property to the dynamic table to free up memory",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q46a4",
          "text": "Drop the dynamic table and re-create it with a Streams+Tasks pattern instead",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    },
    {
      "id": "q47",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer uses `session.sql()` to run a SQL query and then calls `df.collect()`. What does `collect()` return?",
      "options": [
        {
          "id": "q47a1",
          "text": "A Snowpark DataFrame that can be further transformed",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q47a2",
          "text": "A Python list of `Row` objects containing the query results",
          "correct": true,
          "explanation": "`df.collect()` is a Snowpark action that executes the query in Snowflake and returns the complete result set as a Python list of `Row` objects to the client. Each `Row` is an immutable object with named field access (e.g., `row['column_name']`). It returns a list, not a DataFrame (A), not a generator (C \u2014 use `to_local_iterator()` for that), and not a pandas DataFrame (D \u2014 use `to_pandas()` for that)."
        },
        {
          "id": "q47a3",
          "text": "A generator that lazily fetches rows from Snowflake",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q47a4",
          "text": "A pandas DataFrame with the query results",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q48",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer is registering a permanent Snowpark Python UDF. They specify `stage_location='@my_schema.udf_stage'`. What does the `stage_location` parameter control?",
      "options": [
        {
          "id": "q48a1",
          "text": "The Snowflake internal stage where Snowpark uploads the UDF's Python source file and dependency artifacts for persistence",
          "correct": true,
          "explanation": "When registering a permanent Snowpark UDF (`is_permanent=True`), `stage_location` specifies the Snowflake internal (or external) stage where Snowpark uploads the serialized UDF code, its Python file, and any inline imports. These artifacts are stored persistently so the UDF survives session termination and can be called by other sessions and users. Without a stage location, UDFs are temporary (session-scoped). It is not for execution logs (C) or package downloads from PyPI (D)."
        },
        {
          "id": "q48a2",
          "text": "The external S3 bucket path where Snowflake reads the UDF source code at execution time",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q48a3",
          "text": "The path where Snowflake writes UDF execution logs for debugging",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q48a4",
          "text": "The stage from which Snowflake downloads package dependencies at UDF registration time",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q49",
      "domain": "snowpark",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A data engineer needs to apply a per-store machine learning model to predict demand. Each store has a different model coefficients row. The call is `df.groupBy('store_id').applyInPandas(predict_fn, output_schema)`. Select TWO TRUE statements about this pattern. (Select TWO)",
      "options": [
        {
          "id": "q49a1",
          "text": "`applyInPandas` is a Snowpark method that applies a Python function to each group (partition) of data as a pandas DataFrame",
          "correct": true,
          "explanation": "Snowpark's `groupBy(...).applyInPandas(fn, schema)` applies a Python function to each group as a pandas DataFrame \u2014 enabling per-partition (per-store) operations like loading a store-specific model and generating predictions (**A** and **B** are correct). This is analogous to PySpark's `groupBy().applyInPandas()`. Option **C** is wrong \u2014 pre-sorting is not required; Snowflake handles grouping internally. Option **D** is wrong \u2014 `applyInPandas` is a valid Snowpark API method for this use case."
        },
        {
          "id": "q49a2",
          "text": "Each store's partition is processed as a separate pandas DataFrame, allowing per-store model inference without cross-partition dependencies",
          "correct": true,
          "explanation": "Snowpark's `groupBy(...).applyInPandas(fn, schema)` applies a Python function to each group as a pandas DataFrame \u2014 enabling per-partition (per-store) operations like loading a store-specific model and generating predictions (**A** and **B** are correct). This is analogous to PySpark's `groupBy().applyInPandas()`. Option **C** is wrong \u2014 pre-sorting is not required; Snowflake handles grouping internally. Option **D** is wrong \u2014 `applyInPandas` is a valid Snowpark API method for this use case."
        },
        {
          "id": "q49a3",
          "text": "`applyInPandas` requires the data to be sorted by `store_id` before calling `groupBy`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q49a4",
          "text": "`applyInPandas` is not available in Snowpark; the correct approach is `groupBy().agg()` with a pandas UDF",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q50",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer wants to inspect the SQL that Snowpark will generate for a complex DataFrame transformation before executing it. They call `df.explain(extended=True)`. What additional information does `extended=True` provide compared to a plain `df.explain()`?",
      "options": [
        {
          "id": "q50a1",
          "text": "The estimated row count and credit cost for each plan node",
          "correct": false,
          "explanation": "Snowpark's `explain()` does not produce estimated row counts or credit cost estimates; those appear in Snowflake's query profile after execution."
        },
        {
          "id": "q50a2",
          "text": "The physical execution plan including partition pruning decisions",
          "correct": false,
          "explanation": "The physical execution plan with partition pruning is part of Snowflake's server-side query profile, not the Snowpark client-side `explain()` output."
        },
        {
          "id": "q50a3",
          "text": "Both the logical plan and the generated SQL string that Snowpark will execute",
          "correct": true,
          "explanation": "`df.explain(extended=True)` in Snowpark prints both the logical plan (the tree of Snowpark operations) AND the SQL string that Snowpark will generate and send to Snowflake. Without `extended=True`, only the logical plan is shown. Seeing the generated SQL is valuable for verifying that Snowpark is producing the intended query."
        },
        {
          "id": "q50a4",
          "text": "The explain output from a real dry-run execution in Snowflake",
          "correct": false,
          "explanation": "`explain()` does not execute the query in any form; it only displays the plan that would be sent to Snowflake."
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q51",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer writes:\n\n```python\nfor row in df.to_local_iterator():\n    process_row(row['customer_id'], row['amount'])\n```\n\nCompared to `df.collect()`, what is the primary advantage of `to_local_iterator()` for this pattern?",
      "options": [
        {
          "id": "q51a1",
          "text": "It processes rows inside Snowflake, avoiding any data transfer to the client",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q51a2",
          "text": "It streams rows to the client incrementally, so only a small buffer of rows needs to be in memory at a time",
          "correct": true,
          "explanation": "`to_local_iterator()` returns a Python iterator that fetches rows from Snowflake in chunks, rather than buffering the entire result set in memory like `collect()` does. For large result sets, this means the client only holds a small buffer of rows in RAM at any given time. Option **A** is wrong \u2014 data IS transferred to the client (it's a client-side iterator). Option **C** is a misconception \u2014 for small result sets, `collect()` may be slightly faster due to fewer round-trips. Option **D** is wrong \u2014 Python iterators are single-threaded; parallelizing `process_row()` would require additional concurrency libraries."
        },
        {
          "id": "q51a3",
          "text": "It is faster than `collect()` for small result sets",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q51a4",
          "text": "It automatically parallelizes `process_row()` across multiple CPU cores",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpark Developer Guide for Python](https://docs.snowflake.com/en/developer-guide/snowpark/python/index)"
    },
    {
      "id": "q52",
      "domain": "snowpark",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowpark Python stored procedure calls `session.sql('SELECT ...').collect()` inside its body. The procedure is registered as `EXECUTE AS OWNER`. Which privileges determine whether the SELECT succeeds?",
      "options": [
        {
          "id": "q52a1",
          "text": "The caller's current role privileges, since SQL inside stored procedures always uses the caller's context",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q52a2",
          "text": "The owner's role privileges, since `EXECUTE AS OWNER` means all SQL in the procedure runs with the owner's role's access rights",
          "correct": true,
          "explanation": "`EXECUTE AS OWNER` means the stored procedure runs all SQL statements using the privileges of the role that owns the procedure. The caller's role is not checked for the SQL inside the procedure body \u2014 only for the privilege to EXECUTE the procedure itself. This enables privileged data access patterns where callers can read/write tables through a procedure without holding direct table privileges. option **A** describes `EXECUTE AS CALLER`. Option **C** is wrong \u2014 SYSADMIN is not auto-inherited. Option **D** is wrong \u2014 only the owner's privileges matter for SQL inside an `EXECUTE AS OWNER` procedure."
        },
        {
          "id": "q52a3",
          "text": "The SYSADMIN role's privileges, which are inherited by all stored procedures automatically",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q52a4",
          "text": "Both the caller's and owner's privileges are checked; the query fails unless both roles have SELECT access",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Overview of Access Control](https://docs.snowflake.com/en/user-guide/security-access-control-overview)"
    },
    {
      "id": "q53",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer needs to copy all columns from a `STAGING` table into a `TARGET` table, but exclude a column named `INTERNAL_LOAD_ID` that exists in `STAGING` but should not be in `TARGET`. Which Snowflake approach achieves this without listing all other columns explicitly?",
      "options": [
        {
          "id": "q53a1",
          "text": "`INSERT INTO target SELECT * EXCEPT (internal_load_id) FROM staging`",
          "correct": true,
          "explanation": "Snowflake supports the `SELECT * EXCEPT (col1, col2, ...)` syntax, which selects all columns from a table except the listed ones. `INSERT INTO target SELECT * EXCEPT (internal_load_id) FROM staging` is the correct and concise way to copy all columns except `INTERNAL_LOAD_ID` without listing every other column. option **B** uses `MINUS` which is a set operation, not a column exclusion syntax. Option **C** is wrong \u2014 `WHERE column_name != ...` is a row filter, not a column filter. Option **D** is incorrect \u2014 Snowflake does support `EXCEPT` in SELECT."
        },
        {
          "id": "q53a2",
          "text": "`INSERT INTO target SELECT * MINUS internal_load_id FROM staging`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q53a3",
          "text": "`INSERT INTO target SELECT * FROM staging WHERE column_name != 'internal_load_id'`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q53a4",
          "text": "Column exclusion requires listing all columns explicitly; there is no shorthand in Snowflake SQL",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q54",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer runs:\n\n```sql\nMERGE INTO products_target t\nUSING products_staging s\nON t.product_id = s.product_id\nWHEN MATCHED THEN UPDATE SET t.name = s.name, t.price = s.price\nWHEN NOT MATCHED THEN INSERT (product_id, name, price) VALUES (s.product_id, s.name, s.price)\nWHEN NOT MATCHED BY SOURCE THEN DELETE;\n```\n\nThe staging table has 1,000 rows. The target table has 2,000 rows, of which 800 match staging rows. How many rows are deleted by `WHEN NOT MATCHED BY SOURCE`?",
      "options": [
        {
          "id": "q54a1",
          "text": "200 rows \u2014 the rows in staging not in target",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q54a2",
          "text": "1,200 rows \u2014 the rows in target not in staging",
          "correct": true,
          "explanation": "`WHEN NOT MATCHED BY SOURCE` handles rows that exist in the TARGET but have no matching row in the SOURCE (staging). The target has 2,000 rows total, 800 of which match staging rows. The remaining 2,000 - 800 = 1,200 target rows have no match in staging and are deleted by this clause. The 800 matched rows are updated by `WHEN MATCHED`. The 200 staging rows without a target match (1,000 staging - 800 matching) are inserted by `WHEN NOT MATCHED`. option **A** confuses source-not-in-target (inserts) with target-not-in-source (deletes)."
        },
        {
          "id": "q54a3",
          "text": "800 rows \u2014 the rows that matched and were updated",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q54a4",
          "text": "0 rows \u2014 `WHEN NOT MATCHED BY SOURCE` only applies when staging is empty",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q55",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer uses `GENERATOR(ROWCOUNT => 365)` combined with `DATEADD('day', SEQ4(), '2024-01-01'::DATE)` to generate a date dimension table. What does this expression produce?",
      "options": [
        {
          "id": "q55a1",
          "text": "365 rows with a date column containing dates from 2024-01-01 through 2024-12-31",
          "correct": true,
          "explanation": "`TABLE(GENERATOR(ROWCOUNT => 365))` generates 365 rows. `SEQ4()` produces sequential integers 0, 1, 2, ..., 364 for each row. `DATEADD('day', SEQ4(), '2024-01-01'::DATE)` adds 0, 1, 2, ..., 364 days to January 1, 2024, producing dates January 1 through December 30 (365 days inclusive). This is the standard Snowflake pattern for generating a date dimension. option **B** (infinite) is wrong \u2014 `ROWCOUNT` limits the rows. option **C** (random) is wrong \u2014 `SEQ4()` is sequential. Option **D** is wrong \u2014 `DATEADD` works perfectly with `SEQ4()`."
        },
        {
          "id": "q55a2",
          "text": "An infinite sequence of dates starting from 2024-01-01, limited by a WHERE clause",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q55a3",
          "text": "365 random dates within 2024",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q55a4",
          "text": "An error \u2014 `DATEADD` cannot be used with `SEQ4()` because SEQ4 is non-deterministic",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q56",
      "domain": "performance-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer checks the `QUERY_ACCELERATION_MAX_SCALE_FACTOR` setting on a warehouse. This parameter is currently set to 0. What does a value of 0 mean?",
      "options": [
        {
          "id": "q56a1",
          "text": "Query Acceleration is disabled on this warehouse",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q56a2",
          "text": "Query Acceleration is enabled with no upper limit on the scale factor Snowflake can use",
          "correct": true,
          "explanation": "`QUERY_ACCELERATION_MAX_SCALE_FACTOR = 0` means Query Acceleration Service (QAS) is enabled with NO upper limit on the scale factor. Snowflake can use as much elastic compute as it determines necessary to accelerate the outlier query. Setting it to a positive number (e.g., 8) caps the maximum additional compute at 8x the warehouse size. Setting `ENABLE_QUERY_ACCELERATION = FALSE` disables it entirely. A value of 0 does NOT disable it (**A** is wrong) and does not mean 1x (**C** is wrong)."
        },
        {
          "id": "q56a3",
          "text": "Query Acceleration is enabled but limited to using the same size as the current warehouse (1x scale)",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q56a4",
          "text": "Query Acceleration will only activate for queries that use 0% of the warehouse's capacity",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Using the Query Acceleration Service (QAS)](https://docs.snowflake.com/en/user-guide/query-acceleration-service)"
    },
    {
      "id": "q57",
      "domain": "performance-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer wants to reuse the results of an expensive query that ran 2 hours ago, identified by its `QUERY_ID = '01a2b3c4-...'`. Which SQL expression retrieves the cached result without re-executing the query?",
      "options": [
        {
          "id": "q57a1",
          "text": "`SELECT * FROM TABLE(RESULT_SCAN('01a2b3c4-...'))`",
          "correct": true,
          "explanation": "`SELECT * FROM TABLE(RESULT_SCAN('<query_id>'))` is Snowflake's function for reading a previously executed query's result from the result cache without re-running the query. The result is available for up to 24 hours after the original query executed. option **B** uses a non-existent `SNOWFLAKE.RESULT_CACHE` table. option **C** uses non-existent `EXECUTE USING CACHED_RESULT` syntax. option **D** (`QUERY_HISTORY()`) returns metadata about past queries, not their result sets."
        },
        {
          "id": "q57a2",
          "text": "`SELECT * FROM SNOWFLAKE.RESULT_CACHE WHERE QUERY_ID = '01a2b3c4-...'`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q57a3",
          "text": "`EXECUTE USING CACHED_RESULT '01a2b3c4-...'`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q57a4",
          "text": "`SELECT * FROM TABLE(QUERY_HISTORY()) WHERE QUERY_ID = '01a2b3c4-...'`",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Using Persisted Query Results](https://docs.snowflake.com/en/user-guide/querying-persisted-results)"
    },
    {
      "id": "q58",
      "domain": "streams-tasks",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer runs the following to check task run history:\n\n```sql\nSELECT *\nFROM TABLE(INFORMATION_SCHEMA.TASK_HISTORY(\n  TASK_NAME => 'DAILY_SILVER_LOAD',\n  SCHEDULED_TIME_RANGE_START => DATEADD('day', -7, CURRENT_TIMESTAMP())\n))\nWHERE STATE = 'FAILED'\nORDER BY SCHEDULED_TIME DESC;\n```\n\nThe query returns 0 rows even though the engineer saw failures in the Snowflake UI last week. What is the MOST likely cause?",
      "options": [
        {
          "id": "q58a1",
          "text": "`INFORMATION_SCHEMA.TASK_HISTORY()` only retains data for the last 7 days; failures from exactly 7 days ago may have aged out",
          "correct": true,
          "explanation": "`INFORMATION_SCHEMA.TASK_HISTORY()` retains data for the last 7 days. If the engineer saw failures 7 or more days ago, those records may have aged out of the INFORMATION_SCHEMA view. For longer-term history (up to 365 days), use `SNOWFLAKE.ACCOUNT_USAGE.TASK_HISTORY`. Option **B** is a possible issue but task names in INFORMATION_SCHEMA are typically case-insensitive when Snowflake stores them in uppercase by default. Option **C** is wrong \u2014 `TASK_NAME` IS a valid parameter for the function. Option **D** is wrong \u2014 failed runs are definitely recorded."
        },
        {
          "id": "q58a2",
          "text": "The `TASK_NAME` filter is case-sensitive and must match the task's name exactly as stored in Snowflake (uppercase by default)",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q58a3",
          "text": "`TASK_HISTORY()` does not support the `TASK_NAME` parameter; it must be filtered with a WHERE clause instead",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q58a4",
          "text": "Failed task runs are not recorded in `INFORMATION_SCHEMA.TASK_HISTORY()`; only SUCCEEDED runs are tracked",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to streams](https://docs.snowflake.com/en/user-guide/streams-intro)"
    },
    {
      "id": "q59",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer builds a pipeline where a Snowflake Task runs a stored procedure that performs data quality checks. If the check fails, the stored procedure should cause the task to fail and stop downstream tasks from running. Which Snowpark Python construct inside the stored procedure achieves this?",
      "options": [
        {
          "id": "q59a1",
          "text": "`return False` \u2014 returning a falsy value from the stored procedure marks the task as failed",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q59a2",
          "text": "Raise a Python exception using `raise ValueError('DQ check failed: null_pct exceeded threshold')` \u2014 an unhandled exception causes the task to fail",
          "correct": true,
          "explanation": "In a Snowpark Python stored procedure called by a task, raising an unhandled Python exception causes the stored procedure to exit with an error, which in turn marks the task run as FAILED. A FAILED task stops downstream dependent tasks from running in the current DAG cycle. Option **A** is wrong \u2014 returning `False` does not fail the task; only unhandled exceptions do. option **C** (suspending the task from within) is overly aggressive and changes the task's schedule, not just the current run. option **D** uses a non-existent `SYSTEM$FAIL_TASK` procedure."
        },
        {
          "id": "q59a3",
          "text": "`session.sql('ALTER TASK my_task SUSPEND').collect()` \u2014 suspending the task from within stops downstream processing",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q59a4",
          "text": "`CALL SYSTEM$FAIL_TASK('DQ check failed')` \u2014 a system procedure call that marks the current task as failed",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q60",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer is using the Snowflake Kafka Connector to load data from a Kafka topic into Snowflake. By default, in what format does the Kafka Connector stage data before committing it to the target table?",
      "options": [
        {
          "id": "q60a1",
          "text": "CSV files in an internal stage, loaded via COPY INTO",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q60a2",
          "text": "Avro or JSON files in an internal stage, loaded via Snowpipe (auto-ingest) to the target table",
          "correct": true,
          "explanation": "The Snowflake Kafka Connector works by: (1) buffering incoming Kafka messages, (2) writing them as Avro or JSON files to a Snowflake internal stage, and (3) using Snowpipe to auto-ingest those staged files into the target Snowflake table. This file-based staging approach provides durability and enables efficient bulk loading. The connector does not write CSV (A), does not use external stages by default (C), and does not use direct JDBC inserts (D) \u2014 the staging+Snowpipe pattern is the connector's native mechanism."
        },
        {
          "id": "q60a3",
          "text": "Parquet files in an external S3 stage, loaded via COPY INTO",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q60a4",
          "text": "The Kafka Connector inserts rows directly via JDBC without staging",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpipe](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-intro)"
    },
    {
      "id": "q61",
      "domain": "data-pipeline-design",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer is designing a pipeline to ingest 10,000 small IoT device readings per second into Snowflake with end-to-end latency of under 5 seconds. Which ingestion approach is BEST suited for this requirement?",
      "options": [
        {
          "id": "q61a1",
          "text": "Classic Snowpipe with file-based auto-ingest from S3",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q61a2",
          "text": "A Snowflake Task scheduled every 5 minutes that bulk-loads from an external stage",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q61a3",
          "text": "Snowpipe Streaming using the `insertRows()` SDK to push rows directly without file staging",
          "correct": true,
          "explanation": "Snowpipe Streaming (`insertRows()` SDK) is purpose-built for low-latency, high-throughput row insertion. It bypasses file staging entirely \u2014 the SDK sends rows directly to Snowflake's streaming endpoint, achieving sub-second to a few-second end-to-end latency. Classic Snowpipe (A) has higher latency (typically 1\u20133 minutes) due to file batching and staging. A 5-minute task (B) has far too much latency. `COPY INTO` every 30 seconds (C) still involves file staging overhead and 30-second minimum latency."
        },
        {
          "id": "q61a4",
          "text": "`COPY INTO` triggered by a task every 30 seconds from a landing stage",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Snowpipe](https://docs.snowflake.com/en/user-guide/data-load-snowpipe-intro)"
    },
    {
      "id": "q62",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer writes a MERGE statement where the source has two rows with the same key. When two source rows match the same target row, what does Snowflake do by default?",
      "options": [
        {
          "id": "q62a1",
          "text": "Snowflake applies the UPDATE for the first matching source row and ignores subsequent source rows for the same target row",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q62a2",
          "text": "Snowflake raises an error: 'MERGE INTO ... cannot update the same row of the target table multiple times in a single MERGE statement'",
          "correct": true,
          "explanation": "In Snowflake, if a MERGE statement's source dataset contains multiple rows that would match the same target row (violating the merge's one-to-one assumption for matched rows), Snowflake raises a non-determinism error: 'merge into ... cannot be applied to a target table with a variable number of rows for each row of the source table.' This is a critical guard against non-deterministic updates. The developer must deduplicate the source before merging. Options A, C, and D all describe behaviors that would produce non-deterministic results, which is exactly what Snowflake prevents."
        },
        {
          "id": "q62a3",
          "text": "Snowflake applies both UPDATEs sequentially, with the last source row's values winning",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q62a4",
          "text": "Snowflake ignores both rows and leaves the target row unchanged when there are duplicate source matches",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q63",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer needs to insert data from `source_table` into `target_table` but exclude any rows that already exist in `target_table` (identified by `record_id`). They want to avoid a MERGE and use the simplest INSERT approach. Which SQL accomplishes this?",
      "options": [
        {
          "id": "q63a1",
          "text": "`INSERT INTO target_table SELECT * FROM source_table EXCEPT SELECT * FROM target_table`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q63a2",
          "text": "`INSERT INTO target_table SELECT s.* FROM source_table s LEFT JOIN target_table t ON s.record_id = t.record_id WHERE t.record_id IS NULL`",
          "correct": true,
          "explanation": "Using a LEFT JOIN with a NULL filter (`WHERE t.record_id IS NULL`) is the standard Snowflake pattern for an anti-join insert \u2014 inserting only rows from the source that don't exist in the target. option **A** uses `EXCEPT` which compares entire rows (all columns), not just the `record_id` key \u2014 this can miss duplicates if other columns differ. option **C** uses `INSERT IGNORE` which is MySQL syntax, not valid in Snowflake. option **D** (NOT IN subquery) works but can have performance issues with large target tables and NULL handling edge cases \u2014 the LEFT JOIN anti-join (B) is more robust and idiomatic."
        },
        {
          "id": "q63a3",
          "text": "`INSERT IGNORE INTO target_table SELECT * FROM source_table`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q63a4",
          "text": "`INSERT INTO target_table SELECT * FROM source_table WHERE record_id NOT IN (SELECT record_id FROM target_table)`",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q64",
      "domain": "streams-tasks",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "A data engineer wants to create a task that runs three steps in a specific order within a single task (not a DAG). Select TWO constructs that allow multiple SQL statements to execute sequentially in a single task's body. (Select TWO)",
      "options": [
        {
          "id": "q64a1",
          "text": "A stored procedure called from the task body, encapsulating the three SQL statements",
          "correct": true,
          "explanation": "Calling a stored procedure from the task body is a clean, encapsulated approach; the task body is a single `CALL` statement, and the stored procedure contains all three steps."
        },
        {
          "id": "q64a2",
          "text": "A Snowflake scripting block using `BEGIN ... END` syntax containing the three statements",
          "correct": true,
          "explanation": "A Snowflake scripting `BEGIN ... END` block used directly as the task body allows multiple SQL statements to execute sequentially within a single task."
        },
        {
          "id": "q64a3",
          "text": "Separating statements with semicolons in the task body (tasks natively support multi-statement bodies)",
          "correct": false,
          "explanation": "Snowflake task bodies support only a SINGLE SQL statement or a `CALL` to a stored procedure. You cannot chain multiple independent statements with semicolons in a task body."
        },
        {
          "id": "q64a4",
          "text": "An `EXECUTE IMMEDIATE` call with multiple SQL strings concatenated into a single string argument",
          "correct": false,
          "explanation": "`EXECUTE IMMEDIATE` executes a single dynamically-supplied SQL string. Concatenating multiple statements into one string does not create a multi-step task body; it would need to be wrapped inside a scripting block (option B) to work properly."
        }
      ],
      "reference": "[Introduction to tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q65",
      "domain": "dynamic-tables",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer has a dynamic table `REVENUE_SUMMARY` with `TARGET LAG = '1 hour'`. A critical upstream data correction is made, and the team needs `REVENUE_SUMMARY` to reflect the corrected data immediately, not in the next scheduled refresh. Which command achieves this without changing the lag setting?",
      "options": [
        {
          "id": "q65a1",
          "text": "`ALTER DYNAMIC TABLE revenue_summary SET TARGET LAG = '1 minute'` then revert it back after the refresh",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q65a2",
          "text": "`ALTER DYNAMIC TABLE revenue_summary REFRESH`",
          "correct": true,
          "explanation": "`ALTER DYNAMIC TABLE <name> REFRESH` triggers an immediate on-demand refresh of the dynamic table, incorporating the latest data from base tables right now, without altering the `TARGET LAG` setting or affecting the regular schedule. After the manual refresh completes, the table continues refreshing on its normal 1-hour schedule. option **A** would modify the schedule globally and require a follow-up change. option **C** uses a non-existent system procedure. option **D** would rebuild the entire table, causing a full-refresh compute cost and dropping/recreating the table object."
        },
        {
          "id": "q65a3",
          "text": "`CALL SYSTEM$DYNAMIC_TABLE_REFRESH('revenue_summary')`",
          "correct": false,
          "explanation": ""
        },
        {
          "id": "q65a4",
          "text": "`CREATE OR REPLACE DYNAMIC TABLE revenue_summary ...` to force an immediate rebuild",
          "correct": false,
          "explanation": ""
        }
      ],
      "reference": "[Dynamic tables](https://docs.snowflake.com/en/user-guide/dynamic-tables-intro)"
    }
  ]
}
