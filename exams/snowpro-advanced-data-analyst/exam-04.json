{
  "meta": {
    "id": "snowpro-data-analyst-exam-04",
    "certification": "snowpro-advanced-data-analyst",
    "certificationName": "SnowPro Advanced: Data Analyst",
    "title": "SF-SPADA-4: Advanced Practice Scenarios",
    "version": "1.0",
    "timeLimit": 115,
    "passingScore": 75,
    "totalQuestions": 65,
    "domains": [
      {
        "id": "advanced-sql",
        "name": "Advanced SQL",
        "weight": 17
      },
      {
        "id": "semi-structured-data",
        "name": "Semi-structured Data",
        "weight": 17
      },
      {
        "id": "cortex-ai",
        "name": "Cortex AI",
        "weight": 17
      },
      {
        "id": "performance-and-tuning",
        "name": "Performance and Tuning",
        "weight": 17
      },
      {
        "id": "data-governance",
        "name": "Governance and Access",
        "weight": 17
      },
      {
        "id": "visualization-dashboards",
        "name": "Visualization and Reporting",
        "weight": 15
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "easy",
      "stem": "Revenue Pipeline Scenario: **QUALIFY** must handle `checkpoint retries` while preserving `metric comparison`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for revenue pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "B",
          "text": "Use QUALIFY for revenue pipeline scenario to handle `checkpoint retries`."
        },
        {
          "id": "C",
          "text": "Use visual tile for revenue pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "D",
          "text": "Use admin grant for revenue pipeline scenario instead of configuring QUALIFY."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because revenue pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\n**B** is correct because revenue pipeline scenario needs QUALIFY specifically for `checkpoint retries`. The documentation describes that product behavior, and `metric comparison` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because revenue pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\n**D** is incorrect because revenue pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\nSee [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) for the vendor documentation that supports the correct choice.",
      "reference": "[QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify)"
    },
    {
      "id": "q02",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Fraud Pipeline Scenario: **Window functions** must handle `duplicate key ranking` while preserving `labeled quality monitoring`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Window functions for fraud pipeline scenario to handle `duplicate key ranking`."
        },
        {
          "id": "B",
          "text": "Use visual tile for fraud pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "C",
          "text": "Use admin grant for fraud pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "D",
          "text": "Use local notebook for fraud pipeline scenario instead of configuring Window functions."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because fraud pipeline scenario needs Window functions specifically for `duplicate key ranking`. The documentation describes that product behavior, and `labeled quality monitoring` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because fraud pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\n**C** is incorrect because fraud pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\n**D** is incorrect because fraud pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\nSee [Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using) for the vendor documentation that supports the correct choice.",
      "reference": "[Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using)"
    },
    {
      "id": "q03",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "medium",
      "stem": "Telemetry Pipeline Scenario: **FLATTEN** must handle `nested array expansion` while preserving `dual store features`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for telemetry pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "B",
          "text": "Use local notebook for telemetry pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "C",
          "text": "Use driver file for telemetry pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "D",
          "text": "Use FLATTEN for telemetry pipeline scenario to handle `nested array expansion`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because telemetry pipeline scenario would still lack FLATTEN; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**B** is incorrect because telemetry pipeline scenario would still lack FLATTEN; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**C** is incorrect because telemetry pipeline scenario would still lack FLATTEN; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**D** is correct because telemetry pipeline scenario needs FLATTEN specifically for `nested array expansion`. The documentation describes that product behavior, and `dual store features` keeps the workflow inside the managed platform capability.\n\nSee [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) for the vendor documentation that supports the correct choice.",
      "reference": "[FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten)"
    },
    {
      "id": "q04",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "Partner Pipeline Scenario: **Semi-structured querying** must handle `cache invalidation` while preserving `Pipeline condition step`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for partner pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "B",
          "text": "Use driver file for partner pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "C",
          "text": "Use Semi-structured querying for partner pipeline scenario to handle `cache invalidation`."
        },
        {
          "id": "D",
          "text": "Use report table for partner pipeline scenario instead of configuring Semi-structured querying."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because partner pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\n**B** is incorrect because partner pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\n**C** is correct because partner pipeline scenario needs Semi-structured querying specifically for `cache invalidation`. The documentation describes that product behavior, and `Pipeline condition step` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because partner pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\nSee [Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured) for the vendor documentation that supports the correct choice.",
      "reference": "[Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured)"
    },
    {
      "id": "q05",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "easy",
      "stem": "Clinical Pipeline Scenario: **PIVOT** must handle `role visibility` while preserving `scheduled drift statistics`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for clinical pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "B",
          "text": "Use PIVOT for clinical pipeline scenario to handle `role visibility`."
        },
        {
          "id": "C",
          "text": "Use report table for clinical pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "D",
          "text": "Use shell cron for clinical pipeline scenario instead of configuring PIVOT."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because clinical pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `role visibility` or preserve `scheduled drift statistics` in the supported workflow.\n\n**B** is correct because clinical pipeline scenario needs PIVOT specifically for `role visibility`. The documentation describes that product behavior, and `scheduled drift statistics` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because clinical pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `role visibility` or preserve `scheduled drift statistics` in the supported workflow.\n\n**D** is incorrect because clinical pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `role visibility` or preserve `scheduled drift statistics` in the supported workflow.\n\nSee [PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot) for the vendor documentation that supports the correct choice.",
      "reference": "[PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot)"
    },
    {
      "id": "q06",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "medium",
      "stem": "Finance Reconciliation Over Late Arriving Invoices With Approval Cutoffs And Immutable Audit Columns: **GROUP BY extensions** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use GROUP BY extensions for loan pipeline scenario to handle `metric comparison`."
        },
        {
          "id": "B",
          "text": "Use report table for loan pipeline scenario instead of configuring GROUP BY extensions."
        },
        {
          "id": "C",
          "text": "Use shell cron for loan pipeline scenario instead of configuring GROUP BY extensions."
        },
        {
          "id": "D",
          "text": "Use email approval for loan pipeline scenario instead of configuring GROUP BY extensions."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because loan pipeline scenario needs GROUP BY extensions specifically for `metric comparison`. The documentation describes that product behavior, and `recursive hierarchy traversal` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because loan pipeline scenario would still lack GROUP BY extensions; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\n**C** is incorrect because loan pipeline scenario would still lack GROUP BY extensions; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\n**D** is incorrect because loan pipeline scenario would still lack GROUP BY extensions; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\nSee [GROUP BY extensions](https://docs.snowflake.com/en/sql-reference/constructs/group-by-rollup) for the vendor documentation that supports the correct choice.",
      "reference": "[GROUP BY extensions](https://docs.snowflake.com/en/sql-reference/constructs/group-by-rollup)"
    },
    {
      "id": "q07",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "medium",
      "stem": "Slotting Pipeline Scenario: **Cortex COMPLETE** must handle `point in time joins` while preserving `materialized view maintenance`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for slotting pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "B",
          "text": "Use email approval for slotting pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for slotting pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "D",
          "text": "Use Cortex COMPLETE for slotting pipeline scenario to handle `point in time joins`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because slotting pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**B** is incorrect because slotting pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**C** is incorrect because slotting pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**D** is correct because slotting pipeline scenario needs Cortex COMPLETE specifically for `point in time joins`. The documentation describes that product behavior, and `materialized view maintenance` keeps the workflow inside the managed platform capability.\n\nSee [Cortex COMPLETE](https://docs.snowflake.com/en/sql-reference/functions/complete-snowflake-cortex) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex COMPLETE](https://docs.snowflake.com/en/sql-reference/functions/complete-snowflake-cortex)"
    },
    {
      "id": "q08",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "Consent Pipeline Scenario: **Cortex Search** must handle `offline object scoring` while preserving `Search service refresh`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for consent pipeline scenario instead of configuring Cortex Search."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for consent pipeline scenario instead of configuring Cortex Search."
        },
        {
          "id": "C",
          "text": "Use Cortex Search for consent pipeline scenario to handle `offline object scoring`."
        },
        {
          "id": "D",
          "text": "Use visual tile for consent pipeline scenario instead of configuring Cortex Search."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because consent pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\n**B** is incorrect because consent pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\n**C** is correct because consent pipeline scenario needs Cortex Search specifically for `offline object scoring`. The documentation describes that product behavior, and `Search service refresh` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because consent pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\nSee [Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview)"
    },
    {
      "id": "q09",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "easy",
      "stem": "Campaign Pipeline Scenario: **Cortex Analyst** must handle `scheduled drift statistics` while preserving `registry approval state`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for campaign pipeline scenario instead of configuring Cortex Analyst."
        },
        {
          "id": "B",
          "text": "Use Cortex Analyst for campaign pipeline scenario to handle `scheduled drift statistics`."
        },
        {
          "id": "C",
          "text": "Use visual tile for campaign pipeline scenario instead of configuring Cortex Analyst."
        },
        {
          "id": "D",
          "text": "Use admin grant for campaign pipeline scenario instead of configuring Cortex Analyst."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because campaign pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\n**B** is correct because campaign pipeline scenario needs Cortex Analyst specifically for `scheduled drift statistics`. The documentation describes that product behavior, and `registry approval state` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because campaign pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\n**D** is incorrect because campaign pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\nSee [Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst)"
    },
    {
      "id": "q10",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "Intent Pipeline Scenario: **Result cache** must handle `metric gated registration` while preserving `bursty join sizing`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Result cache for intent pipeline scenario to handle `metric gated registration`."
        },
        {
          "id": "B",
          "text": "Use visual tile for intent pipeline scenario instead of configuring Result cache."
        },
        {
          "id": "C",
          "text": "Use admin grant for intent pipeline scenario instead of configuring Result cache."
        },
        {
          "id": "D",
          "text": "Use local notebook for intent pipeline scenario instead of configuring Result cache."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because intent pipeline scenario needs Result cache specifically for `metric gated registration`. The documentation describes that product behavior, and `bursty join sizing` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because intent pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\n**C** is incorrect because intent pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\n**D** is incorrect because intent pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\nSee [Result cache](https://docs.snowflake.com/en/user-guide/querying-persisted-results) for the vendor documentation that supports the correct choice.",
      "reference": "[Result cache](https://docs.snowflake.com/en/user-guide/querying-persisted-results)"
    },
    {
      "id": "q11",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "medium",
      "stem": "Insurance Reserve Triangle Review With Actuarial Cohorts And Locked Reporting Periods: **Query Profile** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for claims pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "B",
          "text": "Use local notebook for claims pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "C",
          "text": "Use driver file for claims pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "D",
          "text": "Use Query Profile for claims pipeline scenario to handle `idle serverless serving`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because claims pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `idle serverless serving` or preserve `Hyperopt parallel search` in the supported workflow.\n\n**B** is incorrect because claims pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `idle serverless serving` or preserve `Hyperopt parallel search` in the supported workflow.\n\n**C** is incorrect because claims pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `idle serverless serving` or preserve `Hyperopt parallel search` in the supported workflow.\n\n**D** is correct because claims pipeline scenario needs Query Profile specifically for `idle serverless serving`. The documentation describes that product behavior, and `Hyperopt parallel search` keeps the workflow inside the managed platform capability.\n\nSee [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) for the vendor documentation that supports the correct choice.",
      "reference": "[Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile)"
    },
    {
      "id": "q12",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "hard",
      "stem": "Inventory Pipeline Scenario: **Clustering keys** must handle `registry approval state` while preserving `OBJECT CONSTRUCT payload`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for inventory pipeline scenario instead of configuring Clustering keys."
        },
        {
          "id": "B",
          "text": "Use driver file for inventory pipeline scenario instead of configuring Clustering keys."
        },
        {
          "id": "C",
          "text": "Use Clustering keys for inventory pipeline scenario to handle `registry approval state`."
        },
        {
          "id": "D",
          "text": "Use report table for inventory pipeline scenario instead of configuring Clustering keys."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because inventory pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\n**B** is incorrect because inventory pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\n**C** is correct because inventory pipeline scenario needs Clustering keys specifically for `registry approval state`. The documentation describes that product behavior, and `OBJECT CONSTRUCT payload` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because inventory pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\nSee [Clustering keys](https://docs.snowflake.com/en/user-guide/tables-clustering-keys) for the vendor documentation that supports the correct choice.",
      "reference": "[Clustering keys](https://docs.snowflake.com/en/user-guide/tables-clustering-keys)"
    },
    {
      "id": "q13",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "easy",
      "stem": "Payment Pipeline Scenario: **Materialized views** must handle `profile spill diagnosis` while preserving `SQL language model call`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for payment pipeline scenario instead of configuring Materialized views."
        },
        {
          "id": "B",
          "text": "Use Materialized views for payment pipeline scenario to handle `profile spill diagnosis`."
        },
        {
          "id": "C",
          "text": "Use report table for payment pipeline scenario instead of configuring Materialized views."
        },
        {
          "id": "D",
          "text": "Use shell cron for payment pipeline scenario instead of configuring Materialized views."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because payment pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\n**B** is correct because payment pipeline scenario needs Materialized views specifically for `profile spill diagnosis`. The documentation describes that product behavior, and `SQL language model call` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because payment pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\n**D** is incorrect because payment pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\nSee [Materialized views](https://docs.snowflake.com/en/user-guide/views-materialized) for the vendor documentation that supports the correct choice.",
      "reference": "[Materialized views](https://docs.snowflake.com/en/user-guide/views-materialized)"
    },
    {
      "id": "q14",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Recommendation Pipeline Scenario: **Masking policies** must handle `unpivoted measure rows` while preserving `dependency lineage graph`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Masking policies for recommendation pipeline scenario to handle `unpivoted measure rows`."
        },
        {
          "id": "B",
          "text": "Use report table for recommendation pipeline scenario instead of configuring Masking policies."
        },
        {
          "id": "C",
          "text": "Use shell cron for recommendation pipeline scenario instead of configuring Masking policies."
        },
        {
          "id": "D",
          "text": "Use email approval for recommendation pipeline scenario instead of configuring Masking policies."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because recommendation pipeline scenario needs Masking policies specifically for `unpivoted measure rows`. The documentation describes that product behavior, and `dependency lineage graph` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because recommendation pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `unpivoted measure rows` or preserve `dependency lineage graph` in the supported workflow.\n\n**C** is incorrect because recommendation pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `unpivoted measure rows` or preserve `dependency lineage graph` in the supported workflow.\n\n**D** is incorrect because recommendation pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `unpivoted measure rows` or preserve `dependency lineage graph` in the supported workflow.\n\nSee [Masking policies](https://docs.snowflake.com/en/user-guide/security-column-ddm-use) for the vendor documentation that supports the correct choice.",
      "reference": "[Masking policies](https://docs.snowflake.com/en/user-guide/security-column-ddm-use)"
    },
    {
      "id": "q15",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "medium",
      "stem": "Contract Pipeline Scenario: **Row access policies** must handle `SQL language model call` while preserving `Clarify bias baseline`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for contract pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "B",
          "text": "Use email approval for contract pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for contract pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "D",
          "text": "Use Row access policies for contract pipeline scenario to handle `SQL language model call`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because contract pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**B** is incorrect because contract pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**C** is incorrect because contract pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**D** is correct because contract pipeline scenario needs Row access policies specifically for `SQL language model call`. The documentation describes that product behavior, and `Clarify bias baseline` keeps the workflow inside the managed platform capability.\n\nSee [Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro)"
    },
    {
      "id": "q16",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "Customer Contact Center Transcript Mining With Language Tags And Escalation Categories: **Object tagging** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for energy pipeline scenario instead of configuring Object tagging."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for energy pipeline scenario instead of configuring Object tagging."
        },
        {
          "id": "C",
          "text": "Use Object tagging for energy pipeline scenario to handle `traffic split rollout`."
        },
        {
          "id": "D",
          "text": "Use visual tile for energy pipeline scenario instead of configuring Object tagging."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because energy pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\n**B** is incorrect because energy pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\n**C** is correct because energy pipeline scenario needs Object tagging specifically for `traffic split rollout`. The documentation describes that product behavior, and `checkpoint retries` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because energy pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\nSee [Object tagging](https://docs.snowflake.com/en/user-guide/object-tagging) for the vendor documentation that supports the correct choice.",
      "reference": "[Object tagging](https://docs.snowflake.com/en/user-guide/object-tagging)"
    },
    {
      "id": "q17",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "easy",
      "stem": "Supplier Pipeline Scenario: **Access History** must handle `post load aggregate refresh` while preserving `optional JSON extraction`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for supplier pipeline scenario instead of configuring Access History."
        },
        {
          "id": "B",
          "text": "Use Access History for supplier pipeline scenario to handle `post load aggregate refresh`."
        },
        {
          "id": "C",
          "text": "Use visual tile for supplier pipeline scenario instead of configuring Access History."
        },
        {
          "id": "D",
          "text": "Use admin grant for supplier pipeline scenario instead of configuring Access History."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because supplier pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\n**B** is correct because supplier pipeline scenario needs Access History specifically for `post load aggregate refresh`. The documentation describes that product behavior, and `optional JSON extraction` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because supplier pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\n**D** is incorrect because supplier pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\nSee [Access History](https://docs.snowflake.com/en/sql-reference/account-usage/access_history) for the vendor documentation that supports the correct choice.",
      "reference": "[Access History](https://docs.snowflake.com/en/sql-reference/account-usage/access_history)"
    },
    {
      "id": "q18",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "medium",
      "stem": "Dispatch Pipeline Scenario: **Snowsight dashboards** must handle `optional JSON extraction` while preserving `email masking rule`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Snowsight dashboards for dispatch pipeline scenario to handle `optional JSON extraction`."
        },
        {
          "id": "B",
          "text": "Use visual tile for dispatch pipeline scenario instead of configuring Snowsight dashboards."
        },
        {
          "id": "C",
          "text": "Use admin grant for dispatch pipeline scenario instead of configuring Snowsight dashboards."
        },
        {
          "id": "D",
          "text": "Use local notebook for dispatch pipeline scenario instead of configuring Snowsight dashboards."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because dispatch pipeline scenario needs Snowsight dashboards specifically for `optional JSON extraction`. The documentation describes that product behavior, and `email masking rule` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because dispatch pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\n**C** is incorrect because dispatch pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\n**D** is incorrect because dispatch pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\nSee [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) for the vendor documentation that supports the correct choice.",
      "reference": "[Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards)"
    },
    {
      "id": "q19",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "medium",
      "stem": "Session Pipeline Scenario: **Streamlit in Snowflake** must handle `sparse tabular algorithm` while preserving `Model Monitor schedule`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for session pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "B",
          "text": "Use local notebook for session pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "C",
          "text": "Use driver file for session pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "D",
          "text": "Use Streamlit in Snowflake for session pipeline scenario to handle `sparse tabular algorithm`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because session pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**B** is incorrect because session pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**C** is incorrect because session pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**D** is correct because session pipeline scenario needs Streamlit in Snowflake specifically for `sparse tabular algorithm`. The documentation describes that product behavior, and `Model Monitor schedule` keeps the workflow inside the managed platform capability.\n\nSee [Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit) for the vendor documentation that supports the correct choice.",
      "reference": "[Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit)"
    },
    {
      "id": "q20",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "Identity Pipeline Scenario: **Tasks** must handle `script dependency packaging` while preserving `cache invalidation`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for identity pipeline scenario instead of configuring Tasks."
        },
        {
          "id": "B",
          "text": "Use driver file for identity pipeline scenario instead of configuring Tasks."
        },
        {
          "id": "C",
          "text": "Use Tasks for identity pipeline scenario to handle `script dependency packaging`."
        },
        {
          "id": "D",
          "text": "Use report table for identity pipeline scenario instead of configuring Tasks."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because identity pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `script dependency packaging` or preserve `cache invalidation` in the supported workflow.\n\n**B** is incorrect because identity pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `script dependency packaging` or preserve `cache invalidation` in the supported workflow.\n\n**C** is correct because identity pipeline scenario needs Tasks specifically for `script dependency packaging`. The documentation describes that product behavior, and `cache invalidation` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because identity pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `script dependency packaging` or preserve `cache invalidation` in the supported workflow.\n\nSee [Tasks](https://docs.snowflake.com/en/user-guide/tasks-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q21",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "easy",
      "stem": "Label Pipeline Scenario: **QUALIFY** must handle `semantic retrieval setup` while preserving `semantic retrieval setup`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for label pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "B",
          "text": "Use QUALIFY for label pipeline scenario to handle `semantic retrieval setup`."
        },
        {
          "id": "C",
          "text": "Use report table for label pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "D",
          "text": "Use shell cron for label pipeline scenario instead of configuring QUALIFY."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because label pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\n**B** is correct because label pipeline scenario needs QUALIFY specifically for `semantic retrieval setup`. The documentation describes that product behavior, and `semantic retrieval setup` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because label pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\n**D** is incorrect because label pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\nSee [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) for the vendor documentation that supports the correct choice.",
      "reference": "[QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify)"
    },
    {
      "id": "q22",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "Snowflake window functions use an OVER clause with PARTITION BY, ORDER BY, and window frames such as ROWS or RANGE.\n\nA practitioner must choose the documented **Window functions** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use Window functions because the documentation supports this exact behavior: Snowflake window functions use an OVER clause with PARTITION BY, ORDER BY, and window frames such as ROWS or RANGE."
        },
        {
          "id": "B",
          "text": "Use report table for champion pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "C",
          "text": "Use shell cron for champion pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "D",
          "text": "Use email approval for champion pipeline scenario instead of configuring Window functions."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because Snowflake window functions use an OVER clause with PARTITION BY, ORDER BY, and window frames such as ROWS or RANGE. This directly matches the scenario and is supported by the referenced Window functions documentation.\n\n**B** is incorrect because it does not provide the documented Window functions behavior described here: Snowflake window functions use an OVER clause with PARTITION BY, ORDER BY, and window frames such as ROWS or RANGE.\n\n**C** is incorrect because it does not provide the documented Window functions behavior described here: Snowflake window functions use an OVER clause with PARTITION BY, ORDER BY, and window frames such as ROWS or RANGE.\n\n**D** is incorrect because it does not provide the documented Window functions behavior described here: Snowflake window functions use an OVER clause with PARTITION BY, ORDER BY, and window frames such as ROWS or RANGE.\n\nSee [Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using) for the vendor documentation that supports the correct choice.",
      "reference": "[Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using)"
    },
    {
      "id": "q23",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "medium",
      "stem": "Snowflake FLATTEN is a table function for semi-structured VARIANT, OBJECT, and ARRAY values with INPUT, PATH, OUTER, and RECURSIVE arguments.\n\nA practitioner must choose the documented **FLATTEN** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for support pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "B",
          "text": "Use email approval for support pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for support pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "D",
          "text": "Use FLATTEN because the documentation supports this exact behavior: Snowflake FLATTEN is a table function for semi-structured VARIANT, OBJECT, and ARRAY values with INPUT, PATH, OUTER, and RECURSIVE arguments."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN is a table function for semi-structured VARIANT, OBJECT, and ARRAY values with INPUT, PATH, OUTER, and RECURSIVE arguments.\n\n**B** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN is a table function for semi-structured VARIANT, OBJECT, and ARRAY values with INPUT, PATH, OUTER, and RECURSIVE arguments.\n\n**C** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN is a table function for semi-structured VARIANT, OBJECT, and ARRAY values with INPUT, PATH, OUTER, and RECURSIVE arguments.\n\n**D** is correct because Snowflake FLATTEN is a table function for semi-structured VARIANT, OBJECT, and ARRAY values with INPUT, PATH, OUTER, and RECURSIVE arguments. This directly matches the scenario and is supported by the referenced FLATTEN documentation.\n\nSee [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) for the vendor documentation that supports the correct choice.",
      "reference": "[FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten)"
    },
    {
      "id": "q24",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "hard",
      "stem": "Quota Pipeline Scenario: **Semi-structured querying** must handle `feature lineage capture` while preserving `point in time joins`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for quota pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for quota pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "C",
          "text": "Use Semi-structured querying for quota pipeline scenario to handle `feature lineage capture`."
        },
        {
          "id": "D",
          "text": "Use visual tile for quota pipeline scenario instead of configuring Semi-structured querying."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because quota pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\n**B** is incorrect because quota pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\n**C** is correct because quota pipeline scenario needs Semi-structured querying specifically for `feature lineage capture`. The documentation describes that product behavior, and `point in time joins` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because quota pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\nSee [Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured) for the vendor documentation that supports the correct choice.",
      "reference": "[Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured)"
    },
    {
      "id": "q25",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "easy",
      "stem": "Access Pipeline Scenario: **PIVOT** must handle `BI precomputation` while preserving `feature lineage capture`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for access pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "B",
          "text": "Use PIVOT for access pipeline scenario to handle `BI precomputation`."
        },
        {
          "id": "C",
          "text": "Use visual tile for access pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "D",
          "text": "Use admin grant for access pipeline scenario instead of configuring PIVOT."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because access pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `BI precomputation` or preserve `feature lineage capture` in the supported workflow.\n\n**B** is correct because access pipeline scenario needs PIVOT specifically for `BI precomputation`. The documentation describes that product behavior, and `feature lineage capture` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because access pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `BI precomputation` or preserve `feature lineage capture` in the supported workflow.\n\n**D** is incorrect because access pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `BI precomputation` or preserve `feature lineage capture` in the supported workflow.\n\nSee [PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot) for the vendor documentation that supports the correct choice.",
      "reference": "[PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot)"
    },
    {
      "id": "q26",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Snowflake GROUP BY extensions such as ROLLUP, CUBE, and GROUPING SETS produce subtotal and multidimensional aggregation rows.\n\nA practitioner must choose the documented **GROUP BY extensions** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use GROUP BY extensions because the documentation supports this exact behavior: Snowflake GROUP BY extensions such as ROLLUP, CUBE, and GROUPING SETS produce subtotal and multidimensional aggregation rows."
        },
        {
          "id": "B",
          "text": "Use visual tile for document pipeline scenario instead of configuring GROUP BY extensions."
        },
        {
          "id": "C",
          "text": "Use admin grant for document pipeline scenario instead of configuring GROUP BY extensions."
        },
        {
          "id": "D",
          "text": "Use local notebook for document pipeline scenario instead of configuring GROUP BY extensions."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because Snowflake GROUP BY extensions such as ROLLUP, CUBE, and GROUPING SETS produce subtotal and multidimensional aggregation rows. This directly matches the scenario and is supported by the referenced GROUP BY extensions documentation.\n\n**B** is incorrect because it does not provide the documented GROUP BY extensions behavior described here: Snowflake GROUP BY extensions such as ROLLUP, CUBE, and GROUPING SETS produce subtotal and multidimensional aggregation rows.\n\n**C** is incorrect because it does not provide the documented GROUP BY extensions behavior described here: Snowflake GROUP BY extensions such as ROLLUP, CUBE, and GROUPING SETS produce subtotal and multidimensional aggregation rows.\n\n**D** is incorrect because it does not provide the documented GROUP BY extensions behavior described here: Snowflake GROUP BY extensions such as ROLLUP, CUBE, and GROUPING SETS produce subtotal and multidimensional aggregation rows.\n\nSee [GROUP BY extensions](https://docs.snowflake.com/en/sql-reference/constructs/group-by-rollup) for the vendor documentation that supports the correct choice.",
      "reference": "[GROUP BY extensions](https://docs.snowflake.com/en/sql-reference/constructs/group-by-rollup)"
    },
    {
      "id": "q27",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "medium",
      "stem": "Defect Pipeline Scenario: **Cortex COMPLETE** must handle `managed preprocessing container` while preserving `multi model endpoint load`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for defect pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "B",
          "text": "Use local notebook for defect pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "C",
          "text": "Use driver file for defect pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "D",
          "text": "Use Cortex COMPLETE for defect pipeline scenario to handle `managed preprocessing container`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because defect pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**B** is incorrect because defect pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**C** is incorrect because defect pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**D** is correct because defect pipeline scenario needs Cortex COMPLETE specifically for `managed preprocessing container`. The documentation describes that product behavior, and `multi model endpoint load` keeps the workflow inside the managed platform capability.\n\nSee [Cortex COMPLETE](https://docs.snowflake.com/en/sql-reference/functions/complete-snowflake-cortex) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex COMPLETE](https://docs.snowflake.com/en/sql-reference/functions/complete-snowflake-cortex)"
    },
    {
      "id": "q28",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "Residual Pipeline Scenario: **Cortex Search** must handle `registry notification hook` while preserving `metric gated registration`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for residual pipeline scenario instead of configuring Cortex Search."
        },
        {
          "id": "B",
          "text": "Use driver file for residual pipeline scenario instead of configuring Cortex Search."
        },
        {
          "id": "C",
          "text": "Use Cortex Search for residual pipeline scenario to handle `registry notification hook`."
        },
        {
          "id": "D",
          "text": "Use report table for residual pipeline scenario instead of configuring Cortex Search."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because residual pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\n**B** is incorrect because residual pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\n**C** is correct because residual pipeline scenario needs Cortex Search specifically for `registry notification hook`. The documentation describes that product behavior, and `metric gated registration` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because residual pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\nSee [Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview)"
    },
    {
      "id": "q29",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "easy",
      "stem": "Approval Pipeline Scenario: **Cortex Analyst** must handle `bursty join sizing` while preserving `managed preprocessing container`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for approval pipeline scenario instead of configuring Cortex Analyst."
        },
        {
          "id": "B",
          "text": "Use Cortex Analyst for approval pipeline scenario to handle `bursty join sizing`."
        },
        {
          "id": "C",
          "text": "Use report table for approval pipeline scenario instead of configuring Cortex Analyst."
        },
        {
          "id": "D",
          "text": "Use shell cron for approval pipeline scenario instead of configuring Cortex Analyst."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because approval pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `bursty join sizing` or preserve `managed preprocessing container` in the supported workflow.\n\n**B** is correct because approval pipeline scenario needs Cortex Analyst specifically for `bursty join sizing`. The documentation describes that product behavior, and `managed preprocessing container` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because approval pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `bursty join sizing` or preserve `managed preprocessing container` in the supported workflow.\n\n**D** is incorrect because approval pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `bursty join sizing` or preserve `managed preprocessing container` in the supported workflow.\n\nSee [Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst)"
    },
    {
      "id": "q30",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "medium",
      "stem": "Scoring Pipeline Scenario: **Result cache** must handle `online feature serving` while preserving `Cortex analyst semantic model`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Result cache for scoring pipeline scenario to handle `online feature serving`."
        },
        {
          "id": "B",
          "text": "Use report table for scoring pipeline scenario instead of configuring Result cache."
        },
        {
          "id": "C",
          "text": "Use shell cron for scoring pipeline scenario instead of configuring Result cache."
        },
        {
          "id": "D",
          "text": "Use email approval for scoring pipeline scenario instead of configuring Result cache."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because scoring pipeline scenario needs Result cache specifically for `online feature serving`. The documentation describes that product behavior, and `Cortex analyst semantic model` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because scoring pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\n**C** is incorrect because scoring pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\n**D** is incorrect because scoring pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\nSee [Result cache](https://docs.snowflake.com/en/user-guide/querying-persisted-results) for the vendor documentation that supports the correct choice.",
      "reference": "[Result cache](https://docs.snowflake.com/en/user-guide/querying-persisted-results)"
    },
    {
      "id": "q31",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "medium",
      "stem": "Alert Pipeline Scenario: **Query Profile** must handle `recursive termination` while preserving `QUALIFY window filter`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for alert pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "B",
          "text": "Use email approval for alert pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for alert pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "D",
          "text": "Use Query Profile for alert pipeline scenario to handle `recursive termination`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because alert pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `recursive termination` or preserve `QUALIFY window filter` in the supported workflow.\n\n**B** is incorrect because alert pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `recursive termination` or preserve `QUALIFY window filter` in the supported workflow.\n\n**C** is incorrect because alert pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `recursive termination` or preserve `QUALIFY window filter` in the supported workflow.\n\n**D** is correct because alert pipeline scenario needs Query Profile specifically for `recursive termination`. The documentation describes that product behavior, and `QUALIFY window filter` keeps the workflow inside the managed platform capability.\n\nSee [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) for the vendor documentation that supports the correct choice.",
      "reference": "[Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile)"
    },
    {
      "id": "q32",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "Lifetime Pipeline Scenario: **Clustering keys** must handle `dependency lineage graph` while preserving `profile spill diagnosis`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for lifetime pipeline scenario instead of configuring Clustering keys."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for lifetime pipeline scenario instead of configuring Clustering keys."
        },
        {
          "id": "C",
          "text": "Use Clustering keys for lifetime pipeline scenario to handle `dependency lineage graph`."
        },
        {
          "id": "D",
          "text": "Use visual tile for lifetime pipeline scenario instead of configuring Clustering keys."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because lifetime pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\n**B** is incorrect because lifetime pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\n**C** is correct because lifetime pipeline scenario needs Clustering keys specifically for `dependency lineage graph`. The documentation describes that product behavior, and `profile spill diagnosis` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because lifetime pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\nSee [Clustering keys](https://docs.snowflake.com/en/user-guide/tables-clustering-keys) for the vendor documentation that supports the correct choice.",
      "reference": "[Clustering keys](https://docs.snowflake.com/en/user-guide/tables-clustering-keys)"
    },
    {
      "id": "q33",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "easy",
      "stem": "Invoice Pipeline Scenario: **Materialized views** must handle `top ranked customer filter` while preserving `online feature serving`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for invoice pipeline scenario instead of configuring Materialized views."
        },
        {
          "id": "B",
          "text": "Use Materialized views for invoice pipeline scenario to handle `top ranked customer filter`."
        },
        {
          "id": "C",
          "text": "Use visual tile for invoice pipeline scenario instead of configuring Materialized views."
        },
        {
          "id": "D",
          "text": "Use admin grant for invoice pipeline scenario instead of configuring Materialized views."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because invoice pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\n**B** is correct because invoice pipeline scenario needs Materialized views specifically for `top ranked customer filter`. The documentation describes that product behavior, and `online feature serving` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because invoice pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\n**D** is incorrect because invoice pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\nSee [Materialized views](https://docs.snowflake.com/en/user-guide/views-materialized) for the vendor documentation that supports the correct choice.",
      "reference": "[Materialized views](https://docs.snowflake.com/en/user-guide/views-materialized)"
    },
    {
      "id": "q34",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "Summarizer Pipeline Scenario: **Masking policies** must handle `outer flatten rows` while preserving `MLflow autolog capture`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Masking policies for summarizer pipeline scenario to handle `outer flatten rows`."
        },
        {
          "id": "B",
          "text": "Use visual tile for summarizer pipeline scenario instead of configuring Masking policies."
        },
        {
          "id": "C",
          "text": "Use admin grant for summarizer pipeline scenario instead of configuring Masking policies."
        },
        {
          "id": "D",
          "text": "Use local notebook for summarizer pipeline scenario instead of configuring Masking policies."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because summarizer pipeline scenario needs Masking policies specifically for `outer flatten rows`. The documentation describes that product behavior, and `MLflow autolog capture` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because summarizer pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `outer flatten rows` or preserve `MLflow autolog capture` in the supported workflow.\n\n**C** is incorrect because summarizer pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `outer flatten rows` or preserve `MLflow autolog capture` in the supported workflow.\n\n**D** is incorrect because summarizer pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `outer flatten rows` or preserve `MLflow autolog capture` in the supported workflow.\n\nSee [Masking policies](https://docs.snowflake.com/en/user-guide/security-column-ddm-use) for the vendor documentation that supports the correct choice.",
      "reference": "[Masking policies](https://docs.snowflake.com/en/user-guide/security-column-ddm-use)"
    },
    {
      "id": "q35",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "medium",
      "stem": "Heatmap Pipeline Scenario: **Row access policies** must handle `email masking rule` while preserving `Access History trace`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for heatmap pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "B",
          "text": "Use local notebook for heatmap pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "C",
          "text": "Use driver file for heatmap pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "D",
          "text": "Use Row access policies for heatmap pipeline scenario to handle `email masking rule`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because heatmap pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**B** is incorrect because heatmap pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**C** is incorrect because heatmap pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**D** is correct because heatmap pipeline scenario needs Row access policies specifically for `email masking rule`. The documentation describes that product behavior, and `Access History trace` keeps the workflow inside the managed platform capability.\n\nSee [Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro)"
    },
    {
      "id": "q36",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "hard",
      "stem": "Geospatial Delivery Promise Forecast With Depot Zones And Holiday Calendars: **Object tagging** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for clause pipeline scenario instead of configuring Object tagging."
        },
        {
          "id": "B",
          "text": "Use driver file for clause pipeline scenario instead of configuring Object tagging."
        },
        {
          "id": "C",
          "text": "Use Object tagging for clause pipeline scenario to handle `future grant pattern`."
        },
        {
          "id": "D",
          "text": "Use report table for clause pipeline scenario instead of configuring Object tagging."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because clause pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `future grant pattern` or preserve `traffic split rollout` in the supported workflow.\n\n**B** is incorrect because clause pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `future grant pattern` or preserve `traffic split rollout` in the supported workflow.\n\n**C** is correct because clause pipeline scenario needs Object tagging specifically for `future grant pattern`. The documentation describes that product behavior, and `traffic split rollout` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because clause pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `future grant pattern` or preserve `traffic split rollout` in the supported workflow.\n\nSee [Object tagging](https://docs.snowflake.com/en/user-guide/object-tagging) for the vendor documentation that supports the correct choice.",
      "reference": "[Object tagging](https://docs.snowflake.com/en/user-guide/object-tagging)"
    },
    {
      "id": "q37",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "easy",
      "stem": "Drift Pipeline Scenario: **Access History** must handle `async payload routing` while preserving `top ranked customer filter`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for drift pipeline scenario instead of configuring Access History."
        },
        {
          "id": "B",
          "text": "Use Access History for drift pipeline scenario to handle `async payload routing`."
        },
        {
          "id": "C",
          "text": "Use report table for drift pipeline scenario instead of configuring Access History."
        },
        {
          "id": "D",
          "text": "Use shell cron for drift pipeline scenario instead of configuring Access History."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because drift pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\n**B** is correct because drift pipeline scenario needs Access History specifically for `async payload routing`. The documentation describes that product behavior, and `top ranked customer filter` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because drift pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\n**D** is incorrect because drift pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\nSee [Access History](https://docs.snowflake.com/en/sql-reference/account-usage/access_history) for the vendor documentation that supports the correct choice.",
      "reference": "[Access History](https://docs.snowflake.com/en/sql-reference/account-usage/access_history)"
    },
    {
      "id": "q38",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Latency Pipeline Scenario: **Snowsight dashboards** must handle `event scheduled pipeline` while preserving `Glue bookmark state`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Snowsight dashboards for latency pipeline scenario to handle `event scheduled pipeline`."
        },
        {
          "id": "B",
          "text": "Use report table for latency pipeline scenario instead of configuring Snowsight dashboards."
        },
        {
          "id": "C",
          "text": "Use shell cron for latency pipeline scenario instead of configuring Snowsight dashboards."
        },
        {
          "id": "D",
          "text": "Use email approval for latency pipeline scenario instead of configuring Snowsight dashboards."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because latency pipeline scenario needs Snowsight dashboards specifically for `event scheduled pipeline`. The documentation describes that product behavior, and `Glue bookmark state` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because latency pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\n**C** is incorrect because latency pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\n**D** is incorrect because latency pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\nSee [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) for the vendor documentation that supports the correct choice.",
      "reference": "[Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards)"
    },
    {
      "id": "q39",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "medium",
      "stem": "Comparison Pipeline Scenario: **Streamlit in Snowflake** must handle `training baseline capture` while preserving `duplicate key ranking`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for comparison pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "B",
          "text": "Use email approval for comparison pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for comparison pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "D",
          "text": "Use Streamlit in Snowflake for comparison pipeline scenario to handle `training baseline capture`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because comparison pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**B** is incorrect because comparison pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**C** is incorrect because comparison pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**D** is correct because comparison pipeline scenario needs Streamlit in Snowflake specifically for `training baseline capture`. The documentation describes that product behavior, and `duplicate key ranking` keeps the workflow inside the managed platform capability.\n\nSee [Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit) for the vendor documentation that supports the correct choice.",
      "reference": "[Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit)"
    },
    {
      "id": "q40",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "Entitlement Pipeline Scenario: **Tasks** must handle `dual store features` while preserving `sparse tabular algorithm`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for entitlement pipeline scenario instead of configuring Tasks."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for entitlement pipeline scenario instead of configuring Tasks."
        },
        {
          "id": "C",
          "text": "Use Tasks for entitlement pipeline scenario to handle `dual store features`."
        },
        {
          "id": "D",
          "text": "Use visual tile for entitlement pipeline scenario instead of configuring Tasks."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because entitlement pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\n**B** is incorrect because entitlement pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\n**C** is correct because entitlement pipeline scenario needs Tasks specifically for `dual store features`. The documentation describes that product behavior, and `sparse tabular algorithm` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because entitlement pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\nSee [Tasks](https://docs.snowflake.com/en/user-guide/tasks-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q41",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "easy",
      "stem": "Analyst Pipeline Scenario: **QUALIFY** must handle `warehouse scaling policy` while preserving `future grant pattern`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for analyst pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "B",
          "text": "Use QUALIFY for analyst pipeline scenario to handle `warehouse scaling policy`."
        },
        {
          "id": "C",
          "text": "Use visual tile for analyst pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "D",
          "text": "Use admin grant for analyst pipeline scenario instead of configuring QUALIFY."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because analyst pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\n**B** is correct because analyst pipeline scenario needs QUALIFY specifically for `warehouse scaling policy`. The documentation describes that product behavior, and `future grant pattern` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because analyst pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\n**D** is incorrect because analyst pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\nSee [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) for the vendor documentation that supports the correct choice.",
      "reference": "[QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify)"
    },
    {
      "id": "q42",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "medium",
      "stem": "Aggregate Pipeline Scenario: **Window functions** must handle `row access predicate` while preserving `Feature Store online lookup`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Window functions for aggregate pipeline scenario to handle `row access predicate`."
        },
        {
          "id": "B",
          "text": "Use visual tile for aggregate pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "C",
          "text": "Use admin grant for aggregate pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "D",
          "text": "Use local notebook for aggregate pipeline scenario instead of configuring Window functions."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because aggregate pipeline scenario needs Window functions specifically for `row access predicate`. The documentation describes that product behavior, and `Feature Store online lookup` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because aggregate pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\n**C** is incorrect because aggregate pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\n**D** is incorrect because aggregate pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\nSee [Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using) for the vendor documentation that supports the correct choice.",
      "reference": "[Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using)"
    },
    {
      "id": "q43",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "medium",
      "stem": "Snowflake FLATTEN expands nested arrays or objects into rows and is commonly used with LATERAL joins against VARIANT data.\n\nA practitioner must choose the documented **FLATTEN** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for hierarchy pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "B",
          "text": "Use local notebook for hierarchy pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "C",
          "text": "Use driver file for hierarchy pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "D",
          "text": "Use FLATTEN because the documentation supports this exact behavior: Snowflake FLATTEN expands nested arrays or objects into rows and is commonly used with LATERAL joins against VARIANT data."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN expands nested arrays or objects into rows and is commonly used with LATERAL joins against VARIANT data.\n\n**B** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN expands nested arrays or objects into rows and is commonly used with LATERAL joins against VARIANT data.\n\n**C** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN expands nested arrays or objects into rows and is commonly used with LATERAL joins against VARIANT data.\n\n**D** is correct because Snowflake FLATTEN expands nested arrays or objects into rows and is commonly used with LATERAL joins against VARIANT data. This directly matches the scenario and is supported by the referenced FLATTEN documentation.\n\nSee [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) for the vendor documentation that supports the correct choice.",
      "reference": "[FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten)"
    },
    {
      "id": "q44",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "Payload Pipeline Scenario: **Semi-structured querying** must handle `Cortex analyst semantic model` while preserving `sensitivity tag policy`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for payload pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "B",
          "text": "Use driver file for payload pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "C",
          "text": "Use Semi-structured querying for payload pipeline scenario to handle `Cortex analyst semantic model`."
        },
        {
          "id": "D",
          "text": "Use report table for payload pipeline scenario instead of configuring Semi-structured querying."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because payload pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\n**B** is incorrect because payload pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\n**C** is correct because payload pipeline scenario needs Semi-structured querying specifically for `Cortex analyst semantic model`. The documentation describes that product behavior, and `sensitivity tag policy` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because payload pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\nSee [Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured) for the vendor documentation that supports the correct choice.",
      "reference": "[Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured)"
    },
    {
      "id": "q45",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "easy",
      "stem": "Corpus Pipeline Scenario: **PIVOT** must handle `Snowpark dataframe transform` while preserving `training baseline capture`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for corpus pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "B",
          "text": "Use PIVOT for corpus pipeline scenario to handle `Snowpark dataframe transform`."
        },
        {
          "id": "C",
          "text": "Use report table for corpus pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "D",
          "text": "Use shell cron for corpus pipeline scenario instead of configuring PIVOT."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because corpus pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\n**B** is correct because corpus pipeline scenario needs PIVOT specifically for `Snowpark dataframe transform`. The documentation describes that product behavior, and `training baseline capture` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because corpus pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\n**D** is incorrect because corpus pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\nSee [PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot) for the vendor documentation that supports the correct choice.",
      "reference": "[PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot)"
    },
    {
      "id": "q46",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "Snowflake ROLLUP and CUBE extend GROUP BY so subtotal combinations can be computed without writing separate UNION queries.\n\nA practitioner must choose the documented **GROUP BY extensions** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use GROUP BY extensions because the documentation supports this exact behavior: Snowflake ROLLUP and CUBE extend GROUP BY so subtotal combinations can be computed without writing separate UNION queries."
        },
        {
          "id": "B",
          "text": "Use report table for endpoint pipeline scenario instead of configuring GROUP BY extensions."
        },
        {
          "id": "C",
          "text": "Use shell cron for endpoint pipeline scenario instead of configuring GROUP BY extensions."
        },
        {
          "id": "D",
          "text": "Use email approval for endpoint pipeline scenario instead of configuring GROUP BY extensions."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because Snowflake ROLLUP and CUBE extend GROUP BY so subtotal combinations can be computed without writing separate UNION queries. This directly matches the scenario and is supported by the referenced GROUP BY extensions documentation.\n\n**B** is incorrect because it does not provide the documented GROUP BY extensions behavior described here: Snowflake ROLLUP and CUBE extend GROUP BY so subtotal combinations can be computed without writing separate UNION queries.\n\n**C** is incorrect because it does not provide the documented GROUP BY extensions behavior described here: Snowflake ROLLUP and CUBE extend GROUP BY so subtotal combinations can be computed without writing separate UNION queries.\n\n**D** is incorrect because it does not provide the documented GROUP BY extensions behavior described here: Snowflake ROLLUP and CUBE extend GROUP BY so subtotal combinations can be computed without writing separate UNION queries.\n\nSee [GROUP BY extensions](https://docs.snowflake.com/en/sql-reference/constructs/group-by-rollup) for the vendor documentation that supports the correct choice.",
      "reference": "[GROUP BY extensions](https://docs.snowflake.com/en/sql-reference/constructs/group-by-rollup)"
    },
    {
      "id": "q47",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "medium",
      "stem": "Condition Pipeline Scenario: **Cortex COMPLETE** must handle `MLflow autolog capture` while preserving `offline object scoring`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for condition pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "B",
          "text": "Use email approval for condition pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for condition pipeline scenario instead of configuring Cortex COMPLETE."
        },
        {
          "id": "D",
          "text": "Use Cortex COMPLETE for condition pipeline scenario to handle `MLflow autolog capture`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because condition pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**B** is incorrect because condition pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**C** is incorrect because condition pipeline scenario would still lack Cortex COMPLETE; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**D** is correct because condition pipeline scenario needs Cortex COMPLETE specifically for `MLflow autolog capture`. The documentation describes that product behavior, and `offline object scoring` keeps the workflow inside the managed platform capability.\n\nSee [Cortex COMPLETE](https://docs.snowflake.com/en/sql-reference/functions/complete-snowflake-cortex) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex COMPLETE](https://docs.snowflake.com/en/sql-reference/functions/complete-snowflake-cortex)"
    },
    {
      "id": "q48",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "hard",
      "stem": "Queue Pipeline Scenario: **Cortex Search** must handle `pyfunc wrapper loading` while preserving `BI precomputation`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for queue pipeline scenario instead of configuring Cortex Search."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for queue pipeline scenario instead of configuring Cortex Search."
        },
        {
          "id": "C",
          "text": "Use Cortex Search for queue pipeline scenario to handle `pyfunc wrapper loading`."
        },
        {
          "id": "D",
          "text": "Use visual tile for queue pipeline scenario instead of configuring Cortex Search."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because queue pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\n**B** is incorrect because queue pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\n**C** is correct because queue pipeline scenario needs Cortex Search specifically for `pyfunc wrapper loading`. The documentation describes that product behavior, and `BI precomputation` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because queue pipeline scenario would still lack Cortex Search; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\nSee [Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview)"
    },
    {
      "id": "q49",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "easy",
      "stem": "Bookmark Pipeline Scenario: **Cortex Analyst** must handle `Clarify bias baseline` while preserving `row access predicate`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for bookmark pipeline scenario instead of configuring Cortex Analyst."
        },
        {
          "id": "B",
          "text": "Use Cortex Analyst for bookmark pipeline scenario to handle `Clarify bias baseline`."
        },
        {
          "id": "C",
          "text": "Use visual tile for bookmark pipeline scenario instead of configuring Cortex Analyst."
        },
        {
          "id": "D",
          "text": "Use admin grant for bookmark pipeline scenario instead of configuring Cortex Analyst."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because bookmark pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `Clarify bias baseline` or preserve `row access predicate` in the supported workflow.\n\n**B** is correct because bookmark pipeline scenario needs Cortex Analyst specifically for `Clarify bias baseline`. The documentation describes that product behavior, and `row access predicate` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because bookmark pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `Clarify bias baseline` or preserve `row access predicate` in the supported workflow.\n\n**D** is incorrect because bookmark pipeline scenario would still lack Cortex Analyst; the suggested workaround cannot satisfy `Clarify bias baseline` or preserve `row access predicate` in the supported workflow.\n\nSee [Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst) for the vendor documentation that supports the correct choice.",
      "reference": "[Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst)"
    },
    {
      "id": "q50",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Flow Pipeline Scenario: **Result cache** must handle `Glue bookmark state` while preserving `CloudWatch endpoint alarm`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Result cache for flow pipeline scenario to handle `Glue bookmark state`."
        },
        {
          "id": "B",
          "text": "Use visual tile for flow pipeline scenario instead of configuring Result cache."
        },
        {
          "id": "C",
          "text": "Use admin grant for flow pipeline scenario instead of configuring Result cache."
        },
        {
          "id": "D",
          "text": "Use local notebook for flow pipeline scenario instead of configuring Result cache."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because flow pipeline scenario needs Result cache specifically for `Glue bookmark state`. The documentation describes that product behavior, and `CloudWatch endpoint alarm` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because flow pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\n**C** is incorrect because flow pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\n**D** is incorrect because flow pipeline scenario would still lack Result cache; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\nSee [Result cache](https://docs.snowflake.com/en/user-guide/querying-persisted-results) for the vendor documentation that supports the correct choice.",
      "reference": "[Result cache](https://docs.snowflake.com/en/user-guide/querying-persisted-results)"
    },
    {
      "id": "q51",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "medium",
      "stem": "Product Taxonomy Enrichment With Synonym Dictionaries And Merchandiser Stewardship: **Query Profile** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for bias pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "B",
          "text": "Use local notebook for bias pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "C",
          "text": "Use driver file for bias pipeline scenario instead of configuring Query Profile."
        },
        {
          "id": "D",
          "text": "Use Query Profile for bias pipeline scenario to handle `Batch Transform manifest`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because bias pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `Batch Transform manifest` or preserve `idle serverless serving` in the supported workflow.\n\n**B** is incorrect because bias pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `Batch Transform manifest` or preserve `idle serverless serving` in the supported workflow.\n\n**C** is incorrect because bias pipeline scenario would still lack Query Profile; the suggested workaround cannot satisfy `Batch Transform manifest` or preserve `idle serverless serving` in the supported workflow.\n\n**D** is correct because bias pipeline scenario needs Query Profile specifically for `Batch Transform manifest`. The documentation describes that product behavior, and `idle serverless serving` keeps the workflow inside the managed platform capability.\n\nSee [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) for the vendor documentation that supports the correct choice.",
      "reference": "[Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile)"
    },
    {
      "id": "q52",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "Retention Pipeline Scenario: **Clustering keys** must handle `Model Monitor schedule` while preserving `registry notification hook`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for retention pipeline scenario instead of configuring Clustering keys."
        },
        {
          "id": "B",
          "text": "Use driver file for retention pipeline scenario instead of configuring Clustering keys."
        },
        {
          "id": "C",
          "text": "Use Clustering keys for retention pipeline scenario to handle `Model Monitor schedule`."
        },
        {
          "id": "D",
          "text": "Use report table for retention pipeline scenario instead of configuring Clustering keys."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because retention pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\n**B** is incorrect because retention pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\n**C** is correct because retention pipeline scenario needs Clustering keys specifically for `Model Monitor schedule`. The documentation describes that product behavior, and `registry notification hook` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because retention pipeline scenario would still lack Clustering keys; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\nSee [Clustering keys](https://docs.snowflake.com/en/user-guide/tables-clustering-keys) for the vendor documentation that supports the correct choice.",
      "reference": "[Clustering keys](https://docs.snowflake.com/en/user-guide/tables-clustering-keys)"
    },
    {
      "id": "q53",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "easy",
      "stem": "Ranking Pipeline Scenario: **Materialized views** must handle `Feature Store online lookup` while preserving `Snowpark dataframe transform`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for ranking pipeline scenario instead of configuring Materialized views."
        },
        {
          "id": "B",
          "text": "Use Materialized views for ranking pipeline scenario to handle `Feature Store online lookup`."
        },
        {
          "id": "C",
          "text": "Use report table for ranking pipeline scenario instead of configuring Materialized views."
        },
        {
          "id": "D",
          "text": "Use shell cron for ranking pipeline scenario instead of configuring Materialized views."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because ranking pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\n**B** is correct because ranking pipeline scenario needs Materialized views specifically for `Feature Store online lookup`. The documentation describes that product behavior, and `Snowpark dataframe transform` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because ranking pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\n**D** is incorrect because ranking pipeline scenario would still lack Materialized views; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\nSee [Materialized views](https://docs.snowflake.com/en/user-guide/views-materialized) for the vendor documentation that supports the correct choice.",
      "reference": "[Materialized views](https://docs.snowflake.com/en/user-guide/views-materialized)"
    },
    {
      "id": "q54",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "medium",
      "stem": "Arrays Pipeline Scenario: **Masking policies** must handle `Autopilot candidate review` while preserving `GROUPING SETS subtotal`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Masking policies for arrays pipeline scenario to handle `Autopilot candidate review`."
        },
        {
          "id": "B",
          "text": "Use report table for arrays pipeline scenario instead of configuring Masking policies."
        },
        {
          "id": "C",
          "text": "Use shell cron for arrays pipeline scenario instead of configuring Masking policies."
        },
        {
          "id": "D",
          "text": "Use email approval for arrays pipeline scenario instead of configuring Masking policies."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because arrays pipeline scenario needs Masking policies specifically for `Autopilot candidate review`. The documentation describes that product behavior, and `GROUPING SETS subtotal` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because arrays pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `Autopilot candidate review` or preserve `GROUPING SETS subtotal` in the supported workflow.\n\n**C** is incorrect because arrays pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `Autopilot candidate review` or preserve `GROUPING SETS subtotal` in the supported workflow.\n\n**D** is incorrect because arrays pipeline scenario would still lack Masking policies; the suggested workaround cannot satisfy `Autopilot candidate review` or preserve `GROUPING SETS subtotal` in the supported workflow.\n\nSee [Masking policies](https://docs.snowflake.com/en/user-guide/security-column-ddm-use) for the vendor documentation that supports the correct choice.",
      "reference": "[Masking policies](https://docs.snowflake.com/en/user-guide/security-column-ddm-use)"
    },
    {
      "id": "q55",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "medium",
      "stem": "Cache Pipeline Scenario: **Row access policies** must handle `Data Wrangler export` while preserving `unpivoted measure rows`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for cache pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "B",
          "text": "Use email approval for cache pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for cache pipeline scenario instead of configuring Row access policies."
        },
        {
          "id": "D",
          "text": "Use Row access policies for cache pipeline scenario to handle `Data Wrangler export`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because cache pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**B** is incorrect because cache pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**C** is incorrect because cache pipeline scenario would still lack Row access policies; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**D** is correct because cache pipeline scenario needs Row access policies specifically for `Data Wrangler export`. The documentation describes that product behavior, and `unpivoted measure rows` keeps the workflow inside the managed platform capability.\n\nSee [Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro)"
    },
    {
      "id": "q56",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "Visibility Pipeline Scenario: **Object tagging** must handle `Processing job output` while preserving `recursive termination`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for visibility pipeline scenario instead of configuring Object tagging."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for visibility pipeline scenario instead of configuring Object tagging."
        },
        {
          "id": "C",
          "text": "Use Object tagging for visibility pipeline scenario to handle `Processing job output`."
        },
        {
          "id": "D",
          "text": "Use visual tile for visibility pipeline scenario instead of configuring Object tagging."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because visibility pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\n**B** is incorrect because visibility pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\n**C** is correct because visibility pipeline scenario needs Object tagging specifically for `Processing job output`. The documentation describes that product behavior, and `recursive termination` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because visibility pipeline scenario would still lack Object tagging; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\nSee [Object tagging](https://docs.snowflake.com/en/user-guide/object-tagging) for the vendor documentation that supports the correct choice.",
      "reference": "[Object tagging](https://docs.snowflake.com/en/user-guide/object-tagging)"
    },
    {
      "id": "q57",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "easy",
      "stem": "Metrics Pipeline Scenario: **Access History** must handle `Pipeline condition step` while preserving `pyfunc wrapper loading`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for metrics pipeline scenario instead of configuring Access History."
        },
        {
          "id": "B",
          "text": "Use Access History for metrics pipeline scenario to handle `Pipeline condition step`."
        },
        {
          "id": "C",
          "text": "Use visual tile for metrics pipeline scenario instead of configuring Access History."
        },
        {
          "id": "D",
          "text": "Use admin grant for metrics pipeline scenario instead of configuring Access History."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because metrics pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\n**B** is correct because metrics pipeline scenario needs Access History specifically for `Pipeline condition step`. The documentation describes that product behavior, and `pyfunc wrapper loading` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because metrics pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\n**D** is incorrect because metrics pipeline scenario would still lack Access History; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\nSee [Access History](https://docs.snowflake.com/en/sql-reference/account-usage/access_history) for the vendor documentation that supports the correct choice.",
      "reference": "[Access History](https://docs.snowflake.com/en/sql-reference/account-usage/access_history)"
    },
    {
      "id": "q58",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "Leakage Pipeline Scenario: **Snowsight dashboards** must handle `multi model endpoint load` while preserving `Streamlit governed app`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Snowsight dashboards for leakage pipeline scenario to handle `multi model endpoint load`."
        },
        {
          "id": "B",
          "text": "Use visual tile for leakage pipeline scenario instead of configuring Snowsight dashboards."
        },
        {
          "id": "C",
          "text": "Use admin grant for leakage pipeline scenario instead of configuring Snowsight dashboards."
        },
        {
          "id": "D",
          "text": "Use local notebook for leakage pipeline scenario instead of configuring Snowsight dashboards."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because leakage pipeline scenario needs Snowsight dashboards specifically for `multi model endpoint load`. The documentation describes that product behavior, and `Streamlit governed app` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because leakage pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\n**C** is incorrect because leakage pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\n**D** is incorrect because leakage pipeline scenario would still lack Snowsight dashboards; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\nSee [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) for the vendor documentation that supports the correct choice.",
      "reference": "[Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards)"
    },
    {
      "id": "q59",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "medium",
      "stem": "Storage Pipeline Scenario: **Streamlit in Snowflake** must handle `CloudWatch endpoint alarm` while preserving `post load aggregate refresh`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for storage pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "B",
          "text": "Use local notebook for storage pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "C",
          "text": "Use driver file for storage pipeline scenario instead of configuring Streamlit in Snowflake."
        },
        {
          "id": "D",
          "text": "Use Streamlit in Snowflake for storage pipeline scenario to handle `CloudWatch endpoint alarm`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because storage pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**B** is incorrect because storage pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**C** is incorrect because storage pipeline scenario would still lack Streamlit in Snowflake; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**D** is correct because storage pipeline scenario needs Streamlit in Snowflake specifically for `CloudWatch endpoint alarm`. The documentation describes that product behavior, and `post load aggregate refresh` keeps the workflow inside the managed platform capability.\n\nSee [Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit) for the vendor documentation that supports the correct choice.",
      "reference": "[Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit)"
    },
    {
      "id": "q60",
      "domain": "visualization-dashboards",
      "type": "single",
      "difficulty": "hard",
      "stem": "Morning Pipeline Scenario: **Tasks** must handle `Search service refresh` while preserving `outer flatten rows`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for morning pipeline scenario instead of configuring Tasks."
        },
        {
          "id": "B",
          "text": "Use driver file for morning pipeline scenario instead of configuring Tasks."
        },
        {
          "id": "C",
          "text": "Use Tasks for morning pipeline scenario to handle `Search service refresh`."
        },
        {
          "id": "D",
          "text": "Use report table for morning pipeline scenario instead of configuring Tasks."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because morning pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\n**B** is incorrect because morning pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\n**C** is correct because morning pipeline scenario needs Tasks specifically for `Search service refresh`. The documentation describes that product behavior, and `outer flatten rows` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because morning pipeline scenario would still lack Tasks; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\nSee [Tasks](https://docs.snowflake.com/en/user-guide/tasks-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Tasks](https://docs.snowflake.com/en/user-guide/tasks-intro)"
    },
    {
      "id": "q61",
      "domain": "advanced-sql",
      "type": "single",
      "difficulty": "easy",
      "stem": "Validation Pipeline Scenario: **QUALIFY** must handle `QUALIFY window filter` while preserving `Batch Transform manifest`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for validation pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "B",
          "text": "Use QUALIFY for validation pipeline scenario to handle `QUALIFY window filter`."
        },
        {
          "id": "C",
          "text": "Use report table for validation pipeline scenario instead of configuring QUALIFY."
        },
        {
          "id": "D",
          "text": "Use shell cron for validation pipeline scenario instead of configuring QUALIFY."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because validation pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `QUALIFY window filter` or preserve `Batch Transform manifest` in the supported workflow.\n\n**B** is correct because validation pipeline scenario needs QUALIFY specifically for `QUALIFY window filter`. The documentation describes that product behavior, and `Batch Transform manifest` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because validation pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `QUALIFY window filter` or preserve `Batch Transform manifest` in the supported workflow.\n\n**D** is incorrect because validation pipeline scenario would still lack QUALIFY; the suggested workaround cannot satisfy `QUALIFY window filter` or preserve `Batch Transform manifest` in the supported workflow.\n\nSee [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) for the vendor documentation that supports the correct choice.",
      "reference": "[QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify)"
    },
    {
      "id": "q62",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "Idle Pipeline Scenario: **Window functions** must handle `GROUPING SETS subtotal` while preserving `nested array expansion`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Window functions for idle pipeline scenario to handle `GROUPING SETS subtotal`."
        },
        {
          "id": "B",
          "text": "Use report table for idle pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "C",
          "text": "Use shell cron for idle pipeline scenario instead of configuring Window functions."
        },
        {
          "id": "D",
          "text": "Use email approval for idle pipeline scenario instead of configuring Window functions."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because idle pipeline scenario needs Window functions specifically for `GROUPING SETS subtotal`. The documentation describes that product behavior, and `nested array expansion` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because idle pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `GROUPING SETS subtotal` or preserve `nested array expansion` in the supported workflow.\n\n**C** is incorrect because idle pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `GROUPING SETS subtotal` or preserve `nested array expansion` in the supported workflow.\n\n**D** is incorrect because idle pipeline scenario would still lack Window functions; the suggested workaround cannot satisfy `GROUPING SETS subtotal` or preserve `nested array expansion` in the supported workflow.\n\nSee [Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using) for the vendor documentation that supports the correct choice.",
      "reference": "[Window functions](https://docs.snowflake.com/en/user-guide/functions-window-using)"
    },
    {
      "id": "q63",
      "domain": "cortex-ai",
      "type": "single",
      "difficulty": "medium",
      "stem": "Snowflake FLATTEN returns columns such as VALUE, KEY, INDEX, PATH, and THIS when exploding semi-structured data.\n\nA practitioner must choose the documented **FLATTEN** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for status pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "B",
          "text": "Use email approval for status pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for status pipeline scenario instead of configuring FLATTEN."
        },
        {
          "id": "D",
          "text": "Use FLATTEN because the documentation supports this exact behavior: Snowflake FLATTEN returns columns such as VALUE, KEY, INDEX, PATH, and THIS when exploding semi-structured data."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN returns columns such as VALUE, KEY, INDEX, PATH, and THIS when exploding semi-structured data.\n\n**B** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN returns columns such as VALUE, KEY, INDEX, PATH, and THIS when exploding semi-structured data.\n\n**C** is incorrect because it does not provide the documented FLATTEN behavior described here: Snowflake FLATTEN returns columns such as VALUE, KEY, INDEX, PATH, and THIS when exploding semi-structured data.\n\n**D** is correct because Snowflake FLATTEN returns columns such as VALUE, KEY, INDEX, PATH, and THIS when exploding semi-structured data. This directly matches the scenario and is supported by the referenced FLATTEN documentation.\n\nSee [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) for the vendor documentation that supports the correct choice.",
      "reference": "[FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten)"
    },
    {
      "id": "q64",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "Timeline Pipeline Scenario: **Semi-structured querying** must handle `Access History trace` while preserving `async payload routing`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for timeline pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for timeline pipeline scenario instead of configuring Semi-structured querying."
        },
        {
          "id": "C",
          "text": "Use Semi-structured querying for timeline pipeline scenario to handle `Access History trace`."
        },
        {
          "id": "D",
          "text": "Use visual tile for timeline pipeline scenario instead of configuring Semi-structured querying."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because timeline pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `Access History trace` or preserve `async payload routing` in the supported workflow.\n\n**B** is incorrect because timeline pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `Access History trace` or preserve `async payload routing` in the supported workflow.\n\n**C** is correct because timeline pipeline scenario needs Semi-structured querying specifically for `Access History trace`. The documentation describes that product behavior, and `async payload routing` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because timeline pipeline scenario would still lack Semi-structured querying; the suggested workaround cannot satisfy `Access History trace` or preserve `async payload routing` in the supported workflow.\n\nSee [Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured) for the vendor documentation that supports the correct choice.",
      "reference": "[Semi-structured querying](https://docs.snowflake.com/en/user-guide/querying-semistructured)"
    },
    {
      "id": "q65",
      "domain": "data-governance",
      "type": "single",
      "difficulty": "easy",
      "stem": "Columns Pipeline Scenario: **PIVOT** must handle `Streamlit governed app` while preserving `Autopilot candidate review`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for columns pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "B",
          "text": "Use PIVOT for columns pipeline scenario to handle `Streamlit governed app`."
        },
        {
          "id": "C",
          "text": "Use visual tile for columns pipeline scenario instead of configuring PIVOT."
        },
        {
          "id": "D",
          "text": "Use admin grant for columns pipeline scenario instead of configuring PIVOT."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because columns pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `Streamlit governed app` or preserve `Autopilot candidate review` in the supported workflow.\n\n**B** is correct because columns pipeline scenario needs PIVOT specifically for `Streamlit governed app`. The documentation describes that product behavior, and `Autopilot candidate review` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because columns pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `Streamlit governed app` or preserve `Autopilot candidate review` in the supported workflow.\n\n**D** is incorrect because columns pipeline scenario would still lack PIVOT; the suggested workaround cannot satisfy `Streamlit governed app` or preserve `Autopilot candidate review` in the supported workflow.\n\nSee [PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot) for the vendor documentation that supports the correct choice.",
      "reference": "[PIVOT](https://docs.snowflake.com/en/sql-reference/constructs/pivot)"
    }
  ]
}
