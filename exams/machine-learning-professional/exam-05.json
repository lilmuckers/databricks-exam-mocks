{
  "meta": {
    "id": "mlp-exam-05",
    "certification": "machine-learning-professional",
    "certificationName": "Databricks Certified Machine Learning Professional",
    "title": "DBX-MLP-5: Advanced Practice Scenarios",
    "version": "1.0",
    "timeLimit": 120,
    "passingScore": 70,
    "totalQuestions": 60,
    "domains": [
      {
        "id": "experimentation",
        "name": "ML Experimentation",
        "weight": 20
      },
      {
        "id": "model-registry",
        "name": "MLflow Tracking and Registry",
        "weight": 20
      },
      {
        "id": "feature-store",
        "name": "Feature Engineering",
        "weight": 20
      },
      {
        "id": "model-deployment",
        "name": "Model Deployment",
        "weight": 20
      },
      {
        "id": "monitoring",
        "name": "Model Monitoring",
        "weight": 20
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Revenue Pipeline Scenario: **MLflow tracking** must handle `checkpoint retries` while preserving `metric comparison`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for revenue pipeline scenario instead of configuring MLflow tracking."
        },
        {
          "id": "B",
          "text": "Use MLflow tracking for revenue pipeline scenario to handle `checkpoint retries`."
        },
        {
          "id": "C",
          "text": "Use visual tile for revenue pipeline scenario instead of configuring MLflow tracking."
        },
        {
          "id": "D",
          "text": "Use admin grant for revenue pipeline scenario instead of configuring MLflow tracking."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because revenue pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\n**B** is correct because revenue pipeline scenario needs MLflow tracking specifically for `checkpoint retries`. The documentation describes that product behavior, and `metric comparison` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because revenue pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\n**D** is incorrect because revenue pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `checkpoint retries` or preserve `metric comparison` in the supported workflow.\n\nSee [MLflow tracking](https://docs.databricks.com/aws/en/mlflow/tracking) for the vendor documentation that supports the correct choice.",
      "reference": "[MLflow tracking](https://docs.databricks.com/aws/en/mlflow/tracking)"
    },
    {
      "id": "q02",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "medium",
      "stem": "Fraud Pipeline Scenario: **Hyperopt** must handle `duplicate key ranking` while preserving `labeled quality monitoring`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Hyperopt for fraud pipeline scenario to handle `duplicate key ranking`."
        },
        {
          "id": "B",
          "text": "Use visual tile for fraud pipeline scenario instead of configuring Hyperopt."
        },
        {
          "id": "C",
          "text": "Use admin grant for fraud pipeline scenario instead of configuring Hyperopt."
        },
        {
          "id": "D",
          "text": "Use local notebook for fraud pipeline scenario instead of configuring Hyperopt."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because fraud pipeline scenario needs Hyperopt specifically for `duplicate key ranking`. The documentation describes that product behavior, and `labeled quality monitoring` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because fraud pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\n**C** is incorrect because fraud pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\n**D** is incorrect because fraud pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `duplicate key ranking` or preserve `labeled quality monitoring` in the supported workflow.\n\nSee [Hyperopt](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/hyperopt-concepts) for the vendor documentation that supports the correct choice.",
      "reference": "[Hyperopt](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/hyperopt-concepts)"
    },
    {
      "id": "q03",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Telemetry Pipeline Scenario: **AutoML** must handle `nested array expansion` while preserving `dual store features`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for telemetry pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "B",
          "text": "Use local notebook for telemetry pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "C",
          "text": "Use driver file for telemetry pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "D",
          "text": "Use AutoML for telemetry pipeline scenario to handle `nested array expansion`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because telemetry pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**B** is incorrect because telemetry pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**C** is incorrect because telemetry pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `nested array expansion` or preserve `dual store features` in the supported workflow.\n\n**D** is correct because telemetry pipeline scenario needs AutoML specifically for `nested array expansion`. The documentation describes that product behavior, and `dual store features` keeps the workflow inside the managed platform capability.\n\nSee [AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/) for the vendor documentation that supports the correct choice.",
      "reference": "[AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/)"
    },
    {
      "id": "q04",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Partner Pipeline Scenario: **Model Registry** must handle `cache invalidation` while preserving `Pipeline condition step`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for partner pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "B",
          "text": "Use driver file for partner pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "C",
          "text": "Use Model Registry for partner pipeline scenario to handle `cache invalidation`."
        },
        {
          "id": "D",
          "text": "Use report table for partner pipeline scenario instead of configuring Model Registry."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because partner pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\n**B** is incorrect because partner pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\n**C** is correct because partner pipeline scenario needs Model Registry specifically for `cache invalidation`. The documentation describes that product behavior, and `Pipeline condition step` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because partner pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `cache invalidation` or preserve `Pipeline condition step` in the supported workflow.\n\nSee [Model Registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/)"
    },
    {
      "id": "q05",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "Clinical Pipeline Scenario: **Model signatures** must handle `role visibility` while preserving `scheduled drift statistics`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for clinical pipeline scenario instead of configuring Model signatures."
        },
        {
          "id": "B",
          "text": "Use Model signatures for clinical pipeline scenario to handle `role visibility`."
        },
        {
          "id": "C",
          "text": "Use report table for clinical pipeline scenario instead of configuring Model signatures."
        },
        {
          "id": "D",
          "text": "Use shell cron for clinical pipeline scenario instead of configuring Model signatures."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because clinical pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `role visibility` or preserve `scheduled drift statistics` in the supported workflow.\n\n**B** is correct because clinical pipeline scenario needs Model signatures specifically for `role visibility`. The documentation describes that product behavior, and `scheduled drift statistics` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because clinical pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `role visibility` or preserve `scheduled drift statistics` in the supported workflow.\n\n**D** is incorrect because clinical pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `role visibility` or preserve `scheduled drift statistics` in the supported workflow.\n\nSee [Model signatures](https://docs.databricks.com/aws/en/machine-learning/model-serving/model-serving-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Model signatures](https://docs.databricks.com/aws/en/machine-learning/model-serving/model-serving-intro)"
    },
    {
      "id": "q06",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Finance Reconciliation Over Late Arriving Invoices With Approval Cutoffs And Immutable Audit Columns: **Feature Store** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Feature Store for loan pipeline scenario to handle `metric comparison`."
        },
        {
          "id": "B",
          "text": "Use report table for loan pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "C",
          "text": "Use shell cron for loan pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "D",
          "text": "Use email approval for loan pipeline scenario instead of configuring Feature Store."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because loan pipeline scenario needs Feature Store specifically for `metric comparison`. The documentation describes that product behavior, and `recursive hierarchy traversal` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because loan pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\n**C** is incorrect because loan pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\n**D** is incorrect because loan pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `metric comparison` or preserve `recursive hierarchy traversal` in the supported workflow.\n\nSee [Feature Store](https://docs.databricks.com/aws/en/machine-learning/feature-store/) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature Store](https://docs.databricks.com/aws/en/machine-learning/feature-store/)"
    },
    {
      "id": "q07",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "medium",
      "stem": "Slotting Pipeline Scenario: **Feature lookup** must handle `point in time joins` while preserving `materialized view maintenance`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for slotting pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "B",
          "text": "Use email approval for slotting pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for slotting pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "D",
          "text": "Use Feature lookup for slotting pipeline scenario to handle `point in time joins`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because slotting pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**B** is incorrect because slotting pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**C** is incorrect because slotting pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `point in time joins` or preserve `materialized view maintenance` in the supported workflow.\n\n**D** is correct because slotting pipeline scenario needs Feature lookup specifically for `point in time joins`. The documentation describes that product behavior, and `materialized view maintenance` keeps the workflow inside the managed platform capability.\n\nSee [Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store)"
    },
    {
      "id": "q08",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Consent Pipeline Scenario: **Spark ML pipelines** must handle `offline object scoring` while preserving `Search service refresh`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for consent pipeline scenario instead of configuring Spark ML pipelines."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for consent pipeline scenario instead of configuring Spark ML pipelines."
        },
        {
          "id": "C",
          "text": "Use Spark ML pipelines for consent pipeline scenario to handle `offline object scoring`."
        },
        {
          "id": "D",
          "text": "Use visual tile for consent pipeline scenario instead of configuring Spark ML pipelines."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because consent pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\n**B** is incorrect because consent pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\n**C** is correct because consent pipeline scenario needs Spark ML pipelines specifically for `offline object scoring`. The documentation describes that product behavior, and `Search service refresh` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because consent pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `offline object scoring` or preserve `Search service refresh` in the supported workflow.\n\nSee [Spark ML pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Spark ML pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html)"
    },
    {
      "id": "q09",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "easy",
      "stem": "Campaign Pipeline Scenario: **Model Serving** must handle `scheduled drift statistics` while preserving `registry approval state`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for campaign pipeline scenario instead of configuring Model Serving."
        },
        {
          "id": "B",
          "text": "Use Model Serving for campaign pipeline scenario to handle `scheduled drift statistics`."
        },
        {
          "id": "C",
          "text": "Use visual tile for campaign pipeline scenario instead of configuring Model Serving."
        },
        {
          "id": "D",
          "text": "Use admin grant for campaign pipeline scenario instead of configuring Model Serving."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because campaign pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\n**B** is correct because campaign pipeline scenario needs Model Serving specifically for `scheduled drift statistics`. The documentation describes that product behavior, and `registry approval state` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because campaign pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\n**D** is incorrect because campaign pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `scheduled drift statistics` or preserve `registry approval state` in the supported workflow.\n\nSee [Model Serving](https://docs.databricks.com/aws/en/machine-learning/model-serving/) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Serving](https://docs.databricks.com/aws/en/machine-learning/model-serving/)"
    },
    {
      "id": "q10",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Intent Pipeline Scenario: **Batch inference** must handle `metric gated registration` while preserving `bursty join sizing`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Batch inference for intent pipeline scenario to handle `metric gated registration`."
        },
        {
          "id": "B",
          "text": "Use visual tile for intent pipeline scenario instead of configuring Batch inference."
        },
        {
          "id": "C",
          "text": "Use admin grant for intent pipeline scenario instead of configuring Batch inference."
        },
        {
          "id": "D",
          "text": "Use local notebook for intent pipeline scenario instead of configuring Batch inference."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because intent pipeline scenario needs Batch inference specifically for `metric gated registration`. The documentation describes that product behavior, and `bursty join sizing` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because intent pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\n**C** is incorrect because intent pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\n**D** is incorrect because intent pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `metric gated registration` or preserve `bursty join sizing` in the supported workflow.\n\nSee [Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/dl-model-inference) for the vendor documentation that supports the correct choice.",
      "reference": "[Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/dl-model-inference)"
    },
    {
      "id": "q11",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Databricks Lakehouse Monitoring creates monitors for tables and model inference profiles, computes data quality and drift metrics, and stores metric tables for refresh.\n\nA practitioner must choose the documented **Lakehouse Monitoring** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for claims pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "B",
          "text": "Use local notebook for claims pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "C",
          "text": "Use driver file for claims pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "D",
          "text": "Use Lakehouse Monitoring because the documentation supports this exact behavior: Databricks Lakehouse Monitoring creates monitors for tables and model inference profiles, computes data quality and drift metrics, and stores metric tables for refresh."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Databricks Lakehouse Monitoring creates monitors for tables and model inference profiles, computes data quality and drift metrics, and stores metric tables for refresh.\n\n**B** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Databricks Lakehouse Monitoring creates monitors for tables and model inference profiles, computes data quality and drift metrics, and stores metric tables for refresh.\n\n**C** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Databricks Lakehouse Monitoring creates monitors for tables and model inference profiles, computes data quality and drift metrics, and stores metric tables for refresh.\n\n**D** is correct because Databricks Lakehouse Monitoring creates monitors for tables and model inference profiles, computes data quality and drift metrics, and stores metric tables for refresh. This directly matches the scenario and is supported by the referenced Lakehouse Monitoring documentation.\n\nSee [Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/) for the vendor documentation that supports the correct choice.",
      "reference": "[Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/)"
    },
    {
      "id": "q12",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "hard",
      "stem": "Inventory Pipeline Scenario: **Experiment search** must handle `registry approval state` while preserving `OBJECT CONSTRUCT payload`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for inventory pipeline scenario instead of configuring Experiment search."
        },
        {
          "id": "B",
          "text": "Use driver file for inventory pipeline scenario instead of configuring Experiment search."
        },
        {
          "id": "C",
          "text": "Use Experiment search for inventory pipeline scenario to handle `registry approval state`."
        },
        {
          "id": "D",
          "text": "Use report table for inventory pipeline scenario instead of configuring Experiment search."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because inventory pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\n**B** is incorrect because inventory pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\n**C** is correct because inventory pipeline scenario needs Experiment search specifically for `registry approval state`. The documentation describes that product behavior, and `OBJECT CONSTRUCT payload` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because inventory pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `registry approval state` or preserve `OBJECT CONSTRUCT payload` in the supported workflow.\n\nSee [Experiment search](https://mlflow.org/docs/latest/ml/search/search-runs/) for the vendor documentation that supports the correct choice.",
      "reference": "[Experiment search](https://mlflow.org/docs/latest/ml/search/search-runs/)"
    },
    {
      "id": "q13",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "easy",
      "stem": "Payment Pipeline Scenario: **Autologging** must handle `profile spill diagnosis` while preserving `SQL language model call`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for payment pipeline scenario instead of configuring Autologging."
        },
        {
          "id": "B",
          "text": "Use Autologging for payment pipeline scenario to handle `profile spill diagnosis`."
        },
        {
          "id": "C",
          "text": "Use report table for payment pipeline scenario instead of configuring Autologging."
        },
        {
          "id": "D",
          "text": "Use shell cron for payment pipeline scenario instead of configuring Autologging."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because payment pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\n**B** is correct because payment pipeline scenario needs Autologging specifically for `profile spill diagnosis`. The documentation describes that product behavior, and `SQL language model call` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because payment pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\n**D** is incorrect because payment pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `profile spill diagnosis` or preserve `SQL language model call` in the supported workflow.\n\nSee [Autologging](https://mlflow.org/docs/latest/ml/tracking/autolog/) for the vendor documentation that supports the correct choice.",
      "reference": "[Autologging](https://mlflow.org/docs/latest/ml/tracking/autolog/)"
    },
    {
      "id": "q14",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Databricks AutoML and model explainability workflows can calculate SHAP feature importance so practitioners can inspect how features influence predictions.\n\nA practitioner must choose the documented **SHAP explainability** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use SHAP explainability because the documentation supports this exact behavior: Databricks AutoML and model explainability workflows can calculate SHAP feature importance so practitioners can inspect how features influence predictions."
        },
        {
          "id": "B",
          "text": "Use report table for recommendation pipeline scenario instead of configuring SHAP explainability."
        },
        {
          "id": "C",
          "text": "Use shell cron for recommendation pipeline scenario instead of configuring SHAP explainability."
        },
        {
          "id": "D",
          "text": "Use email approval for recommendation pipeline scenario instead of configuring SHAP explainability."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because Databricks AutoML and model explainability workflows can calculate SHAP feature importance so practitioners can inspect how features influence predictions. This directly matches the scenario and is supported by the referenced SHAP explainability documentation.\n\n**B** is incorrect because it does not provide the documented SHAP explainability behavior described here: Databricks AutoML and model explainability workflows can calculate SHAP feature importance so practitioners can inspect how features influence predictions.\n\n**C** is incorrect because it does not provide the documented SHAP explainability behavior described here: Databricks AutoML and model explainability workflows can calculate SHAP feature importance so practitioners can inspect how features influence predictions.\n\n**D** is incorrect because it does not provide the documented SHAP explainability behavior described here: Databricks AutoML and model explainability workflows can calculate SHAP feature importance so practitioners can inspect how features influence predictions.\n\nSee [SHAP explainability](https://docs.databricks.com/aws/en/machine-learning/automl/train-ml-model-automl-api) for the vendor documentation that supports the correct choice.",
      "reference": "[SHAP explainability](https://docs.databricks.com/aws/en/machine-learning/automl/train-ml-model-automl-api)"
    },
    {
      "id": "q15",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Contract Pipeline Scenario: **MLflow pyfunc** must handle `SQL language model call` while preserving `Clarify bias baseline`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for contract pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "B",
          "text": "Use email approval for contract pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for contract pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "D",
          "text": "Use MLflow pyfunc for contract pipeline scenario to handle `SQL language model call`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because contract pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**B** is incorrect because contract pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**C** is incorrect because contract pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `SQL language model call` or preserve `Clarify bias baseline` in the supported workflow.\n\n**D** is correct because contract pipeline scenario needs MLflow pyfunc specifically for `SQL language model call`. The documentation describes that product behavior, and `Clarify bias baseline` keeps the workflow inside the managed platform capability.\n\nSee [MLflow pyfunc](https://mlflow.org/docs/latest/ml/model/python_model/) for the vendor documentation that supports the correct choice.",
      "reference": "[MLflow pyfunc](https://mlflow.org/docs/latest/ml/model/python_model/)"
    },
    {
      "id": "q16",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "hard",
      "stem": "Customer Contact Center Transcript Mining With Language Tags And Escalation Categories: **Serving traffic config** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for energy pipeline scenario instead of configuring Serving traffic config."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for energy pipeline scenario instead of configuring Serving traffic config."
        },
        {
          "id": "C",
          "text": "Use Serving traffic config for energy pipeline scenario to handle `traffic split rollout`."
        },
        {
          "id": "D",
          "text": "Use visual tile for energy pipeline scenario instead of configuring Serving traffic config."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because energy pipeline scenario would still lack Serving traffic config; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\n**B** is incorrect because energy pipeline scenario would still lack Serving traffic config; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\n**C** is correct because energy pipeline scenario needs Serving traffic config specifically for `traffic split rollout`. The documentation describes that product behavior, and `checkpoint retries` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because energy pipeline scenario would still lack Serving traffic config; the suggested workaround cannot satisfy `traffic split rollout` or preserve `checkpoint retries` in the supported workflow.\n\nSee [Serving traffic config](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints) for the vendor documentation that supports the correct choice.",
      "reference": "[Serving traffic config](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints)"
    },
    {
      "id": "q17",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "easy",
      "stem": "Supplier Pipeline Scenario: **Webhooks** must handle `post load aggregate refresh` while preserving `optional JSON extraction`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for supplier pipeline scenario instead of configuring Webhooks."
        },
        {
          "id": "B",
          "text": "Use Webhooks for supplier pipeline scenario to handle `post load aggregate refresh`."
        },
        {
          "id": "C",
          "text": "Use visual tile for supplier pipeline scenario instead of configuring Webhooks."
        },
        {
          "id": "D",
          "text": "Use admin grant for supplier pipeline scenario instead of configuring Webhooks."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because supplier pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\n**B** is correct because supplier pipeline scenario needs Webhooks specifically for `post load aggregate refresh`. The documentation describes that product behavior, and `optional JSON extraction` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because supplier pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\n**D** is incorrect because supplier pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `post load aggregate refresh` or preserve `optional JSON extraction` in the supported workflow.\n\nSee [Webhooks](https://docs.databricks.com/aws/en/mlflow/model-registry-webhooks) for the vendor documentation that supports the correct choice.",
      "reference": "[Webhooks](https://docs.databricks.com/aws/en/mlflow/model-registry-webhooks)"
    },
    {
      "id": "q18",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Dispatch Pipeline Scenario: **Databricks Jobs** must handle `optional JSON extraction` while preserving `email masking rule`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Databricks Jobs for dispatch pipeline scenario to handle `optional JSON extraction`."
        },
        {
          "id": "B",
          "text": "Use visual tile for dispatch pipeline scenario instead of configuring Databricks Jobs."
        },
        {
          "id": "C",
          "text": "Use admin grant for dispatch pipeline scenario instead of configuring Databricks Jobs."
        },
        {
          "id": "D",
          "text": "Use local notebook for dispatch pipeline scenario instead of configuring Databricks Jobs."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because dispatch pipeline scenario needs Databricks Jobs specifically for `optional JSON extraction`. The documentation describes that product behavior, and `email masking rule` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because dispatch pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\n**C** is incorrect because dispatch pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\n**D** is incorrect because dispatch pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `optional JSON extraction` or preserve `email masking rule` in the supported workflow.\n\nSee [Databricks Jobs](https://docs.databricks.com/aws/en/jobs/) for the vendor documentation that supports the correct choice.",
      "reference": "[Databricks Jobs](https://docs.databricks.com/aws/en/jobs/)"
    },
    {
      "id": "q19",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Session Pipeline Scenario: **Unity Catalog models** must handle `sparse tabular algorithm` while preserving `Model Monitor schedule`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for session pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "B",
          "text": "Use local notebook for session pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "C",
          "text": "Use driver file for session pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "D",
          "text": "Use Unity Catalog models for session pipeline scenario to handle `sparse tabular algorithm`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because session pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**B** is incorrect because session pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**C** is incorrect because session pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `sparse tabular algorithm` or preserve `Model Monitor schedule` in the supported workflow.\n\n**D** is correct because session pipeline scenario needs Unity Catalog models specifically for `sparse tabular algorithm`. The documentation describes that product behavior, and `Model Monitor schedule` keeps the workflow inside the managed platform capability.\n\nSee [Unity Catalog models](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/) for the vendor documentation that supports the correct choice.",
      "reference": "[Unity Catalog models](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/)"
    },
    {
      "id": "q20",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "Databricks Feature Engineering in Unity Catalog uses feature tables and FeatureEngineeringClient APIs to create, read, and join features for training and scoring.\n\nA practitioner must choose the documented **Feature engineering client** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for identity pipeline scenario instead of configuring Feature engineering client."
        },
        {
          "id": "B",
          "text": "Use driver file for identity pipeline scenario instead of configuring Feature engineering client."
        },
        {
          "id": "C",
          "text": "Use Feature engineering client because the documentation supports this exact behavior: Databricks Feature Engineering in Unity Catalog uses feature tables and FeatureEngineeringClient APIs to create, read, and join features for training and scoring."
        },
        {
          "id": "D",
          "text": "Use report table for identity pipeline scenario instead of configuring Feature engineering client."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Feature engineering client behavior described here: Databricks Feature Engineering in Unity Catalog uses feature tables and FeatureEngineeringClient APIs to create, read, and join features for training and scoring.\n\n**B** is incorrect because it does not provide the documented Feature engineering client behavior described here: Databricks Feature Engineering in Unity Catalog uses feature tables and FeatureEngineeringClient APIs to create, read, and join features for training and scoring.\n\n**C** is correct because Databricks Feature Engineering in Unity Catalog uses feature tables and FeatureEngineeringClient APIs to create, read, and join features for training and scoring. This directly matches the scenario and is supported by the referenced Feature engineering client documentation.\n\n**D** is incorrect because it does not provide the documented Feature engineering client behavior described here: Databricks Feature Engineering in Unity Catalog uses feature tables and FeatureEngineeringClient APIs to create, read, and join features for training and scoring.\n\nSee [Feature engineering client](https://docs.databricks.com/aws/en/machine-learning/feature-store/uc/feature-tables-uc) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature engineering client](https://docs.databricks.com/aws/en/machine-learning/feature-store/uc/feature-tables-uc)"
    },
    {
      "id": "q21",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Label Pipeline Scenario: **MLflow tracking** must handle `semantic retrieval setup` while preserving `semantic retrieval setup`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for label pipeline scenario instead of configuring MLflow tracking."
        },
        {
          "id": "B",
          "text": "Use MLflow tracking for label pipeline scenario to handle `semantic retrieval setup`."
        },
        {
          "id": "C",
          "text": "Use report table for label pipeline scenario instead of configuring MLflow tracking."
        },
        {
          "id": "D",
          "text": "Use shell cron for label pipeline scenario instead of configuring MLflow tracking."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because label pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\n**B** is correct because label pipeline scenario needs MLflow tracking specifically for `semantic retrieval setup`. The documentation describes that product behavior, and `semantic retrieval setup` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because label pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\n**D** is incorrect because label pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `semantic retrieval setup` or preserve `semantic retrieval setup` in the supported workflow.\n\nSee [MLflow tracking](https://docs.databricks.com/aws/en/mlflow/tracking) for the vendor documentation that supports the correct choice.",
      "reference": "[MLflow tracking](https://docs.databricks.com/aws/en/mlflow/tracking)"
    },
    {
      "id": "q22",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "medium",
      "stem": "Champion Pipeline Scenario: **Hyperopt** must handle `sensitivity tag policy` while preserving `event scheduled pipeline`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Hyperopt for champion pipeline scenario to handle `sensitivity tag policy`."
        },
        {
          "id": "B",
          "text": "Use report table for champion pipeline scenario instead of configuring Hyperopt."
        },
        {
          "id": "C",
          "text": "Use shell cron for champion pipeline scenario instead of configuring Hyperopt."
        },
        {
          "id": "D",
          "text": "Use email approval for champion pipeline scenario instead of configuring Hyperopt."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because champion pipeline scenario needs Hyperopt specifically for `sensitivity tag policy`. The documentation describes that product behavior, and `event scheduled pipeline` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because champion pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `sensitivity tag policy` or preserve `event scheduled pipeline` in the supported workflow.\n\n**C** is incorrect because champion pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `sensitivity tag policy` or preserve `event scheduled pipeline` in the supported workflow.\n\n**D** is incorrect because champion pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `sensitivity tag policy` or preserve `event scheduled pipeline` in the supported workflow.\n\nSee [Hyperopt](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/hyperopt-concepts) for the vendor documentation that supports the correct choice.",
      "reference": "[Hyperopt](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/hyperopt-concepts)"
    },
    {
      "id": "q23",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Support Pipeline Scenario: **AutoML** must handle `labeled quality monitoring` while preserving `Data Wrangler export`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for support pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "B",
          "text": "Use email approval for support pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for support pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "D",
          "text": "Use AutoML for support pipeline scenario to handle `labeled quality monitoring`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because support pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `labeled quality monitoring` or preserve `Data Wrangler export` in the supported workflow.\n\n**B** is incorrect because support pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `labeled quality monitoring` or preserve `Data Wrangler export` in the supported workflow.\n\n**C** is incorrect because support pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `labeled quality monitoring` or preserve `Data Wrangler export` in the supported workflow.\n\n**D** is correct because support pipeline scenario needs AutoML specifically for `labeled quality monitoring`. The documentation describes that product behavior, and `Data Wrangler export` keeps the workflow inside the managed platform capability.\n\nSee [AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/) for the vendor documentation that supports the correct choice.",
      "reference": "[AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/)"
    },
    {
      "id": "q24",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Quota Pipeline Scenario: **Model Registry** must handle `feature lineage capture` while preserving `point in time joins`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for quota pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for quota pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "C",
          "text": "Use Model Registry for quota pipeline scenario to handle `feature lineage capture`."
        },
        {
          "id": "D",
          "text": "Use visual tile for quota pipeline scenario instead of configuring Model Registry."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because quota pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\n**B** is incorrect because quota pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\n**C** is correct because quota pipeline scenario needs Model Registry specifically for `feature lineage capture`. The documentation describes that product behavior, and `point in time joins` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because quota pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `feature lineage capture` or preserve `point in time joins` in the supported workflow.\n\nSee [Model Registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/)"
    },
    {
      "id": "q25",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "Access Pipeline Scenario: **Model signatures** must handle `BI precomputation` while preserving `feature lineage capture`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for access pipeline scenario instead of configuring Model signatures."
        },
        {
          "id": "B",
          "text": "Use Model signatures for access pipeline scenario to handle `BI precomputation`."
        },
        {
          "id": "C",
          "text": "Use visual tile for access pipeline scenario instead of configuring Model signatures."
        },
        {
          "id": "D",
          "text": "Use admin grant for access pipeline scenario instead of configuring Model signatures."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because access pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `BI precomputation` or preserve `feature lineage capture` in the supported workflow.\n\n**B** is correct because access pipeline scenario needs Model signatures specifically for `BI precomputation`. The documentation describes that product behavior, and `feature lineage capture` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because access pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `BI precomputation` or preserve `feature lineage capture` in the supported workflow.\n\n**D** is incorrect because access pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `BI precomputation` or preserve `feature lineage capture` in the supported workflow.\n\nSee [Model signatures](https://docs.databricks.com/aws/en/machine-learning/model-serving/model-serving-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Model signatures](https://docs.databricks.com/aws/en/machine-learning/model-serving/model-serving-intro)"
    },
    {
      "id": "q26",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Maintenance Telemetry Rollup For Turbine Sensors With Calibration Batches And Technician Exception Queues: **Feature Store** is being selected for this specific workflow. The design must satisfy the documented feature behavior without replacing it with a manual workaround.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Feature Store for document pipeline scenario to handle `recursive hierarchy traversal`."
        },
        {
          "id": "B",
          "text": "Use visual tile for document pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "C",
          "text": "Use admin grant for document pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "D",
          "text": "Use local notebook for document pipeline scenario instead of configuring Feature Store."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because document pipeline scenario needs Feature Store specifically for `recursive hierarchy traversal`. The documentation describes that product behavior, and `warehouse scaling policy` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because document pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `recursive hierarchy traversal` or preserve `warehouse scaling policy` in the supported workflow.\n\n**C** is incorrect because document pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `recursive hierarchy traversal` or preserve `warehouse scaling policy` in the supported workflow.\n\n**D** is incorrect because document pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `recursive hierarchy traversal` or preserve `warehouse scaling policy` in the supported workflow.\n\nSee [Feature Store](https://docs.databricks.com/aws/en/machine-learning/feature-store/) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature Store](https://docs.databricks.com/aws/en/machine-learning/feature-store/)"
    },
    {
      "id": "q27",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "medium",
      "stem": "Defect Pipeline Scenario: **Feature lookup** must handle `managed preprocessing container` while preserving `multi model endpoint load`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for defect pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "B",
          "text": "Use local notebook for defect pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "C",
          "text": "Use driver file for defect pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "D",
          "text": "Use Feature lookup for defect pipeline scenario to handle `managed preprocessing container`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because defect pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**B** is incorrect because defect pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**C** is incorrect because defect pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `managed preprocessing container` or preserve `multi model endpoint load` in the supported workflow.\n\n**D** is correct because defect pipeline scenario needs Feature lookup specifically for `managed preprocessing container`. The documentation describes that product behavior, and `multi model endpoint load` keeps the workflow inside the managed platform capability.\n\nSee [Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store)"
    },
    {
      "id": "q28",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Residual Pipeline Scenario: **Spark ML pipelines** must handle `registry notification hook` while preserving `metric gated registration`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for residual pipeline scenario instead of configuring Spark ML pipelines."
        },
        {
          "id": "B",
          "text": "Use driver file for residual pipeline scenario instead of configuring Spark ML pipelines."
        },
        {
          "id": "C",
          "text": "Use Spark ML pipelines for residual pipeline scenario to handle `registry notification hook`."
        },
        {
          "id": "D",
          "text": "Use report table for residual pipeline scenario instead of configuring Spark ML pipelines."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because residual pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\n**B** is incorrect because residual pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\n**C** is correct because residual pipeline scenario needs Spark ML pipelines specifically for `registry notification hook`. The documentation describes that product behavior, and `metric gated registration` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because residual pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `registry notification hook` or preserve `metric gated registration` in the supported workflow.\n\nSee [Spark ML pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Spark ML pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html)"
    },
    {
      "id": "q29",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "easy",
      "stem": "Approval Pipeline Scenario: **Model Serving** must handle `bursty join sizing` while preserving `managed preprocessing container`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for approval pipeline scenario instead of configuring Model Serving."
        },
        {
          "id": "B",
          "text": "Use Model Serving for approval pipeline scenario to handle `bursty join sizing`."
        },
        {
          "id": "C",
          "text": "Use report table for approval pipeline scenario instead of configuring Model Serving."
        },
        {
          "id": "D",
          "text": "Use shell cron for approval pipeline scenario instead of configuring Model Serving."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because approval pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `bursty join sizing` or preserve `managed preprocessing container` in the supported workflow.\n\n**B** is correct because approval pipeline scenario needs Model Serving specifically for `bursty join sizing`. The documentation describes that product behavior, and `managed preprocessing container` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because approval pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `bursty join sizing` or preserve `managed preprocessing container` in the supported workflow.\n\n**D** is incorrect because approval pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `bursty join sizing` or preserve `managed preprocessing container` in the supported workflow.\n\nSee [Model Serving](https://docs.databricks.com/aws/en/machine-learning/model-serving/) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Serving](https://docs.databricks.com/aws/en/machine-learning/model-serving/)"
    },
    {
      "id": "q30",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Scoring Pipeline Scenario: **Batch inference** must handle `online feature serving` while preserving `Cortex analyst semantic model`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Batch inference for scoring pipeline scenario to handle `online feature serving`."
        },
        {
          "id": "B",
          "text": "Use report table for scoring pipeline scenario instead of configuring Batch inference."
        },
        {
          "id": "C",
          "text": "Use shell cron for scoring pipeline scenario instead of configuring Batch inference."
        },
        {
          "id": "D",
          "text": "Use email approval for scoring pipeline scenario instead of configuring Batch inference."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because scoring pipeline scenario needs Batch inference specifically for `online feature serving`. The documentation describes that product behavior, and `Cortex analyst semantic model` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because scoring pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\n**C** is incorrect because scoring pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\n**D** is incorrect because scoring pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `online feature serving` or preserve `Cortex analyst semantic model` in the supported workflow.\n\nSee [Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/dl-model-inference) for the vendor documentation that supports the correct choice.",
      "reference": "[Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/dl-model-inference)"
    },
    {
      "id": "q31",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Lakehouse Monitoring tracks inference tables and baseline comparisons so teams can review drift and model quality over scheduled refreshes.\n\nA practitioner must choose the documented **Lakehouse Monitoring** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for alert pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "B",
          "text": "Use email approval for alert pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for alert pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "D",
          "text": "Use Lakehouse Monitoring because the documentation supports this exact behavior: Lakehouse Monitoring tracks inference tables and baseline comparisons so teams can review drift and model quality over scheduled refreshes."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Lakehouse Monitoring tracks inference tables and baseline comparisons so teams can review drift and model quality over scheduled refreshes.\n\n**B** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Lakehouse Monitoring tracks inference tables and baseline comparisons so teams can review drift and model quality over scheduled refreshes.\n\n**C** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Lakehouse Monitoring tracks inference tables and baseline comparisons so teams can review drift and model quality over scheduled refreshes.\n\n**D** is correct because Lakehouse Monitoring tracks inference tables and baseline comparisons so teams can review drift and model quality over scheduled refreshes. This directly matches the scenario and is supported by the referenced Lakehouse Monitoring documentation.\n\nSee [Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/) for the vendor documentation that supports the correct choice.",
      "reference": "[Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/)"
    },
    {
      "id": "q32",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "hard",
      "stem": "Lifetime Pipeline Scenario: **Experiment search** must handle `dependency lineage graph` while preserving `profile spill diagnosis`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for lifetime pipeline scenario instead of configuring Experiment search."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for lifetime pipeline scenario instead of configuring Experiment search."
        },
        {
          "id": "C",
          "text": "Use Experiment search for lifetime pipeline scenario to handle `dependency lineage graph`."
        },
        {
          "id": "D",
          "text": "Use visual tile for lifetime pipeline scenario instead of configuring Experiment search."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because lifetime pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\n**B** is incorrect because lifetime pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\n**C** is correct because lifetime pipeline scenario needs Experiment search specifically for `dependency lineage graph`. The documentation describes that product behavior, and `profile spill diagnosis` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because lifetime pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `dependency lineage graph` or preserve `profile spill diagnosis` in the supported workflow.\n\nSee [Experiment search](https://mlflow.org/docs/latest/ml/search/search-runs/) for the vendor documentation that supports the correct choice.",
      "reference": "[Experiment search](https://mlflow.org/docs/latest/ml/search/search-runs/)"
    },
    {
      "id": "q33",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "easy",
      "stem": "Invoice Pipeline Scenario: **Autologging** must handle `top ranked customer filter` while preserving `online feature serving`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for invoice pipeline scenario instead of configuring Autologging."
        },
        {
          "id": "B",
          "text": "Use Autologging for invoice pipeline scenario to handle `top ranked customer filter`."
        },
        {
          "id": "C",
          "text": "Use visual tile for invoice pipeline scenario instead of configuring Autologging."
        },
        {
          "id": "D",
          "text": "Use admin grant for invoice pipeline scenario instead of configuring Autologging."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because invoice pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\n**B** is correct because invoice pipeline scenario needs Autologging specifically for `top ranked customer filter`. The documentation describes that product behavior, and `online feature serving` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because invoice pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\n**D** is incorrect because invoice pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `top ranked customer filter` or preserve `online feature serving` in the supported workflow.\n\nSee [Autologging](https://mlflow.org/docs/latest/ml/tracking/autolog/) for the vendor documentation that supports the correct choice.",
      "reference": "[Autologging](https://mlflow.org/docs/latest/ml/tracking/autolog/)"
    },
    {
      "id": "q34",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Databricks AutoML can train classification, regression, and forecasting models through the API and records generated notebooks, metrics, and feature importance outputs for model review.\n\nA machine learning practitioner needs the documented AutoML evidence for a model review. Which option best fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Databricks AutoML and review the generated run outputs, metrics, notebooks, and feature importance information."
        },
        {
          "id": "B",
          "text": "Use visual tile for summarizer pipeline scenario instead of configuring Databricks AutoML feature importance."
        },
        {
          "id": "C",
          "text": "Use admin grant for summarizer pipeline scenario instead of configuring Databricks AutoML feature importance."
        },
        {
          "id": "D",
          "text": "Use local notebook for summarizer pipeline scenario instead of configuring Databricks AutoML feature importance."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because Databricks AutoML can train classification, regression, and forecasting models through the API and records generated notebooks, metrics, and feature importance outputs for model review. That is the documented Databricks workflow for collecting model-review evidence from AutoML experiments.\n\n**B** is incorrect because it does not use the Databricks AutoML API workflow that records generated notebooks, metrics, and feature-importance review artifacts.\n\n**C** is incorrect because it does not use the Databricks AutoML API workflow that records generated notebooks, metrics, and feature-importance review artifacts.\n\n**D** is incorrect because it does not use the Databricks AutoML API workflow that records generated notebooks, metrics, and feature-importance review artifacts.\n\nSee [Databricks AutoML feature importance](https://docs.databricks.com/aws/en/machine-learning/automl/train-ml-model-automl-api) for the vendor documentation that supports the correct choice.",
      "reference": "[Databricks AutoML feature importance](https://docs.databricks.com/aws/en/machine-learning/automl/train-ml-model-automl-api)"
    },
    {
      "id": "q35",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Heatmap Pipeline Scenario: **MLflow pyfunc** must handle `email masking rule` while preserving `Access History trace`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for heatmap pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "B",
          "text": "Use local notebook for heatmap pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "C",
          "text": "Use driver file for heatmap pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "D",
          "text": "Use MLflow pyfunc for heatmap pipeline scenario to handle `email masking rule`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because heatmap pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**B** is incorrect because heatmap pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**C** is incorrect because heatmap pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `email masking rule` or preserve `Access History trace` in the supported workflow.\n\n**D** is correct because heatmap pipeline scenario needs MLflow pyfunc specifically for `email masking rule`. The documentation describes that product behavior, and `Access History trace` keeps the workflow inside the managed platform capability.\n\nSee [MLflow pyfunc](https://mlflow.org/docs/latest/ml/model/python_model/) for the vendor documentation that supports the correct choice.",
      "reference": "[MLflow pyfunc](https://mlflow.org/docs/latest/ml/model/python_model/)"
    },
    {
      "id": "q36",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "hard",
      "stem": "Databricks Model Serving endpoint configuration supports served entities, traffic configuration, and traffic percentages for routing requests between model versions.\n\nA practitioner must choose the documented **Serving traffic config** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for clause pipeline scenario instead of configuring Serving traffic config."
        },
        {
          "id": "B",
          "text": "Use driver file for clause pipeline scenario instead of configuring Serving traffic config."
        },
        {
          "id": "C",
          "text": "Use Serving traffic config because the documentation supports this exact behavior: Databricks Model Serving endpoint configuration supports served entities, traffic configuration, and traffic percentages for routing requests between model versions."
        },
        {
          "id": "D",
          "text": "Use report table for clause pipeline scenario instead of configuring Serving traffic config."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Serving traffic config behavior described here: Databricks Model Serving endpoint configuration supports served entities, traffic configuration, and traffic percentages for routing requests between model versions.\n\n**B** is incorrect because it does not provide the documented Serving traffic config behavior described here: Databricks Model Serving endpoint configuration supports served entities, traffic configuration, and traffic percentages for routing requests between model versions.\n\n**C** is correct because Databricks Model Serving endpoint configuration supports served entities, traffic configuration, and traffic percentages for routing requests between model versions. This directly matches the scenario and is supported by the referenced Serving traffic config documentation.\n\n**D** is incorrect because it does not provide the documented Serving traffic config behavior described here: Databricks Model Serving endpoint configuration supports served entities, traffic configuration, and traffic percentages for routing requests between model versions.\n\nSee [Serving traffic config](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints) for the vendor documentation that supports the correct choice.",
      "reference": "[Serving traffic config](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints)"
    },
    {
      "id": "q37",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "easy",
      "stem": "Drift Pipeline Scenario: **Webhooks** must handle `async payload routing` while preserving `top ranked customer filter`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for drift pipeline scenario instead of configuring Webhooks."
        },
        {
          "id": "B",
          "text": "Use Webhooks for drift pipeline scenario to handle `async payload routing`."
        },
        {
          "id": "C",
          "text": "Use report table for drift pipeline scenario instead of configuring Webhooks."
        },
        {
          "id": "D",
          "text": "Use shell cron for drift pipeline scenario instead of configuring Webhooks."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because drift pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\n**B** is correct because drift pipeline scenario needs Webhooks specifically for `async payload routing`. The documentation describes that product behavior, and `top ranked customer filter` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because drift pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\n**D** is incorrect because drift pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `async payload routing` or preserve `top ranked customer filter` in the supported workflow.\n\nSee [Webhooks](https://docs.databricks.com/aws/en/mlflow/model-registry-webhooks) for the vendor documentation that supports the correct choice.",
      "reference": "[Webhooks](https://docs.databricks.com/aws/en/mlflow/model-registry-webhooks)"
    },
    {
      "id": "q38",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Latency Pipeline Scenario: **Databricks Jobs** must handle `event scheduled pipeline` while preserving `Glue bookmark state`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Databricks Jobs for latency pipeline scenario to handle `event scheduled pipeline`."
        },
        {
          "id": "B",
          "text": "Use report table for latency pipeline scenario instead of configuring Databricks Jobs."
        },
        {
          "id": "C",
          "text": "Use shell cron for latency pipeline scenario instead of configuring Databricks Jobs."
        },
        {
          "id": "D",
          "text": "Use email approval for latency pipeline scenario instead of configuring Databricks Jobs."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because latency pipeline scenario needs Databricks Jobs specifically for `event scheduled pipeline`. The documentation describes that product behavior, and `Glue bookmark state` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because latency pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\n**C** is incorrect because latency pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\n**D** is incorrect because latency pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `event scheduled pipeline` or preserve `Glue bookmark state` in the supported workflow.\n\nSee [Databricks Jobs](https://docs.databricks.com/aws/en/jobs/) for the vendor documentation that supports the correct choice.",
      "reference": "[Databricks Jobs](https://docs.databricks.com/aws/en/jobs/)"
    },
    {
      "id": "q39",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Comparison Pipeline Scenario: **Unity Catalog models** must handle `training baseline capture` while preserving `duplicate key ranking`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for comparison pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "B",
          "text": "Use email approval for comparison pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for comparison pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "D",
          "text": "Use Unity Catalog models for comparison pipeline scenario to handle `training baseline capture`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because comparison pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**B** is incorrect because comparison pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**C** is incorrect because comparison pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `training baseline capture` or preserve `duplicate key ranking` in the supported workflow.\n\n**D** is correct because comparison pipeline scenario needs Unity Catalog models specifically for `training baseline capture`. The documentation describes that product behavior, and `duplicate key ranking` keeps the workflow inside the managed platform capability.\n\nSee [Unity Catalog models](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/) for the vendor documentation that supports the correct choice.",
      "reference": "[Unity Catalog models](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/)"
    },
    {
      "id": "q40",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "Entitlement Pipeline Scenario: **Feature engineering client** must handle `dual store features` while preserving `sparse tabular algorithm`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for entitlement pipeline scenario instead of configuring Feature engineering client."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for entitlement pipeline scenario instead of configuring Feature engineering client."
        },
        {
          "id": "C",
          "text": "Use Feature engineering client for entitlement pipeline scenario to handle `dual store features`."
        },
        {
          "id": "D",
          "text": "Use visual tile for entitlement pipeline scenario instead of configuring Feature engineering client."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because entitlement pipeline scenario would still lack Feature engineering client; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\n**B** is incorrect because entitlement pipeline scenario would still lack Feature engineering client; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\n**C** is correct because entitlement pipeline scenario needs Feature engineering client specifically for `dual store features`. The documentation describes that product behavior, and `sparse tabular algorithm` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because entitlement pipeline scenario would still lack Feature engineering client; the suggested workaround cannot satisfy `dual store features` or preserve `sparse tabular algorithm` in the supported workflow.\n\nSee [Feature engineering client](https://docs.databricks.com/aws/en/machine-learning/feature-store/uc/feature-tables-uc) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature engineering client](https://docs.databricks.com/aws/en/machine-learning/feature-store/uc/feature-tables-uc)"
    },
    {
      "id": "q41",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Analyst Pipeline Scenario: **MLflow tracking** must handle `warehouse scaling policy` while preserving `future grant pattern`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for analyst pipeline scenario instead of configuring MLflow tracking."
        },
        {
          "id": "B",
          "text": "Use MLflow tracking for analyst pipeline scenario to handle `warehouse scaling policy`."
        },
        {
          "id": "C",
          "text": "Use visual tile for analyst pipeline scenario instead of configuring MLflow tracking."
        },
        {
          "id": "D",
          "text": "Use admin grant for analyst pipeline scenario instead of configuring MLflow tracking."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because analyst pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\n**B** is correct because analyst pipeline scenario needs MLflow tracking specifically for `warehouse scaling policy`. The documentation describes that product behavior, and `future grant pattern` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because analyst pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\n**D** is incorrect because analyst pipeline scenario would still lack MLflow tracking; the suggested workaround cannot satisfy `warehouse scaling policy` or preserve `future grant pattern` in the supported workflow.\n\nSee [MLflow tracking](https://docs.databricks.com/aws/en/mlflow/tracking) for the vendor documentation that supports the correct choice.",
      "reference": "[MLflow tracking](https://docs.databricks.com/aws/en/mlflow/tracking)"
    },
    {
      "id": "q42",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "medium",
      "stem": "Aggregate Pipeline Scenario: **Hyperopt** must handle `row access predicate` while preserving `Feature Store online lookup`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Hyperopt for aggregate pipeline scenario to handle `row access predicate`."
        },
        {
          "id": "B",
          "text": "Use visual tile for aggregate pipeline scenario instead of configuring Hyperopt."
        },
        {
          "id": "C",
          "text": "Use admin grant for aggregate pipeline scenario instead of configuring Hyperopt."
        },
        {
          "id": "D",
          "text": "Use local notebook for aggregate pipeline scenario instead of configuring Hyperopt."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because aggregate pipeline scenario needs Hyperopt specifically for `row access predicate`. The documentation describes that product behavior, and `Feature Store online lookup` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because aggregate pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\n**C** is incorrect because aggregate pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\n**D** is incorrect because aggregate pipeline scenario would still lack Hyperopt; the suggested workaround cannot satisfy `row access predicate` or preserve `Feature Store online lookup` in the supported workflow.\n\nSee [Hyperopt](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/hyperopt-concepts) for the vendor documentation that supports the correct choice.",
      "reference": "[Hyperopt](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/hyperopt-concepts)"
    },
    {
      "id": "q43",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Hierarchy Pipeline Scenario: **AutoML** must handle `materialized view maintenance` while preserving `role visibility`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for hierarchy pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "B",
          "text": "Use local notebook for hierarchy pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "C",
          "text": "Use driver file for hierarchy pipeline scenario instead of configuring AutoML."
        },
        {
          "id": "D",
          "text": "Use AutoML for hierarchy pipeline scenario to handle `materialized view maintenance`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because hierarchy pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `materialized view maintenance` or preserve `role visibility` in the supported workflow.\n\n**B** is incorrect because hierarchy pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `materialized view maintenance` or preserve `role visibility` in the supported workflow.\n\n**C** is incorrect because hierarchy pipeline scenario would still lack AutoML; the suggested workaround cannot satisfy `materialized view maintenance` or preserve `role visibility` in the supported workflow.\n\n**D** is correct because hierarchy pipeline scenario needs AutoML specifically for `materialized view maintenance`. The documentation describes that product behavior, and `role visibility` keeps the workflow inside the managed platform capability.\n\nSee [AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/) for the vendor documentation that supports the correct choice.",
      "reference": "[AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/)"
    },
    {
      "id": "q44",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Payload Pipeline Scenario: **Model Registry** must handle `Cortex analyst semantic model` while preserving `sensitivity tag policy`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for payload pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "B",
          "text": "Use driver file for payload pipeline scenario instead of configuring Model Registry."
        },
        {
          "id": "C",
          "text": "Use Model Registry for payload pipeline scenario to handle `Cortex analyst semantic model`."
        },
        {
          "id": "D",
          "text": "Use report table for payload pipeline scenario instead of configuring Model Registry."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because payload pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\n**B** is incorrect because payload pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\n**C** is correct because payload pipeline scenario needs Model Registry specifically for `Cortex analyst semantic model`. The documentation describes that product behavior, and `sensitivity tag policy` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because payload pipeline scenario would still lack Model Registry; the suggested workaround cannot satisfy `Cortex analyst semantic model` or preserve `sensitivity tag policy` in the supported workflow.\n\nSee [Model Registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/)"
    },
    {
      "id": "q45",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "Corpus Pipeline Scenario: **Model signatures** must handle `Snowpark dataframe transform` while preserving `training baseline capture`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for corpus pipeline scenario instead of configuring Model signatures."
        },
        {
          "id": "B",
          "text": "Use Model signatures for corpus pipeline scenario to handle `Snowpark dataframe transform`."
        },
        {
          "id": "C",
          "text": "Use report table for corpus pipeline scenario instead of configuring Model signatures."
        },
        {
          "id": "D",
          "text": "Use shell cron for corpus pipeline scenario instead of configuring Model signatures."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because corpus pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\n**B** is correct because corpus pipeline scenario needs Model signatures specifically for `Snowpark dataframe transform`. The documentation describes that product behavior, and `training baseline capture` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because corpus pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\n**D** is incorrect because corpus pipeline scenario would still lack Model signatures; the suggested workaround cannot satisfy `Snowpark dataframe transform` or preserve `training baseline capture` in the supported workflow.\n\nSee [Model signatures](https://docs.databricks.com/aws/en/machine-learning/model-serving/model-serving-intro) for the vendor documentation that supports the correct choice.",
      "reference": "[Model signatures](https://docs.databricks.com/aws/en/machine-learning/model-serving/model-serving-intro)"
    },
    {
      "id": "q46",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Endpoint Pipeline Scenario: **Feature Store** must handle `Hyperopt parallel search` while preserving `Processing job output`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Feature Store for endpoint pipeline scenario to handle `Hyperopt parallel search`."
        },
        {
          "id": "B",
          "text": "Use report table for endpoint pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "C",
          "text": "Use shell cron for endpoint pipeline scenario instead of configuring Feature Store."
        },
        {
          "id": "D",
          "text": "Use email approval for endpoint pipeline scenario instead of configuring Feature Store."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because endpoint pipeline scenario needs Feature Store specifically for `Hyperopt parallel search`. The documentation describes that product behavior, and `Processing job output` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because endpoint pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `Hyperopt parallel search` or preserve `Processing job output` in the supported workflow.\n\n**C** is incorrect because endpoint pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `Hyperopt parallel search` or preserve `Processing job output` in the supported workflow.\n\n**D** is incorrect because endpoint pipeline scenario would still lack Feature Store; the suggested workaround cannot satisfy `Hyperopt parallel search` or preserve `Processing job output` in the supported workflow.\n\nSee [Feature Store](https://docs.databricks.com/aws/en/machine-learning/feature-store/) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature Store](https://docs.databricks.com/aws/en/machine-learning/feature-store/)"
    },
    {
      "id": "q47",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "medium",
      "stem": "Condition Pipeline Scenario: **Feature lookup** must handle `MLflow autolog capture` while preserving `offline object scoring`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for condition pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "B",
          "text": "Use email approval for condition pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for condition pipeline scenario instead of configuring Feature lookup."
        },
        {
          "id": "D",
          "text": "Use Feature lookup for condition pipeline scenario to handle `MLflow autolog capture`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because condition pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**B** is incorrect because condition pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**C** is incorrect because condition pipeline scenario would still lack Feature lookup; the suggested workaround cannot satisfy `MLflow autolog capture` or preserve `offline object scoring` in the supported workflow.\n\n**D** is correct because condition pipeline scenario needs Feature lookup specifically for `MLflow autolog capture`. The documentation describes that product behavior, and `offline object scoring` keeps the workflow inside the managed platform capability.\n\nSee [Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store)"
    },
    {
      "id": "q48",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Queue Pipeline Scenario: **Spark ML pipelines** must handle `pyfunc wrapper loading` while preserving `BI precomputation`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for queue pipeline scenario instead of configuring Spark ML pipelines."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for queue pipeline scenario instead of configuring Spark ML pipelines."
        },
        {
          "id": "C",
          "text": "Use Spark ML pipelines for queue pipeline scenario to handle `pyfunc wrapper loading`."
        },
        {
          "id": "D",
          "text": "Use visual tile for queue pipeline scenario instead of configuring Spark ML pipelines."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because queue pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\n**B** is incorrect because queue pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\n**C** is correct because queue pipeline scenario needs Spark ML pipelines specifically for `pyfunc wrapper loading`. The documentation describes that product behavior, and `BI precomputation` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because queue pipeline scenario would still lack Spark ML pipelines; the suggested workaround cannot satisfy `pyfunc wrapper loading` or preserve `BI precomputation` in the supported workflow.\n\nSee [Spark ML pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html) for the vendor documentation that supports the correct choice.",
      "reference": "[Spark ML pipelines](https://spark.apache.org/docs/latest/ml-pipeline.html)"
    },
    {
      "id": "q49",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "easy",
      "stem": "Bookmark Pipeline Scenario: **Model Serving** must handle `Clarify bias baseline` while preserving `row access predicate`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for bookmark pipeline scenario instead of configuring Model Serving."
        },
        {
          "id": "B",
          "text": "Use Model Serving for bookmark pipeline scenario to handle `Clarify bias baseline`."
        },
        {
          "id": "C",
          "text": "Use visual tile for bookmark pipeline scenario instead of configuring Model Serving."
        },
        {
          "id": "D",
          "text": "Use admin grant for bookmark pipeline scenario instead of configuring Model Serving."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because bookmark pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `Clarify bias baseline` or preserve `row access predicate` in the supported workflow.\n\n**B** is correct because bookmark pipeline scenario needs Model Serving specifically for `Clarify bias baseline`. The documentation describes that product behavior, and `row access predicate` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because bookmark pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `Clarify bias baseline` or preserve `row access predicate` in the supported workflow.\n\n**D** is incorrect because bookmark pipeline scenario would still lack Model Serving; the suggested workaround cannot satisfy `Clarify bias baseline` or preserve `row access predicate` in the supported workflow.\n\nSee [Model Serving](https://docs.databricks.com/aws/en/machine-learning/model-serving/) for the vendor documentation that supports the correct choice.",
      "reference": "[Model Serving](https://docs.databricks.com/aws/en/machine-learning/model-serving/)"
    },
    {
      "id": "q50",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Flow Pipeline Scenario: **Batch inference** must handle `Glue bookmark state` while preserving `CloudWatch endpoint alarm`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Batch inference for flow pipeline scenario to handle `Glue bookmark state`."
        },
        {
          "id": "B",
          "text": "Use visual tile for flow pipeline scenario instead of configuring Batch inference."
        },
        {
          "id": "C",
          "text": "Use admin grant for flow pipeline scenario instead of configuring Batch inference."
        },
        {
          "id": "D",
          "text": "Use local notebook for flow pipeline scenario instead of configuring Batch inference."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because flow pipeline scenario needs Batch inference specifically for `Glue bookmark state`. The documentation describes that product behavior, and `CloudWatch endpoint alarm` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because flow pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\n**C** is incorrect because flow pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\n**D** is incorrect because flow pipeline scenario would still lack Batch inference; the suggested workaround cannot satisfy `Glue bookmark state` or preserve `CloudWatch endpoint alarm` in the supported workflow.\n\nSee [Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/dl-model-inference) for the vendor documentation that supports the correct choice.",
      "reference": "[Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/dl-model-inference)"
    },
    {
      "id": "q51",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Lakehouse Monitoring produces profile and drift metrics from monitored tables, giving production model owners a managed way to inspect data changes.\n\nA practitioner must choose the documented **Lakehouse Monitoring** behavior for this workflow. Which option best matches the documentation?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for bias pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "B",
          "text": "Use local notebook for bias pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "C",
          "text": "Use driver file for bias pipeline scenario instead of configuring Lakehouse Monitoring."
        },
        {
          "id": "D",
          "text": "Use Lakehouse Monitoring because the documentation supports this exact behavior: Lakehouse Monitoring produces profile and drift metrics from monitored tables, giving production model owners a managed way to inspect data changes."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Lakehouse Monitoring produces profile and drift metrics from monitored tables, giving production model owners a managed way to inspect data changes.\n\n**B** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Lakehouse Monitoring produces profile and drift metrics from monitored tables, giving production model owners a managed way to inspect data changes.\n\n**C** is incorrect because it does not provide the documented Lakehouse Monitoring behavior described here: Lakehouse Monitoring produces profile and drift metrics from monitored tables, giving production model owners a managed way to inspect data changes.\n\n**D** is correct because Lakehouse Monitoring produces profile and drift metrics from monitored tables, giving production model owners a managed way to inspect data changes. This directly matches the scenario and is supported by the referenced Lakehouse Monitoring documentation.\n\nSee [Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/) for the vendor documentation that supports the correct choice.",
      "reference": "[Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/)"
    },
    {
      "id": "q52",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "hard",
      "stem": "Retention Pipeline Scenario: **Experiment search** must handle `Model Monitor schedule` while preserving `registry notification hook`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for retention pipeline scenario instead of configuring Experiment search."
        },
        {
          "id": "B",
          "text": "Use driver file for retention pipeline scenario instead of configuring Experiment search."
        },
        {
          "id": "C",
          "text": "Use Experiment search for retention pipeline scenario to handle `Model Monitor schedule`."
        },
        {
          "id": "D",
          "text": "Use report table for retention pipeline scenario instead of configuring Experiment search."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because retention pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\n**B** is incorrect because retention pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\n**C** is correct because retention pipeline scenario needs Experiment search specifically for `Model Monitor schedule`. The documentation describes that product behavior, and `registry notification hook` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because retention pipeline scenario would still lack Experiment search; the suggested workaround cannot satisfy `Model Monitor schedule` or preserve `registry notification hook` in the supported workflow.\n\nSee [Experiment search](https://mlflow.org/docs/latest/ml/search/search-runs/) for the vendor documentation that supports the correct choice.",
      "reference": "[Experiment search](https://mlflow.org/docs/latest/ml/search/search-runs/)"
    },
    {
      "id": "q53",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "easy",
      "stem": "Ranking Pipeline Scenario: **Autologging** must handle `Feature Store online lookup` while preserving `Snowpark dataframe transform`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use driver file for ranking pipeline scenario instead of configuring Autologging."
        },
        {
          "id": "B",
          "text": "Use Autologging for ranking pipeline scenario to handle `Feature Store online lookup`."
        },
        {
          "id": "C",
          "text": "Use report table for ranking pipeline scenario instead of configuring Autologging."
        },
        {
          "id": "D",
          "text": "Use shell cron for ranking pipeline scenario instead of configuring Autologging."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because ranking pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\n**B** is correct because ranking pipeline scenario needs Autologging specifically for `Feature Store online lookup`. The documentation describes that product behavior, and `Snowpark dataframe transform` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because ranking pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\n**D** is incorrect because ranking pipeline scenario would still lack Autologging; the suggested workaround cannot satisfy `Feature Store online lookup` or preserve `Snowpark dataframe transform` in the supported workflow.\n\nSee [Autologging](https://mlflow.org/docs/latest/ml/tracking/autolog/) for the vendor documentation that supports the correct choice.",
      "reference": "[Autologging](https://mlflow.org/docs/latest/ml/tracking/autolog/)"
    },
    {
      "id": "q54",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Forecasting notebook handoff: For a forecasting experiment, the Databricks AutoML API records generated notebooks and trial metrics so reviewers can inspect the selected model and its feature-importance evidence.\n\nWhich documented choice should the practitioner use?",
      "options": [
        {
          "id": "A",
          "text": "Use the Databricks AutoML API output notebooks and experiment metrics to review the forecasting model evidence."
        },
        {
          "id": "B",
          "text": "Use report table for arrays pipeline scenario instead of configuring Databricks AutoML feature importance."
        },
        {
          "id": "C",
          "text": "Use shell cron for arrays pipeline scenario instead of configuring Databricks AutoML feature importance."
        },
        {
          "id": "D",
          "text": "Use email approval for arrays pipeline scenario instead of configuring Databricks AutoML feature importance."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because For a forecasting experiment, the Databricks AutoML API records generated notebooks and trial metrics so reviewers can inspect the selected model and its feature-importance evidence. This is different from an ad hoc notebook because AutoML logs the generated artifacts and metrics for review.\n\n**B** is incorrect because a forecasting notebook handoff still needs the AutoML-generated notebooks, metrics, and feature-importance evidence rather than an unrelated workaround.\n\n**C** is incorrect because a forecasting notebook handoff still needs the AutoML-generated notebooks, metrics, and feature-importance evidence rather than an unrelated workaround.\n\n**D** is incorrect because a forecasting notebook handoff still needs the AutoML-generated notebooks, metrics, and feature-importance evidence rather than an unrelated workaround.\n\nSee [Databricks AutoML feature importance](https://docs.databricks.com/aws/en/machine-learning/automl/train-ml-model-automl-api) for the vendor documentation that supports the correct choice.",
      "reference": "[Databricks AutoML feature importance](https://docs.databricks.com/aws/en/machine-learning/automl/train-ml-model-automl-api)"
    },
    {
      "id": "q55",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Cache Pipeline Scenario: **MLflow pyfunc** must handle `Data Wrangler export` while preserving `unpivoted measure rows`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use shell cron for cache pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "B",
          "text": "Use email approval for cache pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "C",
          "text": "Use spreadsheet export for cache pipeline scenario instead of configuring MLflow pyfunc."
        },
        {
          "id": "D",
          "text": "Use MLflow pyfunc for cache pipeline scenario to handle `Data Wrangler export`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because cache pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**B** is incorrect because cache pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**C** is incorrect because cache pipeline scenario would still lack MLflow pyfunc; the suggested workaround cannot satisfy `Data Wrangler export` or preserve `unpivoted measure rows` in the supported workflow.\n\n**D** is correct because cache pipeline scenario needs MLflow pyfunc specifically for `Data Wrangler export`. The documentation describes that product behavior, and `unpivoted measure rows` keeps the workflow inside the managed platform capability.\n\nSee [MLflow pyfunc](https://mlflow.org/docs/latest/ml/model/python_model/) for the vendor documentation that supports the correct choice.",
      "reference": "[MLflow pyfunc](https://mlflow.org/docs/latest/ml/model/python_model/)"
    },
    {
      "id": "q56",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "hard",
      "stem": "Visibility Pipeline Scenario: **Serving traffic config** must handle `Processing job output` while preserving `recursive termination`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use email approval for visibility pipeline scenario instead of configuring Serving traffic config."
        },
        {
          "id": "B",
          "text": "Use spreadsheet export for visibility pipeline scenario instead of configuring Serving traffic config."
        },
        {
          "id": "C",
          "text": "Use Serving traffic config for visibility pipeline scenario to handle `Processing job output`."
        },
        {
          "id": "D",
          "text": "Use visual tile for visibility pipeline scenario instead of configuring Serving traffic config."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because visibility pipeline scenario would still lack Serving traffic config; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\n**B** is incorrect because visibility pipeline scenario would still lack Serving traffic config; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\n**C** is correct because visibility pipeline scenario needs Serving traffic config specifically for `Processing job output`. The documentation describes that product behavior, and `recursive termination` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because visibility pipeline scenario would still lack Serving traffic config; the suggested workaround cannot satisfy `Processing job output` or preserve `recursive termination` in the supported workflow.\n\nSee [Serving traffic config](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints) for the vendor documentation that supports the correct choice.",
      "reference": "[Serving traffic config](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints)"
    },
    {
      "id": "q57",
      "domain": "model-registry",
      "type": "single",
      "difficulty": "easy",
      "stem": "Metrics Pipeline Scenario: **Webhooks** must handle `Pipeline condition step` while preserving `pyfunc wrapper loading`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use spreadsheet export for metrics pipeline scenario instead of configuring Webhooks."
        },
        {
          "id": "B",
          "text": "Use Webhooks for metrics pipeline scenario to handle `Pipeline condition step`."
        },
        {
          "id": "C",
          "text": "Use visual tile for metrics pipeline scenario instead of configuring Webhooks."
        },
        {
          "id": "D",
          "text": "Use admin grant for metrics pipeline scenario instead of configuring Webhooks."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because metrics pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\n**B** is correct because metrics pipeline scenario needs Webhooks specifically for `Pipeline condition step`. The documentation describes that product behavior, and `pyfunc wrapper loading` keeps the workflow inside the managed platform capability.\n\n**C** is incorrect because metrics pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\n**D** is incorrect because metrics pipeline scenario would still lack Webhooks; the suggested workaround cannot satisfy `Pipeline condition step` or preserve `pyfunc wrapper loading` in the supported workflow.\n\nSee [Webhooks](https://docs.databricks.com/aws/en/mlflow/model-registry-webhooks) for the vendor documentation that supports the correct choice.",
      "reference": "[Webhooks](https://docs.databricks.com/aws/en/mlflow/model-registry-webhooks)"
    },
    {
      "id": "q58",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Leakage Pipeline Scenario: **Databricks Jobs** must handle `multi model endpoint load` while preserving `Streamlit governed app`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use Databricks Jobs for leakage pipeline scenario to handle `multi model endpoint load`."
        },
        {
          "id": "B",
          "text": "Use visual tile for leakage pipeline scenario instead of configuring Databricks Jobs."
        },
        {
          "id": "C",
          "text": "Use admin grant for leakage pipeline scenario instead of configuring Databricks Jobs."
        },
        {
          "id": "D",
          "text": "Use local notebook for leakage pipeline scenario instead of configuring Databricks Jobs."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because leakage pipeline scenario needs Databricks Jobs specifically for `multi model endpoint load`. The documentation describes that product behavior, and `Streamlit governed app` keeps the workflow inside the managed platform capability.\n\n**B** is incorrect because leakage pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\n**C** is incorrect because leakage pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\n**D** is incorrect because leakage pipeline scenario would still lack Databricks Jobs; the suggested workaround cannot satisfy `multi model endpoint load` or preserve `Streamlit governed app` in the supported workflow.\n\nSee [Databricks Jobs](https://docs.databricks.com/aws/en/jobs/) for the vendor documentation that supports the correct choice.",
      "reference": "[Databricks Jobs](https://docs.databricks.com/aws/en/jobs/)"
    },
    {
      "id": "q59",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Storage Pipeline Scenario: **Unity Catalog models** must handle `CloudWatch endpoint alarm` while preserving `post load aggregate refresh`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use admin grant for storage pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "B",
          "text": "Use local notebook for storage pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "C",
          "text": "Use driver file for storage pipeline scenario instead of configuring Unity Catalog models."
        },
        {
          "id": "D",
          "text": "Use Unity Catalog models for storage pipeline scenario to handle `CloudWatch endpoint alarm`."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because storage pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**B** is incorrect because storage pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**C** is incorrect because storage pipeline scenario would still lack Unity Catalog models; the suggested workaround cannot satisfy `CloudWatch endpoint alarm` or preserve `post load aggregate refresh` in the supported workflow.\n\n**D** is correct because storage pipeline scenario needs Unity Catalog models specifically for `CloudWatch endpoint alarm`. The documentation describes that product behavior, and `post load aggregate refresh` keeps the workflow inside the managed platform capability.\n\nSee [Unity Catalog models](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/) for the vendor documentation that supports the correct choice.",
      "reference": "[Unity Catalog models](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/)"
    },
    {
      "id": "q60",
      "domain": "monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "Morning Pipeline Scenario: **Feature engineering client** must handle `Search service refresh` while preserving `outer flatten rows`.\n\nWhich documented choice fits?",
      "options": [
        {
          "id": "A",
          "text": "Use local notebook for morning pipeline scenario instead of configuring Feature engineering client."
        },
        {
          "id": "B",
          "text": "Use driver file for morning pipeline scenario instead of configuring Feature engineering client."
        },
        {
          "id": "C",
          "text": "Use Feature engineering client for morning pipeline scenario to handle `Search service refresh`."
        },
        {
          "id": "D",
          "text": "Use report table for morning pipeline scenario instead of configuring Feature engineering client."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because morning pipeline scenario would still lack Feature engineering client; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\n**B** is incorrect because morning pipeline scenario would still lack Feature engineering client; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\n**C** is correct because morning pipeline scenario needs Feature engineering client specifically for `Search service refresh`. The documentation describes that product behavior, and `outer flatten rows` keeps the workflow inside the managed platform capability.\n\n**D** is incorrect because morning pipeline scenario would still lack Feature engineering client; the suggested workaround cannot satisfy `Search service refresh` or preserve `outer flatten rows` in the supported workflow.\n\nSee [Feature engineering client](https://docs.databricks.com/aws/en/machine-learning/feature-store/uc/feature-tables-uc) for the vendor documentation that supports the correct choice.",
      "reference": "[Feature engineering client](https://docs.databricks.com/aws/en/machine-learning/feature-store/uc/feature-tables-uc)"
    }
  ]
}
