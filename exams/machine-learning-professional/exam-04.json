{
  "meta": {
    "id": "mlp-exam-04",
    "certification": "machine-learning-professional",
    "certificationName": "Databricks Certified Machine Learning Professional",
    "title": "DBX-MLP-4: Experiment Tracking, Feature Engineering, and Serving Operations",
    "version": "1.0",
    "timeLimit": 120,
    "passingScore": 70,
    "totalQuestions": 60,
    "domains": [
      {
        "id": "experimentation",
        "name": "Experimentation",
        "weight": 25
      },
      {
        "id": "model-lifecycle",
        "name": "Model Lifecycle Management",
        "weight": 25
      },
      {
        "id": "feature-store",
        "name": "Feature Engineering",
        "weight": 20
      },
      {
        "id": "model-deployment",
        "name": "Model Deployment",
        "weight": 20
      },
      {
        "id": "solution-monitoring",
        "name": "Solution & Data Monitoring",
        "weight": 10
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `calibrate-orders-01`: unapproved model; ticket OC001; asset forecasts_01. Required capability: `claims_features_01`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure MLflow experiment tracking for claims_features_01 so distributed hyperparameter search is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Increase the runtime size for claims_features_01; more capacity does not create the required distributed hyperparameter search approval or lineage behavior."
        },
        {
          "id": "C",
          "text": "Ask analysts to rerun a local script for claims_features_01; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "D",
          "text": "Open access to every workspace user for claims_features_01; broad access weakens least privilege and still does not configure distributed hyperparameter search."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `claims_features_01`, MLflow experiment tracking is the documented mechanism that addresses `distributed hyperparameter search` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `claims_features_01`, this choice avoids the specific `distributed hyperparameter search` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**C** is incorrect because for `claims_features_01`, this choice avoids the specific `distributed hyperparameter search` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**D** is incorrect because for `claims_features_01`, this choice avoids the specific `distributed hyperparameter search` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\nSee [MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q01) for the documented behavior.",
      "reference": "[MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q01)"
    },
    {
      "id": "q02",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `promote-vectors-02`: cold endpoint; ticket OC002; asset baselines_02. Required capability: `daily_revenue_02`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Ask analysts to rerun a local script for daily_revenue_02; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "B",
          "text": "Route daily_revenue_02 through MLflow autologging; it directly supports AutoML baseline selection while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Open access to every workspace user for daily_revenue_02; broad access weakens least privilege and still does not configure AutoML baseline selection."
        },
        {
          "id": "D",
          "text": "Replace the feature with a static CSV extract for daily_revenue_02; an extract removes the dynamic service behavior being tested."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `daily_revenue_02`, this choice avoids the specific `AutoML baseline selection` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**B** is correct because for `daily_revenue_02`, MLflow autologging is the documented mechanism that addresses `AutoML baseline selection` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `daily_revenue_02`, this choice avoids the specific `AutoML baseline selection` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**D** is incorrect because for `daily_revenue_02`, this choice avoids the specific `AutoML baseline selection` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\nSee [MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q02) for the documented behavior.",
      "reference": "[MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q02)"
    },
    {
      "id": "q03",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `flatten-policies-03`: empty search; ticket OC003; asset sensor_03. Required capability: `endpoint_blue_03`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Open access to every workspace user for endpoint_blue_03; broad access weakens least privilege and still does not configure SHAP explanation review."
        },
        {
          "id": "B",
          "text": "Replace the feature with a static CSV extract for endpoint_blue_03; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "C",
          "text": "Configure Hyperparameter tuning for endpoint_blue_03 so SHAP explanation review is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Create a presentation slide for endpoint_blue_03; documentation alone cannot enforce or execute SHAP explanation review."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `endpoint_blue_03`, this choice avoids the specific `SHAP explanation review` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**B** is incorrect because for `endpoint_blue_03`, this choice avoids the specific `SHAP explanation review` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**C** is correct because for `endpoint_blue_03`, Hyperparameter tuning is the documented mechanism that addresses `SHAP explanation review` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `endpoint_blue_03`, this choice avoids the specific `SHAP explanation review` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\nSee [Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q03) for the documented behavior.",
      "reference": "[Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q03)"
    },
    {
      "id": "q04",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `score-dashboards-04`: missing lineage; ticket OC004; asset segments_04. Required capability: `customer_events_04`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Replace the feature with a static CSV extract for customer_events_04; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "B",
          "text": "Create a presentation slide for customer_events_04; documentation alone cannot enforce or execute reproducible experiment artifacts."
        },
        {
          "id": "C",
          "text": "Move the data to an unrelated storage tier for customer_events_04; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "D",
          "text": "Route customer_events_04 through Databricks AutoML; it directly supports reproducible experiment artifacts while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `customer_events_04`, this choice avoids the specific `reproducible experiment artifacts` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**B** is incorrect because for `customer_events_04`, this choice avoids the specific `reproducible experiment artifacts` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**C** is incorrect because for `customer_events_04`, this choice avoids the specific `reproducible experiment artifacts` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**D** is correct because for `customer_events_04`, Databricks AutoML is the documented mechanism that addresses `reproducible experiment artifacts` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q04) for the documented behavior.",
      "reference": "[Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q04)"
    },
    {
      "id": "q05",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `profile-manifests-05`: unapproved model; ticket OC005; asset manifests_05. Required capability: `risk_scores_05`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure MLflow models for risk_scores_05 so metric comparison across runs is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Create a presentation slide for risk_scores_05; documentation alone cannot enforce or execute metric comparison across runs."
        },
        {
          "id": "C",
          "text": "Move the data to an unrelated storage tier for risk_scores_05; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "D",
          "text": "Disable validation for risk_scores_05; skipping checks hides the failure instead of implementing metric comparison across runs correctly."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `risk_scores_05`, MLflow models is the documented mechanism that addresses `metric comparison across runs` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `risk_scores_05`, this choice avoids the specific `metric comparison across runs` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**C** is incorrect because for `risk_scores_05`, this choice avoids the specific `metric comparison across runs` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**D** is incorrect because for `risk_scores_05`, this choice avoids the specific `metric comparison across runs` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\nSee [MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q05) for the documented behavior.",
      "reference": "[MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q05)"
    },
    {
      "id": "q06",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `register-profiles-06`: cold endpoint; ticket OC006; asset claims_06. Required capability: `sales_rollup_06`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Move the data to an unrelated storage tier for sales_rollup_06; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "B",
          "text": "Route sales_rollup_06 through Unity Catalog model registry; it directly supports lineage from training run while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Disable validation for sales_rollup_06; skipping checks hides the failure instead of implementing lineage from training run correctly."
        },
        {
          "id": "D",
          "text": "Use a dashboard filter for sales_rollup_06; presentation filtering cannot manage the underlying lineage from training run workflow."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `sales_rollup_06`, this choice avoids the specific `lineage from training run` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**B** is correct because for `sales_rollup_06`, Unity Catalog model registry is the documented mechanism that addresses `lineage from training run` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `sales_rollup_06`, this choice avoids the specific `lineage from training run` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**D** is incorrect because for `sales_rollup_06`, this choice avoids the specific `lineage from training run` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\nSee [Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q06) for the documented behavior.",
      "reference": "[Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q06)"
    },
    {
      "id": "q07",
      "domain": "model-lifecycle",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Case `baseline-sensor-07`: empty search; ticket OC007; asset policies_07. Required capability: `ticket_vectors_07`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Route ticket_vectors_07 through Feature engineering workspace; it directly supports approval status automation while preserving operational evidence."
        },
        {
          "id": "B",
          "text": "Disable validation for ticket_vectors_07; skipping checks hides the failure instead of implementing approval status automation correctly."
        },
        {
          "id": "C",
          "text": "Configure Feature engineering workspace for ticket_vectors_07 so approval status automation is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Use a dashboard filter for ticket_vectors_07; presentation filtering cannot manage the underlying approval status automation workflow."
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "**A** is correct because for `ticket_vectors_07`, Feature engineering workspace is the documented mechanism that addresses `approval status automation` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `ticket_vectors_07`, this choice avoids the specific `approval status automation` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**C** is correct because for `ticket_vectors_07`, Feature engineering workspace is the documented mechanism that addresses `approval status automation` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `ticket_vectors_07`, this choice avoids the specific `approval status automation` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\nSee [Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q07) for the documented behavior.",
      "reference": "[Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q07)"
    },
    {
      "id": "q08",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `partition-tickets-08`: missing lineage; ticket OC008; asset variants_08. Required capability: `training_baseline_08`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Use a dashboard filter for training_baseline_08; presentation filtering cannot manage the underlying registry approval workflow workflow."
        },
        {
          "id": "B",
          "text": "Schedule an email reminder for training_baseline_08; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "C",
          "text": "Archive only the raw output file for training_baseline_08; that keeps a copy but leaves registry approval workflow outside the managed control plane."
        },
        {
          "id": "D",
          "text": "Route training_baseline_08 through Feature lookup; it directly supports registry approval workflow while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `training_baseline_08`, this choice avoids the specific `registry approval workflow` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**B** is incorrect because for `training_baseline_08`, this choice avoids the specific `registry approval workflow` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**C** is incorrect because for `training_baseline_08`, this choice avoids the specific `registry approval workflow` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**D** is correct because for `training_baseline_08`, Feature lookup is the documented mechanism that addresses `registry approval workflow` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q08) for the documented behavior.",
      "reference": "[Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q08)"
    },
    {
      "id": "q09",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `approve-forecasts-09`: unapproved model; ticket OC009; asset orders_09. Required capability: `loan_predictions_09`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Model serving endpoints for loan_predictions_09 so model signature enforcement is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Schedule an email reminder for loan_predictions_09; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "C",
          "text": "Archive only the raw output file for loan_predictions_09; that keeps a copy but leaves model signature enforcement outside the managed control plane."
        },
        {
          "id": "D",
          "text": "Increase the runtime size for loan_predictions_09; more capacity does not create the required model signature enforcement approval or lineage behavior."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `loan_predictions_09`, Model serving endpoints is the documented mechanism that addresses `model signature enforcement` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `loan_predictions_09`, this choice avoids the specific `model signature enforcement` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**C** is incorrect because for `loan_predictions_09`, this choice avoids the specific `model signature enforcement` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**D** is incorrect because for `loan_predictions_09`, this choice avoids the specific `model signature enforcement` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\nSee [Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q09) for the documented behavior.",
      "reference": "[Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q09)"
    },
    {
      "id": "q10",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `monitor-lineage-10`: cold endpoint; ticket OC010; asset lineage_10. Required capability: `inventory_snapshot_10`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Archive only the raw output file for inventory_snapshot_10; that keeps a copy but leaves model version rollback outside the managed control plane."
        },
        {
          "id": "B",
          "text": "Route inventory_snapshot_10 through Serving endpoint traffic; it directly supports model version rollback while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Increase the runtime size for inventory_snapshot_10; more capacity does not create the required model version rollback approval or lineage behavior."
        },
        {
          "id": "D",
          "text": "Ask analysts to rerun a local script for inventory_snapshot_10; local execution cannot provide the shared audit trail required here."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `inventory_snapshot_10`, this choice avoids the specific `model version rollback` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**B** is correct because for `inventory_snapshot_10`, Serving endpoint traffic is the documented mechanism that addresses `model version rollback` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `inventory_snapshot_10`, this choice avoids the specific `model version rollback` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**D** is incorrect because for `inventory_snapshot_10`, this choice avoids the specific `model version rollback` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\nSee [Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q10) for the documented behavior.",
      "reference": "[Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q10)"
    },
    {
      "id": "q11",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `reshape-endpoints-11`: empty search; ticket OC011; asset approvals_11. Required capability: `support_extract_11`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Increase the runtime size for support_extract_11; more capacity does not create the required training set construction approval or lineage behavior."
        },
        {
          "id": "B",
          "text": "Ask analysts to rerun a local script for support_extract_11; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "C",
          "text": "Configure Batch inference for support_extract_11 so training set construction is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Open access to every workspace user for support_extract_11; broad access weakens least privilege and still does not configure training set construction."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `support_extract_11`, this choice avoids the specific `training set construction` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**B** is incorrect because for `support_extract_11`, this choice avoids the specific `training set construction` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**C** is correct because for `support_extract_11`, Batch inference is the documented mechanism that addresses `training set construction` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `support_extract_11`, this choice avoids the specific `training set construction` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\nSee [Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q11) for the documented behavior.",
      "reference": "[Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q11)"
    },
    {
      "id": "q12",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `classify-variants-12`: missing lineage; ticket OC012; asset tickets_12. Required capability: `churn_labels_12`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Ask analysts to rerun a local script for churn_labels_12; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "B",
          "text": "Open access to every workspace user for churn_labels_12; broad access weakens least privilege and still does not configure online/offline consistency."
        },
        {
          "id": "C",
          "text": "Replace the feature with a static CSV extract for churn_labels_12; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "D",
          "text": "Route churn_labels_12 through Lakehouse Monitoring; it directly supports online/offline consistency while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `churn_labels_12`, this choice avoids the specific `online/offline consistency` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**B** is incorrect because for `churn_labels_12`, this choice avoids the specific `online/offline consistency` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**C** is incorrect because for `churn_labels_12`, this choice avoids the specific `online/offline consistency` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**D** is correct because for `churn_labels_12`, Lakehouse Monitoring is the documented mechanism that addresses `online/offline consistency` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q12) for the documented behavior.",
      "reference": "[Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q12)"
    },
    {
      "id": "q13",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `search-schemas-13`: unapproved model; ticket OC013; asset payloads_13. Required capability: `claims_features_13`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Inference tables for claims_features_13 so feature table primary keys is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Open access to every workspace user for claims_features_13; broad access weakens least privilege and still does not configure feature table primary keys."
        },
        {
          "id": "C",
          "text": "Replace the feature with a static CSV extract for claims_features_13; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "D",
          "text": "Create a presentation slide for claims_features_13; documentation alone cannot enforce or execute feature table primary keys."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `claims_features_13`, Inference tables is the documented mechanism that addresses `feature table primary keys` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `claims_features_13`, this choice avoids the specific `feature table primary keys` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**C** is incorrect because for `claims_features_13`, this choice avoids the specific `feature table primary keys` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**D** is incorrect because for `claims_features_13`, this choice avoids the specific `feature table primary keys` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\nSee [Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q13) for the documented behavior.",
      "reference": "[Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q13)"
    },
    {
      "id": "q14",
      "domain": "feature-store",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Case `mask-claims-14`: cold endpoint; ticket OC014; asset profiles_14. Required capability: `daily_revenue_14`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Replace the feature with a static CSV extract for daily_revenue_14; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "B",
          "text": "Route daily_revenue_14 through MLflow model signatures; it directly supports time-series feature joins while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Create a presentation slide for daily_revenue_14; documentation alone cannot enforce or execute time-series feature joins."
        },
        {
          "id": "D",
          "text": "Configure MLflow model signatures for daily_revenue_14 so time-series feature joins is handled by the managed service and can be reviewed later."
        }
      ],
      "correct": [
        "B",
        "D"
      ],
      "explanation": "**A** is incorrect because for `daily_revenue_14`, this choice avoids the specific `time-series feature joins` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**B** is correct because for `daily_revenue_14`, MLflow model signatures is the documented mechanism that addresses `time-series feature joins` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `daily_revenue_14`, this choice avoids the specific `time-series feature joins` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**D** is correct because for `daily_revenue_14`, MLflow model signatures is the documented mechanism that addresses `time-series feature joins` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q14) for the documented behavior.",
      "reference": "[MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q14)"
    },
    {
      "id": "q15",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `refresh-features-15`: empty search; ticket OC015; asset features_15. Required capability: `endpoint_blue_15`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Create a presentation slide for endpoint_blue_15; documentation alone cannot enforce or execute endpoint update rollback."
        },
        {
          "id": "B",
          "text": "Move the data to an unrelated storage tier for endpoint_blue_15; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "C",
          "text": "Configure Databricks Workflows for endpoint_blue_15 so endpoint update rollback is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Disable validation for endpoint_blue_15; skipping checks hides the failure instead of implementing endpoint update rollback correctly."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `endpoint_blue_15`, this choice avoids the specific `endpoint update rollback` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**B** is incorrect because for `endpoint_blue_15`, this choice avoids the specific `endpoint update rollback` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**C** is correct because for `endpoint_blue_15`, Databricks Workflows is the documented mechanism that addresses `endpoint update rollback` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `endpoint_blue_15`, this choice avoids the specific `endpoint update rollback` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\nSee [Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q15) for the documented behavior.",
      "reference": "[Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q15)"
    },
    {
      "id": "q16",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `route-segments-16`: missing lineage; ticket OC016; asset dashboards_16. Required capability: `customer_events_16`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Move the data to an unrelated storage tier for customer_events_16; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "B",
          "text": "Disable validation for customer_events_16; skipping checks hides the failure instead of implementing real-time endpoint routing correctly."
        },
        {
          "id": "C",
          "text": "Use a dashboard filter for customer_events_16; presentation filtering cannot manage the underlying real-time endpoint routing workflow."
        },
        {
          "id": "D",
          "text": "Route customer_events_16 through MLflow experiment tracking; it directly supports real-time endpoint routing while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `customer_events_16`, this choice avoids the specific `real-time endpoint routing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**B** is incorrect because for `customer_events_16`, this choice avoids the specific `real-time endpoint routing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**C** is incorrect because for `customer_events_16`, this choice avoids the specific `real-time endpoint routing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**D** is correct because for `customer_events_16`, MLflow experiment tracking is the documented mechanism that addresses `real-time endpoint routing` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q16) for the documented behavior.",
      "reference": "[MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q16)"
    },
    {
      "id": "q17",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `parse-payloads-17`: unapproved model; ticket OC017; asset schemas_17. Required capability: `risk_scores_17`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure MLflow autologging for risk_scores_17 so batch scoring with Spark is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Disable validation for risk_scores_17; skipping checks hides the failure instead of implementing batch scoring with Spark correctly."
        },
        {
          "id": "C",
          "text": "Use a dashboard filter for risk_scores_17; presentation filtering cannot manage the underlying batch scoring with Spark workflow."
        },
        {
          "id": "D",
          "text": "Schedule an email reminder for risk_scores_17; reminders do not change the platform configuration or runtime behavior."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `risk_scores_17`, MLflow autologging is the documented mechanism that addresses `batch scoring with Spark` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `risk_scores_17`, this choice avoids the specific `batch scoring with Spark` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**C** is incorrect because for `risk_scores_17`, this choice avoids the specific `batch scoring with Spark` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**D** is incorrect because for `risk_scores_17`, this choice avoids the specific `batch scoring with Spark` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\nSee [MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q17) for the documented behavior.",
      "reference": "[MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q17)"
    },
    {
      "id": "q18",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `cluster-baselines-18`: cold endpoint; ticket OC018; asset vectors_18. Required capability: `sales_rollup_18`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Use a dashboard filter for sales_rollup_18; presentation filtering cannot manage the underlying canary traffic split workflow."
        },
        {
          "id": "B",
          "text": "Route sales_rollup_18 through Hyperparameter tuning; it directly supports canary traffic split while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Schedule an email reminder for sales_rollup_18; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "D",
          "text": "Archive only the raw output file for sales_rollup_18; that keeps a copy but leaves canary traffic split outside the managed control plane."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `sales_rollup_18`, this choice avoids the specific `canary traffic split` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**B** is correct because for `sales_rollup_18`, Hyperparameter tuning is the documented mechanism that addresses `canary traffic split` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `sales_rollup_18`, this choice avoids the specific `canary traffic split` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**D** is incorrect because for `sales_rollup_18`, this choice avoids the specific `canary traffic split` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\nSee [Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q18) for the documented behavior.",
      "reference": "[Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q18)"
    },
    {
      "id": "q19",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `summarize-approvals-19`: empty search; ticket OC019; asset endpoints_19. Required capability: `ticket_vectors_19`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Schedule an email reminder for ticket_vectors_19; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "B",
          "text": "Archive only the raw output file for ticket_vectors_19; that keeps a copy but leaves scheduled monitor refresh outside the managed control plane."
        },
        {
          "id": "C",
          "text": "Configure Databricks AutoML for ticket_vectors_19 so scheduled monitor refresh is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Increase the runtime size for ticket_vectors_19; more capacity does not create the required scheduled monitor refresh approval or lineage behavior."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `ticket_vectors_19`, this choice avoids the specific `scheduled monitor refresh` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**B** is incorrect because for `ticket_vectors_19`, this choice avoids the specific `scheduled monitor refresh` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**C** is correct because for `ticket_vectors_19`, Databricks AutoML is the documented mechanism that addresses `scheduled monitor refresh` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `ticket_vectors_19`, this choice avoids the specific `scheduled monitor refresh` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\nSee [Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q19) for the documented behavior.",
      "reference": "[Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q19)"
    },
    {
      "id": "q20",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `rollback-invoice-20`: missing lineage; ticket OC020; asset invoice_20. Required capability: `training_baseline_20`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Archive only the raw output file for training_baseline_20; that keeps a copy but leaves alert threshold design outside the managed control plane."
        },
        {
          "id": "B",
          "text": "Increase the runtime size for training_baseline_20; more capacity does not create the required alert threshold design approval or lineage behavior."
        },
        {
          "id": "C",
          "text": "Ask analysts to rerun a local script for training_baseline_20; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "D",
          "text": "Route training_baseline_20 through MLflow models; it directly supports alert threshold design while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `training_baseline_20`, this choice avoids the specific `alert threshold design` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**B** is incorrect because for `training_baseline_20`, this choice avoids the specific `alert threshold design` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**C** is incorrect because for `training_baseline_20`, this choice avoids the specific `alert threshold design` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**D** is correct because for `training_baseline_20`, MLflow models is the documented mechanism that addresses `alert threshold design` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q20) for the documented behavior.",
      "reference": "[MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q20)"
    },
    {
      "id": "q21",
      "domain": "experimentation",
      "type": "multiple",
      "difficulty": "easy",
      "stem": "Case `calibrate-orders-21`: unapproved model; ticket OC021; asset forecasts_21. Required capability: `loan_predictions_21`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Configure Unity Catalog model registry for loan_predictions_21 so train-validation-test split design is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Increase the runtime size for loan_predictions_21; more capacity does not create the required train-validation-test split design approval or lineage behavior."
        },
        {
          "id": "C",
          "text": "Route loan_predictions_21 through Unity Catalog model registry; it directly supports train-validation-test split design while preserving operational evidence."
        },
        {
          "id": "D",
          "text": "Ask analysts to rerun a local script for loan_predictions_21; local execution cannot provide the shared audit trail required here."
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "**A** is correct because for `loan_predictions_21`, Unity Catalog model registry is the documented mechanism that addresses `train-validation-test split design` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `loan_predictions_21`, this choice avoids the specific `train-validation-test split design` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**C** is correct because for `loan_predictions_21`, Unity Catalog model registry is the documented mechanism that addresses `train-validation-test split design` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `loan_predictions_21`, this choice avoids the specific `train-validation-test split design` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\nSee [Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q21) for the documented behavior.",
      "reference": "[Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q21)"
    },
    {
      "id": "q22",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `promote-vectors-22`: cold endpoint; ticket OC022; asset baselines_22. Required capability: `inventory_snapshot_22`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Ask analysts to rerun a local script for inventory_snapshot_22; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "B",
          "text": "Route inventory_snapshot_22 through Feature engineering workspace; it directly supports random seed control while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Open access to every workspace user for inventory_snapshot_22; broad access weakens least privilege and still does not configure random seed control."
        },
        {
          "id": "D",
          "text": "Replace the feature with a static CSV extract for inventory_snapshot_22; an extract removes the dynamic service behavior being tested."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `inventory_snapshot_22`, this choice avoids the specific `random seed control` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**B** is correct because for `inventory_snapshot_22`, Feature engineering workspace is the documented mechanism that addresses `random seed control` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `inventory_snapshot_22`, this choice avoids the specific `random seed control` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**D** is incorrect because for `inventory_snapshot_22`, this choice avoids the specific `random seed control` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\nSee [Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q22) for the documented behavior.",
      "reference": "[Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q22)"
    },
    {
      "id": "q23",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `flatten-policies-23`: empty search; ticket OC023; asset sensor_23. Required capability: `support_extract_23`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Open access to every workspace user for support_extract_23; broad access weakens least privilege and still does not configure cross-validation leakage control."
        },
        {
          "id": "B",
          "text": "Replace the feature with a static CSV extract for support_extract_23; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "C",
          "text": "Configure Feature lookup for support_extract_23 so cross-validation leakage control is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Create a presentation slide for support_extract_23; documentation alone cannot enforce or execute cross-validation leakage control."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `support_extract_23`, this choice avoids the specific `cross-validation leakage control` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**B** is incorrect because for `support_extract_23`, this choice avoids the specific `cross-validation leakage control` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**C** is correct because for `support_extract_23`, Feature lookup is the documented mechanism that addresses `cross-validation leakage control` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `support_extract_23`, this choice avoids the specific `cross-validation leakage control` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\nSee [Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q23) for the documented behavior.",
      "reference": "[Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q23)"
    },
    {
      "id": "q24",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `score-dashboards-24`: missing lineage; ticket OC024; asset segments_24. Required capability: `churn_labels_24`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Replace the feature with a static CSV extract for churn_labels_24; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "B",
          "text": "Create a presentation slide for churn_labels_24; documentation alone cannot enforce or execute distributed hyperparameter search."
        },
        {
          "id": "C",
          "text": "Move the data to an unrelated storage tier for churn_labels_24; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "D",
          "text": "Route churn_labels_24 through Model serving endpoints; it directly supports distributed hyperparameter search while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `churn_labels_24`, this choice avoids the specific `distributed hyperparameter search` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**B** is incorrect because for `churn_labels_24`, this choice avoids the specific `distributed hyperparameter search` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**C** is incorrect because for `churn_labels_24`, this choice avoids the specific `distributed hyperparameter search` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**D** is correct because for `churn_labels_24`, Model serving endpoints is the documented mechanism that addresses `distributed hyperparameter search` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q24) for the documented behavior.",
      "reference": "[Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q24)"
    },
    {
      "id": "q25",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `profile-manifests-25`: unapproved model; ticket OC025; asset manifests_25. Required capability: `claims_features_25`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Serving endpoint traffic for claims_features_25 so AutoML baseline selection is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Create a presentation slide for claims_features_25; documentation alone cannot enforce or execute AutoML baseline selection."
        },
        {
          "id": "C",
          "text": "Move the data to an unrelated storage tier for claims_features_25; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "D",
          "text": "Disable validation for claims_features_25; skipping checks hides the failure instead of implementing AutoML baseline selection correctly."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `claims_features_25`, Serving endpoint traffic is the documented mechanism that addresses `AutoML baseline selection` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `claims_features_25`, this choice avoids the specific `AutoML baseline selection` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**C** is incorrect because for `claims_features_25`, this choice avoids the specific `AutoML baseline selection` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**D** is incorrect because for `claims_features_25`, this choice avoids the specific `AutoML baseline selection` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\nSee [Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q25) for the documented behavior.",
      "reference": "[Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q25)"
    },
    {
      "id": "q26",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `register-profiles-26`: cold endpoint; ticket OC026; asset claims_26. Required capability: `daily_revenue_26`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Move the data to an unrelated storage tier for daily_revenue_26; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "B",
          "text": "Route daily_revenue_26 through Batch inference; it directly supports run artifact retention while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Disable validation for daily_revenue_26; skipping checks hides the failure instead of implementing run artifact retention correctly."
        },
        {
          "id": "D",
          "text": "Use a dashboard filter for daily_revenue_26; presentation filtering cannot manage the underlying run artifact retention workflow."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `daily_revenue_26`, this choice avoids the specific `run artifact retention` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**B** is correct because for `daily_revenue_26`, Batch inference is the documented mechanism that addresses `run artifact retention` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `daily_revenue_26`, this choice avoids the specific `run artifact retention` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**D** is incorrect because for `daily_revenue_26`, this choice avoids the specific `run artifact retention` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\nSee [Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q26) for the documented behavior.",
      "reference": "[Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q26)"
    },
    {
      "id": "q27",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `baseline-sensor-27`: empty search; ticket OC027; asset policies_27. Required capability: `endpoint_blue_27`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Disable validation for endpoint_blue_27; skipping checks hides the failure instead of implementing registered model aliasing correctly."
        },
        {
          "id": "B",
          "text": "Use a dashboard filter for endpoint_blue_27; presentation filtering cannot manage the underlying registered model aliasing workflow."
        },
        {
          "id": "C",
          "text": "Configure Lakehouse Monitoring for endpoint_blue_27 so registered model aliasing is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Schedule an email reminder for endpoint_blue_27; reminders do not change the platform configuration or runtime behavior."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `endpoint_blue_27`, this choice avoids the specific `registered model aliasing` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**B** is incorrect because for `endpoint_blue_27`, this choice avoids the specific `registered model aliasing` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**C** is correct because for `endpoint_blue_27`, Lakehouse Monitoring is the documented mechanism that addresses `registered model aliasing` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `endpoint_blue_27`, this choice avoids the specific `registered model aliasing` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\nSee [Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q27) for the documented behavior.",
      "reference": "[Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q27)"
    },
    {
      "id": "q28",
      "domain": "model-lifecycle",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Case `partition-tickets-28`: missing lineage; ticket OC028; asset variants_28. Required capability: `customer_events_28`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Use a dashboard filter for customer_events_28; presentation filtering cannot manage the underlying evaluation gate before promotion workflow."
        },
        {
          "id": "B",
          "text": "Configure Inference tables for customer_events_28 so evaluation gate before promotion is handled by the managed service and can be reviewed later."
        },
        {
          "id": "C",
          "text": "Schedule an email reminder for customer_events_28; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "D",
          "text": "Route customer_events_28 through Inference tables; it directly supports evaluation gate before promotion while preserving operational evidence."
        }
      ],
      "correct": [
        "B",
        "D"
      ],
      "explanation": "**A** is incorrect because for `customer_events_28`, this choice avoids the specific `evaluation gate before promotion` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**B** is correct because for `customer_events_28`, Inference tables is the documented mechanism that addresses `evaluation gate before promotion` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `customer_events_28`, this choice avoids the specific `evaluation gate before promotion` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**D** is correct because for `customer_events_28`, Inference tables is the documented mechanism that addresses `evaluation gate before promotion` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q28) for the documented behavior.",
      "reference": "[Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q28)"
    },
    {
      "id": "q29",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `approve-forecasts-29`: unapproved model; ticket OC029; asset orders_29. Required capability: `risk_scores_29`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure MLflow model signatures for risk_scores_29 so lineage from training run is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Schedule an email reminder for risk_scores_29; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "C",
          "text": "Archive only the raw output file for risk_scores_29; that keeps a copy but leaves lineage from training run outside the managed control plane."
        },
        {
          "id": "D",
          "text": "Increase the runtime size for risk_scores_29; more capacity does not create the required lineage from training run approval or lineage behavior."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `risk_scores_29`, MLflow model signatures is the documented mechanism that addresses `lineage from training run` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `risk_scores_29`, this choice avoids the specific `lineage from training run` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**C** is incorrect because for `risk_scores_29`, this choice avoids the specific `lineage from training run` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**D** is incorrect because for `risk_scores_29`, this choice avoids the specific `lineage from training run` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\nSee [MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q29) for the documented behavior.",
      "reference": "[MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q29)"
    },
    {
      "id": "q30",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `monitor-lineage-30`: cold endpoint; ticket OC030; asset lineage_30. Required capability: `sales_rollup_30`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Archive only the raw output file for sales_rollup_30; that keeps a copy but leaves approval status automation outside the managed control plane."
        },
        {
          "id": "B",
          "text": "Route sales_rollup_30 through Databricks Workflows; it directly supports approval status automation while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Increase the runtime size for sales_rollup_30; more capacity does not create the required approval status automation approval or lineage behavior."
        },
        {
          "id": "D",
          "text": "Ask analysts to rerun a local script for sales_rollup_30; local execution cannot provide the shared audit trail required here."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `sales_rollup_30`, this choice avoids the specific `approval status automation` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**B** is correct because for `sales_rollup_30`, Databricks Workflows is the documented mechanism that addresses `approval status automation` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `sales_rollup_30`, this choice avoids the specific `approval status automation` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**D** is incorrect because for `sales_rollup_30`, this choice avoids the specific `approval status automation` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\nSee [Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q30) for the documented behavior.",
      "reference": "[Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q30)"
    },
    {
      "id": "q31",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `reshape-endpoints-31`: empty search; ticket OC031; asset approvals_31. Required capability: `ticket_vectors_31`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Increase the runtime size for ticket_vectors_31; more capacity does not create the required point-in-time feature lookup approval or lineage behavior."
        },
        {
          "id": "B",
          "text": "Ask analysts to rerun a local script for ticket_vectors_31; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "C",
          "text": "Configure MLflow experiment tracking for ticket_vectors_31 so point-in-time feature lookup is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Open access to every workspace user for ticket_vectors_31; broad access weakens least privilege and still does not configure point-in-time feature lookup."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `ticket_vectors_31`, this choice avoids the specific `point-in-time feature lookup` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**B** is incorrect because for `ticket_vectors_31`, this choice avoids the specific `point-in-time feature lookup` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**C** is correct because for `ticket_vectors_31`, MLflow experiment tracking is the documented mechanism that addresses `point-in-time feature lookup` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `ticket_vectors_31`, this choice avoids the specific `point-in-time feature lookup` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\nSee [MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q31) for the documented behavior.",
      "reference": "[MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q31)"
    },
    {
      "id": "q32",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `classify-variants-32`: missing lineage; ticket OC032; asset tickets_32. Required capability: `training_baseline_32`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Ask analysts to rerun a local script for training_baseline_32; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "B",
          "text": "Open access to every workspace user for training_baseline_32; broad access weakens least privilege and still does not configure offline feature table publishing."
        },
        {
          "id": "C",
          "text": "Replace the feature with a static CSV extract for training_baseline_32; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "D",
          "text": "Route training_baseline_32 through MLflow autologging; it directly supports offline feature table publishing while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `training_baseline_32`, this choice avoids the specific `offline feature table publishing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**B** is incorrect because for `training_baseline_32`, this choice avoids the specific `offline feature table publishing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**C** is incorrect because for `training_baseline_32`, this choice avoids the specific `offline feature table publishing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**D** is correct because for `training_baseline_32`, MLflow autologging is the documented mechanism that addresses `offline feature table publishing` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q32) for the documented behavior.",
      "reference": "[MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q32)"
    },
    {
      "id": "q33",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `search-schemas-33`: unapproved model; ticket OC033; asset payloads_33. Required capability: `loan_predictions_33`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Hyperparameter tuning for loan_predictions_33 so feature lineage capture is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Open access to every workspace user for loan_predictions_33; broad access weakens least privilege and still does not configure feature lineage capture."
        },
        {
          "id": "C",
          "text": "Replace the feature with a static CSV extract for loan_predictions_33; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "D",
          "text": "Create a presentation slide for loan_predictions_33; documentation alone cannot enforce or execute feature lineage capture."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `loan_predictions_33`, Hyperparameter tuning is the documented mechanism that addresses `feature lineage capture` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `loan_predictions_33`, this choice avoids the specific `feature lineage capture` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**C** is incorrect because for `loan_predictions_33`, this choice avoids the specific `feature lineage capture` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**D** is incorrect because for `loan_predictions_33`, this choice avoids the specific `feature lineage capture` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\nSee [Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q33) for the documented behavior.",
      "reference": "[Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q33)"
    },
    {
      "id": "q34",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `mask-claims-34`: cold endpoint; ticket OC034; asset profiles_34. Required capability: `inventory_snapshot_34`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Replace the feature with a static CSV extract for inventory_snapshot_34; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "B",
          "text": "Route inventory_snapshot_34 through Databricks AutoML; it directly supports training set construction while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Create a presentation slide for inventory_snapshot_34; documentation alone cannot enforce or execute training set construction."
        },
        {
          "id": "D",
          "text": "Move the data to an unrelated storage tier for inventory_snapshot_34; storage relocation does not solve the stated service-level requirement."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `inventory_snapshot_34`, this choice avoids the specific `training set construction` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**B** is correct because for `inventory_snapshot_34`, Databricks AutoML is the documented mechanism that addresses `training set construction` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `inventory_snapshot_34`, this choice avoids the specific `training set construction` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**D** is incorrect because for `inventory_snapshot_34`, this choice avoids the specific `training set construction` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\nSee [Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q34) for the documented behavior.",
      "reference": "[Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q34)"
    },
    {
      "id": "q35",
      "domain": "model-deployment",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Case `refresh-features-35`: empty search; ticket OC035; asset features_35. Required capability: `support_extract_35`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Route support_extract_35 through MLflow models; it directly supports serving scale-out behavior while preserving operational evidence."
        },
        {
          "id": "B",
          "text": "Create a presentation slide for support_extract_35; documentation alone cannot enforce or execute serving scale-out behavior."
        },
        {
          "id": "C",
          "text": "Configure MLflow models for support_extract_35 so serving scale-out behavior is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Move the data to an unrelated storage tier for support_extract_35; storage relocation does not solve the stated service-level requirement."
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "**A** is correct because for `support_extract_35`, MLflow models is the documented mechanism that addresses `serving scale-out behavior` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `support_extract_35`, this choice avoids the specific `serving scale-out behavior` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**C** is correct because for `support_extract_35`, MLflow models is the documented mechanism that addresses `serving scale-out behavior` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `support_extract_35`, this choice avoids the specific `serving scale-out behavior` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\nSee [MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q35) for the documented behavior.",
      "reference": "[MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q35)"
    },
    {
      "id": "q36",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `route-segments-36`: missing lineage; ticket OC036; asset dashboards_36. Required capability: `churn_labels_36`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Move the data to an unrelated storage tier for churn_labels_36; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "B",
          "text": "Disable validation for churn_labels_36; skipping checks hides the failure instead of implementing multi-model packaging correctly."
        },
        {
          "id": "C",
          "text": "Use a dashboard filter for churn_labels_36; presentation filtering cannot manage the underlying multi-model packaging workflow."
        },
        {
          "id": "D",
          "text": "Route churn_labels_36 through Unity Catalog model registry; it directly supports multi-model packaging while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `churn_labels_36`, this choice avoids the specific `multi-model packaging` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**B** is incorrect because for `churn_labels_36`, this choice avoids the specific `multi-model packaging` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**C** is incorrect because for `churn_labels_36`, this choice avoids the specific `multi-model packaging` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**D** is correct because for `churn_labels_36`, Unity Catalog model registry is the documented mechanism that addresses `multi-model packaging` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q36) for the documented behavior.",
      "reference": "[Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q36)"
    },
    {
      "id": "q37",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `parse-payloads-37`: unapproved model; ticket OC037; asset schemas_37. Required capability: `claims_features_37`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Feature engineering workspace for claims_features_37 so pyfunc custom inference is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Disable validation for claims_features_37; skipping checks hides the failure instead of implementing pyfunc custom inference correctly."
        },
        {
          "id": "C",
          "text": "Use a dashboard filter for claims_features_37; presentation filtering cannot manage the underlying pyfunc custom inference workflow."
        },
        {
          "id": "D",
          "text": "Schedule an email reminder for claims_features_37; reminders do not change the platform configuration or runtime behavior."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `claims_features_37`, Feature engineering workspace is the documented mechanism that addresses `pyfunc custom inference` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `claims_features_37`, this choice avoids the specific `pyfunc custom inference` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**C** is incorrect because for `claims_features_37`, this choice avoids the specific `pyfunc custom inference` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**D** is incorrect because for `claims_features_37`, this choice avoids the specific `pyfunc custom inference` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\nSee [Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q37) for the documented behavior.",
      "reference": "[Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q37)"
    },
    {
      "id": "q38",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `cluster-baselines-38`: cold endpoint; ticket OC038; asset vectors_38. Required capability: `daily_revenue_38`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Use a dashboard filter for daily_revenue_38; presentation filtering cannot manage the underlying endpoint update rollback workflow."
        },
        {
          "id": "B",
          "text": "Route daily_revenue_38 through Feature lookup; it directly supports endpoint update rollback while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Schedule an email reminder for daily_revenue_38; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "D",
          "text": "Archive only the raw output file for daily_revenue_38; that keeps a copy but leaves endpoint update rollback outside the managed control plane."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `daily_revenue_38`, this choice avoids the specific `endpoint update rollback` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**B** is correct because for `daily_revenue_38`, Feature lookup is the documented mechanism that addresses `endpoint update rollback` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `daily_revenue_38`, this choice avoids the specific `endpoint update rollback` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**D** is incorrect because for `daily_revenue_38`, this choice avoids the specific `endpoint update rollback` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\nSee [Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q38) for the documented behavior.",
      "reference": "[Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q38)"
    },
    {
      "id": "q39",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `summarize-approvals-39`: empty search; ticket OC039; asset endpoints_39. Required capability: `endpoint_blue_39`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Schedule an email reminder for endpoint_blue_39; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "B",
          "text": "Archive only the raw output file for endpoint_blue_39; that keeps a copy but leaves input drift detection outside the managed control plane."
        },
        {
          "id": "C",
          "text": "Configure Model serving endpoints for endpoint_blue_39 so input drift detection is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Increase the runtime size for endpoint_blue_39; more capacity does not create the required input drift detection approval or lineage behavior."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `endpoint_blue_39`, this choice avoids the specific `input drift detection` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**B** is incorrect because for `endpoint_blue_39`, this choice avoids the specific `input drift detection` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**C** is correct because for `endpoint_blue_39`, Model serving endpoints is the documented mechanism that addresses `input drift detection` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `endpoint_blue_39`, this choice avoids the specific `input drift detection` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\nSee [Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q39) for the documented behavior.",
      "reference": "[Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q39)"
    },
    {
      "id": "q40",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `rollback-invoice-40`: missing lineage; ticket OC040; asset invoice_40. Required capability: `customer_events_40`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Archive only the raw output file for customer_events_40; that keeps a copy but leaves prediction quality baseline outside the managed control plane."
        },
        {
          "id": "B",
          "text": "Increase the runtime size for customer_events_40; more capacity does not create the required prediction quality baseline approval or lineage behavior."
        },
        {
          "id": "C",
          "text": "Ask analysts to rerun a local script for customer_events_40; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "D",
          "text": "Route customer_events_40 through Serving endpoint traffic; it directly supports prediction quality baseline while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `customer_events_40`, this choice avoids the specific `prediction quality baseline` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**B** is incorrect because for `customer_events_40`, this choice avoids the specific `prediction quality baseline` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**C** is incorrect because for `customer_events_40`, this choice avoids the specific `prediction quality baseline` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**D** is correct because for `customer_events_40`, Serving endpoint traffic is the documented mechanism that addresses `prediction quality baseline` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q40) for the documented behavior.",
      "reference": "[Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q40)"
    },
    {
      "id": "q41",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `calibrate-orders-41`: unapproved model; ticket OC041; asset forecasts_41. Required capability: `risk_scores_41`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Batch inference for risk_scores_41 so SHAP explanation review is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Increase the runtime size for risk_scores_41; more capacity does not create the required SHAP explanation review approval or lineage behavior."
        },
        {
          "id": "C",
          "text": "Ask analysts to rerun a local script for risk_scores_41; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "D",
          "text": "Open access to every workspace user for risk_scores_41; broad access weakens least privilege and still does not configure SHAP explanation review."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `risk_scores_41`, Batch inference is the documented mechanism that addresses `SHAP explanation review` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `risk_scores_41`, this choice avoids the specific `SHAP explanation review` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**C** is incorrect because for `risk_scores_41`, this choice avoids the specific `SHAP explanation review` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**D** is incorrect because for `risk_scores_41`, this choice avoids the specific `SHAP explanation review` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\nSee [Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q41) for the documented behavior.",
      "reference": "[Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q41)"
    },
    {
      "id": "q42",
      "domain": "experimentation",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Case `promote-vectors-42`: cold endpoint; ticket OC042; asset baselines_42. Required capability: `sales_rollup_42`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Ask analysts to rerun a local script for sales_rollup_42; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "B",
          "text": "Route sales_rollup_42 through Lakehouse Monitoring; it directly supports reproducible experiment artifacts while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Open access to every workspace user for sales_rollup_42; broad access weakens least privilege and still does not configure reproducible experiment artifacts."
        },
        {
          "id": "D",
          "text": "Configure Lakehouse Monitoring for sales_rollup_42 so reproducible experiment artifacts is handled by the managed service and can be reviewed later."
        }
      ],
      "correct": [
        "B",
        "D"
      ],
      "explanation": "**A** is incorrect because for `sales_rollup_42`, this choice avoids the specific `reproducible experiment artifacts` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**B** is correct because for `sales_rollup_42`, Lakehouse Monitoring is the documented mechanism that addresses `reproducible experiment artifacts` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `sales_rollup_42`, this choice avoids the specific `reproducible experiment artifacts` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**D** is correct because for `sales_rollup_42`, Lakehouse Monitoring is the documented mechanism that addresses `reproducible experiment artifacts` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q42) for the documented behavior.",
      "reference": "[Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q42)"
    },
    {
      "id": "q43",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `flatten-policies-43`: empty search; ticket OC043; asset sensor_43. Required capability: `ticket_vectors_43`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Open access to every workspace user for ticket_vectors_43; broad access weakens least privilege and still does not configure metric comparison across runs."
        },
        {
          "id": "B",
          "text": "Replace the feature with a static CSV extract for ticket_vectors_43; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "C",
          "text": "Configure Inference tables for ticket_vectors_43 so metric comparison across runs is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Create a presentation slide for ticket_vectors_43; documentation alone cannot enforce or execute metric comparison across runs."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `ticket_vectors_43`, this choice avoids the specific `metric comparison across runs` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**B** is incorrect because for `ticket_vectors_43`, this choice avoids the specific `metric comparison across runs` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**C** is correct because for `ticket_vectors_43`, Inference tables is the documented mechanism that addresses `metric comparison across runs` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `ticket_vectors_43`, this choice avoids the specific `metric comparison across runs` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\nSee [Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q43) for the documented behavior.",
      "reference": "[Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q43)"
    },
    {
      "id": "q44",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `score-dashboards-44`: missing lineage; ticket OC044; asset segments_44. Required capability: `training_baseline_44`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Replace the feature with a static CSV extract for training_baseline_44; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "B",
          "text": "Create a presentation slide for training_baseline_44; documentation alone cannot enforce or execute train-validation-test split design."
        },
        {
          "id": "C",
          "text": "Move the data to an unrelated storage tier for training_baseline_44; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "D",
          "text": "Route training_baseline_44 through MLflow model signatures; it directly supports train-validation-test split design while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `training_baseline_44`, this choice avoids the specific `train-validation-test split design` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**B** is incorrect because for `training_baseline_44`, this choice avoids the specific `train-validation-test split design` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**C** is incorrect because for `training_baseline_44`, this choice avoids the specific `train-validation-test split design` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**D** is correct because for `training_baseline_44`, MLflow model signatures is the documented mechanism that addresses `train-validation-test split design` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q44) for the documented behavior.",
      "reference": "[MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q44)"
    },
    {
      "id": "q45",
      "domain": "experimentation",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `profile-manifests-45`: unapproved model; ticket OC045; asset manifests_45. Required capability: `loan_predictions_45`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Databricks Workflows for loan_predictions_45 so random seed control is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Create a presentation slide for loan_predictions_45; documentation alone cannot enforce or execute random seed control."
        },
        {
          "id": "C",
          "text": "Move the data to an unrelated storage tier for loan_predictions_45; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "D",
          "text": "Disable validation for loan_predictions_45; skipping checks hides the failure instead of implementing random seed control correctly."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `loan_predictions_45`, Databricks Workflows is the documented mechanism that addresses `random seed control` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `loan_predictions_45`, this choice avoids the specific `random seed control` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**C** is incorrect because for `loan_predictions_45`, this choice avoids the specific `random seed control` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**D** is incorrect because for `loan_predictions_45`, this choice avoids the specific `random seed control` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\nSee [Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q45) for the documented behavior.",
      "reference": "[Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q45)"
    },
    {
      "id": "q46",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `register-profiles-46`: cold endpoint; ticket OC046; asset claims_46. Required capability: `inventory_snapshot_46`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Move the data to an unrelated storage tier for inventory_snapshot_46; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "B",
          "text": "Route inventory_snapshot_46 through MLflow experiment tracking; it directly supports registry approval workflow while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Disable validation for inventory_snapshot_46; skipping checks hides the failure instead of implementing registry approval workflow correctly."
        },
        {
          "id": "D",
          "text": "Use a dashboard filter for inventory_snapshot_46; presentation filtering cannot manage the underlying registry approval workflow workflow."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `inventory_snapshot_46`, this choice avoids the specific `registry approval workflow` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**B** is correct because for `inventory_snapshot_46`, MLflow experiment tracking is the documented mechanism that addresses `registry approval workflow` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `inventory_snapshot_46`, this choice avoids the specific `registry approval workflow` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\n**D** is incorrect because for `inventory_snapshot_46`, this choice avoids the specific `registry approval workflow` mechanism. It may change surrounding operations, but it does not provide the documented MLflow experiment tracking behavior required by the scenario.\n\nSee [MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q46) for the documented behavior.",
      "reference": "[MLflow experiment tracking](https://docs.databricks.com/aws/en/mlflow/tracking#q46)"
    },
    {
      "id": "q47",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `baseline-sensor-47`: empty search; ticket OC047; asset policies_47. Required capability: `support_extract_47`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Disable validation for support_extract_47; skipping checks hides the failure instead of implementing model signature enforcement correctly."
        },
        {
          "id": "B",
          "text": "Use a dashboard filter for support_extract_47; presentation filtering cannot manage the underlying model signature enforcement workflow."
        },
        {
          "id": "C",
          "text": "Configure MLflow autologging for support_extract_47 so model signature enforcement is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Schedule an email reminder for support_extract_47; reminders do not change the platform configuration or runtime behavior."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `support_extract_47`, this choice avoids the specific `model signature enforcement` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**B** is incorrect because for `support_extract_47`, this choice avoids the specific `model signature enforcement` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\n**C** is correct because for `support_extract_47`, MLflow autologging is the documented mechanism that addresses `model signature enforcement` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `support_extract_47`, this choice avoids the specific `model signature enforcement` mechanism. It may change surrounding operations, but it does not provide the documented MLflow autologging behavior required by the scenario.\n\nSee [MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q47) for the documented behavior.",
      "reference": "[MLflow autologging](https://docs.databricks.com/aws/en/mlflow/databricks-autologging#q47)"
    },
    {
      "id": "q48",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `partition-tickets-48`: missing lineage; ticket OC048; asset variants_48. Required capability: `churn_labels_48`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Use a dashboard filter for churn_labels_48; presentation filtering cannot manage the underlying model version rollback workflow."
        },
        {
          "id": "B",
          "text": "Schedule an email reminder for churn_labels_48; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "C",
          "text": "Archive only the raw output file for churn_labels_48; that keeps a copy but leaves model version rollback outside the managed control plane."
        },
        {
          "id": "D",
          "text": "Route churn_labels_48 through Hyperparameter tuning; it directly supports model version rollback while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `churn_labels_48`, this choice avoids the specific `model version rollback` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**B** is incorrect because for `churn_labels_48`, this choice avoids the specific `model version rollback` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**C** is incorrect because for `churn_labels_48`, this choice avoids the specific `model version rollback` mechanism. It may change surrounding operations, but it does not provide the documented Hyperparameter tuning behavior required by the scenario.\n\n**D** is correct because for `churn_labels_48`, Hyperparameter tuning is the documented mechanism that addresses `model version rollback` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q48) for the documented behavior.",
      "reference": "[Hyperparameter tuning](https://docs.databricks.com/aws/en/machine-learning/automl-hyperparam-tuning/#q48)"
    },
    {
      "id": "q49",
      "domain": "model-lifecycle",
      "type": "multiple",
      "difficulty": "easy",
      "stem": "Case `approve-forecasts-49`: unapproved model; ticket OC049; asset orders_49. Required capability: `claims_features_49`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Configure Databricks AutoML for claims_features_49 so run artifact retention is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Schedule an email reminder for claims_features_49; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "C",
          "text": "Route claims_features_49 through Databricks AutoML; it directly supports run artifact retention while preserving operational evidence."
        },
        {
          "id": "D",
          "text": "Archive only the raw output file for claims_features_49; that keeps a copy but leaves run artifact retention outside the managed control plane."
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "**A** is correct because for `claims_features_49`, Databricks AutoML is the documented mechanism that addresses `run artifact retention` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `claims_features_49`, this choice avoids the specific `run artifact retention` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\n**C** is correct because for `claims_features_49`, Databricks AutoML is the documented mechanism that addresses `run artifact retention` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `claims_features_49`, this choice avoids the specific `run artifact retention` mechanism. It may change surrounding operations, but it does not provide the documented Databricks AutoML behavior required by the scenario.\n\nSee [Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q49) for the documented behavior.",
      "reference": "[Databricks AutoML](https://docs.databricks.com/aws/en/machine-learning/automl/#q49)"
    },
    {
      "id": "q50",
      "domain": "model-lifecycle",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `monitor-lineage-50`: cold endpoint; ticket OC050; asset lineage_50. Required capability: `daily_revenue_50`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Archive only the raw output file for daily_revenue_50; that keeps a copy but leaves registered model aliasing outside the managed control plane."
        },
        {
          "id": "B",
          "text": "Route daily_revenue_50 through MLflow models; it directly supports registered model aliasing while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Increase the runtime size for daily_revenue_50; more capacity does not create the required registered model aliasing approval or lineage behavior."
        },
        {
          "id": "D",
          "text": "Ask analysts to rerun a local script for daily_revenue_50; local execution cannot provide the shared audit trail required here."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `daily_revenue_50`, this choice avoids the specific `registered model aliasing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**B** is correct because for `daily_revenue_50`, MLflow models is the documented mechanism that addresses `registered model aliasing` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `daily_revenue_50`, this choice avoids the specific `registered model aliasing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\n**D** is incorrect because for `daily_revenue_50`, this choice avoids the specific `registered model aliasing` mechanism. It may change surrounding operations, but it does not provide the documented MLflow models behavior required by the scenario.\n\nSee [MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q50) for the documented behavior.",
      "reference": "[MLflow models](https://docs.databricks.com/aws/en/mlflow/models#q50)"
    },
    {
      "id": "q51",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `reshape-endpoints-51`: empty search; ticket OC051; asset approvals_51. Required capability: `endpoint_blue_51`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Increase the runtime size for endpoint_blue_51; more capacity does not create the required feature table primary keys approval or lineage behavior."
        },
        {
          "id": "B",
          "text": "Ask analysts to rerun a local script for endpoint_blue_51; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "C",
          "text": "Configure Unity Catalog model registry for endpoint_blue_51 so feature table primary keys is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Open access to every workspace user for endpoint_blue_51; broad access weakens least privilege and still does not configure feature table primary keys."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `endpoint_blue_51`, this choice avoids the specific `feature table primary keys` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**B** is incorrect because for `endpoint_blue_51`, this choice avoids the specific `feature table primary keys` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\n**C** is correct because for `endpoint_blue_51`, Unity Catalog model registry is the documented mechanism that addresses `feature table primary keys` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `endpoint_blue_51`, this choice avoids the specific `feature table primary keys` mechanism. It may change surrounding operations, but it does not provide the documented Unity Catalog model registry behavior required by the scenario.\n\nSee [Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q51) for the documented behavior.",
      "reference": "[Unity Catalog model registry](https://docs.databricks.com/aws/en/machine-learning/manage-model-lifecycle/#q51)"
    },
    {
      "id": "q52",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `classify-variants-52`: missing lineage; ticket OC052; asset tickets_52. Required capability: `customer_events_52`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Ask analysts to rerun a local script for customer_events_52; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "B",
          "text": "Open access to every workspace user for customer_events_52; broad access weakens least privilege and still does not configure time-series feature joins."
        },
        {
          "id": "C",
          "text": "Replace the feature with a static CSV extract for customer_events_52; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "D",
          "text": "Route customer_events_52 through Feature engineering workspace; it directly supports time-series feature joins while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `customer_events_52`, this choice avoids the specific `time-series feature joins` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**B** is incorrect because for `customer_events_52`, this choice avoids the specific `time-series feature joins` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**C** is incorrect because for `customer_events_52`, this choice avoids the specific `time-series feature joins` mechanism. It may change surrounding operations, but it does not provide the documented Feature engineering workspace behavior required by the scenario.\n\n**D** is correct because for `customer_events_52`, Feature engineering workspace is the documented mechanism that addresses `time-series feature joins` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q52) for the documented behavior.",
      "reference": "[Feature engineering workspace](https://docs.databricks.com/aws/en/machine-learning/feature-store/#q52)"
    },
    {
      "id": "q53",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `search-schemas-53`: unapproved model; ticket OC053; asset payloads_53. Required capability: `risk_scores_53`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Feature lookup for risk_scores_53 so feature reuse governance is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Open access to every workspace user for risk_scores_53; broad access weakens least privilege and still does not configure feature reuse governance."
        },
        {
          "id": "C",
          "text": "Replace the feature with a static CSV extract for risk_scores_53; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "D",
          "text": "Create a presentation slide for risk_scores_53; documentation alone cannot enforce or execute feature reuse governance."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `risk_scores_53`, Feature lookup is the documented mechanism that addresses `feature reuse governance` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `risk_scores_53`, this choice avoids the specific `feature reuse governance` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**C** is incorrect because for `risk_scores_53`, this choice avoids the specific `feature reuse governance` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\n**D** is incorrect because for `risk_scores_53`, this choice avoids the specific `feature reuse governance` mechanism. It may change surrounding operations, but it does not provide the documented Feature lookup behavior required by the scenario.\n\nSee [Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q53) for the documented behavior.",
      "reference": "[Feature lookup](https://docs.databricks.com/aws/en/machine-learning/feature-store/train-models-with-feature-store#q53)"
    },
    {
      "id": "q54",
      "domain": "feature-store",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `mask-claims-54`: cold endpoint; ticket OC054; asset profiles_54. Required capability: `sales_rollup_54`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Replace the feature with a static CSV extract for sales_rollup_54; an extract removes the dynamic service behavior being tested."
        },
        {
          "id": "B",
          "text": "Route sales_rollup_54 through Model serving endpoints; it directly supports point-in-time feature lookup while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Create a presentation slide for sales_rollup_54; documentation alone cannot enforce or execute point-in-time feature lookup."
        },
        {
          "id": "D",
          "text": "Move the data to an unrelated storage tier for sales_rollup_54; storage relocation does not solve the stated service-level requirement."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `sales_rollup_54`, this choice avoids the specific `point-in-time feature lookup` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**B** is correct because for `sales_rollup_54`, Model serving endpoints is the documented mechanism that addresses `point-in-time feature lookup` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `sales_rollup_54`, this choice avoids the specific `point-in-time feature lookup` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\n**D** is incorrect because for `sales_rollup_54`, this choice avoids the specific `point-in-time feature lookup` mechanism. It may change surrounding operations, but it does not provide the documented Model serving endpoints behavior required by the scenario.\n\nSee [Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q54) for the documented behavior.",
      "reference": "[Model serving endpoints](https://docs.databricks.com/aws/en/machine-learning/model-serving/#q54)"
    },
    {
      "id": "q55",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `refresh-features-55`: empty search; ticket OC055; asset features_55. Required capability: `ticket_vectors_55`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Create a presentation slide for ticket_vectors_55; documentation alone cannot enforce or execute batch scoring with Spark."
        },
        {
          "id": "B",
          "text": "Move the data to an unrelated storage tier for ticket_vectors_55; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "C",
          "text": "Configure Serving endpoint traffic for ticket_vectors_55 so batch scoring with Spark is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Disable validation for ticket_vectors_55; skipping checks hides the failure instead of implementing batch scoring with Spark correctly."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `ticket_vectors_55`, this choice avoids the specific `batch scoring with Spark` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**B** is incorrect because for `ticket_vectors_55`, this choice avoids the specific `batch scoring with Spark` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\n**C** is correct because for `ticket_vectors_55`, Serving endpoint traffic is the documented mechanism that addresses `batch scoring with Spark` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `ticket_vectors_55`, this choice avoids the specific `batch scoring with Spark` mechanism. It may change surrounding operations, but it does not provide the documented Serving endpoint traffic behavior required by the scenario.\n\nSee [Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q55) for the documented behavior.",
      "reference": "[Serving endpoint traffic](https://docs.databricks.com/aws/en/machine-learning/model-serving/manage-serving-endpoints#q55)"
    },
    {
      "id": "q56",
      "domain": "model-deployment",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Case `route-segments-56`: missing lineage; ticket OC056; asset dashboards_56. Required capability: `training_baseline_56`.\n\nWhich TWO controls fix this? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Move the data to an unrelated storage tier for training_baseline_56; storage relocation does not solve the stated service-level requirement."
        },
        {
          "id": "B",
          "text": "Configure Batch inference for training_baseline_56 so canary traffic split is handled by the managed service and can be reviewed later."
        },
        {
          "id": "C",
          "text": "Disable validation for training_baseline_56; skipping checks hides the failure instead of implementing canary traffic split correctly."
        },
        {
          "id": "D",
          "text": "Route training_baseline_56 through Batch inference; it directly supports canary traffic split while preserving operational evidence."
        }
      ],
      "correct": [
        "B",
        "D"
      ],
      "explanation": "**A** is incorrect because for `training_baseline_56`, this choice avoids the specific `canary traffic split` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**B** is correct because for `training_baseline_56`, Batch inference is the documented mechanism that addresses `canary traffic split` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `training_baseline_56`, this choice avoids the specific `canary traffic split` mechanism. It may change surrounding operations, but it does not provide the documented Batch inference behavior required by the scenario.\n\n**D** is correct because for `training_baseline_56`, Batch inference is the documented mechanism that addresses `canary traffic split` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q56) for the documented behavior.",
      "reference": "[Batch inference](https://docs.databricks.com/aws/en/machine-learning/model-inference/#q56)"
    },
    {
      "id": "q57",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "easy",
      "stem": "Case `parse-payloads-57`: unapproved model; ticket OC057; asset schemas_57. Required capability: `loan_predictions_57`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Configure Lakehouse Monitoring for loan_predictions_57 so endpoint authentication is handled by the managed service and can be reviewed later."
        },
        {
          "id": "B",
          "text": "Disable validation for loan_predictions_57; skipping checks hides the failure instead of implementing endpoint authentication correctly."
        },
        {
          "id": "C",
          "text": "Use a dashboard filter for loan_predictions_57; presentation filtering cannot manage the underlying endpoint authentication workflow."
        },
        {
          "id": "D",
          "text": "Schedule an email reminder for loan_predictions_57; reminders do not change the platform configuration or runtime behavior."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "**A** is correct because for `loan_predictions_57`, Lakehouse Monitoring is the documented mechanism that addresses `endpoint authentication` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**B** is incorrect because for `loan_predictions_57`, this choice avoids the specific `endpoint authentication` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**C** is incorrect because for `loan_predictions_57`, this choice avoids the specific `endpoint authentication` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\n**D** is incorrect because for `loan_predictions_57`, this choice avoids the specific `endpoint authentication` mechanism. It may change surrounding operations, but it does not provide the documented Lakehouse Monitoring behavior required by the scenario.\n\nSee [Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q57) for the documented behavior.",
      "reference": "[Lakehouse Monitoring](https://docs.databricks.com/aws/en/lakehouse-monitoring/#q57)"
    },
    {
      "id": "q58",
      "domain": "model-deployment",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `cluster-baselines-58`: cold endpoint; ticket OC058; asset vectors_58. Required capability: `inventory_snapshot_58`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Use a dashboard filter for inventory_snapshot_58; presentation filtering cannot manage the underlying serving scale-out behavior workflow."
        },
        {
          "id": "B",
          "text": "Route inventory_snapshot_58 through Inference tables; it directly supports serving scale-out behavior while preserving operational evidence."
        },
        {
          "id": "C",
          "text": "Schedule an email reminder for inventory_snapshot_58; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "D",
          "text": "Archive only the raw output file for inventory_snapshot_58; that keeps a copy but leaves serving scale-out behavior outside the managed control plane."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "**A** is incorrect because for `inventory_snapshot_58`, this choice avoids the specific `serving scale-out behavior` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**B** is correct because for `inventory_snapshot_58`, Inference tables is the documented mechanism that addresses `serving scale-out behavior` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**C** is incorrect because for `inventory_snapshot_58`, this choice avoids the specific `serving scale-out behavior` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\n**D** is incorrect because for `inventory_snapshot_58`, this choice avoids the specific `serving scale-out behavior` mechanism. It may change surrounding operations, but it does not provide the documented Inference tables behavior required by the scenario.\n\nSee [Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q58) for the documented behavior.",
      "reference": "[Inference tables](https://docs.databricks.com/aws/en/machine-learning/model-serving/inference-tables#q58)"
    },
    {
      "id": "q59",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "Case `summarize-approvals-59`: empty search; ticket OC059; asset endpoints_59. Required capability: `support_extract_59`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Schedule an email reminder for support_extract_59; reminders do not change the platform configuration or runtime behavior."
        },
        {
          "id": "B",
          "text": "Archive only the raw output file for support_extract_59; that keeps a copy but leaves data quality slices outside the managed control plane."
        },
        {
          "id": "C",
          "text": "Configure MLflow model signatures for support_extract_59 so data quality slices is handled by the managed service and can be reviewed later."
        },
        {
          "id": "D",
          "text": "Increase the runtime size for support_extract_59; more capacity does not create the required data quality slices approval or lineage behavior."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "**A** is incorrect because for `support_extract_59`, this choice avoids the specific `data quality slices` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**B** is incorrect because for `support_extract_59`, this choice avoids the specific `data quality slices` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\n**C** is correct because for `support_extract_59`, MLflow model signatures is the documented mechanism that addresses `data quality slices` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\n**D** is incorrect because for `support_extract_59`, this choice avoids the specific `data quality slices` mechanism. It may change surrounding operations, but it does not provide the documented MLflow model signatures behavior required by the scenario.\n\nSee [MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q59) for the documented behavior.",
      "reference": "[MLflow model signatures](https://docs.databricks.com/aws/en/mlflow/models#q59)"
    },
    {
      "id": "q60",
      "domain": "solution-monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "Case `rollback-invoice-60`: missing lineage; ticket OC060; asset invoice_60. Required capability: `churn_labels_60`.\n\nWhich control fixes this?",
      "options": [
        {
          "id": "A",
          "text": "Archive only the raw output file for churn_labels_60; that keeps a copy but leaves concept drift triage outside the managed control plane."
        },
        {
          "id": "B",
          "text": "Increase the runtime size for churn_labels_60; more capacity does not create the required concept drift triage approval or lineage behavior."
        },
        {
          "id": "C",
          "text": "Ask analysts to rerun a local script for churn_labels_60; local execution cannot provide the shared audit trail required here."
        },
        {
          "id": "D",
          "text": "Route churn_labels_60 through Databricks Workflows; it directly supports concept drift triage while preserving operational evidence."
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "**A** is incorrect because for `churn_labels_60`, this choice avoids the specific `concept drift triage` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**B** is incorrect because for `churn_labels_60`, this choice avoids the specific `concept drift triage` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**C** is incorrect because for `churn_labels_60`, this choice avoids the specific `concept drift triage` mechanism. It may change surrounding operations, but it does not provide the documented Databricks Workflows behavior required by the scenario.\n\n**D** is correct because for `churn_labels_60`, Databricks Workflows is the documented mechanism that addresses `concept drift triage` with platform-managed state, permissions, or execution history. That directly matches the scenario's audit and repeatability constraint.\n\nSee [Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q60) for the documented behavior.",
      "reference": "[Databricks Workflows](https://docs.databricks.com/aws/en/jobs/#q60)"
    }
  ]
}
