{
  "meta": {
    "id": "sfadm-exam-02",
    "certification": "snowpro-advanced-administrator",
    "certificationName": "SnowPro Advanced: Administrator",
    "title": "Mock Exam 2",
    "version": "1.0",
    "timeLimit": 115,
    "passingScore": 75,
    "totalQuestions": 30,
    "domains": [
      { "id": "account-administration", "name": "Account Administration", "weight": 25 },
      { "id": "resource-monitoring", "name": "Resource Monitoring", "weight": 20 },
      { "id": "access-control", "name": "Access Control", "weight": 20 },
      { "id": "network-security", "name": "Network & Security", "weight": 15 },
      { "id": "storage-compute", "name": "Storage & Compute", "weight": 15 },
      { "id": "audit-compliance", "name": "Audit & Compliance", "weight": 5 }
    ]
  },
  "questions": [
    {
      "id": "q01",
      "domain": "account-administration",
      "type": "single",
      "difficulty": "easy",
      "stem": "A new Snowflake account is provisioned. Which role is automatically granted to every user created in the account, including service accounts?",
      "options": [
        { "id": "A", "text": "SYSADMIN" },
        { "id": "B", "text": "USERADMIN" },
        { "id": "C", "text": "PUBLIC" },
        { "id": "D", "text": "SECURITYADMIN" }
      ],
      "correct": ["C"],
      "explanation": "The PUBLIC role is automatically granted to every user in a Snowflake account. It represents the lowest common denominator of access. Privileges granted to PUBLIC are available to all users. SYSADMIN, USERADMIN, and SECURITYADMIN are system roles that must be explicitly granted to users — they are not automatic.",
      "reference": "System-Defined Roles: PUBLIC"
    },
    {
      "id": "q02",
      "domain": "account-administration",
      "type": "single",
      "difficulty": "medium",
      "stem": "An administrator needs to enforce that all new users must change their password on first login. Which parameter in the CREATE USER statement controls this?",
      "options": [
        { "id": "A", "text": "FORCE_PASSWORD_RESET = TRUE" },
        { "id": "B", "text": "MUST_CHANGE_PASSWORD = TRUE" },
        { "id": "C", "text": "PASSWORD_EXPIRY = IMMEDIATE" },
        { "id": "D", "text": "CHANGE_ON_LOGIN = TRUE" }
      ],
      "correct": ["B"],
      "explanation": "MUST_CHANGE_PASSWORD = TRUE forces the user to change their password at the next login. This is a best practice for new user accounts. The other options (FORCE_PASSWORD_RESET, PASSWORD_EXPIRY = IMMEDIATE, CHANGE_ON_LOGIN) are not valid Snowflake user parameters.",
      "reference": "User Management: MUST_CHANGE_PASSWORD"
    },
    {
      "id": "q03",
      "domain": "account-administration",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "A Snowflake administrator is designing a role hierarchy for a healthcare organization. Select TWO best practices that should be followed when designing this hierarchy.",
      "options": [
        { "id": "A", "text": "Grant custom roles to SYSADMIN so SYSADMIN can manage objects owned by those roles" },
        { "id": "B", "text": "Use ACCOUNTADMIN as the daily operational role for all senior DBAs to ensure maximum privileges" },
        { "id": "C", "text": "Create separate functional roles (e.g., ANALYST, ENGINEER) and access roles (e.g., READ_DB1, WRITE_DB1), granting access roles to functional roles" },
        { "id": "D", "text": "Grant all custom roles directly to PUBLIC to ensure all users have access to common resources" }
      ],
      "correct": ["A", "C"],
      "explanation": "Granting custom roles to SYSADMIN ensures SYSADMIN can manage all objects through the inherited role hierarchy — critical for centralized administration. Separating functional roles from access roles is a Snowflake best practice: functional roles represent job functions and are granted to users; access roles represent specific object permissions and are granted to functional roles. This separation makes auditing and privilege management cleaner. Using ACCOUNTADMIN for daily operations violates least privilege. Granting all roles to PUBLIC gives every user those privileges, which is a major security violation.",
      "reference": "Role Design: Functional vs Access Roles"
    },
    {
      "id": "q04",
      "domain": "account-administration",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake account administrator sets the following at the account level: ALTER ACCOUNT SET DATA_RETENTION_TIME_IN_DAYS = 14. A developer then creates a table with CREATE TABLE orders (...). What is the Time Travel retention period for the orders table?",
      "options": [
        { "id": "A", "text": "0 days, because new tables default to no retention" },
        { "id": "B", "text": "1 day, which is the Snowflake default for Standard edition" },
        { "id": "C", "text": "14 days, because the table inherits the account-level setting" },
        { "id": "D", "text": "90 days, which is the maximum allowed retention period" }
      ],
      "correct": ["C"],
      "explanation": "When DATA_RETENTION_TIME_IN_DAYS is set at the account level and no explicit value is set at the database, schema, or table level, the object inherits the account-level setting. Since the table was created without specifying a retention period, it inherits the account setting of 14 days. Note that 14 days of Time Travel requires Enterprise edition or higher — Standard edition is limited to 1 day. The question implies Enterprise edition context.",
      "reference": "Time Travel: Retention Inheritance"
    },
    {
      "id": "q05",
      "domain": "account-administration",
      "type": "single",
      "difficulty": "hard",
      "stem": "A company is implementing Snowflake in a regulated environment and needs to ensure that the SECURITYADMIN role cannot be used to access business data, only to manage security. Which architectural control achieves this?",
      "options": [
        { "id": "A", "text": "Remove the SECURITYADMIN role from all users and only assign it when needed via a PAM tool" },
        { "id": "B", "text": "Ensure that no data access roles (SELECT, INSERT, etc.) are granted to SECURITYADMIN, and that SECURITYADMIN is not granted SYSADMIN or any data-owning custom role" },
        { "id": "C", "text": "Use a network policy to prevent SECURITYADMIN users from accessing data tables" },
        { "id": "D", "text": "Set STATEMENT_TIMEOUT_IN_SECONDS = 0 for SECURITYADMIN to prevent data queries" }
      ],
      "correct": ["B"],
      "explanation": "SECURITYADMIN inherits USERADMIN but does not inherently have data access. The risk comes from SECURITYADMIN's ability to GRANT any privilege to any role — including granting itself data access. The architectural control is to ensure no data privileges are granted to SECURITYADMIN and SECURITYADMIN is not placed in a role hierarchy that includes data-owning roles. Separating security administration from data access is a standard compliance control. Option A (PAM tool) is an operational control but not the architectural role design answer. Network policies cannot scope access by role. STATEMENT_TIMEOUT = 0 disables timeouts, not data access.",
      "reference": "Account Administration: Separating Security from Data Access"
    },
    {
      "id": "q06",
      "domain": "account-administration",
      "type": "single",
      "difficulty": "hard",
      "stem": "An organization uses Snowflake's object hierarchy: Organization → Account → Database → Schema → Table. A login policy is created and needs to be assigned to a specific set of users. Which command correctly assigns a login policy to a user?",
      "options": [
        { "id": "A", "text": "ALTER ACCOUNT SET LOGIN_POLICY = strict_login_policy;" },
        { "id": "B", "text": "ALTER USER john_doe SET LOGIN_POLICY = strict_login_policy;" },
        { "id": "C", "text": "GRANT LOGIN_POLICY strict_login_policy TO USER john_doe;" },
        { "id": "D", "text": "CREATE USER john_doe WITH LOGIN_POLICY = strict_login_policy;" }
      ],
      "correct": ["B"],
      "explanation": "Login policies are assigned to users using ALTER USER ... SET LOGIN_POLICY. A login policy can also be set at the account level (ALTER ACCOUNT SET LOGIN_POLICY) to apply to all users as a default, but assigning to a specific user requires the ALTER USER syntax. GRANT LOGIN_POLICY is not valid syntax — login policies are assigned, not granted. While CREATE USER can include many parameters, SET LOGIN_POLICY is not a CREATE USER parameter in standard Snowflake syntax — it must be applied with ALTER USER after creation.",
      "reference": "Login Policies: Assigning to Users"
    },
    {
      "id": "q07",
      "domain": "account-administration",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A Snowflake administrator needs to enumerate all users who have not logged in for more than 90 days. Select TWO approaches that can accomplish this.",
      "options": [
        { "id": "A", "text": "Query SNOWFLAKE.ACCOUNT_USAGE.USERS and filter where LAST_SUCCESS_LOGIN < DATEADD('day', -90, CURRENT_TIMESTAMP())" },
        { "id": "B", "text": "Run SHOW USERS and check the last_success_login column" },
        { "id": "C", "text": "Query SNOWFLAKE.ACCOUNT_USAGE.LOGIN_HISTORY and find users with no records in the last 90 days" },
        { "id": "D", "text": "Run SELECT * FROM TABLE(INFORMATION_SCHEMA.USER_HISTORY()) WHERE last_login < DATEADD('day', -90, CURRENT_TIMESTAMP())" }
      ],
      "correct": ["A", "B"],
      "explanation": "ACCOUNT_USAGE.USERS view contains user metadata including LAST_SUCCESS_LOGIN, which can be queried with a date filter to find inactive users. SHOW USERS also surfaces last_success_login for each user. Both approaches work. ACCOUNT_USAGE.LOGIN_HISTORY contains login attempt records and could be used indirectly (find users not present in recent 90 days), but it does not directly show 'last login' for all users — users who never logged in would be missed. INFORMATION_SCHEMA.USER_HISTORY() is not a valid function in Snowflake.",
      "reference": "User Management: Identifying Inactive Users"
    },
    {
      "id": "q08",
      "domain": "account-administration",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Snowflake system role has the ability to manage grants (GRANT/REVOKE privileges) for ALL securable objects in the account, including granting any privilege to any role?",
      "options": [
        { "id": "A", "text": "SYSADMIN" },
        { "id": "B", "text": "USERADMIN" },
        { "id": "C", "text": "SECURITYADMIN" },
        { "id": "D", "text": "PUBLIC" }
      ],
      "correct": ["C"],
      "explanation": "SECURITYADMIN has the MANAGE GRANTS global privilege, which allows it to grant or revoke any privilege on any securable object in the account. This makes it the role responsible for all access control management. SYSADMIN creates and owns objects but does not have the broad MANAGE GRANTS privilege. USERADMIN can create users and roles but cannot grant arbitrary privileges on objects. PUBLIC has minimal privileges.",
      "reference": "System-Defined Roles: SECURITYADMIN"
    },
    {
      "id": "q09",
      "domain": "resource-monitoring",
      "type": "single",
      "difficulty": "easy",
      "stem": "An administrator wants to see the current credit quota and the amount of credits already consumed for all resource monitors in the account. Which command provides this information?",
      "options": [
        { "id": "A", "text": "SELECT * FROM SNOWFLAKE.ACCOUNT_USAGE.METERING_DAILY_HISTORY;" },
        { "id": "B", "text": "SHOW RESOURCE MONITORS;" },
        { "id": "C", "text": "SELECT * FROM SNOWFLAKE.ACCOUNT_USAGE.RESOURCE_MONITORS;" },
        { "id": "D", "text": "DESCRIBE RESOURCE MONITOR all;" }
      ],
      "correct": ["B"],
      "explanation": "SHOW RESOURCE MONITORS displays all resource monitors with their quota, used credits, status, frequency, and trigger configuration. It is the direct DDL command for viewing resource monitor state. METERING_DAILY_HISTORY shows overall credit consumption by service type, not resource monitor details. There is no ACCOUNT_USAGE.RESOURCE_MONITORS view in Snowflake. DESCRIBE RESOURCE MONITOR all is not valid syntax.",
      "reference": "Resource Monitors: SHOW RESOURCE MONITORS"
    },
    {
      "id": "q10",
      "domain": "resource-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company wants to set up credit spend controls per department. The Data Engineering department has three warehouses (ETL_WH, STAGING_WH, TRANSFORM_WH) and a monthly budget of 2000 credits. How should the administrator configure resource monitoring?",
      "options": [
        { "id": "A", "text": "Create one resource monitor with CREDIT_QUOTA = 2000, assign all three warehouses to it" },
        { "id": "B", "text": "Create three resource monitors each with CREDIT_QUOTA = 666, one per warehouse" },
        { "id": "C", "text": "Set CREDIT_QUOTA = 2000 at the account level and rely on overall account monitoring" },
        { "id": "D", "text": "Create a BUDGET object with BUDGET_LIMIT = 2000 assigned to the department role" }
      ],
      "correct": ["A"],
      "explanation": "A single resource monitor can be assigned to multiple warehouses. Credits consumed by all assigned warehouses count toward the single shared quota. This is the correct approach for department-level spend control — one monitor with the total budget, assigned to all departmental warehouses. Splitting into three monitors (option B) would give each warehouse only 666 credits and they would not share the pool. Account-level monitoring (option C) does not isolate departmental spend. BUDGET is a valid Snowflake feature but BUDGET_LIMIT assigned to a role is not the correct BUDGET syntax — Budgets are assigned to specific objects or account-level.",
      "reference": "Resource Monitors: Multiple Warehouse Assignment"
    },
    {
      "id": "q11",
      "domain": "resource-monitoring",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A Snowflake administrator is investigating why a warehouse was unexpectedly suspended mid-day. Select TWO queries that would help determine if a resource monitor triggered the suspension.",
      "options": [
        { "id": "A", "text": "SHOW RESOURCE MONITORS; — check STATUS and USED_CREDITS vs CREDIT_QUOTA" },
        { "id": "B", "text": "SELECT * FROM SNOWFLAKE.ACCOUNT_USAGE.QUERY_HISTORY WHERE WAREHOUSE_NAME = 'MY_WH' AND ERROR_CODE IS NOT NULL ORDER BY START_TIME DESC;" },
        { "id": "C", "text": "SELECT * FROM TABLE(INFORMATION_SCHEMA.WAREHOUSE_EVENTS_HISTORY(WAREHOUSE_NAME => 'MY_WH')) WHERE EVENT_STATE = 'SUSPENDED';" },
        { "id": "D", "text": "SELECT * FROM SNOWFLAKE.ACCOUNT_USAGE.METERING_DAILY_HISTORY WHERE SERVICE_TYPE = 'WAREHOUSE_METERING' ORDER BY USAGE_DATE DESC;" }
      ],
      "correct": ["A", "C"],
      "explanation": "SHOW RESOURCE MONITORS shows whether a monitor associated with the warehouse has STATUS = SUSPENDED and whether USED_CREDITS has reached or exceeded CREDIT_QUOTA. WAREHOUSE_EVENTS_HISTORY (an INFORMATION_SCHEMA table function) records warehouse lifecycle events including suspension events and their reasons — this can directly show if a resource monitor caused the suspension. QUERY_HISTORY with error codes shows failed queries but does not explain why the warehouse was suspended. METERING_DAILY_HISTORY shows credit usage totals, not suspension events.",
      "reference": "Resource Monitors: Diagnosing Warehouse Suspension"
    },
    {
      "id": "q12",
      "domain": "resource-monitoring",
      "type": "single",
      "difficulty": "medium",
      "stem": "A resource monitor is set with FREQUENCY = DAILY and CREDIT_QUOTA = 50. It is now 3 PM and 48 credits have been consumed. The SUSPEND trigger is set at 100%. A new large query is submitted that will consume approximately 5 credits. What happens?",
      "options": [
        { "id": "A", "text": "The query is rejected before starting because submitting it would exceed the daily quota" },
        { "id": "B", "text": "The query starts and runs; when cumulative consumption hits 50 credits (100%), the warehouse is suspended after the query completes" },
        { "id": "C", "text": "The query starts and runs; when cumulative consumption hits 50 credits (100%), the warehouse suspends immediately even if the query is still running" },
        { "id": "D", "text": "The query is queued until midnight when the daily quota resets" }
      ],
      "correct": ["B"],
      "explanation": "With a SUSPEND trigger (not SUSPEND_IMMEDIATELY), the warehouse allows the currently running query to complete before suspending. The resource monitor detects that the 100% threshold is crossed while the query is running and queues the SUSPEND action, but the query finishes first. Only SUSPEND_IMMEDIATELY would cancel the in-flight query. Snowflake does not pre-emptively reject queries based on predicted future consumption. Queries are not queued for the reset period.",
      "reference": "Resource Monitors: SUSPEND vs SUSPEND_IMMEDIATELY Behavior"
    },
    {
      "id": "q13",
      "domain": "resource-monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "A company wants more granular credit monitoring than resource monitors provide — specifically, they want to track and alert on spending for individual Snowflake objects like a specific table with automatic clustering enabled. Which Snowflake feature should the administrator use?",
      "options": [
        { "id": "A", "text": "Create a resource monitor with OBJECT_SCOPE = TABLE and assign it to the specific table" },
        { "id": "B", "text": "Use the BUDGET feature (CREATE BUDGET) to create a budget scoped to the specific object" },
        { "id": "C", "text": "Query METERING_DAILY_HISTORY daily with a custom alert threshold in an external monitoring tool" },
        { "id": "D", "text": "Enable CREDIT_TRACKING = TRUE on the table using ALTER TABLE" }
      ],
      "correct": ["B"],
      "explanation": "The BUDGET feature (introduced after resource monitors) provides more granular spending control. Budgets can be scoped to individual Snowflake objects, specific accounts, or even specific services, allowing per-object credit tracking and alerting. Resource monitors cannot be scoped to individual objects like tables — they only apply at the warehouse level or account level. METERING_DAILY_HISTORY can provide data for custom monitoring but is not a built-in alerting feature. CREDIT_TRACKING = TRUE on ALTER TABLE is not a valid Snowflake parameter.",
      "reference": "Budgets: Object-Level Credit Monitoring"
    },
    {
      "id": "q14",
      "domain": "access-control",
      "type": "single",
      "difficulty": "easy",
      "stem": "A developer reports getting the error 'Object does not exist or operation cannot be performed' when querying a table they believe they have access to. The administrator confirms the developer's role has SELECT on the table. What is the most likely cause?",
      "options": [
        { "id": "A", "text": "The developer needs the REFERENCES privilege in addition to SELECT" },
        { "id": "B", "text": "The developer's role is missing USAGE privilege on the database or schema containing the table" },
        { "id": "C", "text": "The table has been encrypted and the developer needs a decryption key" },
        { "id": "D", "text": "The developer needs to run USE WAREHOUSE before querying" }
      ],
      "correct": ["B"],
      "explanation": "In Snowflake, to access an object a role must have USAGE on every container in the hierarchy: USAGE on the database, USAGE on the schema, and then SELECT on the table. 'Object does not exist or operation cannot be performed' is Snowflake's generic error that appears when the role cannot even see the object — which happens when USAGE on the database or schema is missing. The object appears to not exist from the role's perspective. REFERENCES is for foreign key definitions, not needed for SELECT. Tables are not individually encrypted with customer keys in standard setups. A missing warehouse only prevents query execution with a different error.",
      "reference": "Access Control: Object Hierarchy and USAGE Privilege"
    },
    {
      "id": "q15",
      "domain": "access-control",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake administrator applies a Dynamic Data Masking policy that replaces SSN values with 'XXX-XX-XXXX' for all roles except ANALYST_FULL_ACCESS. The policy is applied to the SSN column in the EMPLOYEES table. A user with both ANALYST_ROLE and ANALYST_FULL_ACCESS roles granted queries the table using the ANALYST_ROLE as their active primary role (USE ROLE ANALYST_ROLE). What does the user see for the SSN column?",
      "options": [
        { "id": "A", "text": "The actual SSN value, because the user has ANALYST_FULL_ACCESS granted to them even if it is not their active role" },
        { "id": "B", "text": "The masked value 'XXX-XX-XXXX', because the masking policy evaluates CURRENT_ROLE() which is ANALYST_ROLE" },
        { "id": "C", "text": "The actual SSN value, because Snowflake evaluates all granted roles when applying masking" },
        { "id": "D", "text": "NULL, because the masking policy cannot determine which role to apply" }
      ],
      "correct": ["B"],
      "explanation": "Dynamic Data Masking policies typically use CURRENT_ROLE() (or CURRENT_ROLE() = 'ANALYST_FULL_ACCESS') to determine what to show. CURRENT_ROLE() returns the currently active primary role, which is ANALYST_ROLE in this scenario. Even though the user has ANALYST_FULL_ACCESS granted, it is not the active role, so the masking condition is not met and the masked value is returned. To see unmasked data, the user would need to USE ROLE ANALYST_FULL_ACCESS or use secondary roles if the policy checks for secondary roles. This is a key distinction in how masking policies evaluate role context.",
      "reference": "Dynamic Data Masking: CURRENT_ROLE() Evaluation"
    },
    {
      "id": "q16",
      "domain": "access-control",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "An administrator needs to audit all privileges granted to the REPORTING_ROLE. Select TWO commands that provide different aspects of this audit.",
      "options": [
        { "id": "A", "text": "SHOW GRANTS TO ROLE REPORTING_ROLE;" },
        { "id": "B", "text": "SHOW GRANTS OF ROLE REPORTING_ROLE;" },
        { "id": "C", "text": "SELECT * FROM SNOWFLAKE.ACCOUNT_USAGE.GRANTS_TO_ROLES WHERE GRANTEE_NAME = 'REPORTING_ROLE';" },
        { "id": "D", "text": "DESCRIBE ROLE REPORTING_ROLE PRIVILEGES;" }
      ],
      "correct": ["A", "B"],
      "explanation": "SHOW GRANTS TO ROLE REPORTING_ROLE lists all privileges (on databases, schemas, tables, warehouses, etc.) granted TO that role. SHOW GRANTS OF ROLE REPORTING_ROLE shows which users and roles have been granted this role (i.e., who has REPORTING_ROLE in their hierarchy). Together they provide a complete picture of what REPORTING_ROLE can do and who uses it. ACCOUNT_USAGE.GRANTS_TO_ROLES can also provide this information with historical context but has a 45-minute latency. DESCRIBE ROLE PRIVILEGES is not valid Snowflake syntax.",
      "reference": "Access Control Auditing: SHOW GRANTS TO/OF ROLE"
    },
    {
      "id": "q17",
      "domain": "access-control",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowflake administrator is troubleshooting a future grant issue. The command GRANT SELECT ON FUTURE TABLES IN SCHEMA ANALYTICS TO ROLE BI_ROLE was run 2 months ago. A new table ANALYTICS.NEW_METRICS was created last week. The BI_ROLE cannot SELECT from NEW_METRICS. What is the most likely cause?",
      "options": [
        { "id": "A", "text": "Future grants only apply to tables created within 24 hours of the GRANT command" },
        { "id": "B", "text": "The table was created by a role different from the one that owns the schema, and future grants do not apply in this case" },
        { "id": "C", "text": "The future grant was revoked or a REVOKE ALL PRIVILEGES ON FUTURE TABLES was issued, removing the future grant" },
        { "id": "D", "text": "Future grants require re-issuance every 30 days to remain active" }
      ],
      "correct": ["C"],
      "explanation": "Future grants persist indefinitely — they do not expire after 24 hours or 30 days. They also apply regardless of which role creates the table within that schema. The most common reason a future grant stops working is that it was explicitly revoked with REVOKE SELECT ON FUTURE TABLES IN SCHEMA ANALYTICS FROM ROLE BI_ROLE, or a blanket REVOKE ALL PRIVILEGES ON FUTURE TABLES was issued. Administrators should run SHOW FUTURE GRANTS IN SCHEMA ANALYTICS to verify the future grant still exists. Option B is incorrect — future grants apply to all new tables in the schema regardless of creator role.",
      "reference": "Future Grants: Persistence and Revocation"
    },
    {
      "id": "q18",
      "domain": "access-control",
      "type": "single",
      "difficulty": "hard",
      "stem": "A user activates secondary roles by running USE SECONDARY ROLES ALL. The user's primary role is DEVELOPER_ROLE. The user also has ANALYST_ROLE granted. ANALYST_ROLE has SELECT on a sensitive table. DEVELOPER_ROLE does not. Which statement about this query scenario is correct?",
      "options": [
        { "id": "A", "text": "The query will fail because primary role DEVELOPER_ROLE does not have SELECT on the table" },
        { "id": "B", "text": "The query will succeed because USE SECONDARY ROLES ALL activates all granted roles simultaneously, and ANALYST_ROLE's SELECT is usable" },
        { "id": "C", "text": "The query will succeed only if DEVELOPER_ROLE is a parent role of ANALYST_ROLE in the hierarchy" },
        { "id": "D", "text": "USE SECONDARY ROLES ALL only applies to DDL operations, not DML like SELECT" }
      ],
      "correct": ["B"],
      "explanation": "USE SECONDARY ROLES ALL is a Snowflake feature that activates all roles granted to the user simultaneously during the session. With this enabled, the user can use privileges from any of their granted roles, not just the active primary role. In this scenario, ANALYST_ROLE's SELECT privilege on the sensitive table becomes available even though DEVELOPER_ROLE (the primary role) does not have it. This is a significant access control consideration — administrators should be aware that users with secondary roles active can access anything any of their roles can access. Options A and C are incorrect given secondary roles. Option D is incorrect; secondary roles apply to all SQL operations.",
      "reference": "Secondary Roles: USE SECONDARY ROLES ALL"
    },
    {
      "id": "q19",
      "domain": "network-security",
      "type": "single",
      "difficulty": "easy",
      "stem": "An administrator creates a network policy and wants to apply it to the entire Snowflake account. Which command correctly applies the policy at the account level?",
      "options": [
        { "id": "A", "text": "ALTER ACCOUNT SET NETWORK_POLICY = my_corp_policy;" },
        { "id": "B", "text": "GRANT NETWORK_POLICY my_corp_policy TO ACCOUNT;" },
        { "id": "C", "text": "CREATE NETWORK POLICY my_corp_policy ACCOUNT_LEVEL = TRUE;" },
        { "id": "D", "text": "ALTER NETWORK POLICY my_corp_policy SET SCOPE = ACCOUNT;" }
      ],
      "correct": ["A"],
      "explanation": "The correct command to apply a network policy at the account level is ALTER ACCOUNT SET NETWORK_POLICY = policy_name. This restricts all access to the account based on the policy's IP rules. The other options use invalid syntax — GRANT NETWORK_POLICY, ACCOUNT_LEVEL = TRUE in CREATE, and ALTER NETWORK POLICY SET SCOPE are not valid Snowflake commands.",
      "reference": "Network Policies: Account-Level Application"
    },
    {
      "id": "q20",
      "domain": "network-security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company has implemented network rules using the newer network rule construct. They want to allow access only from specific AWS VPC endpoints. Which type of network rule should be used?",
      "options": [
        { "id": "A", "text": "TYPE = IPV4 with the VPC CIDR range" },
        { "id": "B", "text": "TYPE = AWSVPCEID with the VPC endpoint IDs" },
        { "id": "C", "text": "TYPE = HOST with the VPC DNS hostname" },
        { "id": "D", "text": "TYPE = PRIVATELINK with the endpoint service name" }
      ],
      "correct": ["B"],
      "explanation": "Network rules support different types for different use cases. TYPE = AWSVPCEID allows specifying AWS VPC endpoint IDs to restrict access to traffic coming from specific VPC endpoints. This is used when you want to ensure traffic originates only from approved VPCs via PrivateLink endpoints. TYPE = IPV4 uses IP addresses/CIDR ranges (public IPs, not VPC endpoints). TYPE = HOST uses DNS hostnames for external functions/API integrations. TYPE = PRIVATELINK is not a valid network rule type.",
      "reference": "Network Rules: AWSVPCEID Type"
    },
    {
      "id": "q21",
      "domain": "network-security",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A security team requires MFA for all interactive users but wants service accounts to bypass MFA for automation. Select TWO approaches to implement this in Snowflake.",
      "options": [
        { "id": "A", "text": "Set up MFA enrollment for all users, then configure service account users to use key pair authentication instead of password-based login (which bypasses MFA)" },
        { "id": "B", "text": "Create a login policy with MFA_ENROLLMENT = REQUIRED and apply it at the account level, then create a separate login policy with MFA_ENROLLMENT = OPTIONAL and assign it to service account users" },
        { "id": "C", "text": "Enable MFA at the account level and then run ALTER USER svc_account DISABLE MFA = TRUE for each service account" },
        { "id": "D", "text": "Use a network policy to restrict service accounts to specific IP ranges, then exempt those IPs from MFA requirements" }
      ],
      "correct": ["A", "B"],
      "explanation": "Key pair authentication for service accounts (option A) is the standard approach — service accounts authenticate via RSA keys and do not use the password/MFA flow at all. MFA only applies to the username/password authentication pathway. Option B using login policies with MFA_ENROLLMENT is also valid — account-level login policy requires MFA, and user-level overrides (OPTIONAL or OPTIONAL policy) exempt specific service account users. ALTER USER DISABLE MFA = TRUE (option C) is not valid Snowflake syntax. Network policies control IP-based access, not MFA requirements (option D).",
      "reference": "MFA: Service Account Exemption Strategies"
    },
    {
      "id": "q22",
      "domain": "network-security",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowflake administrator is reviewing a security concern: a user's RSA_PUBLIC_KEY was compromised. The user currently has both RSA_PUBLIC_KEY (compromised) and RSA_PUBLIC_KEY_2 (valid and in use by the application) set. What is the immediate action to revoke the compromised key without disrupting the running application?",
      "options": [
        { "id": "A", "text": "ALTER USER affected_user UNSET RSA_PUBLIC_KEY;" },
        { "id": "B", "text": "ALTER USER affected_user UNSET RSA_PUBLIC_KEY_2;" },
        { "id": "C", "text": "ALTER USER affected_user SET RSA_PUBLIC_KEY = NULL;" },
        { "id": "D", "text": "DROP USER affected_user; CREATE USER affected_user RSA_PUBLIC_KEY_2 = '<valid_key>';" }
      ],
      "correct": ["A"],
      "explanation": "Since RSA_PUBLIC_KEY is the compromised key and RSA_PUBLIC_KEY_2 is the valid key in use by the application, the correct action is to unset only RSA_PUBLIC_KEY. ALTER USER affected_user UNSET RSA_PUBLIC_KEY removes only the first key slot, leaving RSA_PUBLIC_KEY_2 intact and the application unaffected. Option B (UNSET RSA_PUBLIC_KEY_2) would remove the valid key, disrupting the application. Option C (SET RSA_PUBLIC_KEY = NULL) may not be valid syntax for key removal. Option D destroys the user account and all associated grants.",
      "reference": "Key Pair Authentication: Revoking a Compromised Key"
    },
    {
      "id": "q23",
      "domain": "storage-compute",
      "type": "single",
      "difficulty": "easy",
      "stem": "A warehouse is configured with MIN_CLUSTER_COUNT = 1 and MAX_CLUSTER_COUNT = 4, with SCALING_POLICY = STANDARD. When does Snowflake add additional clusters?",
      "options": [
        { "id": "A", "text": "When a single query exceeds a configurable execution time threshold" },
        { "id": "B", "text": "When queries are queuing and the system determines additional clusters would reduce wait times" },
        { "id": "C", "text": "At a fixed schedule defined by the administrator" },
        { "id": "D", "text": "Only when manually triggered using ALTER WAREHOUSE RESUME CLUSTER" }
      ],
      "correct": ["B"],
      "explanation": "In a multi-cluster warehouse, Snowflake's auto-scaling adds additional clusters when it detects query queuing — queries waiting for an available slot. With SCALING_POLICY = STANDARD, Snowflake scales out promptly when queuing is detected. SCALING_POLICY = ECONOMY waits longer before adding clusters (more cost-efficient but slower to scale). Adding clusters is not triggered by individual query execution time, is not scheduled, and does not require manual intervention when auto-scaling is configured.",
      "reference": "Multi-Cluster Warehouses: SCALING_POLICY"
    },
    {
      "id": "q24",
      "domain": "storage-compute",
      "type": "single",
      "difficulty": "medium",
      "stem": "An administrator wants to minimize idle warehouse costs for a warehouse used only during business hours (8 AM to 6 PM). The warehouse should not be running overnight. Which configuration is MOST appropriate?",
      "options": [
        { "id": "A", "text": "Set AUTO_SUSPEND = 60 and AUTO_RESUME = TRUE; schedule a task to resume at 8 AM and suspend at 6 PM" },
        { "id": "B", "text": "Set AUTO_SUSPEND = 60 and AUTO_RESUME = TRUE; rely on auto-suspend to handle overnight shutdown automatically" },
        { "id": "C", "text": "Set AUTO_SUSPEND = 28800 (8 hours) to ensure the warehouse stays running all day" },
        { "id": "D", "text": "Set INITIALLY_SUSPENDED = TRUE and manually start the warehouse each morning" }
      ],
      "correct": ["B"],
      "explanation": "With AUTO_SUSPEND = 60 seconds and AUTO_RESUME = TRUE, the warehouse automatically suspends 60 seconds after the last query completes (which would be shortly after the last business-hours query), and automatically resumes when users submit queries the next morning. This is the simplest and most cost-effective configuration for business-hours usage. Option A adds unnecessary operational complexity with scheduling tasks. AUTO_SUSPEND = 28800 (8 hours) means the warehouse would stay running for 8 hours after the last query — potentially running all night and wasting credits. INITIALLY_SUSPENDED with manual start (option D) is operationally burdensome.",
      "reference": "Warehouse Configuration: AUTO_SUSPEND for Cost Optimization"
    },
    {
      "id": "q25",
      "domain": "storage-compute",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A large table is showing degraded query performance after months of continuous INSERT operations. Clustering depth has increased significantly. Select TWO actions that can help restore clustering efficiency.",
      "options": [
        { "id": "A", "text": "Enable automatic clustering with ALTER TABLE orders CLUSTER BY (order_date, region);" },
        { "id": "B", "text": "Run ALTER TABLE orders RECLUSTER to manually trigger a one-time reclustering operation" },
        { "id": "C", "text": "Increase the warehouse size to compensate for the poor clustering" },
        { "id": "D", "text": "Drop and recreate the table with data loaded in sorted order by the clustering key" }
      ],
      "correct": ["A", "D"],
      "explanation": "Enabling automatic clustering (ALTER TABLE ... CLUSTER BY) tells Snowflake to continuously maintain clustering in the background — the service reclusters micro-partitions as DML operations degrade the clustering. Dropping and recreating the table with data loaded in the correct sort order achieves a perfectly clustered initial state (though this requires downtime and a full reload). ALTER TABLE orders RECLUSTER (option B) is not valid Snowflake syntax — Snowflake does not have a manual RECLUSTER command. When automatic clustering is enabled, Snowflake manages reclustering automatically. Increasing warehouse size (option C) may speed up queries marginally but does not fix the clustering problem.",
      "reference": "Automatic Clustering: Enabling and Maintenance"
    },
    {
      "id": "q26",
      "domain": "storage-compute",
      "type": "single",
      "difficulty": "hard",
      "stem": "A table has TIME_TRAVEL_RETENTION = 30 days. A user accidentally drops the table. After 25 days, the administrator discovers the dropped table. Can the table be recovered, and how?",
      "options": [
        { "id": "A", "text": "Yes, using UNDROP TABLE table_name; because the table is within the 30-day retention period" },
        { "id": "B", "text": "No, dropped tables cannot be recovered using Time Travel; only DML changes can be rolled back" },
        { "id": "C", "text": "Yes, using CREATE TABLE table_name CLONE table_name AT (OFFSET => -2160000);" },
        { "id": "D", "text": "No, because Time Travel only applies to table data modifications, not DROP TABLE operations" }
      ],
      "correct": ["A"],
      "explanation": "UNDROP TABLE is the correct command to recover a dropped table within its Time Travel retention period. When a table is dropped, Snowflake retains it in a 'dropped' state for the duration of DATA_RETENTION_TIME_IN_DAYS. Since 25 days is within the 30-day retention, UNDROP TABLE table_name will restore it. Time Travel applies to DROP operations as well as DML changes — options B and D are incorrect. Option C (cloning with a time offset) is a different technique for recovering historical data states, not for recovering a dropped table.",
      "reference": "Time Travel: UNDROP TABLE"
    },
    {
      "id": "q27",
      "domain": "audit-compliance",
      "type": "single",
      "difficulty": "medium",
      "stem": "A compliance officer needs to verify that all login attempts (both successful and failed) from external applications have been logged for the past 30 days. Which ACCOUNT_USAGE view and key columns should the administrator reference?",
      "options": [
        { "id": "A", "text": "SNOWFLAKE.ACCOUNT_USAGE.QUERY_HISTORY — columns: USER_NAME, START_TIME, EXECUTION_STATUS" },
        { "id": "B", "text": "SNOWFLAKE.ACCOUNT_USAGE.LOGIN_HISTORY — columns: USER_NAME, EVENT_TIMESTAMP, IS_SUCCESS, ERROR_MESSAGE, CLIENT_TYPE" },
        { "id": "C", "text": "SNOWFLAKE.ACCOUNT_USAGE.ACCESS_HISTORY — columns: USER_NAME, QUERY_START_TIME, OBJECTS_ACCESSED" },
        { "id": "D", "text": "SNOWFLAKE.ACCOUNT_USAGE.SESSIONS — columns: USER_NAME, CREATED_ON, CLIENT_APPLICATION_ID" }
      ],
      "correct": ["B"],
      "explanation": "LOGIN_HISTORY is the correct view for auditing authentication events. Key columns include USER_NAME (who attempted), EVENT_TIMESTAMP (when), IS_SUCCESS (whether login succeeded), ERROR_MESSAGE (failure reason for failed attempts), and CLIENT_TYPE/CLIENT_APPLICATION_ID (what application was used). This provides the complete login audit trail for both successes and failures. QUERY_HISTORY covers executed queries, not login events. ACCESS_HISTORY covers data object access, not login events. SESSIONS tracks active session information but not login attempt history.",
      "reference": "Account Usage: LOGIN_HISTORY"
    },
    {
      "id": "q28",
      "domain": "audit-compliance",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data governance team wants to track data lineage — specifically which downstream queries and users accessed data that originated from a source table PROD.SALES.TRANSACTIONS. Which ACCOUNT_USAGE view supports column-level lineage tracking?",
      "options": [
        { "id": "A", "text": "SNOWFLAKE.ACCOUNT_USAGE.QUERY_HISTORY joined with EXECUTION_PLAN" },
        { "id": "B", "text": "SNOWFLAKE.ACCOUNT_USAGE.ACCESS_HISTORY with DIRECT_OBJECTS_ACCESSED and BASE_OBJECTS_ACCESSED columns" },
        { "id": "C", "text": "SNOWFLAKE.ACCOUNT_USAGE.OBJECT_DEPENDENCIES" },
        { "id": "D", "text": "SNOWFLAKE.ACCOUNT_USAGE.POLICY_REFERENCES filtered by POLICY_KIND = 'LINEAGE'" }
      ],
      "correct": ["B"],
      "explanation": "ACCESS_HISTORY provides the most detailed lineage information in Snowflake. DIRECT_OBJECTS_ACCESSED shows the immediate objects a query accessed, while BASE_OBJECTS_ACCESSED shows the underlying base tables (even when queries went through views or other intermediate objects). Combined, these columns enable column-level data lineage tracking — you can trace which users accessed columns from a source table, even indirectly through views. QUERY_HISTORY contains query text but not structured lineage. OBJECT_DEPENDENCIES tracks static schema-level dependencies (view → table) but not runtime access lineage. POLICY_REFERENCES tracks policy assignments, not lineage.",
      "reference": "Account Usage: ACCESS_HISTORY for Data Lineage"
    },
    {
      "id": "q29",
      "domain": "access-control",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake administrator applies a masking policy to a column using:\nALTER TABLE employees ALTER COLUMN ssn SET MASKING POLICY ssn_mask;\nLater, the administrator needs to replace this policy with a new, updated policy. What is the correct approach?",
      "options": [
        { "id": "A", "text": "ALTER TABLE employees ALTER COLUMN ssn SET MASKING POLICY new_ssn_mask; (Directly replaces the existing policy)" },
        { "id": "B", "text": "ALTER TABLE employees ALTER COLUMN ssn UNSET MASKING POLICY; then ALTER TABLE employees ALTER COLUMN ssn SET MASKING POLICY new_ssn_mask;" },
        { "id": "C", "text": "DROP MASKING POLICY ssn_mask; then ALTER TABLE employees ALTER COLUMN ssn SET MASKING POLICY new_ssn_mask;" },
        { "id": "D", "text": "ALTER TABLE employees ALTER COLUMN ssn REPLACE MASKING POLICY ssn_mask WITH new_ssn_mask;" }
      ],
      "correct": ["A"],
      "explanation": "In Snowflake, you can directly SET a new masking policy on a column that already has one — the new SET replaces the existing policy assignment in a single atomic operation. This is the simplest approach. Option B (UNSET then SET) also works but requires two commands. Option C (dropping the old policy) would fail if the policy is still applied to any column — you would need to unset it first. Option D uses an invalid REPLACE syntax that does not exist in Snowflake.",
      "reference": "Dynamic Data Masking: Replacing Applied Policies"
    },
    {
      "id": "q30",
      "domain": "resource-monitoring",
      "type": "single",
      "difficulty": "hard",
      "stem": "An administrator creates a resource monitor: CREATE RESOURCE MONITOR dept_monitor WITH CREDIT_QUOTA = 1000 FREQUENCY = MONTHLY START_TIMESTAMP = IMMEDIATELY TRIGGERS ON 90 PERCENT DO SUSPEND ON 100 PERCENT DO SUSPEND_IMMEDIATELY; The monitor is assigned to PROD_WH. Three days before month end, the warehouse hits 100% and is suspended. The business needs the warehouse running urgently. What must the administrator do?",
      "options": [
        { "id": "A", "text": "Wait until the start of next month when the monthly quota resets automatically" },
        { "id": "B", "text": "Increase the CREDIT_QUOTA on the resource monitor, then resume the warehouse" },
        { "id": "C", "text": "Run ALTER WAREHOUSE PROD_WH RESUME — the warehouse can be manually resumed even when suspended by a resource monitor" },
        { "id": "D", "text": "Delete the resource monitor assignment, resume the warehouse, and reassign the monitor next month" }
      ],
      "correct": ["B"],
      "explanation": "When a warehouse is suspended by a resource monitor, it cannot be simply resumed with ALTER WAREHOUSE RESUME — the resource monitor will immediately re-suspend it because the quota is still exceeded. The correct approach is to first increase the CREDIT_QUOTA on the resource monitor (ALTER RESOURCE MONITOR dept_monitor SET CREDIT_QUOTA = 1200, for example), and then resume the warehouse. Option C is partially true in that ALTER WAREHOUSE RESUME will briefly resume it, but the monitor will immediately re-suspend it. Option A requires waiting until month end. Option D works operationally but is overly disruptive and removes monitoring entirely.",
      "reference": "Resource Monitors: Recovering from Suspension"
    }
  ]
}
