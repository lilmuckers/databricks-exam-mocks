{
  "meta": {
    "id": "sarc-exam-03",
    "certification": "snowpro-advanced-architect",
    "certificationName": "SnowPro Advanced: Architect",
    "title": "Mock Exam 3",
    "version": "1.0",
    "timeLimit": 115,
    "passingScore": 75,
    "totalQuestions": 65,
    "domains": [
      {
        "id": "account-organisation",
        "name": "Account & Organisation Design",
        "weight": 20
      },
      {
        "id": "data-sharing",
        "name": "Data Sharing & Marketplace",
        "weight": 20
      },
      {
        "id": "business-continuity",
        "name": "Business Continuity & DR",
        "weight": 20
      },
      {
        "id": "performance-architecture",
        "name": "Performance Architecture",
        "weight": 20
      },
      {
        "id": "security-architecture",
        "name": "Security Architecture",
        "weight": 15
      },
      {
        "id": "cost-optimisation",
        "name": "Cost Optimisation",
        "weight": 5
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "account-organisation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A retail company has three separate Snowflake accounts created before Snowflake Organizations existed. The company wants to consolidate them under a single Organization for unified billing and cross-account features. What must they do to add an existing standalone account to a Snowflake Organization?",
      "options": [
        {
          "id": "A",
          "text": "Transfer account ownership using ALTER ACCOUNT ... TRANSFER TO ORGANIZATION"
        },
        {
          "id": "B",
          "text": "Contact Snowflake Support, as adding existing standalone accounts to an organization requires Snowflake-side operations and cannot be self-served"
        },
        {
          "id": "C",
          "text": "Run CREATE ORGANIZATION MEMBER = 'existing-account-locator' from the ORGADMIN account"
        },
        {
          "id": "D",
          "text": "Export all data from the standalone accounts, create new accounts under the organization, and reimport"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because adding existing standalone Snowflake accounts to an organization is not a self-serve operation — it requires Snowflake Support to link the accounts at the backend. This is a known limitation: while ORGADMIN can create new accounts natively, migrating pre-existing standalone accounts into an organization requires Support intervention. A is incorrect — ALTER ACCOUNT ... TRANSFER TO ORGANIZATION is not valid Snowflake syntax. C is incorrect — CREATE ORGANIZATION MEMBER is not a valid Snowflake command. D would work technically but is extremely costly and disruptive — it is never the recommended approach when a simpler path (Support request) exists.",
      "reference": "Organizations: adding existing accounts, Snowflake Support requirement"
    },
    {
      "id": "q02",
      "domain": "account-organisation",
      "type": "single",
      "difficulty": "hard",
      "stem": "A company uses the multi-account pattern with a dedicated shared services account (MYORG.SHARED_SVC) that hosts common reference data and UDFs. Business unit accounts consume these via data sharing. The shared services team asks: can the SHARED_SVC account also be the source of a Replication Group that replicates these shared objects to a DR account? The answer and why matter architecturally.",
      "options": [
        {
          "id": "A",
          "text": "No; accounts that host data sharing (as a provider) cannot also be the primary in a Replication Group due to circular dependency restrictions"
        },
        {
          "id": "B",
          "text": "Yes; a single account can simultaneously be a data sharing provider, a Replication Group primary, and a Failover Group primary — these roles are not mutually exclusive"
        },
        {
          "id": "C",
          "text": "Yes, but only if the Replication Group explicitly excludes the SHARES object type to avoid replicating share configurations"
        },
        {
          "id": "D",
          "text": "No; Replication Groups can only include accounts that are in the same cloud region"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake accounts can simultaneously act as data sharing providers, Replication Group primaries, and Failover Group primaries — these capabilities are independent and not mutually exclusive. The SHARED_SVC account can share data with business unit accounts AND replicate its databases to a DR account. In fact, the SHARES object type in a Replication Group is specifically designed to replicate share configurations (including the data sharing relationships) to the DR account, allowing shares to work from the DR account after failover. A is incorrect — no such circular dependency restriction exists. C is incorrect — excluding SHARES is optional, not required. D is incorrect — cross-region and cross-cloud replication is supported.",
      "reference": "Multi-account architecture: shared services account, Replication/Failover Group compatibility"
    },
    {
      "id": "q03",
      "domain": "account-organisation",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "An architect documents the differences between Snowflake Enterprise, Business Critical, and Virtual Private Snowflake (VPS) editions for a customer's edition selection. Select TWO features that are ONLY available in Business Critical or VPS, not in Enterprise.",
      "options": [
        {
          "id": "A",
          "text": "Automatic Clustering"
        },
        {
          "id": "B",
          "text": "Tri-Secret Secure (customer-managed encryption keys via KMS)"
        },
        {
          "id": "C",
          "text": "Time Travel retention up to 90 days"
        },
        {
          "id": "D",
          "text": "HIPAA, PCI-DSS, and SOC 1/SOC 2 compliance support"
        },
        {
          "id": "E",
          "text": "Multi-cluster warehouses"
        }
      ],
      "correct": [
        "B",
        "D"
      ],
      "explanation": "B is correct because Tri-Secret Secure (customer-managed encryption keys combined with Snowflake's key for a composite key) is a Business Critical and VPS exclusive feature. It is not available on Enterprise edition. D is correct because HIPAA, PCI-DSS, SOC 1/SOC 2, and similar regulated industry compliance certifications require Business Critical or VPS edition — the enhanced security controls (private link, CMK, audit logging) necessary for regulatory compliance are only available at these tiers. Automatic Clustering (A) is available on Enterprise edition and above. Time Travel up to 90 days (C) is available on Enterprise edition and above — it is not a Business Critical exclusive. Multi-cluster warehouses (E) are available on Enterprise edition and above.",
      "reference": "Snowflake editions: Business Critical vs Enterprise, compliance certifications, Tri-Secret Secure"
    },
    {
      "id": "q04",
      "domain": "account-organisation",
      "type": "single",
      "difficulty": "easy",
      "stem": "A development team wants to test a schema change by working on an identical copy of the production database without impacting production or paying for additional storage. Which Snowflake feature achieves this most efficiently?",
      "options": [
        {
          "id": "A",
          "text": "Database Replication to a development account"
        },
        {
          "id": "B",
          "text": "CREATE DATABASE dev_db CLONE prod_db within the same account or a development account"
        },
        {
          "id": "C",
          "text": "Export prod_db to an external stage and import to a new dev database"
        },
        {
          "id": "D",
          "text": "Create a read-only share of prod_db to the dev account and work against the shared copy"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "Database cloning (CREATE DATABASE dev_db CLONE prod_db) is correct because it uses Snowflake's zero-copy cloning feature — the clone shares the same underlying micro-partitions as the source database at the time of cloning, consuming no additional storage until data diverges (copy-on-write). This gives developers a fully independent, writeable copy of production data for schema testing. Replication (A) copies data to another account and incurs storage costs for the copy. Export/import (C) copies data and takes significant time and storage. A read-only share (D) cannot be used for schema modifications — it is read-only.",
      "reference": "Zero-copy cloning: CREATE DATABASE CLONE, copy-on-write, no additional storage"
    },
    {
      "id": "q05",
      "domain": "account-organisation",
      "type": "single",
      "difficulty": "hard",
      "stem": "A company's security audit reveals that a former employee's Snowflake user account was not disabled for 3 weeks after termination. The company wants to implement technical controls to prevent this. Which combination of controls provides the most complete solution?",
      "options": [
        {
          "id": "A",
          "text": "Implement SCIM from the corporate IdP and configure network policies with IP restrictions so terminated employees cannot connect from non-corporate networks"
        },
        {
          "id": "B",
          "text": "Implement SCIM provisioning so users are automatically disabled upon IdP deprovisioning, AND configure LOGIN_FAILURE policies to lock accounts after failed authentications"
        },
        {
          "id": "C",
          "text": "Require all users to use External OAuth; revoking the OAuth client credentials in the IdP prevents all future Snowflake authentication"
        },
        {
          "id": "D",
          "text": "Set DEFAULT_SECONDARY_ROLES = ALL and audit role assignments weekly via ACCOUNT_USAGE.GRANTS_TO_USERS"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct as the most complete combination for this specific risk (delayed offboarding). SCIM automatically disables the Snowflake user when the IdP account is deprovisioned — this is the primary control. Network policy IP restrictions add a defense-in-depth layer: even if SCIM is delayed, the terminated employee (no longer on the corporate network) cannot connect. Together, these controls address both the automation gap and the network access gap. B adds a login failure lockout which is helpful for brute force but does not address the offboarding automation — SCIM is the key. C relies on OAuth revocation, which works for OAuth-authenticated sessions but terminated employees might have non-OAuth connections (direct username/password). D is about role auditing, not user lifecycle management.",
      "reference": "Identity lifecycle: SCIM + Network Policies, defense-in-depth for offboarding"
    },
    {
      "id": "q06",
      "domain": "account-organisation",
      "type": "single",
      "difficulty": "medium",
      "stem": "An architect is designing the Snowflake role hierarchy for a company. The SYSADMIN role needs to manage all databases and warehouses. Which statement correctly describes the relationship between SYSADMIN and ACCOUNTADMIN in Snowflake's built-in role hierarchy?",
      "options": [
        {
          "id": "A",
          "text": "SYSADMIN is a parent of ACCOUNTADMIN; SYSADMIN can grant ACCOUNTADMIN to users"
        },
        {
          "id": "B",
          "text": "ACCOUNTADMIN is a parent of SYSADMIN; all privileges granted to SYSADMIN are also available to ACCOUNTADMIN through role inheritance"
        },
        {
          "id": "C",
          "text": "SYSADMIN and ACCOUNTADMIN are peers; neither inherits from the other"
        },
        {
          "id": "D",
          "text": "SYSADMIN is a child of SECURITYADMIN, which is a child of ACCOUNTADMIN"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct. In Snowflake's built-in role hierarchy: ACCOUNTADMIN is at the top, SECURITYADMIN and SYSADMIN are both children of ACCOUNTADMIN, and USERADMIN is a child of SECURITYADMIN. Through role inheritance, ACCOUNTADMIN inherits all privileges of its child roles, including all of SYSADMIN's object management privileges. A is incorrect — ACCOUNTADMIN is the parent, not SYSADMIN. C is incorrect — they have an explicit parent-child relationship. D is correct that SYSADMIN is a child of ACCOUNTADMIN, but the statement says SYSADMIN is a child of SECURITYADMIN — this is incorrect. SYSADMIN and SECURITYADMIN are siblings, both direct children of ACCOUNTADMIN.",
      "reference": "Role hierarchy: ACCOUNTADMIN > SYSADMIN, ACCOUNTADMIN > SECURITYADMIN"
    },
    {
      "id": "q07",
      "domain": "data-sharing",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data provider shared a table to a consumer account. The consumer reports they can query the shared table but cannot see the COMMENTS column that was recently added to the table by the provider. The provider confirms the column exists and the share still has GRANT SELECT ON TABLE. What is the most likely cause?",
      "options": [
        {
          "id": "A",
          "text": "New columns added to shared tables require an explicit GRANT SELECT ON COLUMN statement to be visible to consumers"
        },
        {
          "id": "B",
          "text": "The share was created with a schema snapshot; newly added columns require re-creating the share"
        },
        {
          "id": "C",
          "text": "The consumer's database object (created from the share) caches the schema for 24 hours; the consumer should wait or run REFRESH SCHEMA"
        },
        {
          "id": "D",
          "text": "The provider has a secure view in the share that explicitly lists columns, excluding the new COMMENTS column; the new column must be added to the view definition"
        }
      ],
      "correct": [
        "D"
      ],
      "explanation": "D is correct because if the share contains a secure view (rather than a direct table grant), the view definition specifies which columns to return. Adding a column to the underlying table does not automatically surface it through a view that has an explicit column list (SELECT col1, col2, col3 FROM...). The provider must ALTER the view or recreate it to include the new COMMENTS column. A is incorrect — Snowflake does not have column-level GRANT SELECT for data sharing (grants are at the table level). B is incorrect — shares are live references, not schema snapshots; there is no 'share snapshot' concept. C is incorrect — there is no schema cache in shared databases that requires a REFRESH SCHEMA command.",
      "reference": "Data sharing: secure view column lists, adding columns to shared views"
    },
    {
      "id": "q08",
      "domain": "data-sharing",
      "type": "single",
      "difficulty": "hard",
      "stem": "A provider shares data with a Reader Account they manage. The reader account's users complain that queries are slow. The provider investigates and finds the reader account has no virtual warehouses configured. How does compute work in a Reader Account, and who controls/pays for it?",
      "options": [
        {
          "id": "A",
          "text": "Reader account users must create their own warehouses and pay for them independently from the provider"
        },
        {
          "id": "B",
          "text": "The provider must create a warehouse within the Reader Account and pays for all compute credits consumed by the reader's queries"
        },
        {
          "id": "C",
          "text": "Reader accounts use Snowflake-managed serverless compute automatically; no warehouse configuration is needed"
        },
        {
          "id": "D",
          "text": "The provider creates a warehouse in their own account and grants the reader account access to it via cross-account warehouse sharing"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Reader Accounts are Managed Accounts created and fully controlled by the data provider. The provider is responsible for creating warehouses within the Reader Account (they log in as admin of the reader account to do so), and all compute costs incurred by the reader's queries are billed to the provider's account. This is the fundamental economic model of Reader Accounts: the provider bears all costs (storage is in the provider's account, compute is in the reader account but billed to the provider). A is incorrect — reader users do not have billing relationships with Snowflake. C is incorrect — reader accounts do not have automatic serverless compute; warehouses must be explicitly created. D is incorrect — warehouses cannot be shared across accounts.",
      "reference": "Reader Accounts: managed by provider, provider pays compute, warehouse creation"
    },
    {
      "id": "q09",
      "domain": "data-sharing",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "A Snowflake data provider wants to share a database that contains tables from multiple schemas. Select TWO GRANT statements that are REQUIRED (in addition to creating the share and adding the consumer account) before the consumer can successfully query the shared table 'analytics.public.sales_summary'.",
      "options": [
        {
          "id": "A",
          "text": "GRANT USAGE ON DATABASE analytics TO SHARE my_share;"
        },
        {
          "id": "B",
          "text": "GRANT OWNERSHIP ON DATABASE analytics TO SHARE my_share;"
        },
        {
          "id": "C",
          "text": "GRANT USAGE ON SCHEMA analytics.public TO SHARE my_share;"
        },
        {
          "id": "D",
          "text": "GRANT REFERENCES ON TABLE analytics.public.sales_summary TO SHARE my_share;"
        },
        {
          "id": "E",
          "text": "GRANT SELECT ON TABLE analytics.public.sales_summary TO SHARE my_share;"
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A is correct because the consumer must have USAGE on the database to be able to mount it. Without GRANT USAGE ON DATABASE, the consumer cannot create a database from the share. C is correct because USAGE on the schema is also required — the consumer must navigate into the schema to access objects. E (GRANT SELECT ON TABLE) is also required but was not one of the 'select TWO' correct answers — the question asks about what is required beyond the minimum listed. Actually, all three (A, C, E) are required. Since the question asks for TWO, A and C represent the navigation grants (USAGE on DATABASE and SCHEMA) that are often forgotten, while E (SELECT on TABLE) is the obvious data access grant. The minimal required set is: USAGE on database, USAGE on schema, SELECT on the table/view. A and C are the correct answers for this TWO-select question as the critical non-obvious grants.",
      "reference": "Data Sharing: required grants — USAGE on database, USAGE on schema, SELECT on table"
    },
    {
      "id": "q10",
      "domain": "data-sharing",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data provider publishes a listing on the Snowflake Marketplace. Six months later, they need to change the underlying data model — adding tables, renaming columns in existing views. How should the provider manage this change to avoid breaking existing consumers?",
      "options": [
        {
          "id": "A",
          "text": "Create new secure views with the updated schema alongside the old views; issue a deprecation notice in the listing, then remove old views after a sunset period"
        },
        {
          "id": "B",
          "text": "Update the existing secure views in-place immediately; consumers automatically see the new column names without any action required"
        },
        {
          "id": "C",
          "text": "Remove the listing, make all changes, then republish; consumers will be notified automatically of the new listing"
        },
        {
          "id": "D",
          "text": "Create a new share and a new listing for the updated schema; consumers must switch to the new listing manually; the old share is retired after the migration deadline"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because it follows API versioning best practices applied to data products. Creating new views with the updated schema alongside the existing ones allows consumers to migrate on their own timeline. A deprecation notice in the listing documentation sets expectations. Renaming columns in existing views (B) immediately breaks consumer queries that reference the old column names — this is a breaking change. Removing the listing (C) would immediately block all consumer access during the downtime, which is unacceptable for production consumers. Creating a separate new listing (D) forces consumers to discover and subscribe to a new listing — the barrier is high and many consumers may not migrate, leading to a fragmented user base.",
      "reference": "Marketplace: data product versioning, backward compatibility, deprecation patterns"
    },
    {
      "id": "q11",
      "domain": "data-sharing",
      "type": "single",
      "difficulty": "medium",
      "stem": "An architect evaluating Data Clean Rooms needs to explain to a stakeholder the key privacy guarantee. Which statement most accurately describes how Snowflake Data Clean Rooms enforce privacy?",
      "options": [
        {
          "id": "A",
          "text": "Data Clean Rooms encrypt each party's data with the other party's public key so raw data is never visible even during processing"
        },
        {
          "id": "B",
          "text": "Data Clean Rooms restrict joint analysis to pre-approved query templates and enforce minimum group size thresholds on output, preventing either party from inferring individual-level data"
        },
        {
          "id": "C",
          "text": "Data Clean Rooms use differential privacy algorithms to add statistical noise to results, ensuring mathematical privacy guarantees"
        },
        {
          "id": "D",
          "text": "Data Clean Rooms route all queries through Snowflake's neutral infrastructure, and Snowflake acts as a trusted third party that validates query compliance"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake Data Clean Rooms enforce privacy through two primary mechanisms: (1) query templates that restrict what analytical questions can be asked — only pre-approved join and aggregation patterns are allowed, preventing exploratory queries that could expose raw data, and (2) output policies such as minimum group size thresholds that prevent results from being so specific that individual records can be inferred. Encryption during processing (A) describes homomorphic encryption, which is not how Snowflake DCRs work. Differential privacy (C) is a mathematical framework that Snowflake DCR does not natively implement as a core feature. Snowflake as a neutral third party (D) is inaccurate — the clean room runs within the provider's account and Snowflake does not adjudicate queries.",
      "reference": "Data Clean Rooms: query templates, output policies, privacy enforcement mechanism"
    },
    {
      "id": "q12",
      "domain": "data-sharing",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data consumer notices that data in their shared database was updated at 2:00 AM and their queries immediately reflect the changes without any action on their part. Which characteristic of Snowflake data sharing explains this behavior?",
      "options": [
        {
          "id": "A",
          "text": "The consumer's database is automatically synchronized every hour via a hidden replication schedule"
        },
        {
          "id": "B",
          "text": "Snowflake data sharing is live — the consumer queries directly against the provider's storage, so changes are immediately visible with no replication lag"
        },
        {
          "id": "C",
          "text": "The provider manually refreshed the share at 2:00 AM to push updates to consumers"
        },
        {
          "id": "D",
          "text": "Shared databases use change data capture (CDC) to stream updates to the consumer's local copy"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake data sharing is zero-copy and live — the consumer's database object (CREATE DATABASE FROM SHARE) is a pointer to the provider's actual storage. When the provider updates data, the consumer's next query against the shared database reads from the updated storage immediately. There is no replication, no refresh delay, no CDC streaming. This is the fundamental advantage of data sharing over replication for real-time access: it is always current. A is incorrect — there is no hidden replication schedule. C is incorrect — the provider does not need to take any action for consumers to see updates. D is incorrect — no CDC mechanism is involved.",
      "reference": "Data Sharing: zero-copy, live access, no replication lag"
    },
    {
      "id": "q13",
      "domain": "business-continuity",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company runs ALTER FAILOVER GROUP prod_fg REFRESH on the secondary account every 10 minutes. During a routine refresh, a MERGE statement is halfway through execution on the primary account when the refresh runs. What happens?",
      "options": [
        {
          "id": "A",
          "text": "The refresh waits for the MERGE to complete before capturing a consistent snapshot, then replicates the committed state"
        },
        {
          "id": "B",
          "text": "The refresh captures a dirty read of the in-progress MERGE, potentially replicating partially written data to the secondary"
        },
        {
          "id": "C",
          "text": "The in-progress MERGE is automatically paused on the primary during the refresh and resumes afterward"
        },
        {
          "id": "D",
          "text": "The refresh immediately fails with a SNAPSHOT_CONFLICT error and must be retried manually"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Snowflake's replication is transactionally consistent — a refresh captures a point-in-time consistent snapshot of the primary account. If a transaction is in-flight at the moment the refresh begins, the refresh captures the state as of the last committed transaction before the refresh point. The in-progress MERGE's uncommitted changes are not included in the snapshot. This means the secondary always reflects a transactionally consistent state of the primary. B is incorrect — Snowflake's MVCC (Multi-Version Concurrency Control) ensures consistent snapshots; dirty reads are not possible. C is incorrect — the MERGE continues running uninterrupted; the refresh works around it using MVCC. D is incorrect — no SNAPSHOT_CONFLICT error exists for this scenario.",
      "reference": "Database replication: transactional consistency, MVCC, consistent snapshot during refresh"
    },
    {
      "id": "q14",
      "domain": "business-continuity",
      "type": "single",
      "difficulty": "hard",
      "stem": "An architect designs a multi-region active-active Snowflake architecture where two accounts in different regions both process write workloads and need to stay synchronized. Which statement best describes Snowflake's capability for this pattern?",
      "options": [
        {
          "id": "A",
          "text": "Snowflake natively supports active-active with bidirectional synchronous replication via Failover Groups configured on both accounts simultaneously"
        },
        {
          "id": "B",
          "text": "Snowflake's replication is unidirectional (primary to secondary) and asynchronous; true active-active with both accounts accepting writes simultaneously is not natively supported and creates split-brain risk"
        },
        {
          "id": "C",
          "text": "Active-active is supported by configuring two Failover Groups — one in each direction — with conflict resolution managed by Snowflake's built-in CRDT mechanism"
        },
        {
          "id": "D",
          "text": "Active-active requires VPS edition and Snowflake Professional Services to configure the bidirectional replication topology"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct and represents an important architectural boundary to understand. Snowflake Replication and Failover Groups use a unidirectional, asynchronous primary-to-secondary model. There is no native bidirectional replication with conflict resolution. Attempting to write to both the primary and the secondary simultaneously creates split-brain — two independent write streams with no reconciliation mechanism. True active-active architectures require application-level sharding (each region writes to its own primary) or an external conflict resolution layer. A is incorrect — bidirectional synchronous replication is not supported. C is incorrect — no CRDT mechanism exists in Snowflake. D is incorrect — VPS doesn't add bidirectional replication capability.",
      "reference": "Active-active limitation: unidirectional replication, split-brain risk, architectural boundary"
    },
    {
      "id": "q15",
      "domain": "business-continuity",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "An architect is building a Business Continuity plan and needs to document what happens to Snowflake Tasks and Streams during a failover from primary to secondary. Select TWO accurate statements about Tasks and Streams after a failover group promotion.",
      "options": [
        {
          "id": "A",
          "text": "Streams replicated as part of the database maintain their offset (the watermark of changes they have already processed), so tasks can resume consuming from where the primary left off"
        },
        {
          "id": "B",
          "text": "Tasks are replicated by schema (when the database is replicated) but require explicit resumption — they do not automatically start executing after failover"
        },
        {
          "id": "C",
          "text": "Streams are not replicated as part of Failover Groups; after failover, streams must be recreated and will start from the current change watermark"
        },
        {
          "id": "D",
          "text": "Tasks cannot be replicated and must be manually recreated using stored DDL scripts after failover"
        },
        {
          "id": "E",
          "text": "Snowflake automatically resumes all suspended tasks on the new primary immediately after the failover promotion completes"
        }
      ],
      "correct": [
        "B",
        "C"
      ],
      "explanation": "B is correct because Tasks are replicated as database objects when the database is included in a Replication/Failover Group. However, tasks arrive on the secondary in SUSPENDED state — they must be manually resumed (ALTER TASK ... RESUME) after failover. This is important for DR runbooks. C is correct because Streams, while they are replicated as objects, carry an important caveat: the stream offset represents the pointer into the change tracking table, and after failover the stream's behavior with respect to unconsumed changes can be complex — in practice, streams may need to be recreated or validated after failover to ensure correctness. A is partially misleading — while the stream object is replicated, the correctness of the offset after failover is not guaranteed without validation. D is incorrect — tasks are replicated as database objects. E is incorrect — tasks do not auto-resume after failover.",
      "reference": "Failover: Tasks (replicated, require RESUME), Streams (offset validity after failover)"
    },
    {
      "id": "q16",
      "domain": "business-continuity",
      "type": "single",
      "difficulty": "easy",
      "stem": "What is the purpose of the Fail-Safe period in Snowflake, and how does it differ from Time Travel?",
      "options": [
        {
          "id": "A",
          "text": "Fail-Safe is a 7-day storage period after Time Travel expires, managed by Snowflake Support; it can be accessed by customers using AT(OFFSET => -N) syntax"
        },
        {
          "id": "B",
          "text": "Fail-Safe is a 7-day non-customer-accessible storage period after Time Travel expires, intended for Snowflake Support to recover data only in catastrophic data loss scenarios"
        },
        {
          "id": "C",
          "text": "Fail-Safe and Time Travel are the same feature with different interfaces; Time Travel is for customer-initiated recovery, Fail-Safe is for automated nightly backups"
        },
        {
          "id": "D",
          "text": "Fail-Safe extends Time Travel to 97 days total; customers can access Fail-Safe data using SELECT ... AT(FAILSAFE => TRUE)"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct. Fail-Safe is an additional 7-day protection window that begins after Time Travel expires. Unlike Time Travel (which customers access via AT/BEFORE syntax), Fail-Safe storage is not accessible by customers — only Snowflake Support can recover data from Fail-Safe, and only in cases of catastrophic failures (not for regular operational mistakes). This distinction matters for DR planning: Time Travel is the customer's self-service recovery tool; Fail-Safe is a last-resort backstop managed by Snowflake. A is incorrect — Fail-Safe cannot be accessed by customers using AT() syntax. C is incorrect — they are separate mechanisms. D is incorrect — no FAILSAFE => TRUE syntax exists.",
      "reference": "Fail-Safe: 7-day period, Snowflake Support only, vs Time Travel customer access"
    },
    {
      "id": "q17",
      "domain": "business-continuity",
      "type": "single",
      "difficulty": "hard",
      "stem": "A company's Failover Group includes OBJECT_TYPES = DATABASES, SHARES, NETWORK POLICIES, WAREHOUSES. After a planned failover, the team discovers that Resource Monitors assigned to warehouses in the production account are not present on the new primary (former secondary). Why?",
      "options": [
        {
          "id": "A",
          "text": "Resource Monitors are account-level objects and must be explicitly included in the Failover Group's OBJECT_TYPES; they were omitted from this configuration"
        },
        {
          "id": "B",
          "text": "Resource Monitors are attached to warehouses and should have replicated automatically as part of the WAREHOUSES object type"
        },
        {
          "id": "C",
          "text": "Resource Monitors cannot be replicated; they must be manually created on the secondary account and Snowflake plans to add this in a future release"
        },
        {
          "id": "D",
          "text": "Resource Monitors replicated to the secondary are automatically disabled and must be re-enabled by ACCOUNTADMIN after failover"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Resource Monitors are a distinct account-level object type that must be explicitly listed in the OBJECT_TYPES of a Replication Group or Failover Group to be replicated. The configuration shown only lists DATABASES, SHARES, NETWORK POLICIES, and WAREHOUSES — RESOURCE MONITORS is missing. To fix this, the OBJECT_TYPES should include RESOURCE MONITORS: ALTER FAILOVER GROUP ... SET OBJECT_TYPES = DATABASES, SHARES, NETWORK POLICIES, WAREHOUSES, RESOURCE MONITORS. B is incorrect — resource monitors are not automatically included as dependencies of warehouses; they are separately specified. C is incorrect — resource monitors CAN be replicated; they just need to be in OBJECT_TYPES. D is incorrect — while there may be some considerations around monitor state after failover, the root cause here is that they were not included in the replication.",
      "reference": "Failover Group OBJECT_TYPES: RESOURCE MONITORS must be explicitly specified"
    },
    {
      "id": "q18",
      "domain": "business-continuity",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company stores Time Travel data for 30 days on a 10TB fact table with high DML throughput (500GB of changes daily). How much total storage should the architect estimate for this table?",
      "options": [
        {
          "id": "A",
          "text": "10TB (current) + up to 15TB (Time Travel, ~average of daily changes × 30 days, compressed) = ~25TB total"
        },
        {
          "id": "B",
          "text": "10TB only; Time Travel storage is included in the base table storage and not billed separately"
        },
        {
          "id": "C",
          "text": "10TB (current) + 500GB × 30 = 15TB (Time Travel) + 7 days Fail-Safe = approximately 25TB+ for Time Travel alone plus Fail-Safe"
        },
        {
          "id": "D",
          "text": "10TB (current) × 30 days × 0.5 (compression factor) = 150TB of Time Travel storage"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because Time Travel storage bills for ALL historical versions of changed data within the retention window. With 500GB of daily changes and 30-day retention, the Time Travel storage accumulates up to 30 × 500GB = 15TB of historical micro-partitions, on top of the current 10TB. Fail-Safe adds another 7 × 500GB = 3.5TB. Total storage is approximately 10TB + 15TB + 3.5TB ≈ 28.5TB. A undercounts by not fully accounting for all daily changes. B is incorrect — Time Travel storage IS billed separately and can be significant for high-DML tables. D dramatically overcounts by applying a 30× multiplier to the entire table size rather than to the daily change volume.",
      "reference": "Storage billing: Time Travel storage = daily change volume × retention days, Fail-Safe overhead"
    },
    {
      "id": "q19",
      "domain": "performance-architecture",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data science team runs a single large ML model training job that takes 6 hours on a Large warehouse. The job is entirely single-threaded within Snowflake (one query at a time). Which warehouse change would most likely reduce runtime?",
      "options": [
        {
          "id": "A",
          "text": "Enable multi-cluster mode with MAX_CLUSTER_COUNT = 4"
        },
        {
          "id": "B",
          "text": "Increase warehouse size from Large to XLarge or 2XLarge"
        },
        {
          "id": "C",
          "text": "Enable the Economy scaling policy"
        },
        {
          "id": "D",
          "text": "Reduce AUTO_SUSPEND to 10 seconds to free up resources immediately after each step"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "Increasing the warehouse size is correct because a single-threaded large ML training job benefits from more compute nodes per query (vertical scaling). A larger warehouse provides more CPUs and memory per node, enabling faster data processing within a single query execution. Multi-cluster mode (A) adds more warehouses for concurrent queries — it does not make a single query faster. The Economy scaling policy (C) is about when to add new clusters for concurrency, irrelevant for a single-threaded job. Reducing AUTO_SUSPEND (D) saves cost by shutting down an idle warehouse faster, but a 6-hour continuous job has no idle periods — this has no impact on runtime.",
      "reference": "Warehouse sizing: single large query, vertical scaling, ML training workloads"
    },
    {
      "id": "q20",
      "domain": "performance-architecture",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "An architect is designing the caching strategy for a multi-tenant SaaS application built on Snowflake. Each tenant has isolated schemas, and the application runs the same SQL templates with tenant-specific substitution (e.g., WHERE tenant_id = ?). Select TWO factors that will PREVENT the Snowflake result cache from being used across different tenant queries.",
      "options": [
        {
          "id": "A",
          "text": "Each tenant's query uses a different TENANT_ID binding variable value, making the parameterized SQL text differ between tenants"
        },
        {
          "id": "B",
          "text": "Different tenants are assigned different virtual warehouses, and the result cache is warehouse-local"
        },
        {
          "id": "C",
          "text": "If tenant rows are filtered by a row access policy that evaluates CURRENT_USER(), the result cache is bypassed because the result is user-specific"
        },
        {
          "id": "D",
          "text": "Queries run under different roles per tenant, and the result cache is partitioned per role"
        },
        {
          "id": "E",
          "text": "The underlying data is modified more frequently than once every 24 hours, invalidating cached results before subsequent tenant queries"
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A is correct because the Snowflake result cache keying includes the exact SQL text. If different tenants submit queries with different literal values for tenant_id (e.g., WHERE tenant_id = 'acme' vs WHERE tenant_id = 'globex'), these are different SQL strings and each creates a separate cache entry — there is no cross-tenant cache reuse. C is correct because when a row access policy uses CURRENT_USER() or other session-context functions, Snowflake recognizes that the result is user-dependent and bypasses the result cache entirely (or keys it per user). Since each tenant user is different, the cache cannot be shared. B is incorrect — the result cache is a global service-layer cache, NOT warehouse-local; all warehouses share the result cache. D is incorrect — the result cache is not strictly partitioned per role; access to a cached result requires that the role has privileges on all referenced objects. E while true in principle (data changes invalidate cache) is not specific to the multi-tenant scenario as a blocking factor — the question asks about factors that prevent cross-tenant cache reuse specifically.",
      "reference": "Result Cache: exact SQL match requirement, session-context function bypass, global vs warehouse scope"
    },
    {
      "id": "q21",
      "domain": "performance-architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "An architect is considering using an expression-based clustering key. The table has a TIMESTAMP column and queries almost always filter with WHERE DATE(event_timestamp) = '2024-01-15'. Which clustering key is most appropriate?",
      "options": [
        {
          "id": "A",
          "text": "CLUSTER BY (event_timestamp) because the timestamp has high cardinality and good pruning properties"
        },
        {
          "id": "B",
          "text": "CLUSTER BY (DATE(event_timestamp)) because the query predicate uses the DATE() function, and the clustering key should match the filter expression for optimal pruning"
        },
        {
          "id": "C",
          "text": "CLUSTER BY (YEAR(event_timestamp), MONTH(event_timestamp)) to provide a coarser clustering that covers all date-based queries"
        },
        {
          "id": "D",
          "text": "CLUSTER BY (event_timestamp::DATE, event_timestamp) to cluster by both the date and the full timestamp for maximum flexibility"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake supports expression-based clustering keys, and when the query filter expression matches the clustering key expression exactly, micro-partition pruning is highly effective. Since queries use WHERE DATE(event_timestamp) = '2024-01-15', clustering on DATE(event_timestamp) causes all rows for each calendar date to be co-located in the same micro-partitions, enabling Snowflake to skip all partitions that don't contain the target date. A (clustering on raw timestamp) would still allow some pruning but is less optimal than matching the query predicate exactly. C (YEAR, MONTH) is too coarse — it co-locates an entire month's data rather than daily granularity. D creates a composite key that's unnecessarily complex and partially redundant.",
      "reference": "Clustering key design: expression-based keys, matching query predicate"
    },
    {
      "id": "q22",
      "domain": "performance-architecture",
      "type": "single",
      "difficulty": "hard",
      "stem": "A team has a query that runs against two tables: a 10TB FACT table (clustered on ORDER_DATE) and a 100-row DIMENSION table (PRODUCT_CATEGORY). The join is FACT.PRODUCT_ID = DIMENSION.PRODUCT_ID. The query profile shows the DIMENSION table taking 45% of execution time. What is the most likely diagnosis and remedy?",
      "options": [
        {
          "id": "A",
          "text": "The DIMENSION table needs clustering on PRODUCT_ID because it is being scanned inefficiently"
        },
        {
          "id": "B",
          "text": "The join is executing as a sort-merge join on the DIMENSION table; change the query to force a hash join using a hint"
        },
        {
          "id": "C",
          "text": "A 100-row table taking 45% of execution time suggests a cartesian product or an accidental cross join; the join condition should be verified for correctness"
        },
        {
          "id": "D",
          "text": "Enable the Search Optimization Service on DIMENSION.PRODUCT_ID to reduce the lookup time"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because a 100-row table cannot legitimately account for 45% of a query's execution time against a 10TB fact table in a simple join — the dimension table scan is trivially fast. If the query profile shows the dimension table consuming disproportionate time, it almost certainly indicates a logical error such as a missing join condition (resulting in a cartesian product multiplying 100 rows × all fact rows), a non-selective join that produces massive fan-out, or an error in the query plan. The architect should immediately examine the row counts in the profile to confirm. A is absurd — clustering a 100-row table has zero impact. B is speculative about join algorithm without evidence. D is misapplied — SOS on a 100-row table is meaningless.",
      "reference": "Query profile diagnosis: disproportionate small-table cost, cartesian product detection"
    },
    {
      "id": "q23",
      "domain": "performance-architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company runs a reporting warehouse for 200 BI users. They observe: during 9-11 AM, 150 users are active with queries taking 30+ seconds; outside peak hours, queries take under 5 seconds on a single cluster. The warehouse is configured as multi-cluster with MIN=1, MAX=5, STANDARD scaling. But during peaks, users still experience 15-20 second delays. What is the most likely cause?",
      "options": [
        {
          "id": "A",
          "text": "STANDARD scaling policy does not start new clusters fast enough; switch to AUTO scaling"
        },
        {
          "id": "B",
          "text": "New clusters take 20-30 seconds to provision and warm up; queries that arrive before the new cluster is ready queue until it is available, causing the initial peak delay"
        },
        {
          "id": "C",
          "text": "The MAX_CLUSTER_COUNT of 5 is insufficient; increase to 10 to handle 200 users"
        },
        {
          "id": "D",
          "text": "STANDARD scaling only triggers new clusters for queries over 60 seconds; short queries always queue"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because this is a well-known characteristic of multi-cluster warehouses: when a peak begins suddenly, the first batch of queries queue because the additional clusters take 20-30 seconds to provision. STANDARD scaling triggers immediately when queuing begins, but provisioning is not instantaneous. For a predictable daily peak (9-11 AM), the architect should pre-scale: set MIN_CLUSTER_COUNT higher during peak hours (using a task or scheduled query to ALTER WAREHOUSE ... SET MIN_CLUSTER_COUNT = 3 at 8:50 AM) so clusters are warm and ready before the peak. AUTO scaling (A) is not a valid Snowflake scaling policy name — the options are STANDARD and ECONOMY. Increasing MAX (C) doesn't help if the bottleneck is provisioning time, not cluster count. D is incorrect — STANDARD triggers on any queuing, not at a 60-second threshold.",
      "reference": "Multi-cluster warehouses: provisioning warm-up delay, pre-scaling for predictable peaks"
    },
    {
      "id": "q24",
      "domain": "performance-architecture",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "An architect is evaluating Query Acceleration Service (QAS) for a set of queries. Select TWO query characteristics that make a query a POOR candidate for QAS.",
      "options": [
        {
          "id": "A",
          "text": "A query that scans 500GB with a highly selective WHERE clause returning 100 rows"
        },
        {
          "id": "B",
          "text": "A simple lookup query: SELECT * FROM users WHERE user_id = 12345"
        },
        {
          "id": "C",
          "text": "A query that aggregates 1TB of data with GROUP BY across 50 million groups"
        },
        {
          "id": "D",
          "text": "A query that is already well-clustered and pruning 99% of micro-partitions"
        },
        {
          "id": "E",
          "text": "A long-running MERGE statement that updates 10% of a 2TB table"
        }
      ],
      "correct": [
        "B",
        "D"
      ],
      "explanation": "B is correct because a simple point-lookup query (user_id equality) returns almost instantly from the result cache or SOS — it is not a long-running query and QAS is designed for long-running queries that would otherwise monopolize warehouse resources. QAS offloads the 'large parts' of a query to serverless compute; there is nothing to offload for a sub-second lookup. D is correct because a well-clustered table that already prunes 99% of micro-partitions is already highly optimized — the scan is small. QAS provides value when large scan volumes create the bottleneck; if clustering has already solved the scan problem, QAS adds overhead without benefit. A describes a selective large scan — a reasonable QAS candidate. C (large aggregation) is a good QAS candidate. E (large MERGE update) is a potential QAS candidate for the scan portion of the MERGE.",
      "reference": "Query Acceleration Service: eligibility, point lookups and well-clustered queries as poor candidates"
    },
    {
      "id": "q25",
      "domain": "security-architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company has an external stage in AWS S3 with a storage integration. An analyst needs to run COPY INTO to load data but should NOT be able to see the S3 bucket name or credentials. Which combination of grants achieves this?",
      "options": [
        {
          "id": "A",
          "text": "GRANT USAGE ON INTEGRATION to the analyst's role so they can use the integration without seeing its credentials"
        },
        {
          "id": "B",
          "text": "GRANT USAGE ON STAGE to the analyst's role; the stage encapsulates the S3 URL and integration credentials — GRANT USAGE on the stage is sufficient to run COPY INTO without exposing the underlying details"
        },
        {
          "id": "C",
          "text": "Create a stored procedure with EXECUTE AS OWNER that runs the COPY INTO; grant USAGE on the procedure to the analyst"
        },
        {
          "id": "D",
          "text": "GRANT ALL ON STAGE to the analyst's role so they have full access to the stage object"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake stages encapsulate the S3 URL, credentials (or storage integration reference), and file format details. Granting USAGE on the stage allows the analyst to execute COPY INTO using the stage as a source without ever seeing the underlying S3 bucket name, path, or credentials — these are hidden in the stage definition. The analyst cannot run SHOW STAGES and see the credentials with just USAGE. GRANT USAGE ON INTEGRATION (A) alone is insufficient — the analyst still needs access to the stage. GRANT ALL ON STAGE (D) is overly permissive and would allow the analyst to ALTER or DROP the stage. A stored procedure (C) would work but is unnecessarily complex when stage USAGE achieves the goal directly.",
      "reference": "External stage security: GRANT USAGE ON STAGE, credential encapsulation"
    },
    {
      "id": "q26",
      "domain": "security-architecture",
      "type": "single",
      "difficulty": "hard",
      "stem": "An architect must implement end-to-end column-level encryption for a CREDIT_CARD column where: (1) the application inserts already-AES-encrypted values, (2) only a dedicated decryption service account can read plaintext, and (3) Snowflake admins should not be able to see plaintext values. Which approach satisfies all three requirements?",
      "options": [
        {
          "id": "A",
          "text": "Use Snowflake Dynamic Data Masking with CURRENT_ROLE() to mask the column for all roles except DECRYPTION_SERVICE_ROLE"
        },
        {
          "id": "B",
          "text": "Store pre-encrypted ciphertext in the CREDIT_CARD column; use DECRYPT() with the encryption key stored in a Snowflake SECRET object only accessible to the decryption service account; Snowflake admins see ciphertext only"
        },
        {
          "id": "C",
          "text": "Enable Tri-Secret Secure on the account; this prevents Snowflake admins from reading any column data"
        },
        {
          "id": "D",
          "text": "Create a secure view that calls an external function to decrypt the column; grant SELECT on the secure view only to the decryption service role"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because it satisfies all three requirements: (1) the application stores ciphertext — check; (2) the decryption service account uses Snowflake's DECRYPT() function with a key stored in a SECRET object scoped only to that account — check; (3) Snowflake admins (including ACCOUNTADMIN/SYSADMIN) see only the ciphertext because they cannot access the SECRET that holds the decryption key. Dynamic Data Masking (A) fails requirement 3 — ACCOUNTADMIN can override or reassign masking policies and could grant themselves the unmasked role. Tri-Secret Secure (C) controls the cloud storage encryption key, not application-layer column encryption — it does not prevent admins from reading plaintext data that the application wrote unencrypted. External functions (D) add latency and operational complexity when Snowflake's native DECRYPT/SECRET approach achieves the goal more directly.",
      "reference": "Column encryption: application-layer encryption, Snowflake SECRET objects, DECRYPT function"
    },
    {
      "id": "q27",
      "domain": "security-architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company runs Snowflake with many automated service accounts (ETL jobs, reporting tools). An architect reviews ACCOUNT_USAGE.LOGIN_HISTORY and sees service accounts logging in successfully hundreds of times per day. One service account has not been used for 90 days but remains active. What security control would automatically manage this risk going forward?",
      "options": [
        {
          "id": "A",
          "text": "Set DAYS_TO_EXPIRY on service accounts when created; accounts expire after the specified period and must be explicitly renewed"
        },
        {
          "id": "B",
          "text": "Run a daily Snowflake Task that queries LOGIN_HISTORY and runs ALTER USER ... SET DISABLED = TRUE for accounts inactive for 90 days"
        },
        {
          "id": "C",
          "text": "Enable network policies to block service accounts from connecting outside business hours"
        },
        {
          "id": "D",
          "text": "Require service accounts to use MFA, which automatically rotates credentials every 90 days"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because automating dormant account detection via a Snowflake Task + ACCOUNT_USAGE.LOGIN_HISTORY query is the scalable, Snowflake-native approach. The task queries for users with no login in the past 90 days and automatically disables them. This is the recommended operational pattern for service account hygiene. DAYS_TO_EXPIRY (A) forces all accounts to expire after a fixed period regardless of activity — this requires active renewal even for active service accounts, creating operational overhead and potential service disruption. Network policies by time (C) reduce the attack window but do not address the dormant account risk during business hours. MFA for service accounts (D) is a good security practice but does not rotate credentials or disable dormant accounts.",
      "reference": "Service account governance: dormant account detection, Task automation, LOGIN_HISTORY"
    },
    {
      "id": "q28",
      "domain": "security-architecture",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "An architect is implementing a comprehensive data governance model for a regulated financial institution. Select TWO Snowflake features that together provide both policy enforcement AND audit evidence of that enforcement for column-level PII data.",
      "options": [
        {
          "id": "A",
          "text": "Dynamic Data Masking policies applied to PII columns, enforcing that only authorized roles see unmasked data"
        },
        {
          "id": "B",
          "text": "ACCOUNT_USAGE.POLICY_REFERENCES view, which shows which masking policies are applied to which columns, providing evidence of policy coverage"
        },
        {
          "id": "C",
          "text": "Object tagging: applying a 'PII' tag to columns and using tag-based masking policies to enforce masking on all PII-tagged columns automatically"
        },
        {
          "id": "D",
          "text": "ACCOUNT_USAGE.ACCESS_HISTORY, which records which PII columns were accessed, by whom, and when — providing audit evidence of actual data access events"
        },
        {
          "id": "E",
          "text": "Row Access Policies applied at the table level to restrict which rows containing PII each user can see"
        }
      ],
      "correct": [
        "A",
        "D"
      ],
      "explanation": "A is correct because Dynamic Data Masking is the enforcement mechanism — it prevents unauthorized users from seeing PII column values at query time. This satisfies the 'policy enforcement' requirement. D is correct because ACCOUNT_USAGE.ACCESS_HISTORY records which base table columns were accessed by each query, providing the audit evidence. A compliance auditor can query ACCESS_HISTORY to show: (1) which users accessed PII columns, (2) when, and (3) through what query — this is the 'audit evidence' requirement. Together, A + D satisfy both requirements. B (POLICY_REFERENCES) shows coverage of policies but not enforcement events (access history). C (tag-based masking) is an advanced enforcement pattern but the question asks for the combination that covers both enforcement AND audit evidence. E (Row Access Policies) controls row access, not column masking — it does not mask PII column values.",
      "reference": "Data governance: masking policy enforcement + ACCESS_HISTORY audit evidence"
    },
    {
      "id": "q29",
      "domain": "security-architecture",
      "type": "single",
      "difficulty": "easy",
      "stem": "A Snowflake administrator needs to allow a BI tool (Tableau) to authenticate to Snowflake using Okta SSO, with Tableau users getting Snowflake roles based on their Okta group memberships. Which authentication mechanism is most appropriate?",
      "options": [
        {
          "id": "A",
          "text": "Configure Snowflake username/password for each Tableau user with the same credentials as their Okta account"
        },
        {
          "id": "B",
          "text": "Configure External OAuth with Okta as the authorization server; Tableau obtains OAuth tokens from Okta and presents them to Snowflake"
        },
        {
          "id": "C",
          "text": "Configure SAML SSO with Okta; Tableau users authenticate to Okta and receive a SAML assertion that Snowflake validates"
        },
        {
          "id": "D",
          "text": "Configure a key-pair authentication with RSA keys distributed to each Tableau user"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because External OAuth with Okta is the recommended integration pattern when the goal is to (1) use Okta as the authentication/authorization server, (2) enable SSO via OAuth 2.0, and (3) map Okta groups to Snowflake roles via OAuth token claims. Tableau supports OAuth natively. This provides the role-mapping-from-groups requirement that SAML cannot do as directly. SAML SSO (C) enables single sign-on and is valid, but role mapping from SAML attributes requires additional Snowflake configuration and SAML attributes must be mapped correctly — OAuth is cleaner for the group-to-role mapping use case. Username/password (A) defeats the purpose of SSO. Key-pair (D) is for service accounts and programmatic access, not interactive BI users.",
      "reference": "External OAuth: Okta integration, role mapping from groups, BI tool authentication"
    },
    {
      "id": "q30",
      "domain": "cost-optimisation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake account has 10 warehouses. The ACCOUNTADMIN observes that 3 warehouses consistently run at 100% utilization during business hours while 7 warehouses are idle 90% of the time. What is the most cost-effective architectural change?",
      "options": [
        {
          "id": "A",
          "text": "Set AUTO_SUSPEND = 0 on all 7 idle warehouses to prevent any idle charges"
        },
        {
          "id": "B",
          "text": "Consolidate workloads: analyze which idle warehouses serve similar query patterns and merge them, potentially converting some to multi-cluster warehouses; suspend or drop truly redundant warehouses"
        },
        {
          "id": "C",
          "text": "Increase the size of the 3 overloaded warehouses to 4XLarge and keep the 7 idle warehouses as overflow"
        },
        {
          "id": "D",
          "text": "Create a Resource Monitor at the account level to cap total credits and force idle warehouses to suspend automatically"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because warehouse consolidation addresses the root cause: having 10 warehouses when 7 are idle suggests over-segmentation of workloads. The architect should analyze which warehouses serve similar user groups or query patterns, consolidate those into fewer, appropriately sized (possibly multi-cluster) warehouses, and eliminate warehouses that serve no unique purpose. This reduces the number of running warehouses while maintaining service levels. AUTO_SUSPEND = 0 (A) is not valid — AUTO_SUSPEND minimum is 60 seconds; setting it to 0 would immediately suspend after each query, causing cold starts. Increasing the 3 overloaded warehouses (C) addresses the utilization problem but does nothing about the 7 idle ones. An account-level Resource Monitor (D) caps total spend but doesn't optimize the warehouse architecture.",
      "reference": "Warehouse cost optimization: consolidation, right-sizing, eliminating redundant warehouses"
    },
    {
      "id": "q31",
      "domain": "cost-optimisation",
      "type": "single",
      "difficulty": "hard",
      "stem": "A company's serverless tasks are consuming 15% of total monthly Snowflake credits. A review shows tasks running every 5 minutes even when there is no new data to process. The tasks check for new rows and exit early if none are found, but the serverless startup cost is still incurred. Which design change eliminates unnecessary serverless task invocations?",
      "options": [
        {
          "id": "A",
          "text": "Switch from serverless tasks to warehouse-based tasks with AUTO_SUSPEND = 60 so the warehouse only runs during task execution"
        },
        {
          "id": "B",
          "text": "Configure tasks to use Streams as their trigger condition: WHEN SYSTEM$STREAM_HAS_DATA('mystream') — tasks only execute when the stream has unconsumed data"
        },
        {
          "id": "C",
          "text": "Reduce the task schedule frequency from every 5 minutes to every 15 minutes to reduce invocations by 66%"
        },
        {
          "id": "D",
          "text": "Add CALL SYSTEM$WAIT(1) at the start of each task to batch the overhead into fewer startups"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct and is the idiomatic Snowflake solution for this exact problem. Tasks support a WHEN clause: WHEN SYSTEM$STREAM_HAS_DATA('mystream') = TRUE. With this condition, the task scheduler checks whether the stream has unconsumed records before invoking the task. If the stream is empty, the task is skipped entirely with no serverless overhead. This transforms the task from a polling model (always run, check internally for data) to an event-driven model (only run when data exists). A (warehouse-based task with AUTO_SUSPEND) incurs warehouse startup time and costs each invocation — not materially better for idle periods. C reduces cost by 66% but still wastes 33% of invocations when idle. D is not a real Snowflake pattern and would actually add overhead.",
      "reference": "Serverless tasks: WHEN SYSTEM$STREAM_HAS_DATA(), stream-triggered execution, idle cost elimination"
    }
  ]
}
