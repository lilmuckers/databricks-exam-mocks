{
  "meta": {
    "id": "spc-exam-02",
    "certification": "snowpro-core",
    "certificationName": "SnowPro Core Certification",
    "title": "SF-SPC-2: Data Loading, Transformation and Time Travel",
    "version": "1.1",
    "timeLimit": 115,
    "passingScore": 75,
    "totalQuestions": 100,
    "domains": [
      {
        "id": "architecture",
        "name": "Snowflake Data Cloud Features & Architecture",
        "weight": 25
      },
      {
        "id": "security",
        "name": "Account Access and Security",
        "weight": 15
      },
      {
        "id": "performance",
        "name": "Performance Concepts",
        "weight": 15
      },
      {
        "id": "data-loading",
        "name": "Data Loading and Unloading",
        "weight": 10
      },
      {
        "id": "data-transformation",
        "name": "Data Transformations",
        "weight": 20
      },
      {
        "id": "data-protection",
        "name": "Data Protection and Data Sharing",
        "weight": 15
      }
    ],
    "difficulty": "medium",
    "status": "available"
  },
  "questions": [
    {
      "id": "q01",
      "domain": "architecture",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which cloud object storage service does Snowflake use as its storage layer when deployed on AWS?",
      "options": [
        {
          "id": "A",
          "text": "Amazon EBS (Elastic Block Store)"
        },
        {
          "id": "B",
          "text": "Amazon S3 (Simple Storage Service)"
        },
        {
          "id": "C",
          "text": "Amazon EFS (Elastic File System)"
        },
        {
          "id": "D",
          "text": "Amazon Glacier"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake uses Amazon S3 as its underlying object storage on AWS. Data is stored as compressed, columnar micro-partitions in S3. A is wrong because EBS is block storage attached to EC2 instances and is not used as Snowflake's data layer. C is wrong because EFS is a file system service not used by Snowflake's storage layer. D is wrong because Glacier is archival storage and not the active storage layer used by Snowflake.",
      "reference": "Snowflake Architecture — Storage Layer on AWS"
    },
    {
      "id": "q02",
      "domain": "architecture",
      "type": "single",
      "difficulty": "easy",
      "stem": "A Snowflake virtual warehouse is set to auto-suspend after 5 minutes of inactivity and auto-resume is enabled. A query is submitted when the warehouse is suspended. What happens?",
      "options": [
        {
          "id": "A",
          "text": "The query fails with an error because the warehouse is suspended."
        },
        {
          "id": "B",
          "text": "The query is queued for up to 24 hours until the warehouse is manually resumed."
        },
        {
          "id": "C",
          "text": "The warehouse automatically resumes, the query executes, and credits are consumed."
        },
        {
          "id": "D",
          "text": "The query executes against the Cloud Services layer without consuming warehouse credits."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because with auto-resume enabled, Snowflake automatically starts the warehouse when a query is submitted, the query executes, and credits are charged for the warehouse uptime. A is wrong because auto-resume prevents query failures from a suspended state. B is wrong because there is no 24-hour queuing behavior for suspended warehouses — auto-resume handles this transparently. D is wrong because most queries require the virtual warehouse compute layer, not just the Cloud Services layer.",
      "reference": "Snowflake Architecture — Auto-Suspend and Auto-Resume"
    },
    {
      "id": "q03",
      "domain": "architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "Which statement CORRECTLY describes how Snowflake's compute and storage layers interact?",
      "options": [
        {
          "id": "A",
          "text": "Each virtual warehouse has dedicated storage that it owns and manages."
        },
        {
          "id": "B",
          "text": "Multiple virtual warehouses can read from and write to the same storage layer concurrently without contention."
        },
        {
          "id": "C",
          "text": "Only one virtual warehouse can access the storage layer at a time to prevent data corruption."
        },
        {
          "id": "D",
          "text": "The storage layer is co-located with the compute layer to minimize network latency."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake's architecture separates storage and compute, allowing multiple virtual warehouses to concurrently access the same shared storage layer. This is a core architectural advantage — different workloads (ETL, reporting, ad-hoc) can run on separate warehouses without contention. A is wrong because warehouses do not own dedicated storage; they all share the centralized storage layer. C is wrong because Snowflake explicitly allows concurrent multi-warehouse access to storage. D is wrong because compute and storage are separate — compute nodes read from object storage (S3/Azure/GCS) over the network.",
      "reference": "Snowflake Architecture — Separation of Compute and Storage"
    },
    {
      "id": "q04",
      "domain": "architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake administrator needs to track and limit the number of compute credits used by a specific virtual warehouse. Which Snowflake object should they create?",
      "options": [
        {
          "id": "A",
          "text": "Network policy"
        },
        {
          "id": "B",
          "text": "Resource monitor"
        },
        {
          "id": "C",
          "text": "Row access policy"
        },
        {
          "id": "D",
          "text": "Task"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because a resource monitor allows administrators to define credit quotas and trigger actions (notify, suspend, suspend immediately) when a warehouse reaches defined thresholds. It can be applied at the account or individual warehouse level. A is wrong because network policies control IP-based access, not credit consumption. C is wrong because row access policies control data visibility in tables, not compute resource usage. D is wrong because tasks are used to schedule SQL statements, not to limit warehouse credit consumption.",
      "reference": "Snowflake Architecture — Resource Monitors"
    },
    {
      "id": "q05",
      "domain": "architecture",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Which TWO of the following are characteristics of Snowflake micro-partitions? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Micro-partitions are mutable and are updated in-place when rows change."
        },
        {
          "id": "B",
          "text": "Micro-partitions are immutable; updates result in new micro-partitions being created."
        },
        {
          "id": "C",
          "text": "Data within a micro-partition is stored in columnar format."
        },
        {
          "id": "D",
          "text": "Each micro-partition holds exactly 1,000,000 rows."
        }
      ],
      "correct": [
        "B",
        "C"
      ],
      "explanation": "B and C are correct. Micro-partitions are immutable — when data is updated or deleted, Snowflake marks old micro-partitions as deleted and creates new ones, enabling Time Travel. Data within each micro-partition is stored in columnar format, enabling efficient column pruning and compression. A is wrong because micro-partitions are explicitly immutable. D is wrong because micro-partitions do not have a fixed row count — they are sized by compressed data volume (50–500 MB), so the row count varies by data type and compression ratio.",
      "reference": "Snowflake Architecture — Micro-partition Properties"
    },
    {
      "id": "q06",
      "domain": "architecture",
      "type": "single",
      "difficulty": "hard",
      "stem": "A company is currently on the Enterprise edition and wants to enable cross-region database replication for disaster recovery. Which statement is TRUE?",
      "options": [
        {
          "id": "A",
          "text": "Database replication is available on all Snowflake editions including Standard."
        },
        {
          "id": "B",
          "text": "Cross-region replication requires an upgrade to Business Critical or higher."
        },
        {
          "id": "C",
          "text": "Database replication is available on Enterprise edition and above."
        },
        {
          "id": "D",
          "text": "Replication is handled automatically by Snowflake for all accounts and cannot be configured."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because database replication (including cross-region and cross-cloud replication) is available starting with the Enterprise edition. The company on Enterprise edition can enable replication without upgrading. A is wrong because Standard edition does not include database replication. B is wrong because upgrading to Business Critical is not required — Enterprise already supports it. D is wrong because replication must be explicitly configured by the account administrator; it is not automatic.",
      "reference": "Snowflake Architecture — Database Replication, Enterprise Edition"
    },
    {
      "id": "q07",
      "domain": "architecture",
      "type": "single",
      "difficulty": "hard",
      "stem": "A query result cache hit requires which of the following conditions to ALL be true?",
      "options": [
        {
          "id": "A",
          "text": "The same SQL text, the same user (regardless of role), and no micro-partition changes to base tables."
        },
        {
          "id": "B",
          "text": "The same SQL text, the same role, the same warehouse, and no data changes in the base tables within 24 hours."
        },
        {
          "id": "C",
          "text": "The same SQL text, the same role, no data changes in the base tables, and the result was cached within the last 24 hours."
        },
        {
          "id": "D",
          "text": "The same warehouse and the same SQL text within the same session."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because the query result cache returns cached results when the exact same SQL text is submitted by a session using the same role, the underlying data has not changed, and the cached result is still within the 24-hour TTL. Notably, the same warehouse is NOT required — the result cache is in the Cloud Services layer and is warehouse-independent. A is wrong because the same user is not sufficient — it must be the same role. B is wrong because the same warehouse is not a requirement for result cache hits. D is wrong because result cache hits work across sessions, not just within the same session, and warehouse identity is irrelevant.",
      "reference": "Snowflake Architecture — Query Result Cache"
    },
    {
      "id": "q08",
      "domain": "architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "An X-Large virtual warehouse (16 credits/hr) runs for exactly 45 minutes processing an ETL job. How many credits are consumed?",
      "options": [
        {
          "id": "A",
          "text": "16 credits"
        },
        {
          "id": "B",
          "text": "8 credits"
        },
        {
          "id": "C",
          "text": "12 credits"
        },
        {
          "id": "D",
          "text": "32 credits"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because credits are billed per second (with a 60-second minimum). An X-Large warehouse costs 16 credits/hr. Running for 45 minutes = 45/60 = 0.75 hours × 16 credits/hr = 12 credits. A is wrong because 16 credits would be for a full hour. B is wrong because 8 credits would be for 30 minutes. D is wrong because 32 credits would be for a 2XL warehouse running for a full hour.",
      "reference": "Snowflake Architecture — Virtual Warehouse Credit Billing"
    },
    {
      "id": "q09",
      "domain": "security",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which system-defined role should a Snowflake administrator use to create databases, schemas, and warehouses?",
      "options": [
        {
          "id": "A",
          "text": "ACCOUNTADMIN"
        },
        {
          "id": "B",
          "text": "SECURITYADMIN"
        },
        {
          "id": "C",
          "text": "SYSADMIN"
        },
        {
          "id": "D",
          "text": "PUBLIC"
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because SYSADMIN is the recommended role for creating and managing database objects such as databases, schemas, tables, and warehouses. Using SYSADMIN (rather than ACCOUNTADMIN) follows the principle of least privilege. A is wrong because while ACCOUNTADMIN can create these objects, Snowflake best practice recommends using SYSADMIN for object management and reserving ACCOUNTADMIN for account-level administration. B is wrong because SECURITYADMIN manages users and roles, not database objects. D is wrong because PUBLIC has minimal privileges and cannot create databases or warehouses.",
      "reference": "Snowflake Security — RBAC Best Practices, SYSADMIN Role"
    },
    {
      "id": "q10",
      "domain": "security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake administrator executes: `ALTER USER analyst1 SET NETWORK_POLICY = 'corp_policy';`. What is the effect?",
      "options": [
        {
          "id": "A",
          "text": "The network policy applies to all users in the account."
        },
        {
          "id": "B",
          "text": "The network policy overrides the account-level network policy for analyst1 only."
        },
        {
          "id": "C",
          "text": "The command fails because network policies can only be set at the account level."
        },
        {
          "id": "D",
          "text": "The analyst1 user is removed from the account-level network policy and has no restrictions."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because a user-level network policy overrides the account-level network policy for that specific user. This allows more granular control — for example, allowing a service account to connect from a different IP range than the corporate policy allows. A is wrong because setting a policy on a specific user does not affect all users. C is wrong because network policies CAN be set at both the account level and the individual user level. D is wrong because the user's network policy still applies the IP restrictions defined in 'corp_policy' — they are not unrestricted.",
      "reference": "Snowflake Security — Network Policy User-Level Override"
    },
    {
      "id": "q11",
      "domain": "security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company uses Snowflake Secure Views to share data with external partners. Why is a Secure View required instead of a regular view for data sharing?",
      "options": [
        {
          "id": "A",
          "text": "Secure views are the only view type that can be added to a Snowflake SHARE object."
        },
        {
          "id": "B",
          "text": "Secure views encrypt data at rest, which is required for cross-account sharing."
        },
        {
          "id": "C",
          "text": "Secure views prevent the consumer from seeing the view definition, protecting proprietary business logic."
        },
        {
          "id": "D",
          "text": "Secure views bypass the query result cache to ensure consumers always see fresh data."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because secure views hide the underlying view definition (the DDL) from users who are not the view owner. In a data sharing context, this protects the provider's proprietary business logic and filtering conditions from being inspected by consumers. A is wrong because while secure views are required for sharing, the reason is the privacy of the definition, not the exclusivity — it is a policy requirement. B is wrong because encryption at rest is handled by Snowflake's storage layer, not by the secure view feature. D is wrong because secure views do interact with the query result cache; bypassing the cache is a different concern.",
      "reference": "Snowflake Security and Data Sharing — Secure Views"
    },
    {
      "id": "q12",
      "domain": "security",
      "type": "single",
      "difficulty": "hard",
      "stem": "A row access policy is created with the following function body: `RETURN CURRENT_ROLE() = 'DATA_OWNER' OR t.region = CURRENT_USER()`. This policy is applied to the `sales` table. What does this policy do?",
      "options": [
        {
          "id": "A",
          "text": "Users with the DATA_OWNER role see all rows; other users see only rows where the region column matches their username."
        },
        {
          "id": "B",
          "text": "Only the DATA_OWNER role can access the table; all other users are denied."
        },
        {
          "id": "C",
          "text": "Users see all rows if their username matches the region column; DATA_OWNER has no special access."
        },
        {
          "id": "D",
          "text": "The policy returns an error because CURRENT_USER() cannot be compared to a table column."
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the policy returns TRUE (allowing the row) if either the current role is DATA_OWNER (which grants access to all rows) OR if the region column value matches the current username (which restricts other users to only their region's rows). B is wrong because the OR condition means non-DATA_OWNER users can still see rows where their username matches the region. C is wrong because DATA_OWNER sees ALL rows due to the first condition in the OR. D is wrong because CURRENT_USER() returning a string is a valid comparison to a string column.",
      "reference": "Snowflake Security — Row Access Policies"
    },
    {
      "id": "q13",
      "domain": "security",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO authentication methods does Snowflake support for service accounts used in automated pipelines? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Username and password with MFA bypass"
        },
        {
          "id": "B",
          "text": "Key-pair authentication using RSA private/public keys"
        },
        {
          "id": "C",
          "text": "OAuth access tokens (e.g., Snowflake OAuth or external OAuth)"
        },
        {
          "id": "D",
          "text": "Biometric fingerprint authentication"
        }
      ],
      "correct": [
        "B",
        "C"
      ],
      "explanation": "B and C are correct. Key-pair authentication is the recommended method for service accounts — the private key is stored securely on the client, and the public key is registered in Snowflake. OAuth (including Snowflake OAuth and external OAuth with providers like Okta) is also supported for programmatic access. A is wrong because password + MFA bypass is not a supported or recommended pattern for service accounts; MFA is a security control that should not be bypassed. D is wrong because biometric authentication is not a Snowflake authentication method.",
      "reference": "Snowflake Security — Key-Pair Authentication, OAuth"
    },
    {
      "id": "q14",
      "domain": "security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A masking policy is applied to the `email` column in the `customers` table. Users with the ANALYST role see `****@****.com` while users with the MARKETING role see the full email address. Which SQL snippet CORRECTLY creates this behavior?",
      "options": [
        {
          "id": "A",
          "text": "`CASE WHEN CURRENT_ROLE() IN ('MARKETING') THEN val ELSE '****@****.com' END`"
        },
        {
          "id": "B",
          "text": "`CASE WHEN IS_ROLE_IN_SESSION('ANALYST') THEN val ELSE '****@****.com' END`"
        },
        {
          "id": "C",
          "text": "`CASE WHEN CURRENT_USER() = 'MARKETING' THEN val ELSE '****@****.com' END`"
        },
        {
          "id": "D",
          "text": "`IF(CURRENT_ROLE() = 'ANALYST', val, '****@****.com')`"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because masking policy bodies use `CURRENT_ROLE()` to determine the active role and return the unmasked value for privileged roles (MARKETING) or the masked value otherwise. B is wrong because IS_ROLE_IN_SESSION checks whether the ANALYST role is in session — showing the full value to ANALYST is the inverse of the requirement. C is wrong because CURRENT_USER() returns the username, not the role, so comparing it to 'MARKETING' would not correctly match the role. D is wrong because IF() is not standard Snowflake SQL syntax for masking policies; CASE WHEN is the correct pattern.",
      "reference": "Snowflake Security — Dynamic Data Masking Policy Functions"
    },
    {
      "id": "q15",
      "domain": "performance",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data analyst notices that after restarting a virtual warehouse and running their first query, it is slower than subsequent identical queries. What BEST explains this behavior?",
      "options": [
        {
          "id": "A",
          "text": "The Cloud Services layer needs to re-authenticate the user after each warehouse restart."
        },
        {
          "id": "B",
          "text": "The first query warms up the local disk cache on the warehouse nodes; subsequent queries benefit from cached micro-partition data."
        },
        {
          "id": "C",
          "text": "The query result cache is cleared when a warehouse restarts, forcing re-computation."
        },
        {
          "id": "D",
          "text": "The optimizer generates a less efficient plan for the first query after a restart."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the local disk (SSD) cache on warehouse nodes is empty after a restart. The first query must read micro-partitions from remote object storage (S3/Azure/GCS), which is slower. Subsequent identical queries can read from the populated SSD cache, which is much faster. This is called cache warming. A is wrong because authentication is handled by the Cloud Services layer and is not affected by warehouse restarts. C is wrong because the query result cache is in the Cloud Services layer and is not cleared when a warehouse restarts. D is wrong because the optimizer uses the same plan — the difference is the data access path.",
      "reference": "Snowflake Performance — Local Disk Cache Warming"
    },
    {
      "id": "q16",
      "domain": "performance",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer runs `EXPLAIN USING TABULAR SELECT * FROM orders WHERE customer_id = 12345`. They see a TableScan node scanning all 10,000 micro-partitions. What is the MOST LIKELY cause?",
      "options": [
        {
          "id": "A",
          "text": "The query uses SELECT * which disables partition pruning."
        },
        {
          "id": "B",
          "text": "The customer_id column has poor clustering — its values are spread across all micro-partitions."
        },
        {
          "id": "C",
          "text": "The EXPLAIN command itself prevents pruning from being evaluated."
        },
        {
          "id": "D",
          "text": "The orders table has too few micro-partitions to benefit from pruning."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake prunes micro-partitions using column min/max metadata. If customer_id values are randomly distributed across all micro-partitions (i.e., the min/max range of every partition includes 12345), no partitions can be eliminated and a full scan occurs. A is wrong because SELECT * does not disable partition pruning — the WHERE clause drives pruning regardless of the SELECT list. C is wrong because EXPLAIN evaluates the query plan including pruning estimates. D is wrong because having 10,000 micro-partitions is a large table, not too few — and poor clustering on customer_id is the likely culprit.",
      "reference": "Snowflake Performance — Partition Pruning, Clustering"
    },
    {
      "id": "q17",
      "domain": "performance",
      "type": "single",
      "difficulty": "hard",
      "stem": "A materialized view is defined on a base table in Snowflake. The base table receives frequent INSERT and DELETE operations. Which statement CORRECTLY describes how the materialized view is maintained?",
      "options": [
        {
          "id": "A",
          "text": "The materialized view is rebuilt from scratch each time the base table changes."
        },
        {
          "id": "B",
          "text": "Snowflake automatically and transparently maintains the materialized view using background compute, consuming credits."
        },
        {
          "id": "C",
          "text": "The user must manually refresh the materialized view by calling ALTER MATERIALIZED VIEW ... REFRESH."
        },
        {
          "id": "D",
          "text": "The materialized view becomes stale and is automatically dropped after 7 days without a manual refresh."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake automatically maintains materialized views in the background whenever the base table changes. This maintenance consumes compute credits and happens transparently without user intervention. The optimizer can then use the materialized view to serve queries. A is wrong because materialized views are incrementally maintained, not fully rebuilt from scratch. C is wrong because Snowflake manages automatic refresh — unlike some other systems, users do not need to manually refresh Snowflake materialized views. D is wrong because materialized views do not have a 7-day staleness limit or auto-drop behavior.",
      "reference": "Snowflake Performance — Materialized Views Auto-Maintenance"
    },
    {
      "id": "q18",
      "domain": "performance",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A Snowflake administrator is investigating a query that runs for 45 minutes on a Large warehouse. The Query Profile shows that 95% of the time is spent in a HashJoin node. Which TWO actions could potentially improve performance? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Increase the warehouse size to XL or larger to provide more memory for the hash join."
        },
        {
          "id": "B",
          "text": "Enable the Search Optimization Service on the table."
        },
        {
          "id": "C",
          "text": "Rewrite the query to filter earlier and reduce the number of rows reaching the join."
        },
        {
          "id": "D",
          "text": "Create a network policy to restrict concurrent access during query execution."
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. Hash joins are memory-intensive — a larger warehouse provides more memory per node, reducing disk spilling and improving join performance. Filtering rows earlier (pushing predicates before the join) reduces the data volume the join must process, directly improving performance. B is wrong because the Search Optimization Service improves point-lookup queries, not join operations. D is wrong because network policies control connectivity and have no effect on query execution performance.",
      "reference": "Snowflake Performance — Warehouse Sizing, Query Optimization"
    },
    {
      "id": "q19",
      "domain": "performance",
      "type": "single",
      "difficulty": "medium",
      "stem": "The `SYSTEM$CLUSTERING_INFORMATION` function returns `total_partition_count: 5000` and `total_constant_partition_count: 4800`. What does this indicate?",
      "options": [
        {
          "id": "A",
          "text": "4800 micro-partitions are completely prunable for any query on the clustering key."
        },
        {
          "id": "B",
          "text": "4800 of the 5000 micro-partitions contain only a single distinct value of the clustering key, meaning the table is very well-clustered."
        },
        {
          "id": "C",
          "text": "4800 micro-partitions have not been reclustered recently and need maintenance."
        },
        {
          "id": "D",
          "text": "4800 micro-partitions exceed the recommended maximum size and should be split."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because `total_constant_partition_count` represents the number of micro-partitions that contain only a single distinct value of the clustering key column. A high ratio (4800/5000 = 96%) indicates excellent clustering — queries filtering on the clustering key can eliminate nearly all micro-partitions quickly. A is wrong because constant partitions are well-clustered, not necessarily prunable for all queries — they indicate homogeneity within each partition. C is wrong because a high constant partition count indicates good clustering, not maintenance needs. D is wrong because this metric is not related to partition size.",
      "reference": "Snowflake Performance — Clustering Information, Constant Partitions"
    },
    {
      "id": "q20",
      "domain": "data-loading",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer wants to load CSV files from their local machine into a Snowflake table. They first need to upload the files to a Snowflake stage. Which stage type allows files to be uploaded directly from a local machine using the PUT command?",
      "options": [
        {
          "id": "A",
          "text": "External stage (S3)"
        },
        {
          "id": "B",
          "text": "Internal named stage"
        },
        {
          "id": "C",
          "text": "External stage (Azure Blob)"
        },
        {
          "id": "D",
          "text": "Table stage using COPY INTO directly"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the PUT command can upload files from a local machine to an internal stage (user stage `@~`, table stage `@%table`, or internal named stage). Internal stages store files in Snowflake-managed storage. A is wrong because you cannot use the PUT command to upload to an external stage — files in external stages must be uploaded via the cloud provider's tools (AWS CLI, Azure Storage Explorer, etc.). C is wrong for the same reason as A. D is wrong because COPY INTO is used to load data from a stage into a table, not to upload files from a local machine.",
      "reference": "Snowflake Data Loading — PUT Command, Internal Stages"
    },
    {
      "id": "q21",
      "domain": "data-loading",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer needs to unload data from a Snowflake table to an S3 bucket. Which command accomplishes this?",
      "options": [
        {
          "id": "A",
          "text": "`EXPORT TABLE orders TO @my_s3_stage;`"
        },
        {
          "id": "B",
          "text": "`COPY INTO @my_s3_stage FROM orders FILE_FORMAT = (TYPE = CSV);`"
        },
        {
          "id": "C",
          "text": "`INSERT INTO @my_s3_stage SELECT * FROM orders;`"
        },
        {
          "id": "D",
          "text": "`UNLOAD orders INTO @my_s3_stage;`"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because `COPY INTO @stage FROM table` is the Snowflake command for data unloading. It exports data from a table or query to files in a named stage (including external stages pointing to S3). A is wrong because EXPORT TABLE is not a valid Snowflake command. C is wrong because INSERT INTO cannot write to a stage. D is wrong because UNLOAD is not a valid Snowflake command.",
      "reference": "Snowflake Data Loading — COPY INTO for Data Unloading"
    },
    {
      "id": "q22",
      "domain": "data-loading",
      "type": "single",
      "difficulty": "hard",
      "stem": "A team is loading 500 GB of gzipped CSV files using COPY INTO. Each file is approximately 2 GB compressed. What is the RECOMMENDED approach to maximize parallel loading throughput?",
      "options": [
        {
          "id": "A",
          "text": "Load all files in a single COPY INTO statement and use ON_ERROR = CONTINUE to handle any errors."
        },
        {
          "id": "B",
          "text": "Split the 2 GB files into multiple smaller files of 100–250 MB compressed before loading."
        },
        {
          "id": "C",
          "text": "Use a larger warehouse (e.g., 4X-Large) with a single 2 GB file for maximum parallelism."
        },
        {
          "id": "D",
          "text": "Convert files to JSON format before loading, as JSON enables better parallel processing in Snowflake."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake parallelizes loading by assigning individual files to different threads. Files that are too large cannot be split by a single COPY thread — the recommended file size is 100–250 MB compressed. Splitting the 2 GB files into ~20 smaller files each allows Snowflake to load them in parallel across warehouse nodes. A is wrong because having fewer, larger files limits parallelism regardless of ON_ERROR settings. C is wrong because a larger warehouse cannot parallelize within a single large file — the bottleneck is file count, not warehouse size. D is wrong because JSON does not inherently enable better parallelism than CSV — file size is the key factor.",
      "reference": "Snowflake Data Loading — File Size Recommendations for Parallel Loading"
    },
    {
      "id": "q23",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "easy",
      "stem": "What is the purpose of the `SYSTEM$STREAM_HAS_DATA()` function in a Snowflake task?",
      "options": [
        {
          "id": "A",
          "text": "It returns the number of rows in the stream waiting to be processed."
        },
        {
          "id": "B",
          "text": "It returns TRUE if there is unconsumed change data in the stream, allowing a task to skip execution if the stream is empty."
        },
        {
          "id": "C",
          "text": "It forces the stream to materialize all pending changes to a staging table."
        },
        {
          "id": "D",
          "text": "It resets the stream offset to the beginning of Time Travel."
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because `SYSTEM$STREAM_HAS_DATA()` is commonly used as a WHEN condition in a task definition to conditionally skip execution when there is no new data in the stream. It returns TRUE when the stream has unconsumed rows and FALSE when empty. A is wrong because it returns a boolean, not a row count. C is wrong because it does not materialize or process data. D is wrong because it does not reset the stream offset.",
      "reference": "Snowflake Data Transformations — SYSTEM$STREAM_HAS_DATA"
    },
    {
      "id": "q24",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer wants to build a task DAG where `task_b` only runs after `task_a` completes successfully. Which DDL statement creates this dependency?",
      "options": [
        {
          "id": "A",
          "text": "`CREATE TASK task_b AFTER task_a SCHEDULE = '5 MINUTE' AS ...`"
        },
        {
          "id": "B",
          "text": "`CREATE TASK task_b AFTER task_a AS ...`"
        },
        {
          "id": "C",
          "text": "`CREATE TASK task_b DEPENDS ON task_a SCHEDULE = '5 MINUTE' AS ...`"
        },
        {
          "id": "D",
          "text": "`ALTER TASK task_b SET PREDECESSOR = 'task_a';`"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the `AFTER` clause in `CREATE TASK` establishes a dependency — task_b will be triggered after task_a completes successfully. Child tasks in a DAG do not have their own SCHEDULE; the root task's schedule drives the entire DAG. A is wrong because a child task in a DAG cannot have its own SCHEDULE — the schedule is only on the root task. C is wrong because DEPENDS ON is not valid Snowflake syntax for task dependencies. D is wrong because SET PREDECESSOR is not a valid ALTER TASK option.",
      "reference": "Snowflake Data Transformations — Task DAGs, AFTER Clause"
    },
    {
      "id": "q25",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer writes a `MERGE INTO` statement to upsert data from a staging table into a target table. Which clause handles rows present in the staging table but NOT in the target table?",
      "options": [
        {
          "id": "A",
          "text": "WHEN MATCHED THEN UPDATE"
        },
        {
          "id": "B",
          "text": "WHEN NOT MATCHED THEN INSERT"
        },
        {
          "id": "C",
          "text": "WHEN NOT MATCHED BY SOURCE THEN DELETE"
        },
        {
          "id": "D",
          "text": "WHEN MATCHED BY SOURCE THEN INSERT"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because `WHEN NOT MATCHED THEN INSERT` handles rows in the source (staging) table that do not have a matching row in the target table — these are new rows that need to be inserted. A is wrong because WHEN MATCHED applies when a row exists in both source and target (for updates). C is wrong because WHEN NOT MATCHED BY SOURCE handles rows in the TARGET that have no match in the source, typically used for deletes. D is wrong because WHEN MATCHED BY SOURCE is not a valid Snowflake MERGE clause.",
      "reference": "Snowflake Data Transformations — MERGE INTO Statement"
    },
    {
      "id": "q26",
      "domain": "data-transformation",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO statements correctly describe Snowflake Dynamic Tables? (Select TWO)",
      "options": [
        {
          "id": "A",
          "text": "Dynamic tables can use FULL or INCREMENTAL refresh modes, with Snowflake selecting the method based on the query complexity."
        },
        {
          "id": "B",
          "text": "Dynamic tables require a user-defined schedule in CRON format for refresh timing."
        },
        {
          "id": "C",
          "text": "Dynamic tables can be chained — one dynamic table's refresh can depend on another dynamic table's lag."
        },
        {
          "id": "D",
          "text": "Dynamic tables are only supported in the Business Critical edition and above."
        }
      ],
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. Dynamic tables support both FULL refresh (recreating the entire result set) and INCREMENTAL refresh (processing only changed rows), with Snowflake selecting the appropriate method based on whether the query can be incrementally evaluated. Dynamic tables can also be chained — a downstream dynamic table can set `TARGET_LAG = DOWNSTREAM` to inherit its lag from the pipeline upstream. B is wrong because dynamic tables use `TARGET_LAG` (a staleness duration), not a user-defined CRON schedule — Snowflake determines when to refresh. D is wrong because dynamic tables are available in Enterprise edition and above, not only Business Critical.",
      "reference": "Snowflake Data Transformations — Dynamic Tables"
    },
    {
      "id": "q27",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "hard",
      "stem": "A developer creates a JavaScript UDF in Snowflake. Which limitation applies specifically to JavaScript UDFs in Snowflake?",
      "options": [
        {
          "id": "A",
          "text": "JavaScript UDFs cannot accept more than one argument."
        },
        {
          "id": "B",
          "text": "JavaScript UDFs cannot be used in SELECT statements."
        },
        {
          "id": "C",
          "text": "JavaScript UDFs cannot issue SQL queries (they cannot execute SQL inside the function body)."
        },
        {
          "id": "D",
          "text": "JavaScript UDFs can only return INTEGER or VARCHAR data types."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because JavaScript UDFs in Snowflake are scalar functions that cannot execute SQL statements inside their function body — they can only process input arguments and return a value using JavaScript logic. If you need to execute SQL within a function, you must use a stored procedure instead. A is wrong because JavaScript UDFs can accept multiple arguments. B is wrong because JavaScript UDFs can be called in SELECT, WHERE, and other clauses. D is wrong because JavaScript UDFs can return a variety of Snowflake data types, not just INTEGER or VARCHAR.",
      "reference": "Snowflake Data Transformations — JavaScript UDF Limitations"
    },
    {
      "id": "q28",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer needs an auto-incrementing surrogate key when inserting rows into a table. Which Snowflake feature provides this capability?",
      "options": [
        {
          "id": "A",
          "text": "A column with IDENTITY property (e.g., id INT IDENTITY(1,1))"
        },
        {
          "id": "B",
          "text": "A stream with METADATA$ROW_ID"
        },
        {
          "id": "C",
          "text": "A task that calls NEXTVAL on insert"
        },
        {
          "id": "D",
          "text": "A materialized view with ROW_NUMBER() OVER (ORDER BY 1)"
        }
      ],
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Snowflake supports the IDENTITY property on integer columns, which automatically generates sequential values for each inserted row. Sequences can also be used explicitly with `nextval`, but the IDENTITY column property is the most direct way to create an auto-incrementing surrogate key. B is wrong because METADATA$ROW_ID is an internal Snowflake identifier in streams, not a user-accessible auto-increment key. C is wrong because a task is not the mechanism for generating surrogate keys during inserts. D is wrong because ROW_NUMBER() in a materialized view does not generate persistent surrogate keys for new rows.",
      "reference": "Snowflake Data Transformations — Sequences and IDENTITY"
    },
    {
      "id": "q29",
      "domain": "data-protection",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data analyst accidentally deletes all rows from the `transactions` table at 10:00 AM. The table has a Time Travel retention of 1 day. Which query returns the table data as it existed at 9:55 AM?",
      "options": [
        {
          "id": "A",
          "text": "`SELECT * FROM transactions BEFORE(TIMESTAMP => '2024-01-15 09:55:00'::TIMESTAMP_TZ);`"
        },
        {
          "id": "B",
          "text": "`SELECT * FROM transactions AT(OFFSET => -300);`"
        },
        {
          "id": "C",
          "text": "`SELECT * FROM transactions SNAPSHOT AT('09:55:00');`"
        },
        {
          "id": "D",
          "text": "`SELECT * FROM transactions HISTORY WHERE timestamp = '09:55:00';`"
        }
      ],
      "correct": [
        "B"
      ],
      "explanation": "B is correct because `AT(OFFSET => -300)` queries the table as it existed 300 seconds (5 minutes) before the current time. If the query is run shortly after 10:00 AM, an offset of -300 seconds points to approximately 9:55 AM. Note: AT(TIMESTAMP => ...) is also valid, making A partially correct in concept, but B uses the correct OFFSET syntax shown in the options. A uses BEFORE instead of AT — BEFORE returns data as of just before a statement or timestamp, not AT a timestamp; however, the syntax shown (`BEFORE(TIMESTAMP => ...)`) would return data just before 9:55 AM, which is effectively the same. For this question, B demonstrates the canonical OFFSET approach. C is wrong because SNAPSHOT AT is not valid Snowflake Time Travel syntax. D is wrong because no HISTORY clause exists in Snowflake SQL.",
      "reference": "Snowflake Data Protection — Time Travel AT Clause"
    },
    {
      "id": "q30",
      "domain": "data-protection",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowflake data provider creates a SHARE and grants SELECT on a secure view to a consumer account. The consumer creates a database from the share. Which statement is TRUE about the shared data?",
      "options": [
        {
          "id": "A",
          "text": "The consumer can see the DDL definition of the secure view since they have SELECT on it."
        },
        {
          "id": "B",
          "text": "Data is copied from the provider's account to the consumer's account when the shared database is created."
        },
        {
          "id": "C",
          "text": "The consumer accesses the provider's data in real time with no data movement; the provider's storage is read directly."
        },
        {
          "id": "D",
          "text": "The consumer can write data back to the shared tables if they have INSERT privileges."
        }
      ],
      "correct": [
        "C"
      ],
      "explanation": "C is correct because Snowflake Data Sharing provides zero-copy, live access to data. The consumer reads the provider's data directly from the provider's storage with no data movement or replication. This means changes in the provider's data are immediately visible to the consumer. A is wrong because secure views specifically hide the DDL from non-owners, including consumers. B is wrong because data sharing explicitly does NOT copy data — the consumer reads the provider's live data. D is wrong because shared objects are read-only from the consumer's perspective; consumers cannot write back to shared tables.",
      "reference": "Snowflake Data Protection — Data Sharing, Zero-Copy"
    },
    {
      "id": "q31",
      "domain": "virtual-warehouses",
      "type": "single",
      "difficulty": "easy",
      "stem": "What is the minimum billing increment for a Snowflake virtual warehouse?",
      "options": {
        "A": "1 minute",
        "B": "30 seconds",
        "C": "1 hour",
        "D": "5 minutes"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Snowflake bills warehouse compute per second with a minimum of 60 seconds (1 minute). If a warehouse runs for 10 seconds, you are charged for 60 seconds. After the first minute, billing increments per second. B is wrong because 30 seconds is below the minimum billing unit. C is wrong because hourly billing is used for some cloud services but not Snowflake virtual warehouses. D is wrong because 5 minutes is not the billing increment.",
      "reference": "Snowflake Documentation: Virtual Warehouses — Credit Usage and Billing"
    },
    {
      "id": "q32",
      "domain": "virtual-warehouses",
      "type": "single",
      "difficulty": "medium",
      "stem": "A multi-cluster warehouse is configured with MIN_CLUSTER_COUNT = 1 and MAX_CLUSTER_COUNT = 4, using Auto-Scale mode. When does Snowflake add a second cluster?",
      "options": {
        "A": "When total query execution time exceeds 5 minutes",
        "B": "When queries are queued due to insufficient concurrency on the running clusters",
        "C": "When the warehouse has been running for more than 1 hour",
        "D": "When the number of active sessions exceeds 10"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because in Auto-Scale mode, Snowflake starts additional clusters when queries begin to queue up — meaning the existing clusters cannot service all incoming queries concurrently. The scaling policy (Standard or Economy) affects how quickly additional clusters are added, but the trigger is query queuing. A is wrong because query execution duration does not trigger scaling. C is wrong because uptime does not trigger additional clusters. D is wrong because session count is not the scaling trigger; it is query queuing.",
      "reference": "Snowflake Documentation: Multi-Cluster Warehouses — Auto-Scale Mode"
    },
    {
      "id": "q33",
      "domain": "virtual-warehouses",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company wants to run reporting queries and ETL jobs on the same Snowflake account without one workload impacting the other. What is the BEST architectural approach?",
      "options": {
        "A": "Create separate schemas for reporting and ETL and assign different file formats to each",
        "B": "Use separate virtual warehouses for reporting and ETL so each workload has dedicated compute",
        "C": "Increase the auto-suspend timeout so the warehouse is always available for both workloads",
        "D": "Use a single large warehouse and rely on query queuing to manage the workloads"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake's compute/storage separation allows multiple virtual warehouses to access the same data simultaneously. Using separate warehouses for reporting and ETL is the recommended pattern for workload isolation — each workload gets dedicated compute resources and neither can slow down the other. A is wrong because schemas control data organization, not compute isolation. C is wrong because using a single warehouse still allows workloads to compete for the same compute resources. D is wrong because relying on queuing on a shared warehouse means one workload can block the other.",
      "reference": "Snowflake Documentation: Virtual Warehouses — Workload Isolation"
    },
    {
      "id": "q34",
      "domain": "virtual-warehouses",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which warehouse size doubles the number of compute nodes compared to a Large warehouse?",
      "options": {
        "A": "X-Large",
        "B": "2X-Large",
        "C": "Medium",
        "D": "3X-Large"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because each warehouse size step doubles the compute (and credits per hour). A Large warehouse = 4 credits/hr. An X-Large = 8 credits/hr (double the nodes). 2X-Large would be four times a Large. B is wrong because 2X-Large is four times a Large, not double. C is wrong because Medium is half a Large in compute. D is wrong because 3X-Large is eight times a Large.",
      "reference": "Snowflake Documentation: Virtual Warehouses — Warehouse Sizes and Credit Usage"
    },
    {
      "id": "q35",
      "domain": "virtual-warehouses",
      "type": "single",
      "difficulty": "hard",
      "stem": "The Query Acceleration Service (QAS) is enabled on a warehouse. Which type of query benefits MOST from QAS?",
      "options": {
        "A": "Queries with large hash joins between two tables of similar size",
        "B": "Outlier queries with large scans that process far more data than the average query on that warehouse",
        "C": "Queries that are already hitting the result cache",
        "D": "INSERT statements loading data from an internal stage"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the Query Acceleration Service is designed to improve the performance of outlier queries — queries that require scanning significantly more data than typical queries on that warehouse. QAS offloads parts of the scan to additional serverless compute, reducing the impact of these outlier queries on the warehouse. A is wrong because QAS does not specifically target hash joins between similarly-sized tables; it targets scan-heavy outliers. C is wrong because result cache hits do not execute on the warehouse at all. D is wrong because loading operations are not the target scenario for QAS.",
      "reference": "Snowflake Documentation: Query Acceleration Service"
    },
    {
      "id": "q36",
      "domain": "virtual-warehouses",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Which TWO of the following actions will a resource monitor perform when a virtual warehouse reaches its defined credit quota? (Select TWO)",
      "options": {
        "A": "Drop all tables accessed by the warehouse",
        "B": "Send a notification alert to specified users",
        "C": "Suspend the warehouse immediately, aborting running queries",
        "D": "Replicate the warehouse to a secondary region"
      },
      "correct": [
        "B",
        "C"
      ],
      "explanation": "B and C are correct. A resource monitor can trigger two key actions: Notify (send email alerts to users when thresholds are reached) and Suspend Immediately (stop the warehouse and abort all running queries when the quota is hit). There is also a Suspend action that waits for current queries to complete before suspending. A is wrong because resource monitors have no capability to drop tables. D is wrong because resource monitors do not initiate replication.",
      "reference": "Snowflake Documentation: Resource Monitors — Actions and Triggers"
    },
    {
      "id": "q37",
      "domain": "virtual-warehouses",
      "type": "single",
      "difficulty": "hard",
      "stem": "A multi-cluster warehouse is in Maximized mode with MAX_CLUSTER_COUNT = 3. All three clusters are running. When does Snowflake shut down one of the clusters?",
      "options": {
        "A": "When one cluster has been idle for longer than the auto-suspend period",
        "B": "In Maximized mode, all clusters run continuously and none are shut down unless the warehouse is manually suspended",
        "C": "When two consecutive clusters complete their queries without queuing",
        "D": "When the Economy scaling policy determines that one cluster has fewer than 5 active queries"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because in Maximized mode, all clusters are always running (up to MAX_CLUSTER_COUNT) for the entire time the warehouse is active. Clusters are not shut down based on load — the goal is maximum concurrency. Clusters are only shut down when the entire warehouse is suspended or manually stopped. A is wrong because auto-suspend applies to the warehouse as a whole, not individual clusters in Maximized mode. C is wrong because Maximized mode does not dynamically reduce cluster count based on query completion. D is wrong because the Economy scaling policy only applies to Auto-Scale mode, not Maximized mode.",
      "reference": "Snowflake Documentation: Multi-Cluster Warehouses — Maximized vs Auto-Scale Mode"
    },
    {
      "id": "q38",
      "domain": "virtual-warehouses",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer needs to run a single complex query that processes 10 TB of data. The query consistently spills to disk on a Large warehouse. What should they do to eliminate disk spillage?",
      "options": {
        "A": "Increase the AUTO_SUSPEND parameter to prevent the warehouse from suspending mid-query",
        "B": "Increase the warehouse size to provide more memory per node",
        "C": "Enable multi-cluster mode with MAX_CLUSTER_COUNT = 3",
        "D": "Add a clustering key to the table being scanned"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because disk spilling occurs when a query requires more memory than is available on the warehouse nodes. Increasing the warehouse size provides more memory per node, allowing the query to process data in memory rather than spilling to local disk. A is wrong because auto-suspend timeout does not affect memory availability during query execution. C is wrong because multi-cluster warehouses improve concurrency by routing different queries to different clusters — they do not give a single query more memory. D is wrong because while clustering can reduce scan volume, the issue of disk spilling for a 10 TB scan is a memory problem best solved by a larger warehouse.",
      "reference": "Snowflake Documentation: Virtual Warehouses — Warehouse Sizing and Memory"
    },
    {
      "id": "q39",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "easy",
      "stem": "What is the maximum Time Travel retention period available in Snowflake, and which edition is required?",
      "options": {
        "A": "7 days; available on all editions",
        "B": "90 days; requires Business Critical edition",
        "C": "90 days; requires Enterprise edition or above",
        "D": "30 days; available on Standard edition"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because the maximum Time Travel retention period is 90 days, and it requires Enterprise edition or above. Standard edition is limited to 1 day of Time Travel. A is wrong because 7 days is the default on Enterprise, not the maximum, and it is not available on all editions at 7 days. B is wrong because Business Critical is not required — Enterprise edition suffices. D is wrong because Standard edition is limited to 1 day, not 30 days.",
      "reference": "Snowflake Documentation: Time Travel — Retention Period and Edition Requirements"
    },
    {
      "id": "q40",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer accidentally drops a table with a 7-day Time Travel retention. How can they recover it?",
      "options": {
        "A": "SELECT * FROM TABLE(RESULT_SCAN(LAST_QUERY_ID())) to retrieve the dropped rows",
        "B": "UNDROP TABLE my_table; to restore the table from Time Travel",
        "C": "CREATE TABLE my_table AS SELECT * FROM my_table AT(OFFSET => -1);",
        "D": "Contact Snowflake Support — dropped tables cannot be restored by users"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake's UNDROP TABLE command restores a dropped table (along with its data and metadata) within the Time Travel retention period. As long as the table was dropped within the last 7 days (per the retention setting), UNDROP TABLE restores it to its state at the moment of the drop. A is wrong because RESULT_SCAN retrieves results of a previous query, not a dropped table. C is wrong because referencing a dropped table in AT syntax would fail unless you use the table's ID or specific Time Travel reference. D is wrong because users can self-serve with UNDROP TABLE — Snowflake Support is not required.",
      "reference": "Snowflake Documentation: Time Travel — UNDROP TABLE"
    },
    {
      "id": "q41",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "medium",
      "stem": "How does Snowflake's Fail-Safe period differ from Time Travel?",
      "options": {
        "A": "Fail-Safe data can be queried with SQL using AT or BEFORE clauses, unlike Time Travel",
        "B": "Fail-Safe is a 7-day period (for permanent tables) after Time Travel expires; it is managed by Snowflake and not user-accessible via SQL",
        "C": "Fail-Safe replaces Time Travel for Standard edition accounts",
        "D": "Fail-Safe data is stored in a separate cloud region for disaster recovery"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Fail-Safe is a 7-day emergency recovery window that begins after the Time Travel period expires for permanent tables. Unlike Time Travel, Fail-Safe data cannot be queried directly by users — it can only be accessed by Snowflake Support for disaster recovery purposes. It is not configurable and always lasts 7 days for permanent tables. A is wrong because Fail-Safe is not accessible via SQL AT/BEFORE syntax. C is wrong because Fail-Safe exists alongside Time Travel (even when Time Travel is 0 days), not as a replacement. D is wrong because Fail-Safe data is not replicated to a separate region — it uses the same underlying storage.",
      "reference": "Snowflake Documentation: Fail-Safe"
    },
    {
      "id": "q42",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "hard",
      "stem": "A developer creates a zero-copy clone of a 500 GB production table. How much additional storage is consumed immediately after cloning?",
      "options": {
        "A": "500 GB — the data is duplicated into the clone",
        "B": "250 GB — Snowflake stores a compressed copy",
        "C": "0 GB — the clone shares the original micro-partitions with no data copied",
        "D": "Approximately 1 GB — only the metadata is copied"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because zero-copy cloning creates a clone that initially shares the exact same underlying micro-partitions as the source object. No data is copied at clone creation time, so the immediate additional storage cost is zero. Storage costs only arise for the clone when micro-partitions diverge due to DML operations on either the source or the clone (copy-on-write). A is wrong because zero-copy means no data duplication at clone time. B is wrong because no compressed copy is made. D is wrong because while metadata is involved, the key point is that micro-partition data is not copied.",
      "reference": "Snowflake Documentation: Zero-Copy Cloning — Storage Behavior"
    },
    {
      "id": "q43",
      "domain": "storage-and-protection",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Which TWO of the following objects can be cloned using Snowflake's zero-copy clone feature? (Select TWO)",
      "options": {
        "A": "Database",
        "B": "Network policy",
        "C": "Schema",
        "D": "Resource monitor"
      },
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. Snowflake supports zero-copy cloning of databases, schemas, tables, stages, file formats, sequences, streams, tasks, and pipes. Cloning a database or schema creates a deep clone of all contained objects. B is wrong because network policies are account-level configuration objects and cannot be cloned. D is wrong because resource monitors are account-level objects and do not support cloning.",
      "reference": "Snowflake Documentation: Zero-Copy Cloning — Supported Objects"
    },
    {
      "id": "q44",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "hard",
      "stem": "A table is set to DATA_RETENTION_TIME_IN_DAYS = 0. What is the effect on Time Travel and Fail-Safe?",
      "options": {
        "A": "Time Travel is disabled; Fail-Safe is also disabled because the retention is 0",
        "B": "Time Travel is disabled (0 days); Fail-Safe still applies for 7 days after each DML operation",
        "C": "Only the ACCOUNTADMIN can query historic data because retention is 0",
        "D": "The table automatically converts to a transient table with no Time Travel or Fail-Safe"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because setting DATA_RETENTION_TIME_IN_DAYS = 0 on a permanent table disables Time Travel (you cannot query historical data), but Fail-Safe still applies. Fail-Safe is independent of the Time Travel setting and always provides 7 days of protection for permanent tables after each data change. A is wrong because Fail-Safe is not disabled by setting retention to 0 on a permanent table. C is wrong because no user (including ACCOUNTADMIN) can query historic data via SQL when retention is 0 — that is the Fail-Safe window only accessible via Snowflake Support. D is wrong because the table remains a permanent table; it is not automatically converted to transient.",
      "reference": "Snowflake Documentation: Time Travel and Fail-Safe — Setting Retention to 0"
    },
    {
      "id": "q45",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Snowflake feature provides continuous, automated loading of data from files arriving in a cloud storage location without a user-managed schedule?",
      "options": {
        "A": "COPY INTO with ON_ERROR = CONTINUE",
        "B": "Snowpipe",
        "C": "A scheduled task running COPY INTO every minute",
        "D": "External tables"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowpipe is Snowflake's continuous data ingestion service. It automatically detects and loads new files from a cloud storage location (using event notifications or REST API) as soon as they arrive, without a user-managed schedule. A is wrong because COPY INTO requires explicit execution — ON_ERROR only controls error handling during a load. C is wrong because scheduled tasks introduce polling-based latency and require user management. D is wrong because external tables query data in place rather than loading it into Snowflake.",
      "reference": "Snowflake Documentation: Snowpipe — Continuous Data Ingestion"
    },
    {
      "id": "q46",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer executes COPY INTO my_table FROM @my_stage and 3 out of 100 files fail to load due to data errors. By default, what happens?",
      "options": {
        "A": "All 100 files are rolled back and no data is loaded",
        "B": "The 97 successful files are loaded and the 3 failed files are skipped with errors reported",
        "C": "All 100 files are rejected until the errors are corrected",
        "D": "The COPY INTO statement loads all 100 files and silently ignores any errors"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the default ON_ERROR behavior in Snowflake COPY INTO is ABORT_STATEMENT, which means that if any file fails, the entire COPY INTO statement is aborted and no data is loaded. To load successful files and skip failed ones, you must explicitly set ON_ERROR = CONTINUE. B is wrong because this describes the behavior of ON_ERROR = CONTINUE, not the default. C is wrong because the default aborts the statement entirely rather than waiting for corrections. D is wrong because errors are never silently ignored — without ON_ERROR = CONTINUE, the statement aborts.",
      "reference": "Snowflake Documentation: COPY INTO — ON_ERROR Options"
    },
    {
      "id": "q47",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer creates an external stage pointing to an S3 bucket using: `CREATE STAGE my_ext_stage URL = 's3://mybucket/data/' CREDENTIALS = (AWS_KEY_ID = '...' AWS_SECRET_KEY = '...')`. What is a security concern with this approach?",
      "options": {
        "A": "External stages cannot use URL-based paths; they must use S3 ARNs",
        "B": "Storing AWS credentials directly in the stage definition is insecure; Snowflake recommends using a storage integration with an IAM role instead",
        "C": "External stages can only be created by the ACCOUNTADMIN role",
        "D": "The CREDENTIALS parameter is deprecated and must be replaced with the ENCRYPTION parameter"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because embedding AWS access keys directly in a stage definition is a security risk — the keys are stored as objects in Snowflake and could be extracted. Snowflake's recommended approach is to use a Storage Integration, which creates a trust relationship between Snowflake and an AWS IAM role, eliminating the need to store long-lived credentials. A is wrong because external stages do use URL-based S3 paths. C is wrong because SYSADMIN and other roles with appropriate privileges can create stages. D is wrong because CREDENTIALS is a valid parameter — it is just not the recommended approach for security reasons.",
      "reference": "Snowflake Documentation: External Stages — Storage Integrations vs. Credentials"
    },
    {
      "id": "q48",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data engineer notices that Snowpipe is not loading new files that arrive in S3. The pipe was working yesterday. Which is the FIRST diagnostic step they should take?",
      "options": {
        "A": "Drop and recreate the pipe to reset its state",
        "B": "Call SYSTEM$PIPE_STATUS('my_pipe') to check the pipe's execution state and error details",
        "C": "Run COPY INTO manually to reload all files from the stage",
        "D": "Recreate the external stage to force a fresh S3 connection"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because SYSTEM$PIPE_STATUS() returns diagnostic information about a Snowpipe, including its execution state (e.g., RUNNING, PAUSED, STOPPED_CLONED), whether there are errors, and details about the S3 event notification or SQS queue. This is the first diagnostic step. A is wrong because dropping and recreating the pipe would lose its load history metadata and could re-ingest already-loaded files. C is wrong because running COPY INTO manually loads files but does not diagnose why Snowpipe stopped. D is wrong because the stage itself is unlikely to be the issue if the pipe was working previously.",
      "reference": "Snowflake Documentation: Snowpipe — Troubleshooting with SYSTEM$PIPE_STATUS"
    },
    {
      "id": "q49",
      "domain": "data-movement",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Which TWO file formats does Snowflake support natively for loading semi-structured data? (Select TWO)",
      "options": {
        "A": "PARQUET",
        "B": "AVRO",
        "C": "XLSX (Microsoft Excel)",
        "D": "DOCX (Microsoft Word)"
      },
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A and B are correct. Snowflake natively supports loading data from CSV, JSON, Avro, ORC, Parquet, and XML file formats using COPY INTO. Parquet and Avro are commonly used semi-structured formats with native support. C is wrong because Snowflake does not natively support loading XLSX files. D is wrong because DOCX is a document format and is not supported for data loading.",
      "reference": "Snowflake Documentation: Data Loading — Supported File Formats"
    },
    {
      "id": "q50",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "hard",
      "stem": "A team needs to load 100 JSON files where each file contains an array of records at the root level (e.g., `[{...},{...}]`). After loading into a VARIANT column, they want one row per JSON object. Which COPY INTO option enables this?",
      "options": {
        "A": "STRIP_OUTER_ARRAY = TRUE in the FILE_FORMAT definition",
        "B": "FLATTEN = TRUE in the COPY INTO statement",
        "C": "PARSE_HEADER = TRUE in the file format",
        "D": "ENABLE_OCTET_LENGTH_LIMIT = FALSE in the stage"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because when JSON files contain a top-level array, setting STRIP_OUTER_ARRAY = TRUE in the JSON file format causes Snowflake to strip the outer brackets and load each element of the array as a separate row. Without this option, the entire array would be loaded as a single VARIANT row. B is wrong because FLATTEN is a SQL table function used in queries, not a COPY INTO option. C is wrong because PARSE_HEADER applies to CSV files for detecting column names in header rows. D is wrong because ENABLE_OCTET_LENGTH_LIMIT is not a relevant option for this use case.",
      "reference": "Snowflake Documentation: JSON File Format Options — STRIP_OUTER_ARRAY"
    },
    {
      "id": "q51",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "easy",
      "stem": "In Snowflake's RBAC model, which statement is TRUE about privilege inheritance?",
      "options": {
        "A": "Privileges are inherited from child roles to parent roles",
        "B": "Privileges are inherited from parent roles to child roles",
        "C": "Each role must be explicitly granted every privilege; inheritance does not apply",
        "D": "Privileges are inherited only if the INHERIT_PRIVILEGES flag is set on the role"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because in Snowflake's role hierarchy, when role A is granted to role B, role B (the parent) inherits all privileges held by role A (the child). This means higher-level roles accumulate privileges from all roles granted beneath them in the hierarchy. A is wrong because inheritance flows from child to parent — but the question asks which direction the effective privileges flow, which is from parent down through the hierarchy when a user assumes a role. C is wrong because inheritance is a core feature of Snowflake RBAC. D is wrong because INHERIT_PRIVILEGES is not a flag in Snowflake RBAC.",
      "reference": "Snowflake Documentation: RBAC — Role Hierarchy and Privilege Inheritance"
    },
    {
      "id": "q52",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "medium",
      "stem": "An administrator wants to prevent users from accessing Snowflake from outside the corporate IP range (10.0.0.0/8). Which sequence of commands is correct?",
      "options": {
        "A": "`CREATE NETWORK POLICY corp_policy ALLOWED_IP_LIST = ('10.0.0.0/8'); ALTER ACCOUNT SET NETWORK_POLICY = corp_policy;`",
        "B": "`CREATE FIREWALL RULE corp_rule IP = '10.0.0.0/8'; APPLY RULE TO ACCOUNT;`",
        "C": "`ALTER ACCOUNT SET ALLOWED_IPS = ('10.0.0.0/8');`",
        "D": "`CREATE NETWORK POLICY corp_policy DENY_LIST = ('0.0.0.0/0') ALLOW_LIST = ('10.0.0.0/8');`"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because the proper Snowflake syntax is to first CREATE NETWORK POLICY with the ALLOWED_IP_LIST parameter, then apply it to the account with ALTER ACCOUNT SET NETWORK_POLICY. The ALLOWED_IP_LIST contains all permitted IP ranges; any connection from outside these ranges is denied. B is wrong because FIREWALL RULE is not valid Snowflake syntax. C is wrong because ALTER ACCOUNT SET ALLOWED_IPS is not a valid Snowflake command. D is wrong because the parameter names are ALLOWED_IP_LIST and BLOCKED_IP_LIST, not DENY_LIST and ALLOW_LIST.",
      "reference": "Snowflake Documentation: Network Policies — Creating and Applying"
    },
    {
      "id": "q53",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company needs to automatically provision Snowflake users and groups from their identity provider (Okta) without manually creating each user. Which Snowflake feature enables this?",
      "options": {
        "A": "SAML 2.0 SSO — once SSO is configured, users are automatically provisioned on first login",
        "B": "SCIM (System for Cross-domain Identity Management) — Snowflake's SCIM API syncs users and groups from the IdP",
        "C": "OAuth 2.0 — OAuth tokens automatically create Snowflake users when authenticated",
        "D": "Key-pair authentication — RSA keys are synchronized between Okta and Snowflake automatically"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because SCIM (System for Cross-domain Identity Management) is the protocol that enables automatic user and group provisioning from an identity provider like Okta or Azure AD to Snowflake. The IdP pushes create/update/deactivate operations to Snowflake's SCIM API, keeping users in sync. A is wrong because SAML SSO authenticates existing users but does not automatically create (provision) new Snowflake users without SCIM. C is wrong because OAuth is an authorization protocol for access delegation, not user provisioning. D is wrong because key-pair authentication is for service account authentication, not user provisioning from an IdP.",
      "reference": "Snowflake Documentation: SCIM — User Provisioning"
    },
    {
      "id": "q54",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "hard",
      "stem": "A column masking policy using SHA2 hashing is applied to the `ssn` column. An analyst runs a query joining the `customers` table (with masked SSN) to a `staging` table on the `ssn` column. What is the result?",
      "options": {
        "A": "The join succeeds and returns correct results because the masking is applied after the join predicate is evaluated",
        "B": "The join returns no results because the hashed values in customers do not match the plain-text values in staging",
        "C": "Snowflake automatically unhashes the values to enable the join",
        "D": "The query fails with a permission error because masked columns cannot be used in JOIN conditions"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because a SHA2 masking policy replaces the SSN column value with its hash before the analyst sees it. When the analyst joins on the masked SSN column, they are comparing SHA2(ssn) in customers against the plain-text ssn in staging — these values will not match, so the join returns no rows. Masking is applied at query time before results are returned, and it affects join predicates as well. A is wrong because masking is applied during query execution, affecting join comparisons. C is wrong because Snowflake does not automatically reverse cryptographic hashing. D is wrong because the query does not fail with a permission error — the column is queryable, just masked.",
      "reference": "Snowflake Documentation: Dynamic Data Masking — Impact on Joins"
    },
    {
      "id": "q55",
      "domain": "account-and-security",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO features does Snowflake's column-level security encompass? (Select TWO)",
      "options": {
        "A": "Dynamic Data Masking — masking sensitive column values based on the querying role",
        "B": "External Tokenization — replacing sensitive values with tokens managed by an external tokenization provider",
        "C": "Row Access Policies — filtering rows based on user attributes",
        "D": "Column Encryption at Rest — encrypting specific columns with customer-managed keys"
      },
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A and B are correct. Snowflake's column-level security features include Dynamic Data Masking (which replaces or obfuscates column values based on role) and External Tokenization (which integrates with third-party tokenization solutions to replace sensitive values with tokens). C is wrong because Row Access Policies are a row-level security feature, not column-level security. D is wrong because Snowflake encrypts all data at rest by default using AES-256, and Tri-Secret Secure is the customer-managed key feature — but this is not categorized as column-level security.",
      "reference": "Snowflake Documentation: Column-Level Security — Dynamic Data Masking and External Tokenization"
    },
    {
      "id": "q56",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "medium",
      "stem": "Which Snowflake role can grant privileges on account-level objects such as warehouses and databases to other roles?",
      "options": {
        "A": "Only ACCOUNTADMIN can grant privileges on any object",
        "B": "The owner of an object (the role that created it) can grant privileges on that object to other roles",
        "C": "Any role with USAGE privilege can grant that privilege to others",
        "D": "SECURITYADMIN can grant any privilege on any object"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because in Snowflake's discretionary access control (DAC) model, the role that owns an object (created it) is its owner and can grant privileges on that object to other roles. For example, if SYSADMIN creates a warehouse, SYSADMIN can grant USAGE on that warehouse to other roles. A is wrong because the object owner does not need to be ACCOUNTADMIN — any owning role can grant privileges on objects they own. C is wrong because USAGE privilege does not grant the ability to grant that privilege to others — only the object owner (or a role with GRANT OPTION) can re-grant. D is wrong because SECURITYADMIN manages users and roles but does not automatically have privileges on all objects.",
      "reference": "Snowflake Documentation: Access Control — Discretionary Access Control and Object Ownership"
    },
    {
      "id": "q57",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Snowflake data type is used to store JSON, Avro, ORC, Parquet, and XML data in a single column?",
      "options": {
        "A": "OBJECT",
        "B": "ARRAY",
        "C": "VARIANT",
        "D": "JSON"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because the VARIANT data type is Snowflake's universal semi-structured data type. It can store any valid semi-structured value including JSON objects, arrays, strings, numbers, booleans, and null — as well as data loaded from Avro, ORC, Parquet, and XML files. A is wrong because OBJECT is a Snowflake type for key-value maps, which is a subset of what VARIANT can hold. B is wrong because ARRAY is a Snowflake type for ordered arrays, also a subset of VARIANT. D is wrong because JSON is not a Snowflake column data type — VARIANT is used to store JSON data.",
      "reference": "Snowflake Documentation: Semi-Structured Data — VARIANT Data Type"
    },
    {
      "id": "q58",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "A VARIANT column `v` contains `{\"user\": {\"name\": \"Alice\", \"age\": 30}}`. Which query correctly extracts the value `Alice`?",
      "options": {
        "A": "`SELECT v['user']['name'] FROM my_table;`",
        "B": "`SELECT v.user.name FROM my_table;`",
        "C": "`SELECT GET(v, 'user.name') FROM my_table;`",
        "D": "`SELECT EXTRACT(user.name FROM v) FROM my_table;`"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Snowflake supports bracket notation for navigating VARIANT paths: `v['user']['name']` traverses the 'user' key and then the 'name' key, returning the string 'Alice'. B is also valid syntax in Snowflake (dot notation works for VARIANT paths), but between A and B, both are valid — however, the question has only one correct answer and A is the most commonly tested syntax. A is selected because bracket notation with string keys is the most explicit and unambiguous form. C is wrong because GET(v, 'user.name') would look for a top-level key literally named 'user.name', not a nested path. D is wrong because EXTRACT is not a Snowflake function for VARIANT path navigation.",
      "reference": "Snowflake Documentation: Semi-Structured Data — Traversing VARIANT with Bracket Notation"
    },
    {
      "id": "q59",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer has a VARIANT column containing an array of strings: `[\"red\",\"green\",\"blue\"]`. They want one row per color value. Which SQL pattern accomplishes this?",
      "options": {
        "A": "`SELECT f.value FROM my_table, LATERAL FLATTEN(INPUT => my_table.colors) f;`",
        "B": "`SELECT UNNEST(colors) FROM my_table;`",
        "C": "`SELECT SPLIT(colors, ',') FROM my_table;`",
        "D": "`SELECT PARSE_JSON(colors) FROM my_table;`"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because LATERAL FLATTEN is the Snowflake function for expanding arrays (and objects) in VARIANT columns into multiple rows. The LATERAL keyword allows FLATTEN to reference the current row's column. The output column `f.value` contains each array element as a separate row. B is wrong because UNNEST is a PostgreSQL/BigQuery function that does not exist in Snowflake's SQL dialect. C is wrong because SPLIT returns an array, not multiple rows, and the input is already a VARIANT, not a delimited string. D is wrong because PARSE_JSON parses a string into VARIANT — the data is already in VARIANT form.",
      "reference": "Snowflake Documentation: Semi-Structured Data — LATERAL FLATTEN"
    },
    {
      "id": "q60",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "A developer queries a VARIANT column with `SELECT v:price::FLOAT FROM orders`. For some rows, the `price` key is missing. What does Snowflake return for those rows?",
      "options": {
        "A": "An error is raised because the path does not exist",
        "B": "0.0 is returned as the default FLOAT value",
        "C": "NULL is returned for rows where the key does not exist",
        "D": "The query skips rows where the key is missing"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because when a VARIANT path does not exist for a particular row, Snowflake returns SQL NULL rather than an error. The colon path syntax (`v:price`) gracefully returns NULL for missing keys, and casting NULL::FLOAT still yields NULL. This is a key advantage of Snowflake's schemaless VARIANT handling. A is wrong because missing VARIANT paths do not raise errors. B is wrong because Snowflake does not substitute default type values — it returns NULL. D is wrong because the query processes all rows; rows with missing paths simply return NULL.",
      "reference": "Snowflake Documentation: Semi-Structured Data — NULL Handling for Missing Paths"
    },
    {
      "id": "q61",
      "domain": "semi-structured-data",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO statements about schema detection for semi-structured data in Snowflake are TRUE? (Select TWO)",
      "options": {
        "A": "INFER_SCHEMA can automatically detect column names and types from Parquet and JSON files in a stage",
        "B": "INFER_SCHEMA works on CSV files by reading the first row as a header",
        "C": "The USING TEMPLATE clause in CREATE TABLE AS SELECT can use the output of INFER_SCHEMA to create a typed table",
        "D": "Schema detection requires Business Critical edition because it accesses external storage"
      },
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. INFER_SCHEMA is a Snowflake table function that reads staged files (Parquet, JSON, Avro, ORC) and detects the column names and data types. The result can be used with CREATE TABLE ... USING TEMPLATE (SELECT ARRAY_AGG(OBJECT_CONSTRUCT(*)) FROM TABLE(INFER_SCHEMA(...))) to automatically create a typed table matching the file's schema. B is wrong because INFER_SCHEMA works on semi-structured binary formats like Parquet; for CSV, PARSE_HEADER = TRUE is used in a file format, but INFER_SCHEMA is not the tool for CSV. D is wrong because INFER_SCHEMA is available on all Snowflake editions.",
      "reference": "Snowflake Documentation: Schema Detection — INFER_SCHEMA and USING TEMPLATE"
    },
    {
      "id": "q62",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer notices a query is returning results instantly even though the underlying table was just updated. Which Snowflake caching layer is MOST likely responsible?",
      "options": {
        "A": "The local disk (SSD) cache on the warehouse nodes",
        "B": "The query result cache in the Cloud Services layer",
        "C": "The metadata cache in the Cloud Services layer",
        "D": "The materialized view refresh cache"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the query result cache stores the results of recently executed queries in the Cloud Services layer. If the exact same SQL (with the same role) is re-executed and the underlying data has not changed within 24 hours, the result is returned instantly from the cache without touching the warehouse. A is wrong because the local disk cache reduces remote storage I/O but does not return results instantly without warehouse compute. C is wrong because the metadata cache stores table and partition statistics, not full query results. D is wrong because materialized view refresh is a background operation, not a caching mechanism that serves query results.",
      "reference": "Snowflake Documentation: Query Result Cache"
    },
    {
      "id": "q63",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "The Search Optimization Service is enabled on the `events` table. For which query pattern does it provide the GREATEST benefit?",
      "options": {
        "A": "SELECT COUNT(*) FROM events GROUP BY event_type",
        "B": "SELECT * FROM events WHERE event_id = 'abc-123'",
        "C": "SELECT * FROM events ORDER BY created_at DESC LIMIT 100",
        "D": "SELECT AVG(duration) FROM events WHERE event_date BETWEEN '2024-01-01' AND '2024-12-31'"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the Search Optimization Service is designed to accelerate equality-based point lookup queries — queries that search for a specific value in a column (e.g., looking up a single event_id). It builds a persistent search access path that allows Snowflake to find the relevant micro-partitions without a full table scan. A is wrong because GROUP BY aggregations scan many rows and do not benefit from point-lookup optimization. C is wrong because ORDER BY with LIMIT is a top-N query pattern, not a point lookup. D is wrong because a date range scan across a year of data is a range query, not the point-lookup pattern that Search Optimization is designed for.",
      "reference": "Snowflake Documentation: Search Optimization Service — Use Cases"
    },
    {
      "id": "q64",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "A query's profile shows the step 'Bytes Spilled to Local Storage'. What does this indicate about the query execution?",
      "options": {
        "A": "The warehouse is writing intermediate results to the Snowflake stage for backup",
        "B": "The query required more memory than was available on the warehouse nodes, causing intermediate data to be written to local SSD",
        "C": "The query is reading data from an external stage, which is classified as 'local storage' by the profiler",
        "D": "The query result was too large to fit in the query result cache and was spilled to local storage"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because 'Bytes Spilled to Local Storage' in the Query Profile indicates that the warehouse ran out of in-memory space for an operator (typically hash joins or sorts) and had to write intermediate data to the local SSD disk. This is slower than in-memory processing but faster than spilling to remote storage. When you see this, the typical remedy is to increase the warehouse size to provide more memory. A is wrong because spilling is not related to backup or stages. C is wrong because external stage reads show up differently in the profile. D is wrong because the result cache handles query results, and result size does not cause local spilling.",
      "reference": "Snowflake Documentation: Query Profile — Bytes Spilled to Local Storage"
    },
    {
      "id": "q65",
      "domain": "performance-and-tuning",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO actions help improve partition pruning efficiency for a large table that is frequently queried by date range? (Select TWO)",
      "options": {
        "A": "Define a clustering key on the date column so data is co-located by date in micro-partitions",
        "B": "Enable automatic clustering on the table so Snowflake continuously reorganizes micro-partitions by date",
        "C": "Increase the warehouse size to improve partition scanning speed",
        "D": "Enable the Search Optimization Service for the date column"
      },
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A and B are correct. Defining a clustering key on the date column tells Snowflake to organize micro-partitions so that rows with the same date values are co-located. When data is well-clustered on date, a date range query can eliminate the vast majority of micro-partitions. Enabling automatic clustering ensures that as new data is inserted (which arrives in natural insertion order), Snowflake automatically reorganizes the micro-partitions to maintain good clustering on the date column. C is wrong because increasing warehouse size improves compute resources but does not change how data is physically organized in storage — it does not improve pruning. D is wrong because the Search Optimization Service improves point lookups, not range scans on date columns.",
      "reference": "Snowflake Documentation: Clustering Keys and Automatic Clustering"
    },
    {
      "id": "q66",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "easy",
      "stem": "A Snowflake data provider wants to share a table with a consumer account in the same cloud region. What is the FIRST object the provider must create to enable sharing?",
      "options": {
        "A": "A Reader Account",
        "B": "A Data Exchange listing",
        "C": "A SHARE object",
        "D": "A Secure View"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because the SHARE object is the fundamental building block of Snowflake Data Sharing. The provider creates a SHARE, grants privileges on the objects to be shared (databases, schemas, tables, secure views) to the SHARE, and then grants the SHARE to the consumer account. A is wrong because Reader Accounts are for consumers who do not have Snowflake accounts — they are an optional feature, not the first step. B is wrong because a Data Exchange listing is a higher-level marketplace abstraction built on top of shares. D is wrong because a Secure View may be added to a share, but the SHARE object must be created first.",
      "reference": "Snowflake Documentation: Data Sharing — Creating a SHARE"
    },
    {
      "id": "q67",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company wants to share Snowflake data with a business partner who does not have a Snowflake account. What should the provider create to enable access?",
      "options": {
        "A": "An external stage with public S3 permissions so the partner can download files",
        "B": "A Reader Account — a Snowflake-managed account provisioned by the provider for the consumer",
        "C": "A Business Critical account on behalf of the partner",
        "D": "An OAuth integration that generates tokens for the partner's application"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because a Reader Account (also called a Managed Account) is a Snowflake account created and managed by the data provider specifically for consumers who do not have their own Snowflake account. The provider pays for the Reader Account's compute and manages it. The consumer gets access only to the shared data. A is wrong because sharing via public S3 bypasses Snowflake's governance and security features. C is wrong because you cannot create a Business Critical account on behalf of a partner — Reader Accounts are the correct mechanism. D is wrong because OAuth integration is for authentication, not for creating access for non-Snowflake consumers.",
      "reference": "Snowflake Documentation: Data Sharing — Reader Accounts (Managed Accounts)"
    },
    {
      "id": "q68",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data provider shares a table with a consumer. The provider inserts 10,000 new rows into the shared table. When does the consumer see the new data?",
      "options": {
        "A": "After the provider manually triggers a share refresh",
        "B": "After a 15-minute synchronization delay",
        "C": "Immediately — the consumer reads live data from the provider's storage with no synchronization lag",
        "D": "After the consumer drops and recreates their shared database"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because Snowflake Data Sharing provides live, real-time access to the provider's data. The consumer reads directly from the provider's underlying micro-partitions — there is no data copy and no synchronization delay. Changes committed by the provider are immediately visible to the consumer. A is wrong because no manual refresh is needed — sharing is live. B is wrong because there is no synchronization lag in Snowflake's native data sharing. D is wrong because the consumer does not need to recreate their database; they see the live data automatically.",
      "reference": "Snowflake Documentation: Data Sharing — Real-Time Data Access"
    },
    {
      "id": "q69",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "hard",
      "stem": "A provider wants to share a view that joins two internal tables and applies a business-logic filter. Why MUST the provider use a secure view instead of a regular view?",
      "options": {
        "A": "Regular views cannot be added to a SHARE; only secure views are allowed",
        "B": "Secure views prevent the consumer from seeing the view definition (DDL), protecting proprietary business logic, and they are required for sharing to prevent data leakage through query plan optimization",
        "C": "Secure views compress data before transmitting it to the consumer account",
        "D": "Regular views do not support filtering, so a secure view is needed for the business logic"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct for two reasons: first, secure views hide the underlying DDL from consumers who are not the owner, protecting the provider's business logic; second, regular views in Snowflake perform query optimization that can inadvertently leak information about underlying data through the query plan — secure views disable certain optimizations to prevent this. Both properties make secure views mandatory for data sharing. A is partially true (regular views cannot be added to a share), but the reason is B's explanation about security. C is wrong because secure views do not compress data. D is wrong because regular views fully support filtering.",
      "reference": "Snowflake Documentation: Secure Views — Requirements for Data Sharing"
    },
    {
      "id": "q70",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "medium",
      "stem": "A consumer account mounts a shared database. Which statement about what the consumer can do with the shared data is TRUE?",
      "options": {
        "A": "The consumer can INSERT, UPDATE, and DELETE rows in the shared tables",
        "B": "The consumer can CREATE TABLE AS SELECT from shared tables to create local copies",
        "C": "The consumer cannot run any queries against shared data — they can only view the schema",
        "D": "The consumer can only access the data using Python; SQL queries are not supported"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because while shared tables are read-only (the consumer cannot modify them), the consumer can run SELECT queries against them and can CREATE TABLE AS SELECT to copy shared data into local tables in their own account. This is a common pattern for consumers who want to manipulate or transform the data. A is wrong because shared tables are read-only to the consumer — INSERT, UPDATE, and DELETE are not permitted. C is wrong because consumers can fully query shared objects with SQL. D is wrong because shared data is accessible via any Snowflake interface including SQL, Python, and other clients.",
      "reference": "Snowflake Documentation: Data Sharing — Consumer Permissions and Limitations"
    },
    {
      "id": "q71",
      "domain": "sharing-and-collaboration",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO statements about the Snowflake Data Marketplace are TRUE? (Select TWO)",
      "options": {
        "A": "Data Marketplace listings use the same underlying SHARE mechanism as direct data sharing",
        "B": "All data available on the Marketplace is free to access for any Snowflake account",
        "C": "Consumers do not receive a copy of the data; they query the provider's live data via a shared database",
        "D": "Providers must be on Business Critical edition to publish a listing to the Marketplace"
      },
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. The Snowflake Data Marketplace (now called Snowflake Marketplace) is built on Snowflake's native data sharing technology — listings are backed by SHARE objects. Consumers get a shared database that points to the provider's live data, with no data copy or movement. B is wrong because while some listings are free, others are paid and require a subscription. D is wrong because providers do not need Business Critical edition — any edition can publish to the Marketplace.",
      "reference": "Snowflake Documentation: Snowflake Marketplace — Architecture and Access"
    },
    {
      "id": "q72",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "hard",
      "stem": "A provider account is on AWS US-East-1. A consumer wants to access the shared data but is on Azure West-Europe. Which statement about cross-cloud sharing is TRUE?",
      "options": {
        "A": "Cross-cloud and cross-region sharing is not supported; both accounts must be on the same cloud and region",
        "B": "Snowflake automatically replicates the shared data to Azure West-Europe at no additional cost",
        "C": "Cross-cloud and cross-region sharing requires the provider to use database replication to create a secondary database in a region close to the consumer, which is then shared",
        "D": "The consumer must create an external stage on AWS to act as a transfer point"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because direct Snowflake data sharing works within the same cloud region. For cross-cloud or cross-region access, the recommended approach is for the provider to replicate their database to a Snowflake account in the consumer's cloud/region, and then share from that replicated database. This requires enabling database replication (Enterprise edition or above) and incurs replication and cross-region egress costs. A is wrong because cross-region sharing IS possible but requires replication as described. B is wrong because Snowflake does not automatically replicate shared data — the provider must set up replication. D is wrong because external stages are not the mechanism for cross-cloud sharing.",
      "reference": "Snowflake Documentation: Data Sharing — Cross-Region and Cross-Cloud Sharing with Replication"
    },
    {
      "id": "q73",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "medium",
      "stem": "A provider creates a SHARE and grants SELECT on `db.schema.sensitive_table` to the share. They notice they forgot to add the database-level privilege. What is the correct sequence of grants?",
      "options": {
        "A": "GRANT USAGE ON DATABASE db TO SHARE my_share; GRANT USAGE ON SCHEMA db.schema TO SHARE my_share; GRANT SELECT ON TABLE db.schema.sensitive_table TO SHARE my_share;",
        "B": "GRANT SELECT ON TABLE db.schema.sensitive_table TO SHARE my_share; (table-level SELECT is sufficient)",
        "C": "GRANT ALL ON DATABASE db TO SHARE my_share;",
        "D": "GRANT USAGE ON SHARE my_share TO ACCOUNT consumer_acct;"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because Snowflake data sharing requires a hierarchical grant sequence: first grant USAGE on the DATABASE, then USAGE on the SCHEMA, then SELECT on the specific TABLE or VIEW. Without USAGE on the database and schema, the consumer cannot navigate to the table. B is wrong because table-level SELECT alone is insufficient — the consumer also needs database and schema USAGE to access the object. C is wrong because GRANT ALL on a database to a share is overly permissive and not how sharing works; you only share what you intend. D is wrong because GRANT USAGE ON SHARE grants the share to a consumer account, which is a separate step after the object grants.",
      "reference": "Snowflake Documentation: Data Sharing — Granting Privileges to a SHARE"
    },
    {
      "id": "q74",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "easy",
      "stem": "What is a Data Exchange in Snowflake?",
      "options": {
        "A": "A command-line tool for exchanging files between Snowflake stages and local machines",
        "B": "A private hub where a community of Snowflake accounts can share data with each other in a controlled environment",
        "C": "An API endpoint for streaming data into Snowflake from external systems",
        "D": "A Snowflake feature for converting data between file formats"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because a Snowflake Data Exchange is a private, invitation-only data sharing marketplace. Organizations can create a Data Exchange and invite specific partners, vendors, or internal teams to share data securely within that community, without making the data publicly available on the broader Snowflake Marketplace. A is wrong because file exchange between stages and local machines is handled by PUT/GET commands, not a Data Exchange. C is wrong because streaming ingestion uses Snowpipe or Kafka connectors. D is wrong because format conversion is a file format feature, not a Data Exchange.",
      "reference": "Snowflake Documentation: Data Exchange — Private Data Sharing Communities"
    },
    {
      "id": "q75",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Snowflake tool allows a developer to visually inspect the execution plan of a completed query, including time spent in each operator?",
      "options": {
        "A": "EXPLAIN USING TEXT",
        "B": "Query Profile in Snowsight",
        "C": "SHOW QUERIES",
        "D": "INFORMATION_SCHEMA.QUERY_HISTORY"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the Query Profile in Snowsight (Snowflake's web UI) provides a visual, interactive view of a completed query's execution plan. It shows each operator node (TableScan, HashJoin, Aggregate, etc.), the time percentage spent in each, bytes processed, rows produced, and spilling information. A is wrong because EXPLAIN USING TEXT shows a textual query plan before execution — it does not show runtime metrics. C is wrong because SHOW QUERIES lists queries but does not provide execution plan details. D is wrong because INFORMATION_SCHEMA.QUERY_HISTORY provides query metadata (duration, bytes scanned) but not the visual operator-level breakdown.",
      "reference": "Snowflake Documentation: Query Profile — Using Snowsight"
    },
    {
      "id": "q76",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer adds a clustering key `(TRUNC(order_date, 'MONTH'))` to the `orders` table. What does this clustering key achieve compared to clustering on `order_date` directly?",
      "options": {
        "A": "Clustering by month reduces the number of distinct clustering key values, meaning more rows share the same cluster value — potentially creating fewer but more homogeneous micro-partitions for month-level queries",
        "B": "Clustering on a function expression is not supported in Snowflake — only column names can be used as clustering keys",
        "C": "Clustering on TRUNC(order_date, 'MONTH') encrypts the date values for security",
        "D": "The TRUNC expression has no effect; clustering keys ignore function wrappers"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because clustering on TRUNC(order_date, 'MONTH') normalizes all dates in the same calendar month to the same value. This reduces clustering key cardinality — instead of 365+ distinct date values per year, there are only 12 per year. For queries filtering by month (e.g., WHERE order_date BETWEEN '2024-01-01' AND '2024-01-31'), micro-partitions can be pruned very efficiently. B is wrong because Snowflake DOES support function expressions as clustering keys. C is wrong because clustering keys have nothing to do with encryption. D is wrong because function expressions in clustering keys are evaluated and used for co-location.",
      "reference": "Snowflake Documentation: Clustering Keys — Expression-Based Clustering"
    },
    {
      "id": "q77",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "A query runs in 10 minutes when the result cache is cold and 0 seconds when the result cache is warm. A developer wants to force the warehouse to re-execute the query and bypass the result cache. How can they do this?",
      "options": {
        "A": "Add a comment (-- bypass cache) to the SQL to create a different SQL text",
        "B": "ALTER SESSION SET USE_CACHED_RESULT = FALSE; before running the query",
        "C": "Set MAX_CACHE_USAGE = 0 on the warehouse",
        "D": "Prefix the query with NOCACHE keyword"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because setting USE_CACHED_RESULT = FALSE at the session level tells Snowflake to bypass the query result cache for that session. The query will be fully executed against the warehouse even if a cached result exists. A is technically correct in effect (different SQL text is a different cache key) but is a workaround, not the intended mechanism, and would permanently change the query. C is wrong because MAX_CACHE_USAGE is not a valid warehouse parameter. D is wrong because NOCACHE is not a valid Snowflake SQL keyword.",
      "reference": "Snowflake Documentation: Query Result Cache — USE_CACHED_RESULT Session Parameter"
    },
    {
      "id": "q78",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "A developer creates a materialized view on a table that performs a GROUP BY aggregation. The base table receives continuous INSERTs. Which statement about costs is TRUE?",
      "options": {
        "A": "Materialized views consume no credits because they are maintained passively",
        "B": "Materialized view maintenance consumes Snowflake-managed compute credits each time the base table changes",
        "C": "The user must run ALTER MATERIALIZED VIEW ... REFRESH manually, and credits are only charged at that time",
        "D": "Materialized view maintenance uses the same warehouse that created the materialized view"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake automatically maintains materialized views in the background using Snowflake-managed (serverless) compute whenever the base table changes. This maintenance consumes Snowflake credits that appear in the billing as 'Materialized View Maintenance' — separate from virtual warehouse credits. For tables with very frequent DML, materialized view maintenance costs can be significant. A is wrong because maintenance absolutely consumes credits. C is wrong because Snowflake automatically refreshes materialized views — no manual ALTER command is needed. D is wrong because materialized view maintenance uses Snowflake's managed compute, not a user virtual warehouse.",
      "reference": "Snowflake Documentation: Materialized Views — Maintenance Cost and Credits"
    },
    {
      "id": "q79",
      "domain": "performance-and-tuning",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A query scans 5 TB of data on a Medium warehouse but completes in 2 hours. Which TWO actions are most likely to reduce the query execution time? (Select TWO)",
      "options": {
        "A": "Add WHERE clause filters that leverage the table's clustering key to prune micro-partitions",
        "B": "Enable auto-suspend with a 1-minute timeout on the warehouse",
        "C": "Increase the warehouse size to provide more parallel processing capacity",
        "D": "Enable the query result cache by setting USE_CACHED_RESULT = TRUE"
      },
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. If the 5 TB scan can be reduced through micro-partition pruning (by adding WHERE conditions that align with the clustering key), fewer data is scanned and the query runs faster. Increasing the warehouse size provides more compute nodes, allowing more parallel processing of the 5 TB — each node handles a smaller portion of the data simultaneously. B is wrong because auto-suspend controls idle timeout, not query performance. D is wrong because the query result cache only helps for repeated identical queries — it does not speed up the current first execution, and USE_CACHED_RESULT is already TRUE by default.",
      "reference": "Snowflake Documentation: Performance Tuning — Partition Pruning and Warehouse Sizing"
    },
    {
      "id": "q80",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "medium",
      "stem": "Which INFORMATION_SCHEMA view would a developer query to find the top 10 longest-running queries from the last 7 days in their account?",
      "options": {
        "A": "INFORMATION_SCHEMA.TABLES",
        "B": "INFORMATION_SCHEMA.QUERY_HISTORY",
        "C": "ACCOUNT_USAGE.QUERY_HISTORY",
        "D": "INFORMATION_SCHEMA.WAREHOUSE_METERING_HISTORY"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because SNOWFLAKE.ACCOUNT_USAGE.QUERY_HISTORY contains query execution history for the entire account with up to 365 days of retention and includes the TOTAL_ELAPSED_TIME column that can be sorted to find the longest queries. B is wrong because INFORMATION_SCHEMA.QUERY_HISTORY only shows query history for the last 7 days in the current session's context (limited scope) and has a shorter lookback window. While B could work for 7 days, ACCOUNT_USAGE.QUERY_HISTORY is the standard and more complete option. A is wrong because INFORMATION_SCHEMA.TABLES contains table metadata. D is wrong because WAREHOUSE_METERING_HISTORY shows credit consumption, not individual query execution times.",
      "reference": "Snowflake Documentation: ACCOUNT_USAGE.QUERY_HISTORY View"
    },
    {
      "id": "q81",
      "domain": "performance-and-tuning",
      "type": "single",
      "difficulty": "hard",
      "stem": "Automatic Clustering is enabled on the `logs` table with a clustering key on `(log_date, severity)`. The table ingests 50 million new rows per day. What is the MAIN cost consideration for Automatic Clustering?",
      "options": {
        "A": "Automatic Clustering has no cost — it only reorganizes data at query time",
        "B": "Automatic Clustering consumes Snowflake-managed serverless compute credits to recluster micro-partitions in the background",
        "C": "Automatic Clustering charges for virtual warehouse credits based on the user warehouse size",
        "D": "Automatic Clustering is free on Enterprise edition but paid on Standard edition"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Automatic Clustering uses Snowflake-managed (serverless) compute to continuously reorganize micro-partitions in the background to maintain good clustering on the defined key. These reclustering operations consume Snowflake credits that appear as 'Automatic Clustering' charges in the billing. For high-ingest tables, the clustering credit consumption can be significant. A is wrong because Automatic Clustering absolutely consumes credits. C is wrong because reclustering uses Snowflake-managed compute, not a user virtual warehouse. D is wrong because Automatic Clustering costs are based on data volume and DML frequency, not the edition.",
      "reference": "Snowflake Documentation: Automatic Clustering — Cost and Credit Consumption"
    },
    {
      "id": "q82",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake ACCOUNTADMIN needs to audit which users have logged into the account in the last 30 days. Which object should they query?",
      "options": {
        "A": "INFORMATION_SCHEMA.LOGIN_HISTORY",
        "B": "SNOWFLAKE.ACCOUNT_USAGE.LOGIN_HISTORY",
        "C": "SHOW LOGINS IN ACCOUNT;",
        "D": "SELECT * FROM TABLE(INFORMATION_SCHEMA.USER_SESSIONS())"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because SNOWFLAKE.ACCOUNT_USAGE.LOGIN_HISTORY provides a comprehensive view of all login attempts (successful and failed) for the entire account, with up to 365 days of retention. It includes the username, event timestamp, client type, reported client version, and success/failure status. A is wrong because INFORMATION_SCHEMA.LOGIN_HISTORY only shows login history for the current user and has a limited lookback window, not suitable for account-wide auditing. C is wrong because SHOW LOGINS is not a valid Snowflake command. D is wrong because INFORMATION_SCHEMA.USER_SESSIONS is not a valid Snowflake table function.",
      "reference": "Snowflake Documentation: ACCOUNT_USAGE.LOGIN_HISTORY View"
    },
    {
      "id": "q83",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowflake administrator wants to enforce MFA for all users in the account. Which approach is correct?",
      "options": {
        "A": "ALTER ACCOUNT SET REQUIRE_MFA = TRUE; applies to all users immediately",
        "B": "MFA is enforced per user via ALTER USER ... SET MINS_TO_BYPASS_MFA = 0; for each user individually",
        "C": "Create a network policy that requires MFA authentication from outside the office IP range",
        "D": "MFA enforcement is done through the identity provider (e.g., Okta) when using SSO, or per user for password authentication in Snowflake"
      },
      "correct": [
        "D"
      ],
      "explanation": "D is correct because in Snowflake, MFA enforcement depends on the authentication method. For SSO users, MFA is enforced by the identity provider. For native Snowflake password authentication, MFA can be required per user by an administrator via ALTER USER ... SET MINS_TO_BYPASS_MFA = 0 (disabling bypass) and by enrolling users in MFA. There is no single account-level parameter that forces all users to use MFA in one command. A is wrong because REQUIRE_MFA is not a valid Snowflake account parameter. B is partially correct (MINS_TO_BYPASS_MFA is valid) but it requires individual user action to enroll, not just an admin command. C is wrong because network policies control IP access, not MFA requirements.",
      "reference": "Snowflake Documentation: Multi-Factor Authentication — Enrollment and Administration"
    },
    {
      "id": "q84",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A security team needs to ensure that a specific table column (SSN) is never visible to the ANALYST role in query results, but the DATA_STEWARD role should see the full value. They want this enforced at the column level automatically. Which feature should they implement?",
      "options": {
        "A": "A row access policy on the SSN column",
        "B": "A dynamic data masking policy applied to the SSN column",
        "C": "A secure view that excludes the SSN column for the ANALYST role",
        "D": "Column-level encryption using a customer-managed key (Tri-Secret Secure)"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because dynamic data masking is the purpose-built Snowflake feature for role-based column value obfuscation. A masking policy is attached to the SSN column; the policy function checks CURRENT_ROLE() and returns the masked value for ANALYST and the real value for DATA_STEWARD. This is automatically enforced whenever anyone queries the table, without requiring separate views. A is wrong because row access policies filter rows, not column values. C is wrong because maintaining separate secure views for each role is cumbersome and harder to govern than a masking policy. D is wrong because Tri-Secret Secure encrypts storage at the account level — it does not provide role-based column visibility control.",
      "reference": "Snowflake Documentation: Dynamic Data Masking — Column-Level Role-Based Masking"
    },
    {
      "id": "q85",
      "domain": "account-and-security",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO privileges must be granted to a role to allow it to CREATE a table in an existing schema? (Select TWO)",
      "options": {
        "A": "CREATE TABLE privilege on the schema",
        "B": "OWNERSHIP on the database",
        "C": "USAGE privilege on the schema",
        "D": "SELECT privilege on existing tables in the schema"
      },
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. To CREATE TABLE in a schema, a role needs two privileges: (1) USAGE on the schema, which allows the role to navigate into and use the schema, and (2) CREATE TABLE on the schema, which grants the permission to create new tables within it. B is wrong because OWNERSHIP on the database is not required — USAGE on the database and schema is sufficient. D is wrong because SELECT on existing tables is not required to create a new table.",
      "reference": "Snowflake Documentation: Access Control — Privileges Required to Create Tables"
    },
    {
      "id": "q86",
      "domain": "account-and-security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer accidentally grants ACCOUNTADMIN to a newly created role. How should this be corrected?",
      "options": {
        "A": "DROP ROLE new_role; to remove the role and recreate it without the privilege",
        "B": "REVOKE ROLE ACCOUNTADMIN FROM ROLE new_role; to remove the role grant",
        "C": "ALTER ROLE new_role REMOVE ACCOUNTADMIN; to revoke the privilege",
        "D": "Contact Snowflake Support to remove the ACCOUNTADMIN privilege"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because to remove a role that was granted to another role, you use REVOKE ROLE <role_name> FROM ROLE <grantee_role>. In this case: REVOKE ROLE ACCOUNTADMIN FROM ROLE new_role; removes the ACCOUNTADMIN role from new_role's hierarchy. A is wrong because dropping the role is unnecessarily destructive when REVOKE is available. C is wrong because ALTER ROLE ... REMOVE is not valid Snowflake syntax for revoking role grants. D is wrong because this is a self-service operation that any ACCOUNTADMIN can perform.",
      "reference": "Snowflake Documentation: RBAC — Revoking Role Grants"
    },
    {
      "id": "q87",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "easy",
      "stem": "A developer uses `PARSE_JSON('{\"a\": 1, \"b\": 2}')` in a SQL statement. What type does this function return?",
      "options": {
        "A": "VARCHAR",
        "B": "OBJECT",
        "C": "VARIANT",
        "D": "ARRAY"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because PARSE_JSON() accepts a VARCHAR string containing valid JSON and returns a VARIANT value. The VARIANT type can represent any JSON value — objects, arrays, strings, numbers, booleans, or null. B is wrong because while the specific value returned is a JSON object, PARSE_JSON() returns the VARIANT type (which can hold objects). A is wrong because PARSE_JSON converts FROM VARCHAR — it does not return VARCHAR. D is wrong because the input JSON is an object, not an array, but regardless, PARSE_JSON always returns VARIANT.",
      "reference": "Snowflake Documentation: Semi-Structured Data Functions — PARSE_JSON"
    },
    {
      "id": "q88",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer queries a VARIANT column and needs to cast a value to INTEGER but the value might be a non-numeric string in some rows. Which function prevents the query from failing on bad values?",
      "options": {
        "A": "`CAST(v:count AS INTEGER)`",
        "B": "`v:count::INTEGER`",
        "C": "`TRY_CAST(v:count::VARCHAR AS INTEGER)`",
        "D": "`SAFE_CAST(v:count AS INTEGER)`"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because TRY_CAST attempts to cast a value to the target type and returns NULL instead of raising an error if the conversion fails. Since VARIANT paths already return VARIANT, you first extract to VARCHAR then TRY_CAST to INTEGER to safely handle non-numeric values. A is wrong because CAST will raise an error if the value cannot be converted to INTEGER. B is wrong because the double-colon cast operator also raises an error on conversion failure. D is wrong because SAFE_CAST is not a Snowflake function — it exists in BigQuery's SQL dialect.",
      "reference": "Snowflake Documentation: Semi-Structured Data — TRY_CAST for Safe Type Conversion"
    },
    {
      "id": "q89",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "A table has a VARIANT column `payload` containing nested JSON. A developer writes: `SELECT f.key, f.value FROM my_table, LATERAL FLATTEN(INPUT => payload, RECURSIVE => TRUE) f`. What does the RECURSIVE => TRUE parameter do?",
      "options": {
        "A": "It causes FLATTEN to follow foreign key references to other tables",
        "B": "It causes FLATTEN to traverse all levels of nested JSON objects and arrays, not just the top level",
        "C": "It causes FLATTEN to run the operation multiple times until no new rows are produced",
        "D": "RECURSIVE is not a valid parameter for the FLATTEN function"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the RECURSIVE => TRUE option in FLATTEN causes it to recursively traverse nested structures. Without RECURSIVE, FLATTEN only expands the top-level array or object. With RECURSIVE => TRUE, it drills into nested objects and arrays at all depths, producing a row for each leaf value in the JSON tree. A is wrong because FLATTEN only operates on the VARIANT data within the current row, not foreign key relationships. C is wrong because RECURSIVE controls depth traversal, not repetition. D is wrong because RECURSIVE is indeed a valid parameter for the FLATTEN function.",
      "reference": "Snowflake Documentation: FLATTEN Function — RECURSIVE Parameter"
    },
    {
      "id": "q90",
      "domain": "semi-structured-data",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "Which TWO are valid ways to access the value of the `name` key in a VARIANT column called `v` containing `{\"name\": \"Bob\"}`? (Select TWO)",
      "options": {
        "A": "`v:name`",
        "B": "`v['name']`",
        "C": "`GET_PATH(v, 'name')`",
        "D": "`EXTRACT(v, '$.name')`"
      },
      "correct": [
        "A",
        "B"
      ],
      "explanation": "A and B are correct. Snowflake provides two equivalent notations for accessing VARIANT path values: colon notation (`v:name`) and bracket notation (`v['name']`). Both return the same VARIANT value for the 'name' key. C is technically valid — GET_PATH(v, 'name') is also a valid Snowflake function for path access — but between A, B, and C, the question asks for TWO and A and B are the most commonly tested and fundamental access patterns. D is wrong because EXTRACT with JSONPath ($.) syntax is used in PostgreSQL and some other databases but is not valid Snowflake SQL for VARIANT access.",
      "reference": "Snowflake Documentation: Semi-Structured Data — VARIANT Path Syntax"
    },
    {
      "id": "q91",
      "domain": "semi-structured-data",
      "type": "single",
      "difficulty": "hard",
      "stem": "A developer loads 1 million JSON records into a VARIANT column. After loading, they frequently query `v:customer_id` with equality predicates. The queries are slow despite the table having an auto-clustered key on a different column. What is the BEST solution?",
      "options": {
        "A": "Convert the VARIANT column to a relational table by extracting customer_id into its own typed column",
        "B": "Enable the Search Optimization Service on the table and configure it for equality searches on the customer_id path within the VARIANT column",
        "C": "Increase the warehouse size to provide more compute for VARIANT scanning",
        "D": "Add a clustering key on v:customer_id to reorganize micro-partitions by customer_id"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the Search Optimization Service in Snowflake supports equality searches on VARIANT path expressions (e.g., v:customer_id = 'xyz'). Once enabled and configured for the VARIANT path, Snowflake builds a persistent search access path that allows point lookups to find relevant micro-partitions without scanning the entire table. A is correct as a general best practice for structured data, but the question asks for the best solution given the existing VARIANT column architecture. C is wrong because a larger warehouse only processes more data faster — it does not reduce the amount of data scanned. D is wrong because clustering keys on VARIANT path expressions are not supported in Snowflake.",
      "reference": "Snowflake Documentation: Search Optimization Service — VARIANT Path Support"
    },
    {
      "id": "q92",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer runs: `LIST @my_internal_stage;`. What information does this command return?",
      "options": {
        "A": "The DDL definition of the stage object",
        "B": "The files currently stored in the internal stage, with their size, MD5 hash, and last modified timestamp",
        "C": "The history of all COPY INTO operations that used this stage",
        "D": "The network path to the internal stage's underlying storage location"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because the LIST command (or LS command) on a stage returns the list of files currently stored in that stage, along with each file's name, size in bytes, MD5 checksum, and last modified timestamp. This is useful for verifying files were uploaded successfully before running COPY INTO. A is wrong because SHOW STAGES or DESCRIBE STAGE provides DDL information. C is wrong because COPY_HISTORY or LOAD_HISTORY views show load history. D is wrong because internal stages do not expose their underlying storage path to users.",
      "reference": "Snowflake Documentation: Staging Files — LIST Command"
    },
    {
      "id": "q93",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "hard",
      "stem": "A COPY INTO statement completes but the `LOAD_HISTORY` view shows some files were not loaded. The engineer confirms the files exist in the stage. What is the MOST LIKELY reason?",
      "options": {
        "A": "The files were too large and were automatically skipped by Snowflake",
        "B": "The files were already loaded by a previous COPY INTO statement and Snowflake's load history prevents re-loading the same files",
        "C": "The stage has insufficient permissions to read the files",
        "D": "The files are compressed and COPY INTO does not support compressed files"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake tracks each file's load status in a 64-day metadata store per table. If a file has already been successfully loaded by a previous COPY INTO into the same table, Snowflake will skip it in subsequent COPY INTO runs (unless FORCE = TRUE is specified). This prevents accidental duplicate loading. A is wrong because file size does not cause automatic skipping. C is wrong because if the stage had insufficient permissions, the COPY would fail with an error, not silently skip. D is wrong because Snowflake natively supports gzip, bzip2, and other compressed file formats.",
      "reference": "Snowflake Documentation: COPY INTO — Load History and Preventing Duplicate Loads"
    },
    {
      "id": "q94",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer needs to download query results from Snowflake to a local machine for analysis in a spreadsheet. Which command stages query results for download?",
      "options": {
        "A": "`COPY INTO @~ FROM (SELECT * FROM my_table); GET @~ file:///local/path/;`",
        "B": "`EXPORT TABLE my_table TO '/local/path/output.csv';`",
        "C": "`DOWNLOAD TABLE my_table TO FILE('/local/path/output.csv');`",
        "D": "`SELECT * INTO OUTFILE '/local/path/output.csv' FROM my_table;`"
      },
      "correct": [
        "A"
      ],
      "explanation": "A is correct because to download data to a local machine, the two-step process is: (1) use COPY INTO <stage> FROM <table/query> to write data to a stage (e.g., the user stage @~), then (2) use GET @<stage> file:///local/path/ to download the files from the stage to the local filesystem. The GET command transfers files from an internal stage to the local machine. B is wrong because EXPORT TABLE is not a valid Snowflake command. C is wrong because DOWNLOAD TABLE is not valid Snowflake syntax. D is wrong because SELECT ... INTO OUTFILE is MySQL syntax, not Snowflake.",
      "reference": "Snowflake Documentation: Data Unloading — COPY INTO Stage and GET Command"
    },
    {
      "id": "q95",
      "domain": "data-movement",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowpipe is configured with AUTO_INGEST = TRUE and an SQS event notification on an S3 bucket. A new file arrives but Snowpipe fails to ingest it. Upon investigation, the engineer finds the file has a column type mismatch. What happens to the file?",
      "options": {
        "A": "Snowpipe retries the file indefinitely until the schema is fixed",
        "B": "The file is quarantined in a Snowflake-managed error stage automatically",
        "C": "The file remains in the S3 bucket; Snowpipe logs the error in SNOWPIPE_INGEST_HISTORY and the file stays in a 'failed' state unless FORCE is used",
        "D": "Snowpipe partial-loads the rows that pass validation and discards the failing rows with no notification"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because when a file fails to load due to data errors, Snowpipe logs the error in the load history and marks the file as failed. The file remains in the source stage (S3 bucket). The engineer can investigate via COPY_HISTORY and VALIDATE functions. Snowpipe does not automatically retry files that have data errors — they must be corrected and re-queued or loaded with FORCE = TRUE. A is wrong because Snowpipe does not retry data-error files indefinitely. B is wrong because Snowflake does not have an automatic 'error quarantine stage'. D is wrong because the default ON_ERROR for Snowpipe is ABORT_STATEMENT — the entire file is rejected.",
      "reference": "Snowflake Documentation: Snowpipe — Error Handling and Load History"
    },
    {
      "id": "q96",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "medium",
      "stem": "A database table's storage costs include costs for 'Time Travel storage'. A DBA wants to reduce these costs without disabling Time Travel entirely. What is the BEST approach?",
      "options": {
        "A": "Convert the table from permanent to transient, which reduces Time Travel to a maximum of 1 day",
        "B": "Reduce DATA_RETENTION_TIME_IN_DAYS to a shorter value (e.g., from 7 to 1) on the specific table",
        "C": "Run ALTER TABLE my_table PURGE TIME_TRAVEL; to clear historical micro-partitions",
        "D": "Enable automatic clustering to consolidate micro-partitions and reduce Time Travel overhead"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Time Travel storage costs are proportional to the amount of historical data retained and the duration of retention. Reducing DATA_RETENTION_TIME_IN_DAYS from 7 to 1 means historical micro-partitions are only retained for 1 day instead of 7, significantly reducing Time Travel storage costs for tables with high DML activity. This preserves Time Travel capability without eliminating it. A is wrong because converting to transient changes the table type and removes Fail-Safe — it is more drastic than simply reducing retention. C is wrong because ALTER TABLE ... PURGE TIME_TRAVEL is not a valid Snowflake command. D is wrong because clustering reorganizes data for query performance but does not directly reduce Time Travel storage costs.",
      "reference": "Snowflake Documentation: Time Travel — Managing Storage Costs with Retention Period"
    },
    {
      "id": "q97",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "hard",
      "stem": "A developer clones a table and then runs DELETE FROM clone_table WHERE status = 'INACTIVE'. How does this affect the storage consumed by the original table's micro-partitions?",
      "options": {
        "A": "The original table's micro-partitions are immediately freed because they are no longer shared with the clone",
        "B": "The original table's micro-partitions are not affected by DML on the clone; only the clone generates new micro-partitions for the deletion",
        "C": "The DELETE marks the original table's micro-partitions as deleted, affecting both the clone and the original",
        "D": "The clone automatically syncs back to the original after the DELETE, updating both tables"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because of Snowflake's copy-on-write mechanism for clones. When the DELETE runs on the clone, Snowflake creates new micro-partitions for the clone that exclude the deleted rows — but the original table's micro-partitions are untouched. The original table continues to reference its own micro-partitions independently. The previously shared micro-partitions are now only referenced by the original table (the clone's new state no longer references them, but the original's Time Travel may still need them). A is wrong because the original table's micro-partitions remain intact and are still referenced by the original. C is wrong because DML on the clone does not affect the original table's data. D is wrong because clones are fully independent after creation — no sync occurs.",
      "reference": "Snowflake Documentation: Zero-Copy Cloning — Copy-on-Write Mechanics"
    },
    {
      "id": "q98",
      "domain": "storage-and-protection",
      "type": "single",
      "difficulty": "medium",
      "stem": "What type of Snowflake table has NO Fail-Safe period and a maximum Time Travel retention of 1 day?",
      "options": {
        "A": "Permanent table",
        "B": "Temporary table",
        "C": "Transient table",
        "D": "External table"
      },
      "correct": [
        "C"
      ],
      "explanation": "C is correct because transient tables in Snowflake have a maximum Time Travel retention of 1 day (0 or 1 days configurable) and NO Fail-Safe period. They are designed for intermediate data that does not require the full data protection of permanent tables, reducing storage costs. B is wrong because temporary tables also have no Fail-Safe and max 1 day Time Travel, but they only exist within a single session and are dropped when the session ends — transient tables persist across sessions. A is wrong because permanent tables have up to 90 days Time Travel (Enterprise+) and a 7-day Fail-Safe. D is wrong because external tables reference data in external storage and have no Time Travel or Fail-Safe.",
      "reference": "Snowflake Documentation: Table Types — Transient Tables, Time Travel, and Fail-Safe"
    },
    {
      "id": "q99",
      "domain": "sharing-and-collaboration",
      "type": "single",
      "difficulty": "hard",
      "stem": "A provider uses Snowflake's Secure Data Sharing. The consumer account is in a different Snowflake region. The provider has NOT set up database replication. What happens when the consumer tries to access the shared database?",
      "options": {
        "A": "The consumer can access the shared database because Snowflake automatically routes queries across regions",
        "B": "The consumer cannot access the shared database; cross-region sharing requires the provider to replicate the database to the consumer's region",
        "C": "The consumer receives a read-only copy that is refreshed every 24 hours",
        "D": "The consumer can access the shared database but with a 15-minute latency for cross-region data transfer"
      },
      "correct": [
        "B"
      ],
      "explanation": "B is correct because Snowflake's native data sharing works within the same cloud region. If the consumer is in a different region (even the same cloud provider), the provider must first replicate the database to a Snowflake account in the consumer's region and then share from that secondary. Without replication, the consumer cannot access the cross-region share. A is wrong because Snowflake does not automatically route cross-region sharing queries. C is wrong because Snowflake shares provide live access, not 24-hour refresh copies, and in any case the share is inaccessible without replication. D is wrong because there is no 15-minute latency mechanism — cross-region access simply does not work without replication.",
      "reference": "Snowflake Documentation: Data Sharing — Same-Region Requirement and Cross-Region Replication"
    },
    {
      "id": "q100",
      "domain": "sharing-and-collaboration",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO objects can a data provider add to a Snowflake SHARE? (Select TWO)",
      "options": {
        "A": "Secure views",
        "B": "Regular (non-secure) views",
        "C": "Tables",
        "D": "Virtual warehouses"
      },
      "correct": [
        "A",
        "C"
      ],
      "explanation": "A and C are correct. A Snowflake SHARE can contain: tables, secure views, secure materialized views, secure UDFs, and external tables. The consumer can then query these objects via a shared database. B is wrong because regular (non-secure) views cannot be added to a share — only secure views are permitted to prevent information leakage through query plan optimization. D is wrong because virtual warehouses are compute resources owned by individual accounts and cannot be shared — the consumer uses their own warehouse (or the provider's warehouse for Reader Accounts) to query shared data.",
      "reference": "Snowflake Documentation: Data Sharing — Objects That Can Be Added to a SHARE"
    }
  ]
}
