{
  "meta": {
    "id": "spc-exam-03",
    "certification": "snowpro-core",
    "certificationName": "SnowPro Core Certification",
    "title": "Mock Exam 3",
    "version": "1.0",
    "timeLimit": 115,
    "passingScore": 75,
    "totalQuestions": 100,
    "domains": [
      { "id": "architecture", "name": "Snowflake Data Cloud Features & Architecture", "weight": 25 },
      { "id": "security", "name": "Account Access and Security", "weight": 15 },
      { "id": "performance", "name": "Performance Concepts", "weight": 15 },
      { "id": "data-loading", "name": "Data Loading and Unloading", "weight": 10 },
      { "id": "data-transformation", "name": "Data Transformations", "weight": 20 },
      { "id": "data-protection", "name": "Data Protection and Data Sharing", "weight": 15 }
    ]
  },
  "questions": [
    {
      "id": "q01",
      "domain": "architecture",
      "type": "single",
      "difficulty": "easy",
      "stem": "Which Snowflake architectural layer handles query compilation and optimization before sending work to the virtual warehouse?",
      "options": [
        { "id": "A", "text": "Storage layer" },
        { "id": "B", "text": "Compute layer (virtual warehouse)" },
        { "id": "C", "text": "Cloud Services layer" },
        { "id": "D", "text": "Network ingress layer" }
      ],
      "correct": ["C"],
      "explanation": "C is correct because the Cloud Services layer is responsible for query parsing, optimization, and compilation. It uses the metadata cache (containing table statistics and micro-partition metadata) to generate an efficient execution plan before dispatching work to the virtual warehouse. A is wrong because the storage layer persists micro-partitioned data in cloud object storage. B is wrong because the compute layer (virtual warehouse) executes the already-compiled query plan but does not compile it. D is wrong because 'network ingress layer' is not a Snowflake architectural component.",
      "reference": "Snowflake Architecture — Cloud Services Layer, Query Optimization"
    },
    {
      "id": "q02",
      "domain": "architecture",
      "type": "single",
      "difficulty": "easy",
      "stem": "Snowflake is available on which cloud platforms? (Choose the BEST answer)",
      "options": [
        { "id": "A", "text": "AWS only" },
        { "id": "B", "text": "AWS and Microsoft Azure only" },
        { "id": "C", "text": "AWS, Microsoft Azure, and Google Cloud Platform" },
        { "id": "D", "text": "Any on-premises data center with supported hardware" }
      ],
      "correct": ["C"],
      "explanation": "C is correct because Snowflake is available natively on all three major public cloud providers: AWS, Microsoft Azure, and Google Cloud Platform. Customers choose their cloud provider and region at account creation. A is wrong because Snowflake is not limited to AWS. B is wrong because GCP is also a supported platform. D is wrong because Snowflake is a fully managed cloud service and does not support on-premises deployment.",
      "reference": "Snowflake Architecture — Cloud Provider Support"
    },
    {
      "id": "q03",
      "domain": "architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Medium virtual warehouse (4 credits/hr) starts up, processes a single query, then is manually suspended exactly 90 seconds later. The warehouse had been fully suspended before this run. How many credits are consumed?",
      "options": [
        { "id": "A", "text": "4 credits (full hour billed)" },
        { "id": "B", "text": "0.1 credits (90 seconds at per-second rate)" },
        { "id": "C", "text": "0.067 credits (60-second minimum: 4 credits/hr × 1/60 hr)" },
        { "id": "D", "text": "0 credits because the query result cache handled the query" }
      ],
      "correct": ["B"],
      "explanation": "B is correct because Snowflake bills per second with a 60-second minimum. The warehouse ran for 90 seconds, which exceeds the 60-second minimum, so per-second billing applies: 4 credits/hr ÷ 3600 sec/hr × 90 sec = 0.1 credits. A is wrong because Snowflake does not bill for a full hour — per-second billing after a 60-second minimum is used. C is wrong because 60 seconds (the minimum) would apply only if the warehouse ran for less than 60 seconds; at 90 seconds, the actual runtime of 90 seconds is billed. D is wrong because the query result cache returns results without warehouse compute, but the warehouse itself was running for 90 seconds and those seconds are billed regardless of cache usage.",
      "reference": "Snowflake Architecture — Virtual Warehouse Per-Second Billing, 60-Second Minimum"
    },
    {
      "id": "q04",
      "domain": "architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "Which statement BEST describes the Economy scaling policy for multi-cluster virtual warehouses?",
      "options": [
        { "id": "A", "text": "Snowflake adds a new cluster as soon as any query is queued." },
        { "id": "B", "text": "Snowflake adds a new cluster only when it estimates that the current cluster(s) will be busy for at least 6 minutes." },
        { "id": "C", "text": "The Economy policy prevents Snowflake from ever removing clusters once they are added." },
        { "id": "D", "text": "The Economy policy disables automatic scaling and requires manual cluster management." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because the Economy scaling policy is conservative — Snowflake waits until it predicts the queued load will keep an additional cluster busy for at least 6 minutes before provisioning it. This avoids spinning up clusters for brief spikes, trading some query latency for lower credit consumption. A is wrong because immediately provisioning on any queued query is the Standard (not Economy) scaling policy behavior. C is wrong because both policies remove clusters when demand drops. D is wrong because both Standard and Economy are automatic scaling policies.",
      "reference": "Snowflake Architecture — Multi-Cluster Warehouse Economy Policy"
    },
    {
      "id": "q05",
      "domain": "architecture",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake table has 1 million rows. A developer runs `SELECT COUNT(*) FROM large_table`. Which cache is MOST LIKELY used to serve this query without involving the virtual warehouse?",
      "options": [
        { "id": "A", "text": "Local disk cache" },
        { "id": "B", "text": "Query result cache" },
        { "id": "C", "text": "Metadata cache" },
        { "id": "D", "text": "External cache" }
      ],
      "correct": ["C"],
      "explanation": "C is correct because `SELECT COUNT(*)` on a table is a metadata-only operation that Snowflake can satisfy from the Cloud Services layer's metadata cache, which stores row counts and other statistics about every table. No virtual warehouse is required. B is also a possibility if the exact query was run before within 24 hours, but the metadata cache specifically enables COUNT(*) to be answered without ever engaging the warehouse. A is wrong because the local disk cache is on the warehouse nodes and requires the warehouse to be active. D is wrong because 'external cache' is not a Snowflake concept.",
      "reference": "Snowflake Architecture — Metadata Cache, Cloud Services Layer"
    },
    {
      "id": "q06",
      "domain": "architecture",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A company on the Standard edition asks which features are NOT available to them. Which TWO features are exclusive to Enterprise edition and above? (Select TWO)",
      "options": [
        { "id": "A", "text": "Time Travel retention period of up to 90 days" },
        { "id": "B", "text": "Multi-cluster warehouses" },
        { "id": "C", "text": "Data sharing with reader accounts" },
        { "id": "D", "text": "Fail-Safe data recovery" }
      ],
      "correct": ["A", "B"],
      "explanation": "A and B are correct. Time Travel retention of up to 90 days (Standard is capped at 1 day) and multi-cluster warehouses are both Enterprise edition features not available on Standard. C is wrong because data sharing and reader accounts are available on Standard edition — data sharing is a core Snowflake feature not gated to Enterprise. D is wrong because Fail-Safe (7-day recovery period) is available on all editions for standard tables.",
      "reference": "Snowflake Editions — Standard vs Enterprise Feature Comparison"
    },
    {
      "id": "q07",
      "domain": "architecture",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowflake account is configured to use 'Standard' Time Travel (default 1-day retention). A table is dropped. How long does the administrator have to run UNDROP TABLE before the data is permanently unrecoverable by users?",
      "options": [
        { "id": "A", "text": "1 day (Time Travel expires, then Fail-Safe begins and is also user-accessible)" },
        { "id": "B", "text": "1 day (Time Travel only; after expiry, data moves to Fail-Safe which users cannot access)" },
        { "id": "C", "text": "8 days (1 day Time Travel + 7 day Fail-Safe, both accessible via UNDROP)" },
        { "id": "D", "text": "7 days (only Fail-Safe applies after a DROP TABLE)" }
      ],
      "correct": ["B"],
      "explanation": "B is correct because UNDROP TABLE uses Time Travel data. Once the 1-day Time Travel retention expires, the user can no longer run UNDROP TABLE — the data enters Fail-Safe, which is managed only by Snowflake Support and is NOT accessible to users via SQL commands. So the effective window for user recovery is 1 day. A is wrong because Fail-Safe is NOT user-accessible. C is wrong because UNDROP TABLE cannot access Fail-Safe data. D is wrong because Fail-Safe begins after Time Travel expires, and neither period applies only to DROP TABLE specifically.",
      "reference": "Snowflake Data Protection — Time Travel vs Fail-Safe Accessibility"
    },
    {
      "id": "q08",
      "domain": "architecture",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowflake warehouse is set to AUTO_SUSPEND = 60 (seconds) and AUTO_RESUME = TRUE. Multiple short queries arrive in rapid succession over a 2-hour period, with each burst lasting ~2 minutes followed by 5-minute idle gaps. What is the billing behavior?",
      "options": [
        { "id": "A", "text": "The warehouse is billed continuously for 2 hours since it never fully suspends between bursts." },
        { "id": "B", "text": "The warehouse suspends after each 60-second idle gap, and each resume incurs a new 60-second minimum charge." },
        { "id": "C", "text": "The warehouse suspends after each 60-second idle period within the 5-minute gap, and each resume cycle incurs at least a 60-second minimum." },
        { "id": "D", "text": "Credits are only billed for the 2-minute active bursts, and idle time is always free." }
      ],
      "correct": ["C"],
      "explanation": "C is correct. With AUTO_SUSPEND = 60, the warehouse suspends 60 seconds after the last query in a burst completes. With 5-minute idle gaps, the warehouse will suspend about 60 seconds into each idle period. When the next burst arrives, AUTO_RESUME wakes the warehouse, incurring at least a 60-second minimum charge for that activation cycle. Over 2 hours with multiple burst-idle cycles, this pattern means multiple 60-second minimums accumulate. A is wrong because the 5-minute idle gaps exceed the 60-second auto-suspend threshold, so the warehouse does suspend repeatedly. B describes the mechanism correctly but is slightly imprecise about when suspension occurs — it is 60 seconds after the last query, not after each query individually. D is wrong because idle time before auto-suspend (the 60-second window) IS billed.",
      "reference": "Snowflake Architecture — Auto-Suspend Billing, 60-Second Minimum"
    },
    {
      "id": "q09",
      "domain": "security",
      "type": "single",
      "difficulty": "easy",
      "stem": "A new custom role `REPORTING` is created and granted to a user. For the user to use this role to query a table in `mydb.analytics`, which privileges are required at a minimum?",
      "options": [
        { "id": "A", "text": "USAGE on the database and schema only" },
        { "id": "B", "text": "SELECT on the table only" },
        { "id": "C", "text": "USAGE on the database, USAGE on the schema, and SELECT on the table" },
        { "id": "D", "text": "OWNERSHIP on the database, schema, and table" }
      ],
      "correct": ["C"],
      "explanation": "C is correct because in Snowflake's RBAC model, to query a table, a role needs USAGE on the database (to see the database), USAGE on the schema (to see the schema), and SELECT on the table. All three are required — missing any one of them will result in an access error. A is wrong because USAGE on database and schema without SELECT on the table does not allow querying. B is wrong because SELECT alone is insufficient without USAGE on the parent database and schema. D is wrong because OWNERSHIP is far more than required — the minimum is USAGE/SELECT, not OWNERSHIP.",
      "reference": "Snowflake Security — Minimum Privileges for Table Access"
    },
    {
      "id": "q10",
      "domain": "security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake administrator wants all future tables created in `mydb.public` to automatically have SELECT granted to the `ANALYST` role. Which command achieves this?",
      "options": [
        { "id": "A", "text": "`GRANT SELECT ON ALL TABLES IN SCHEMA mydb.public TO ROLE analyst;`" },
        { "id": "B", "text": "`GRANT SELECT ON FUTURE TABLES IN SCHEMA mydb.public TO ROLE analyst;`" },
        { "id": "C", "text": "`ALTER SCHEMA mydb.public SET DEFAULT GRANT SELECT TO ROLE analyst;`" },
        { "id": "D", "text": "`CREATE POLICY auto_grant AS GRANT SELECT ON NEW TABLES TO ROLE analyst;`" }
      ],
      "correct": ["B"],
      "explanation": "B is correct because `GRANT ... ON FUTURE TABLES IN SCHEMA ...` is the Snowflake feature that automatically grants the specified privilege to new tables created in the schema after the grant is issued. This is the future grants mechanism. A is wrong because ON ALL TABLES applies only to existing tables and does not affect future tables. C is wrong because ALTER SCHEMA does not have a DEFAULT GRANT syntax. D is wrong because CREATE POLICY with auto-grant syntax does not exist in Snowflake.",
      "reference": "Snowflake Security — Future Grants"
    },
    {
      "id": "q11",
      "domain": "security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A company implements SAML 2.0 Single Sign-On (SSO) with Snowflake. Which role in Snowflake is responsible for configuring the SAML identity provider settings at the account level?",
      "options": [
        { "id": "A", "text": "SYSADMIN" },
        { "id": "B", "text": "SECURITYADMIN" },
        { "id": "C", "text": "ACCOUNTADMIN" },
        { "id": "D", "text": "PUBLIC" }
      ],
      "correct": ["C"],
      "explanation": "C is correct because configuring SAML 2.0 SSO at the account level (setting `SAML_IDENTITY_PROVIDER` and `SSO_LOGIN_PAGE`) requires ACCOUNTADMIN privileges, as these are account-level parameters. A is wrong because SYSADMIN manages database objects, not account-level authentication settings. B is wrong because while SECURITYADMIN handles user/role management, configuring the SAML IDP connection at the account level requires ACCOUNTADMIN. D is wrong because PUBLIC has no administrative privileges.",
      "reference": "Snowflake Security — Federated Authentication, SAML SSO Configuration"
    },
    {
      "id": "q12",
      "domain": "security",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO of the following statements are TRUE about Snowflake's Column-Level Security using Dynamic Data Masking? (Select TWO)",
      "options": [
        { "id": "A", "text": "A masking policy can be applied to columns of any data type, including VARIANT." },
        { "id": "B", "text": "Only one masking policy can be assigned to a column at a time." },
        { "id": "C", "text": "The masking policy function must return the same data type as the column it masks." },
        { "id": "D", "text": "Masking policies are evaluated before row access policies when both are applied to the same table." }
      ],
      "correct": ["B", "C"],
      "explanation": "B and C are correct. Each column can have at most one masking policy applied at a time. The return data type of the masking policy function must match the data type of the masked column — for example, a policy on a VARCHAR column must return VARCHAR. A is wrong because masking policies support specific data types, and VARIANT columns are not directly supported by standard masking policies (a separate approach is needed for semi-structured data). D is wrong because row access policies are evaluated first, then masking policies are applied to the resulting rows — the order is row access first, then column masking.",
      "reference": "Snowflake Security — Dynamic Data Masking Constraints"
    },
    {
      "id": "q13",
      "domain": "security",
      "type": "single",
      "difficulty": "hard",
      "stem": "A security team wants to audit all login attempts to their Snowflake account. Which Snowflake object or view provides this information?",
      "options": [
        { "id": "A", "text": "INFORMATION_SCHEMA.LOGIN_HISTORY" },
        { "id": "B", "text": "ACCOUNT_USAGE.LOGIN_HISTORY" },
        { "id": "C", "text": "ACCOUNT_USAGE.QUERY_HISTORY" },
        { "id": "D", "text": "INFORMATION_SCHEMA.SESSIONS" }
      ],
      "correct": ["B"],
      "explanation": "B is correct because `ACCOUNT_USAGE.LOGIN_HISTORY` provides a comprehensive, account-level view of all authentication attempts, including both successful and failed logins, across all users. It retains data for up to 365 days. A is wrong because `INFORMATION_SCHEMA.LOGIN_HISTORY` is a table function scoped to the current user's session and has a shorter retention period (7 days). C is wrong because QUERY_HISTORY records SQL query execution, not authentication attempts. D is wrong because INFORMATION_SCHEMA.SESSIONS provides active session information, not login history.",
      "reference": "Snowflake Security — Account Usage, LOGIN_HISTORY View"
    },
    {
      "id": "q14",
      "domain": "security",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake administrator needs to allow a third-party ETL tool to connect using key-pair authentication. Which parameter is set on the Snowflake user object to register the public key?",
      "options": [
        { "id": "A", "text": "ALTER USER etl_user SET PASSWORD = '<public_key>';`" },
        { "id": "B", "text": "`ALTER USER etl_user SET RSA_PUBLIC_KEY = '<public_key_value>';`" },
        { "id": "C", "text": "`ALTER USER etl_user SET API_KEY = '<public_key_value>';`" },
        { "id": "D", "text": "`GRANT KEY PAIR TO USER etl_user USING '<public_key_value>';`" }
      ],
      "correct": ["B"],
      "explanation": "B is correct because key-pair authentication in Snowflake is configured by setting the `RSA_PUBLIC_KEY` parameter on the user object using `ALTER USER ... SET RSA_PUBLIC_KEY`. The ETL tool then authenticates using the corresponding private key. A is wrong because PASSWORD is for password-based authentication, not key-pair; setting the public key as a password would not enable key-pair auth. C is wrong because API_KEY is not a valid Snowflake user parameter. D is wrong because GRANT KEY PAIR is not valid Snowflake syntax.",
      "reference": "Snowflake Security — Key-Pair Authentication Setup"
    },
    {
      "id": "q15",
      "domain": "performance",
      "type": "single",
      "difficulty": "easy",
      "stem": "A virtual warehouse is consistently showing long query queue times during business hours. Queries are waiting for other queries to finish before executing. What is the MOST likely solution?",
      "options": [
        { "id": "A", "text": "Increase the warehouse size from Medium to Large." },
        { "id": "B", "text": "Enable multi-cluster warehouses to handle concurrent queries." },
        { "id": "C", "text": "Enable the Search Optimization Service on all queried tables." },
        { "id": "D", "text": "Reduce the AUTO_SUSPEND threshold to 30 seconds." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because query queuing is a concurrency problem, not a compute-per-query problem. Multi-cluster warehouses address concurrency by adding additional clusters to handle simultaneous queries, eliminating the queue. A is wrong because increasing warehouse size adds more compute per query (better for slow individual queries) but does NOT increase the number of concurrent queries a warehouse can handle. C is wrong because Search Optimization Service improves individual query performance for selective lookups but does not solve queue buildup from concurrency. D is wrong because reducing auto-suspend affects idle credit usage, not query concurrency.",
      "reference": "Snowflake Performance — Multi-Cluster Warehouses for Concurrency"
    },
    {
      "id": "q16",
      "domain": "performance",
      "type": "single",
      "difficulty": "medium",
      "stem": "A Snowflake developer uses `EXPLAIN USING JSON SELECT * FROM sales WHERE region = 'WEST' AND sale_date = '2024-01-15'`. The output shows `partitionsTotal: 5000` and `partitionsAssigned: 4950`. What does this indicate?",
      "options": [
        { "id": "A", "text": "The query is extremely well-optimized and nearly all partitions are pruned." },
        { "id": "B", "text": "Only 50 partitions will be scanned; the remaining 4950 are pruned." },
        { "id": "C", "text": "4950 partitions will be scanned out of 5000; very little pruning is occurring." },
        { "id": "D", "text": "The query cannot be further optimized because region and sale_date are incompatible filter columns." }
      ],
      "correct": ["C"],
      "explanation": "C is correct because `partitionsAssigned` represents the number of micro-partitions that will actually be scanned (cannot be pruned). With 4950 of 5000 partitions assigned, only 50 are being pruned — this is very poor pruning efficiency, likely because the table is not well-clustered on region or sale_date. A is wrong because nearly all partitions being assigned (scanned) is the opposite of well-optimized pruning. B is wrong because partitionsAssigned = partitions TO BE SCANNED (not pruned). D is wrong because the EXPLAIN result indicates a clustering/pruning problem, not query syntax incompatibility.",
      "reference": "Snowflake Performance — EXPLAIN, Partition Pruning Statistics"
    },
    {
      "id": "q17",
      "domain": "performance",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "Which TWO of the following are valid Snowflake materialized view limitations? (Select TWO)",
      "options": [
        { "id": "A", "text": "Materialized views cannot contain window functions (e.g., ROW_NUMBER(), RANK())." },
        { "id": "B", "text": "Materialized views can only be created on tables in the same schema." },
        { "id": "C", "text": "Materialized views are only available in the Enterprise edition and above." },
        { "id": "D", "text": "Materialized views can only be queried by users with the ACCOUNTADMIN role." }
      ],
      "correct": ["A", "C"],
      "explanation": "A and C are correct. Materialized views in Snowflake do not support window functions such as ROW_NUMBER() or RANK() because these are non-deterministic relative to partition changes and would make incremental maintenance impractical. Materialized views are also an Enterprise edition feature and are not available on the Standard edition. B is wrong because materialized view definitions can join tables across schemas (within the same database), though cross-database joins have limitations. D is wrong because materialized views can be queried by any user with appropriate SELECT privileges — ACCOUNTADMIN is not required.",
      "reference": "Snowflake Performance — Materialized View Limitations, Enterprise Edition"
    },
    {
      "id": "q18",
      "domain": "performance",
      "type": "single",
      "difficulty": "hard",
      "stem": "A data warehouse team finds that their nightly ETL runs on a Large warehouse take 4 hours. After sizing up to an X-Large warehouse, the same jobs take 2 hours. They then size up to a 2X-Large and see no further improvement. What is the MOST LIKELY explanation?",
      "options": [
        { "id": "A", "text": "The 2X-Large warehouse has a bug that prevents it from using the additional compute nodes." },
        { "id": "B", "text": "The ETL jobs are I/O bound — they are bottlenecked by the rate at which data can be read from object storage, not by compute." },
        { "id": "C", "text": "Credit consumption limits in the resource monitor are throttling the 2X-Large warehouse." },
        { "id": "D", "text": "Snowflake limits ETL jobs to 2 hours maximum regardless of warehouse size." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because adding more compute nodes beyond a certain point hits an I/O bottleneck — the rate at which micro-partitions can be read from cloud object storage. If every node is already reading at maximum throughput, adding more nodes does not increase the data delivery rate. This is sometimes called the 'I/O bound' limit. Other bottlenecks like sequential processing logic in stored procedures can also cause this plateau. A is wrong because there is no such bug. C is wrong because resource monitors suspend warehouses but do not silently throttle performance. D is wrong because Snowflake has no 2-hour query duration limit.",
      "reference": "Snowflake Performance — Warehouse Sizing Diminishing Returns"
    },
    {
      "id": "q19",
      "domain": "performance",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer creates a `CLUSTER BY (customer_id, order_date)` key on a large orders table. Which query would benefit MOST from this clustering key?",
      "options": [
        { "id": "A", "text": "`SELECT SUM(amount) FROM orders` (no WHERE clause)" },
        { "id": "B", "text": "`SELECT * FROM orders WHERE customer_id = 999 AND order_date BETWEEN '2024-01-01' AND '2024-01-31'`" },
        { "id": "C", "text": "`SELECT * FROM orders WHERE amount > 10000`" },
        { "id": "D", "text": "`SELECT COUNT(DISTINCT product_id) FROM orders WHERE region = 'EU'`" }
      ],
      "correct": ["B"],
      "explanation": "B is correct because the clustering key is on (customer_id, order_date), so a query filtering on both columns can prune the vast majority of micro-partitions — only partitions containing data for customer_id = 999 in January 2024 need to be scanned. A is wrong because a full table scan with no WHERE clause cannot benefit from pruning. C is wrong because `amount` is not in the clustering key, so no pruning on amount is possible. D is wrong because `region` and `product_id` are not in the clustering key, so no micro-partition pruning occurs on those columns.",
      "reference": "Snowflake Performance — Clustering Key Query Benefit"
    },
    {
      "id": "q20",
      "domain": "data-loading",
      "type": "single",
      "difficulty": "easy",
      "stem": "A data engineer has already loaded a file `data_2024.csv` from an S3 external stage into a Snowflake table. They run the same COPY INTO command the next day without any changes. What happens by default?",
      "options": [
        { "id": "A", "text": "The file is loaded again, creating duplicate rows." },
        { "id": "B", "text": "The file is skipped because Snowflake tracks loaded files and does not reload them by default." },
        { "id": "C", "text": "The COPY INTO command fails with an error about a duplicate file." },
        { "id": "D", "text": "The file is loaded only if the S3 file timestamp has changed." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because Snowflake tracks which files have already been loaded via `LOAD_HISTORY` metadata. By default, COPY INTO skips files that have already been successfully loaded to prevent duplicate ingestion. A is wrong because the default behavior is specifically designed to prevent duplicate loading. C is wrong because COPY INTO does not fail with an error for previously loaded files — it simply skips them. D is wrong because Snowflake uses the file metadata hash/path for deduplication, not just the S3 timestamp. The `FORCE = TRUE` option can override this behavior to reload files.",
      "reference": "Snowflake Data Loading — COPY INTO File Deduplication"
    },
    {
      "id": "q21",
      "domain": "data-loading",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer creates a file format object: `CREATE FILE FORMAT my_json_fmt TYPE = 'JSON' STRIP_OUTER_ARRAY = TRUE;`. What does STRIP_OUTER_ARRAY do?",
      "options": [
        { "id": "A", "text": "Removes the outermost JSON object keys, keeping only the values." },
        { "id": "B", "text": "Treats each element of a top-level JSON array as a separate row during loading, instead of loading the entire array as one VARIANT value." },
        { "id": "C", "text": "Converts the JSON array to a CSV-formatted string before loading." },
        { "id": "D", "text": "Removes all nested arrays from the JSON before loading to prevent errors." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because `STRIP_OUTER_ARRAY = TRUE` instructs Snowflake to parse a top-level JSON array and insert each array element as a separate row. Without this option, the entire JSON array would be loaded as a single VARIANT row. For example, `[{\"id\":1},{\"id\":2}]` would become two rows instead of one. A is wrong because STRIP_OUTER_ARRAY does not affect object keys. C is wrong because the data remains in JSON/VARIANT format — it is not converted to CSV. D is wrong because nested arrays are preserved; only the outermost array wrapper is stripped.",
      "reference": "Snowflake Data Loading — JSON File Format, STRIP_OUTER_ARRAY"
    },
    {
      "id": "q22",
      "domain": "data-loading",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A team wants to query data directly in S3 without loading it into a Snowflake table. Which TWO Snowflake features support querying files directly in cloud storage? (Select TWO)",
      "options": [
        { "id": "A", "text": "External tables" },
        { "id": "B", "text": "Snowpipe with DIRECT_QUERY = TRUE" },
        { "id": "C", "text": "Querying files in a stage using SELECT $1, $2 FROM @stage_name" },
        { "id": "D", "text": "Secure views with EXTERNAL_TABLE = TRUE" }
      ],
      "correct": ["A", "C"],
      "explanation": "A and C are correct. External tables define a schema over files in an external stage, allowing them to be queried with standard SQL. Snowflake can also directly query staged files using `SELECT $1, $2, ... FROM @stage_name` syntax — the $N notation refers to positional columns in the file. B is wrong because Snowpipe is a continuous ingestion mechanism, not a direct query feature; DIRECT_QUERY is not a valid Snowpipe parameter. D is wrong because secure views with EXTERNAL_TABLE = TRUE is not a valid Snowflake feature syntax.",
      "reference": "Snowflake Data Loading — External Tables, Querying Staged Files"
    },
    {
      "id": "q23",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "easy",
      "stem": "A developer creates a standard stream on the `orders` table. A downstream task reads and processes the stream. After the task completes and commits, what happens to the rows in the stream?",
      "options": [
        { "id": "A", "text": "The rows remain in the stream indefinitely until manually deleted." },
        { "id": "B", "text": "The stream offset advances past the consumed changes; those changes no longer appear in the stream." },
        { "id": "C", "text": "The rows are archived in the INFORMATION_SCHEMA for 14 days." },
        { "id": "D", "text": "The stream is automatically dropped after it is consumed once." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because when a stream is consumed in a DML statement within a committed transaction, the stream's offset advances to the current position. Subsequent reads of the stream will only show changes that occurred after the last consumption point. The data that was consumed is no longer visible in the stream. A is wrong because streams automatically advance their offset upon successful consumption. C is wrong because stream consumption data is not archived to INFORMATION_SCHEMA. D is wrong because streams persist after consumption and continue capturing new changes.",
      "reference": "Snowflake Data Transformations — Stream Offset Advancement"
    },
    {
      "id": "q24",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A data engineer creates a serverless task using `USER_TASK_MANAGED_INITIAL_WAREHOUSE_SIZE = 'MEDIUM'`. How is compute billed for this task?",
      "options": [
        { "id": "A", "text": "A Medium virtual warehouse is created, billed at 4 credits/hr for the duration of the task." },
        { "id": "B", "text": "Snowflake manages the compute using serverless resources billed per-second based on actual compute used, not warehouse uptime." },
        { "id": "C", "text": "The task is free because serverless tasks do not consume credits." },
        { "id": "D", "text": "The task must be assigned to an existing warehouse; USER_TASK_MANAGED is only a size hint." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because serverless tasks (using USER_TASK_MANAGED_INITIAL_WAREHOUSE_SIZE instead of WAREHOUSE) use Snowflake-managed compute resources that spin up and down automatically. Billing is based on the actual cloud services compute consumed during task execution, not a full warehouse-hour allocation. The INITIAL_WAREHOUSE_SIZE is a hint for initial sizing that Snowflake may adjust based on historical runs. A is wrong because serverless tasks do not create or bill for a traditional virtual warehouse. C is wrong because serverless tasks do consume compute credits, just differently than warehouse-based billing. D is wrong because USER_TASK_MANAGED_INITIAL_WAREHOUSE_SIZE is the mechanism that opts the task into serverless mode, not a warehouse hint.",
      "reference": "Snowflake Data Transformations — Serverless Tasks"
    },
    {
      "id": "q25",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "Which SQL statement creates a Snowflake table UDF (UDTF) that returns a table of values?",
      "options": [
        { "id": "A", "text": "`CREATE FUNCTION split_values(input VARCHAR) RETURNS TABLE (val VARCHAR) LANGUAGE SQL AS 'SELECT value FROM TABLE(SPLIT_TO_TABLE(input, \",\"))';`" },
        { "id": "B", "text": "`CREATE TABLE FUNCTION split_values(input VARCHAR) AS SELECT value FROM TABLE(SPLIT_TO_TABLE(input, \",\"));`" },
        { "id": "C", "text": "`CREATE PROCEDURE split_values(input VARCHAR) RETURNS TABLE AS BEGIN SELECT value FROM TABLE(SPLIT_TO_TABLE(input, \",\")); END;`" },
        { "id": "D", "text": "`CREATE VIEW split_values AS SELECT value FROM TABLE(SPLIT_TO_TABLE(:input, \",\"));`" }
      ],
      "correct": ["A"],
      "explanation": "A is correct because Snowflake UDTFs (User-Defined Table Functions) use `CREATE FUNCTION ... RETURNS TABLE (column_list) LANGUAGE SQL AS` syntax. This is the correct syntax for creating a table-valued function. B is wrong because CREATE TABLE FUNCTION is not valid Snowflake syntax — it is CREATE FUNCTION with RETURNS TABLE. C is wrong because RETURNS TABLE in a stored procedure context is used for procedure results, and the syntax shown is not the correct UDF pattern. D is wrong because views are static definitions and do not accept parameters.",
      "reference": "Snowflake Data Transformations — User-Defined Table Functions (UDTF)"
    },
    {
      "id": "q26",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "hard",
      "stem": "A developer has a stream `orders_stream` on the `orders` table. The `orders` table has a Time Travel retention period of 1 day. The stream has not been consumed for 3 days. What is the state of the stream?",
      "options": [
        { "id": "A", "text": "The stream is paused and will resume automatically when consumed." },
        { "id": "B", "text": "The stream is stale and must be recreated to capture new changes." },
        { "id": "C", "text": "The stream only contains the last 24 hours of changes, discarding earlier ones." },
        { "id": "D", "text": "The stream is stale but can be reset to the current timestamp using ALTER STREAM." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because a stream becomes stale when its offset falls behind the Time Travel retention period of the source table. If the stream was not consumed for 3 days but the table only has 1 day of Time Travel, the stream's offset position is now beyond the available Time Travel window. The stream is stale and the historical change data it was tracking is no longer accessible. The stream must be dropped and recreated to resume CDC. A is wrong because stale streams are not paused — they are in an invalid state that requires recreation. C is wrong because stale streams do not self-trim to the available Time Travel window. D is wrong because there is no ALTER STREAM command to reset a stale stream's offset.",
      "reference": "Snowflake Data Transformations — Stale Streams"
    },
    {
      "id": "q27",
      "domain": "data-transformation",
      "type": "multiple",
      "difficulty": "hard",
      "stem": "A team is migrating an Oracle PL/SQL pipeline to Snowflake. Which TWO languages can be used to write stored procedures in Snowflake? (Select TWO)",
      "options": [
        { "id": "A", "text": "Snowflake Scripting (Snowpark procedural SQL)" },
        { "id": "B", "text": "PL/SQL (native Oracle compatibility)" },
        { "id": "C", "text": "Python (using Snowpark for Python)" },
        { "id": "D", "text": "Go (Golang)" }
      ],
      "correct": ["A", "C"],
      "explanation": "A and C are correct. Snowflake stored procedures support Snowflake Scripting (the native procedural SQL language), Python (via Snowpark), JavaScript, Java, and Scala. B is wrong because Snowflake does not support Oracle PL/SQL natively — Snowflake Scripting is the procedural SQL alternative. D is wrong because Go (Golang) is not a supported language for Snowflake stored procedures as of the current release.",
      "reference": "Snowflake Data Transformations — Stored Procedure Languages"
    },
    {
      "id": "q28",
      "domain": "data-transformation",
      "type": "single",
      "difficulty": "medium",
      "stem": "A developer creates a sequence and uses it in an INSERT statement: `INSERT INTO orders (id, product) SELECT orders_seq.NEXTVAL, product FROM staging`. What happens if two concurrent sessions insert rows simultaneously using the same sequence?",
      "options": [
        { "id": "A", "text": "Both sessions generate duplicate sequence values, causing primary key violations." },
        { "id": "B", "text": "Snowflake guarantees that each session receives unique, non-overlapping sequence values." },
        { "id": "C", "text": "One session blocks until the other completes to ensure sequential values." },
        { "id": "D", "text": "Sequences are session-scoped, so each session has its own independent counter starting from 1." }
      ],
      "correct": ["B"],
      "explanation": "B is correct because Snowflake sequences are designed for concurrent use — multiple sessions can call NEXTVAL simultaneously and each will receive unique values without collision. Snowflake guarantees uniqueness but not strict monotonic ordering across concurrent sessions (values may not be sequential, but they will be unique). A is wrong because sequences do not generate duplicate values across concurrent sessions. C is wrong because sequences do not cause blocking — concurrent access is one of their key design properties. D is wrong because sequences are account/schema-scoped objects, not session-scoped.",
      "reference": "Snowflake Data Transformations — Sequences, Concurrent Access"
    },
    {
      "id": "q29",
      "domain": "data-protection",
      "type": "multiple",
      "difficulty": "medium",
      "stem": "A Snowflake account on the Enterprise edition has Time Travel enabled with the maximum 90-day retention. Which TWO Time Travel query syntaxes are valid for accessing historical data? (Select TWO)",
      "options": [
        { "id": "A", "text": "`SELECT * FROM orders AT(OFFSET => -86400);`" },
        { "id": "B", "text": "`SELECT * FROM orders HISTORY(DAYS => 1);`" },
        { "id": "C", "text": "`SELECT * FROM orders AT(STATEMENT => '8e5d0ca9-005e-44e6-b858-a8f5b37c5726');`" },
        { "id": "D", "text": "`SELECT * FROM orders TIMETRAVEL OFFSET -86400;`" }
      ],
      "correct": ["A", "C"],
      "explanation": "A and C are correct. The `AT(OFFSET => -N)` syntax returns data as of N seconds before the current time. The `AT(STATEMENT => 'query_id')` syntax returns data as of the time immediately after the specified query statement executed. Both are valid Time Travel syntaxes. B is wrong because `HISTORY(DAYS => 1)` is not valid Snowflake SQL syntax. D is wrong because `TIMETRAVEL OFFSET` is not valid Snowflake SQL syntax.",
      "reference": "Snowflake Data Protection — Time Travel AT Syntax"
    },
    {
      "id": "q30",
      "domain": "data-protection",
      "type": "single",
      "difficulty": "hard",
      "stem": "A Snowflake data provider shares a database with two consumer accounts. Consumer A is a paying Snowflake customer. Consumer B is a non-Snowflake customer. How does Consumer B access the shared data?",
      "options": [
        { "id": "A", "text": "Consumer B receives a nightly data export via email in CSV format." },
        { "id": "B", "text": "Consumer B must upgrade to a Snowflake account before they can access any shared data." },
        { "id": "C", "text": "The provider creates a reader account for Consumer B; the provider pays for the reader account's compute credits." },
        { "id": "D", "text": "Consumer B accesses the data via the provider's internal Snowflake account using guest user credentials." }
      ],
      "correct": ["C"],
      "explanation": "C is correct because Snowflake provides 'reader accounts' specifically for sharing data with non-Snowflake customers. The data provider creates and manages the reader account; the provider is billed for any compute credits the reader account uses to query the shared data. A is wrong because Snowflake data sharing is live and real-time — data is not exported or emailed. B is wrong because reader accounts are specifically designed so that non-Snowflake customers do not need their own full Snowflake account. D is wrong because guest credentials inside the provider's account are not the mechanism for external sharing.",
      "reference": "Snowflake Data Protection — Data Sharing, Reader Accounts"
    }
  ]
}
