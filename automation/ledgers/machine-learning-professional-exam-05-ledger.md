# machine-learning-professional regenerated exam ledger

verified_question_count: 60
domain_distribution: experimentation=25%, feature-store=20%, model-lifecycle=20%, model-deployment=20%, solution-monitoring=15%

| id | domain | concept | scenario | decisive_constraint | correct_answer | distractor_A/B/C | reference_url | not_a_variant_of |
|---|---|---|---|---|---|---|---|---|
| q01 | experimentation | MLflow run context managers | Coherent practitioner scenario for customer_churn. | the decision can be reproduced from logged inputs and outputs. | Use `with mlflow.start_run():` around training and logging so the run closes on exit | Call `mlflow.start_run()` once at cluster startup and reuse that run for every model; Write metrics to a Delta table only and skip MLflow tracking; Create a registry entry before any experiment run exists | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q02 | experimentation | artifact logging | Coherent practitioner scenario for claims_risk. | the same business definition is reused across training and serving. | Save the file locally and call `mlflow.log_artifact()` or `log_artifacts()` for the run | Store the file contents in `log_param` so it appears beside hyperparameters; Log the DataFrame as a numeric metric because metrics are searchable; Attach the file to the registered model without linking it to the run | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q03 | experimentation | nested runs | Coherent practitioner scenario for inventory_signals. | the decision can be reproduced from logged inputs and outputs. | Start a parent run for the search and nested child runs for each trial | Overwrite the same run for every trial to keep the UI short; Register every trial as a production model version; Disable metric logging until the winning trial is known | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q04 | experimentation | Hyperopt SparkTrials | Coherent practitioner scenario for delivery_eta. | the same business definition is reused across training and serving. | Use Hyperopt with `SparkTrials` so trials execute in parallel on the cluster | Use a single-driver Python loop for all parameter combinations; Create one all-purpose job cluster for manual notebook reruns only; Tune by changing parameters after the model is registered | [Hyperopt on Databricks](https://docs.databricks.com/en/machine-learning/automl-hyperparam-tuning/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q05 | experimentation | AutoML baseline | Coherent practitioner scenario for demand_forecast. | the decision can be reproduced from logged inputs and outputs. | Run Databricks AutoML to generate candidate notebooks and baseline models | Use Model Serving because serving endpoints generate training notebooks; Use Lakehouse Monitoring because monitors train candidate models; Use Feature Store alone because it selects algorithms automatically | [Databricks AutoML](https://docs.databricks.com/en/machine-learning/automl/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q06 | feature-store | point-in-time feature lookup | Coherent practitioner scenario for loan_approval. | the same business definition is reused across training and serving. | Create a training set with feature lookups keyed by entity and timestamp to prevent leakage | Join to the latest feature table values regardless of event time; Export online-store values and backfill labels from production predictions; Drop the timestamp column so all rows use a single feature snapshot | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q07 | feature-store | feature table lineage | Coherent practitioner scenario for pricing_model. | the decision can be reproduced from logged inputs and outputs. | Log the model with Feature Engineering metadata so batch and serving inference can retrieve the same features | Copy feature values into model parameters at training time; Recompute features manually in every scoring notebook; Store feature definitions only in a wiki page | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q08 | model-lifecycle | model signature | Coherent practitioner scenario for fraud_scores. | the same business definition is reused across training and serving. | Infer and log a model signature plus an input example with the MLflow model | Rely on notebook comments to describe the input columns; Register the model without schema so any payload is accepted; Put schema details only in the endpoint description | [MLflow Models](https://mlflow.org/docs/latest/ml/model/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q09 | model-lifecycle | model registry approval | Coherent practitioner scenario for product_recs. | the decision can be reproduced from logged inputs and outputs. | Register the model version and move it through the configured review or alias workflow after evaluation | Deploy directly from an untracked local path; Use the experiment name as the production approval state; Archive the prior version before evaluating the candidate | [MLflow Model Registry](https://mlflow.org/docs/latest/ml/model-registry/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q10 | model-lifecycle | copy model versions | Coherent practitioner scenario for support_triage. | the same business definition is reused across training and serving. | Copy the registered model version to the target workspace or registry location with its metadata | Retrain from scratch in the target workspace without preserving source lineage; Download the pickle file and email it to the receiving team; Create a serving endpoint in the source workspace and call it a transfer | [MLflow Model Registry](https://mlflow.org/docs/latest/ml/model-registry/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q11 | model-deployment | real-time serving endpoint | Coherent practitioner scenario for customer_churn. | the decision can be reproduced from logged inputs and outputs. | Deploy the registered model to a Model Serving endpoint for low-latency REST inference | Run a scheduled batch job and ask the application to poll the output table; Use an experiment comparison view as the production API; Store predictions in a static CSV file each morning | [Databricks Model Serving](https://docs.databricks.com/en/machine-learning/model-serving/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q12 | model-deployment | batch inference | Coherent practitioner scenario for claims_risk. | the same business definition is reused across training and serving. | Use distributed batch inference over the input table and write scored results to Delta | Keep a real-time endpoint warm only for the monthly scoring job; Send millions of rows through a notebook widget one at a time; Use the model registry approval page to generate predictions | [Batch inference with MLflow models](https://docs.databricks.com/en/machine-learning/model-inference/batch-inference.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q13 | model-deployment | traffic split | Coherent practitioner scenario for inventory_signals. | the decision can be reproduced from logged inputs and outputs. | Configure endpoint traffic routes so a small percentage reaches the candidate served model | Replace the production model immediately for all requests; Create a second endpoint and ask clients to randomly choose one; Use an offline validation metric as the traffic router | [Model Serving traffic configuration](https://docs.databricks.com/en/machine-learning/model-serving/manage-serving-endpoints.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q14 | model-deployment | environment dependencies | Coherent practitioner scenario for delivery_eta. | the same business definition is reused across training and serving. | Log the model with its conda or pip environment so serving can reproduce dependencies | Install packages manually on the reviewer laptop; Depend on whatever library versions happen to be on the serving cluster; Store package names in a free-text model description only | [MLflow Models](https://mlflow.org/docs/latest/ml/model/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q15 | solution-monitoring | data drift monitor | Coherent practitioner scenario for demand_forecast. | the decision can be reproduced from logged inputs and outputs. | Create a Lakehouse Monitor over the inference or feature table with baseline comparisons | Tune hyperparameters until drift metrics disappear; Use a registry stage transition as the drift detector; Delete historical predictions so the dashboard only shows current rows | [Databricks Lakehouse Monitoring](https://docs.databricks.com/en/lakehouse-monitoring/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q16 | solution-monitoring | quality metrics over labels | Coherent practitioner scenario for loan_approval. | the same business definition is reused across training and serving. | Join predictions with later labels and monitor model-quality metrics over time | Monitor only endpoint CPU because labels are delayed; Use feature table freshness as the accuracy metric; Treat successful HTTP responses as proof of model quality | [Databricks Lakehouse Monitoring](https://docs.databricks.com/en/lakehouse-monitoring/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q17 | solution-monitoring | serving metrics | Coherent practitioner scenario for pricing_model. | the decision can be reproduced from logged inputs and outputs. | Inspect endpoint request, latency, and error metrics for the served model | Search MLflow parameters for HTTP status codes; Use AutoML trial charts as live endpoint telemetry; Assume low training loss means the endpoint is healthy | [Databricks Model Serving](https://docs.databricks.com/en/machine-learning/model-serving/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q18 | experimentation | dataset logging | Coherent practitioner scenario for fraud_scores. | the same business definition is reused across training and serving. | Log the dataset or dataset metadata with the run so the training data is auditable | Only log the final accuracy metric; Record the dataset path in a chat message; Use the model alias as the dataset version | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q19 | feature-store | online and offline feature reuse | Coherent practitioner scenario for product_recs. | the decision can be reproduced from logged inputs and outputs. | Publish feature definitions for offline training and low-latency online lookup when needed | Maintain separate SQL definitions for training and serving; Use request-time features for historical training rows; Bypass feature lookup during training and add it only during serving | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q20 | model-lifecycle | evaluation comparison | Coherent practitioner scenario for support_triage. | the same business definition is reused across training and serving. | Compare runs by validation metrics and logged parameters before selecting a candidate | Promote the run with the shortest name; Select the model version with the largest artifact directory; Choose the candidate that was trained most recently regardless of metrics | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q21 | experimentation | MLflow run context managers | Coherent practitioner scenario for customer_churn. | historical data is not allowed to leak future information. | Use `with mlflow.start_run():` around training and logging so the run closes on exit | Call `mlflow.start_run()` once at cluster startup and reuse that run for every model; Write metrics to a Delta table only and skip MLflow tracking; Create a registry entry before any experiment run exists | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q22 | experimentation | artifact logging | Coherent practitioner scenario for claims_risk. | a failed quality gate stops promotion automatically. | Save the file locally and call `mlflow.log_artifact()` or `log_artifacts()` for the run | Store the file contents in `log_param` so it appears beside hyperparameters; Log the DataFrame as a numeric metric because metrics are searchable; Attach the file to the registered model without linking it to the run | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q23 | experimentation | nested runs | Coherent practitioner scenario for inventory_signals. | historical data is not allowed to leak future information. | Start a parent run for the search and nested child runs for each trial | Overwrite the same run for every trial to keep the UI short; Register every trial as a production model version; Disable metric logging until the winning trial is known | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q24 | experimentation | Hyperopt SparkTrials | Coherent practitioner scenario for delivery_eta. | a failed quality gate stops promotion automatically. | Use Hyperopt with `SparkTrials` so trials execute in parallel on the cluster | Use a single-driver Python loop for all parameter combinations; Create one all-purpose job cluster for manual notebook reruns only; Tune by changing parameters after the model is registered | [Hyperopt on Databricks](https://docs.databricks.com/en/machine-learning/automl-hyperparam-tuning/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q25 | experimentation | AutoML baseline | Coherent practitioner scenario for demand_forecast. | historical data is not allowed to leak future information. | Run Databricks AutoML to generate candidate notebooks and baseline models | Use Model Serving because serving endpoints generate training notebooks; Use Lakehouse Monitoring because monitors train candidate models; Use Feature Store alone because it selects algorithms automatically | [Databricks AutoML](https://docs.databricks.com/en/machine-learning/automl/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q26 | feature-store | point-in-time feature lookup | Coherent practitioner scenario for loan_approval. | a failed quality gate stops promotion automatically. | Create a training set with feature lookups keyed by entity and timestamp to prevent leakage | Join to the latest feature table values regardless of event time; Export online-store values and backfill labels from production predictions; Drop the timestamp column so all rows use a single feature snapshot | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q27 | feature-store | feature table lineage | Coherent practitioner scenario for pricing_model. | historical data is not allowed to leak future information. | Log the model with Feature Engineering metadata so batch and serving inference can retrieve the same features | Copy feature values into model parameters at training time; Recompute features manually in every scoring notebook; Store feature definitions only in a wiki page | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q28 | model-lifecycle | model signature | Coherent practitioner scenario for fraud_scores. | a failed quality gate stops promotion automatically. | Infer and log a model signature plus an input example with the MLflow model | Rely on notebook comments to describe the input columns; Register the model without schema so any payload is accepted; Put schema details only in the endpoint description | [MLflow Models](https://mlflow.org/docs/latest/ml/model/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q29 | model-lifecycle | model registry approval | Coherent practitioner scenario for product_recs. | historical data is not allowed to leak future information. | Register the model version and move it through the configured review or alias workflow after evaluation | Deploy directly from an untracked local path; Use the experiment name as the production approval state; Archive the prior version before evaluating the candidate | [MLflow Model Registry](https://mlflow.org/docs/latest/ml/model-registry/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q30 | model-lifecycle | copy model versions | Coherent practitioner scenario for support_triage. | a failed quality gate stops promotion automatically. | Copy the registered model version to the target workspace or registry location with its metadata | Retrain from scratch in the target workspace without preserving source lineage; Download the pickle file and email it to the receiving team; Create a serving endpoint in the source workspace and call it a transfer | [MLflow Model Registry](https://mlflow.org/docs/latest/ml/model-registry/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q31 | model-deployment | real-time serving endpoint | Coherent practitioner scenario for customer_churn. | historical data is not allowed to leak future information. | Deploy the registered model to a Model Serving endpoint for low-latency REST inference | Run a scheduled batch job and ask the application to poll the output table; Use an experiment comparison view as the production API; Store predictions in a static CSV file each morning | [Databricks Model Serving](https://docs.databricks.com/en/machine-learning/model-serving/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q32 | model-deployment | batch inference | Coherent practitioner scenario for claims_risk. | a failed quality gate stops promotion automatically. | Use distributed batch inference over the input table and write scored results to Delta | Keep a real-time endpoint warm only for the monthly scoring job; Send millions of rows through a notebook widget one at a time; Use the model registry approval page to generate predictions | [Batch inference with MLflow models](https://docs.databricks.com/en/machine-learning/model-inference/batch-inference.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q33 | model-deployment | traffic split | Coherent practitioner scenario for inventory_signals. | historical data is not allowed to leak future information. | Configure endpoint traffic routes so a small percentage reaches the candidate served model | Replace the production model immediately for all requests; Create a second endpoint and ask clients to randomly choose one; Use an offline validation metric as the traffic router | [Model Serving traffic configuration](https://docs.databricks.com/en/machine-learning/model-serving/manage-serving-endpoints.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q34 | model-deployment | environment dependencies | Coherent practitioner scenario for delivery_eta. | a failed quality gate stops promotion automatically. | Log the model with its conda or pip environment so serving can reproduce dependencies | Install packages manually on the reviewer laptop; Depend on whatever library versions happen to be on the serving cluster; Store package names in a free-text model description only | [MLflow Models](https://mlflow.org/docs/latest/ml/model/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q35 | solution-monitoring | data drift monitor | Coherent practitioner scenario for demand_forecast. | historical data is not allowed to leak future information. | Create a Lakehouse Monitor over the inference or feature table with baseline comparisons | Tune hyperparameters until drift metrics disappear; Use a registry stage transition as the drift detector; Delete historical predictions so the dashboard only shows current rows | [Databricks Lakehouse Monitoring](https://docs.databricks.com/en/lakehouse-monitoring/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q36 | solution-monitoring | quality metrics over labels | Coherent practitioner scenario for loan_approval. | a failed quality gate stops promotion automatically. | Join predictions with later labels and monitor model-quality metrics over time | Monitor only endpoint CPU because labels are delayed; Use feature table freshness as the accuracy metric; Treat successful HTTP responses as proof of model quality | [Databricks Lakehouse Monitoring](https://docs.databricks.com/en/lakehouse-monitoring/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q37 | solution-monitoring | serving metrics | Coherent practitioner scenario for pricing_model. | historical data is not allowed to leak future information. | Inspect endpoint request, latency, and error metrics for the served model | Search MLflow parameters for HTTP status codes; Use AutoML trial charts as live endpoint telemetry; Assume low training loss means the endpoint is healthy | [Databricks Model Serving](https://docs.databricks.com/en/machine-learning/model-serving/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q38 | experimentation | dataset logging | Coherent practitioner scenario for fraud_scores. | a failed quality gate stops promotion automatically. | Log the dataset or dataset metadata with the run so the training data is auditable | Only log the final accuracy metric; Record the dataset path in a chat message; Use the model alias as the dataset version | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q39 | feature-store | online and offline feature reuse | Coherent practitioner scenario for product_recs. | historical data is not allowed to leak future information. | Publish feature definitions for offline training and low-latency online lookup when needed | Maintain separate SQL definitions for training and serving; Use request-time features for historical training rows; Bypass feature lookup during training and add it only during serving | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q40 | model-lifecycle | evaluation comparison | Coherent practitioner scenario for support_triage. | a failed quality gate stops promotion automatically. | Compare runs by validation metrics and logged parameters before selecting a candidate | Promote the run with the shortest name; Select the model version with the largest artifact directory; Choose the candidate that was trained most recently regardless of metrics | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q41 | experimentation | MLflow run context managers | Coherent practitioner scenario for customer_churn. | the deployment must be governed before production traffic changes. | Use `with mlflow.start_run():` around training and logging so the run closes on exit | Call `mlflow.start_run()` once at cluster startup and reuse that run for every model; Write metrics to a Delta table only and skip MLflow tracking; Create a registry entry before any experiment run exists | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q42 | experimentation | artifact logging | Coherent practitioner scenario for claims_risk. | operational metrics show whether the service is healthy. | Save the file locally and call `mlflow.log_artifact()` or `log_artifacts()` for the run | Store the file contents in `log_param` so it appears beside hyperparameters; Log the DataFrame as a numeric metric because metrics are searchable; Attach the file to the registered model without linking it to the run | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q43 | experimentation | nested runs | Coherent practitioner scenario for inventory_signals. | the deployment must be governed before production traffic changes. | Start a parent run for the search and nested child runs for each trial | Overwrite the same run for every trial to keep the UI short; Register every trial as a production model version; Disable metric logging until the winning trial is known | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q44 | experimentation | Hyperopt SparkTrials | Coherent practitioner scenario for delivery_eta. | operational metrics show whether the service is healthy. | Use Hyperopt with `SparkTrials` so trials execute in parallel on the cluster | Use a single-driver Python loop for all parameter combinations; Create one all-purpose job cluster for manual notebook reruns only; Tune by changing parameters after the model is registered | [Hyperopt on Databricks](https://docs.databricks.com/en/machine-learning/automl-hyperparam-tuning/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q45 | experimentation | AutoML baseline | Coherent practitioner scenario for demand_forecast. | the deployment must be governed before production traffic changes. | Run Databricks AutoML to generate candidate notebooks and baseline models | Use Model Serving because serving endpoints generate training notebooks; Use Lakehouse Monitoring because monitors train candidate models; Use Feature Store alone because it selects algorithms automatically | [Databricks AutoML](https://docs.databricks.com/en/machine-learning/automl/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q46 | feature-store | point-in-time feature lookup | Coherent practitioner scenario for loan_approval. | operational metrics show whether the service is healthy. | Create a training set with feature lookups keyed by entity and timestamp to prevent leakage | Join to the latest feature table values regardless of event time; Export online-store values and backfill labels from production predictions; Drop the timestamp column so all rows use a single feature snapshot | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q47 | feature-store | feature table lineage | Coherent practitioner scenario for pricing_model. | the deployment must be governed before production traffic changes. | Log the model with Feature Engineering metadata so batch and serving inference can retrieve the same features | Copy feature values into model parameters at training time; Recompute features manually in every scoring notebook; Store feature definitions only in a wiki page | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q48 | model-lifecycle | model signature | Coherent practitioner scenario for fraud_scores. | operational metrics show whether the service is healthy. | Infer and log a model signature plus an input example with the MLflow model | Rely on notebook comments to describe the input columns; Register the model without schema so any payload is accepted; Put schema details only in the endpoint description | [MLflow Models](https://mlflow.org/docs/latest/ml/model/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q49 | model-lifecycle | model registry approval | Coherent practitioner scenario for product_recs. | the deployment must be governed before production traffic changes. | Register the model version and move it through the configured review or alias workflow after evaluation | Deploy directly from an untracked local path; Use the experiment name as the production approval state; Archive the prior version before evaluating the candidate | [MLflow Model Registry](https://mlflow.org/docs/latest/ml/model-registry/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q50 | model-lifecycle | copy model versions | Coherent practitioner scenario for support_triage. | operational metrics show whether the service is healthy. | Copy the registered model version to the target workspace or registry location with its metadata | Retrain from scratch in the target workspace without preserving source lineage; Download the pickle file and email it to the receiving team; Create a serving endpoint in the source workspace and call it a transfer | [MLflow Model Registry](https://mlflow.org/docs/latest/ml/model-registry/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q51 | model-deployment | real-time serving endpoint | Coherent practitioner scenario for customer_churn. | the deployment must be governed before production traffic changes. | Deploy the registered model to a Model Serving endpoint for low-latency REST inference | Run a scheduled batch job and ask the application to poll the output table; Use an experiment comparison view as the production API; Store predictions in a static CSV file each morning | [Databricks Model Serving](https://docs.databricks.com/en/machine-learning/model-serving/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q52 | model-deployment | batch inference | Coherent practitioner scenario for claims_risk. | operational metrics show whether the service is healthy. | Use distributed batch inference over the input table and write scored results to Delta | Keep a real-time endpoint warm only for the monthly scoring job; Send millions of rows through a notebook widget one at a time; Use the model registry approval page to generate predictions | [Batch inference with MLflow models](https://docs.databricks.com/en/machine-learning/model-inference/batch-inference.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q53 | model-deployment | traffic split | Coherent practitioner scenario for inventory_signals. | the deployment must be governed before production traffic changes. | Configure endpoint traffic routes so a small percentage reaches the candidate served model | Replace the production model immediately for all requests; Create a second endpoint and ask clients to randomly choose one; Use an offline validation metric as the traffic router | [Model Serving traffic configuration](https://docs.databricks.com/en/machine-learning/model-serving/manage-serving-endpoints.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q54 | model-deployment | environment dependencies | Coherent practitioner scenario for delivery_eta. | operational metrics show whether the service is healthy. | Log the model with its conda or pip environment so serving can reproduce dependencies | Install packages manually on the reviewer laptop; Depend on whatever library versions happen to be on the serving cluster; Store package names in a free-text model description only | [MLflow Models](https://mlflow.org/docs/latest/ml/model/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q55 | solution-monitoring | data drift monitor | Coherent practitioner scenario for demand_forecast. | the deployment must be governed before production traffic changes. | Create a Lakehouse Monitor over the inference or feature table with baseline comparisons | Tune hyperparameters until drift metrics disappear; Use a registry stage transition as the drift detector; Delete historical predictions so the dashboard only shows current rows | [Databricks Lakehouse Monitoring](https://docs.databricks.com/en/lakehouse-monitoring/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q56 | solution-monitoring | quality metrics over labels | Coherent practitioner scenario for loan_approval. | operational metrics show whether the service is healthy. | Join predictions with later labels and monitor model-quality metrics over time | Monitor only endpoint CPU because labels are delayed; Use feature table freshness as the accuracy metric; Treat successful HTTP responses as proof of model quality | [Databricks Lakehouse Monitoring](https://docs.databricks.com/en/lakehouse-monitoring/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q57 | solution-monitoring | serving metrics | Coherent practitioner scenario for pricing_model. | the deployment must be governed before production traffic changes. | Inspect endpoint request, latency, and error metrics for the served model | Search MLflow parameters for HTTP status codes; Use AutoML trial charts as live endpoint telemetry; Assume low training loss means the endpoint is healthy | [Databricks Model Serving](https://docs.databricks.com/en/machine-learning/model-serving/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q58 | experimentation | dataset logging | Coherent practitioner scenario for fraud_scores. | operational metrics show whether the service is healthy. | Log the dataset or dataset metadata with the run so the training data is auditable | Only log the final accuracy metric; Record the dataset path in a chat message; Use the model alias as the dataset version | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
| q59 | feature-store | online and offline feature reuse | Coherent practitioner scenario for product_recs. | the deployment must be governed before production traffic changes. | Publish feature definitions for offline training and low-latency online lookup when needed | Maintain separate SQL definitions for training and serving; Use request-time features for historical training rows; Bypass feature lookup during training and add it only during serving | [Databricks feature engineering](https://docs.databricks.com/en/machine-learning/feature-store/index.html) | Distinct asset, constraint, or platform decision from earlier rows. |
| q60 | model-lifecycle | evaluation comparison | Coherent practitioner scenario for support_triage. | operational metrics show whether the service is healthy. | Compare runs by validation metrics and logged parameters before selecting a candidate | Promote the run with the shortest name; Select the model version with the largest artifact directory; Choose the candidate that was trained most recently regardless of metrics | [MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/) | Distinct asset, constraint, or platform decision from earlier rows. |
