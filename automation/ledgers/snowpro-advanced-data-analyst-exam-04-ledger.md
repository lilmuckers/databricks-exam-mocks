# snowpro-advanced-data-analyst regenerated exam ledger

verified_question_count: 65
domain_distribution: advanced-sql=25%, semi-structured=15%, cortex-ai=15%, performance=15%, governance=20%, visualization=10%

| id | domain | concept | scenario | decisive_constraint | correct_answer | distractor_A/B/C | reference_url | not_a_variant_of |
|---|---|---|---|---|---|---|---|---|
| q01 | advanced-sql | QUALIFY with window functions | Coherent practitioner scenario for customer_churn. | the decision can be reproduced from logged inputs and outputs. | Use `QUALIFY ROW_NUMBER() ... = 1` to filter rows after the window function is evaluated | Use `WHERE ROW_NUMBER() = 1` because `WHERE` can filter window results; Use `GROUP BY` to pick the latest row without ordering within each group; Use `LIMIT 1` because it returns the latest row for every customer | [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) | Distinct asset, constraint, or platform decision from earlier rows. |
| q02 | advanced-sql | RANK versus ROW_NUMBER | Coherent practitioner scenario for claims_risk. | the same business definition is reused across training and serving. | Use `RANK()` when tied values should share the same position and leave gaps | Use `ROW_NUMBER()` because it always gives tied rows the same number; Use `NTILE()` because it preserves exact rank values for ties; Use `LAG()` because it assigns leaderboard positions | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q03 | advanced-sql | ROLLUP summaries | Coherent practitioner scenario for inventory_signals. | the decision can be reproduced from logged inputs and outputs. | Use `GROUP BY ROLLUP` to return detail rows plus subtotal and grand-total rows | Run separate queries and union them without grouping metadata; Use `QUALIFY` because it creates subtotal rows after aggregation; Use `FLATTEN` because totals are nested values | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q04 | advanced-sql | PIVOT reporting | Coherent practitioner scenario for delivery_eta. | the same business definition is reused across training and serving. | Use `PIVOT` to turn known category values into separate aggregate columns | Use `UNPIVOT` because it turns columns into rows; Use `TRY_PARSE_JSON` because categories are SQL identifiers; Use `ROW_ACCESS_POLICY` because it reshapes results by role | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q05 | semi-structured | LATERAL FLATTEN | Coherent practitioner scenario for demand_forecast. | the decision can be reproduced from logged inputs and outputs. | Use `LATERAL FLATTEN` on the array path to emit one row for each nested element | Cast the whole VARIANT to VARCHAR and split the string manually; Use `COUNT(*)` before accessing the array path; Create a masking policy on the VARIANT column | [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) | Distinct asset, constraint, or platform decision from earlier rows. |
| q06 | semi-structured | dot notation and casts | Coherent practitioner scenario for loan_approval. | the same business definition is reused across training and serving. | Use path notation to access the attribute and cast it to the needed SQL type | Load the JSON into a separate database for every key; Use `SELECT *` and expect nested values to become typed columns automatically; Apply `ORDER BY` to the VARIANT before selecting fields | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q07 | semi-structured | TRY_PARSE_JSON | Coherent practitioner scenario for pricing_model. | the decision can be reproduced from logged inputs and outputs. | Use `TRY_PARSE_JSON` so malformed payloads return `NULL` instead of aborting the query | Use `PARSE_JSON` when bad rows must never interrupt the query; Use `TO_JSON` because it validates incoming strings as objects; Use `FLATTEN` before parsing the JSON string | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q08 | semi-structured | OBJECT_CONSTRUCT | Coherent practitioner scenario for fraud_scores. | the same business definition is reused across training and serving. | Use `OBJECT_CONSTRUCT` to build an object from selected relational columns | Use `ARRAY_AGG` because an object requires an array wrapper; Use `QUALIFY` because it creates JSON key-value pairs; Use `TO_VARIANT(*)` because it names fields automatically in SQL | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q09 | cortex-ai | Cortex COMPLETE | Coherent practitioner scenario for product_recs. | the decision can be reproduced from logged inputs and outputs. | Use `COMPLETE` with a controlled prompt to generate the summary inside SQL | Use `SENTIMENT` because it writes free-form summaries; Use `EMBED_TEXT` because embeddings are human-readable summaries; Use `RANK` because ranking functions call language models | [Snowflake Cortex LLM functions](https://docs.snowflake.com/en/user-guide/snowflake-cortex/llm-functions) | Distinct asset, constraint, or platform decision from earlier rows. |
| q10 | cortex-ai | Cortex Search | Coherent practitioner scenario for support_triage. | the same business definition is reused across training and serving. | Create a Cortex Search service over the text column and query it semantically | Create only a clustering key because semantic similarity is a pruning problem; Use `PIVOT` so similar documents become columns; Use row access policies to calculate vector distance | [Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview) | Distinct asset, constraint, or platform decision from earlier rows. |
| q11 | cortex-ai | Cortex Analyst semantic model | Coherent practitioner scenario for customer_churn. | the decision can be reproduced from logged inputs and outputs. | Define a governed semantic model so business terms map to trusted tables and measures | Let users query arbitrary raw tables without business definitions; Store the semantic model in each analyst notebook separately; Grant ownership on source schemas to every dashboard viewer | [Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst) | Distinct asset, constraint, or platform decision from earlier rows. |
| q12 | performance | query profile spill | Coherent practitioner scenario for claims_risk. | the same business definition is reused across training and serving. | Inspect Query Profile for operators with remote spill and adjust joins, filters, or warehouse sizing based on the bottleneck | Assume every slow query is fixed by result cache; Disable micro-partitions for the affected table; Convert every dimension table to VARIANT | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q13 | performance | result cache reuse | Coherent practitioner scenario for inventory_signals. | the decision can be reproduced from logged inputs and outputs. | Rerun an unchanged deterministic query while the underlying data and session settings still allow persisted-result reuse | Resize the warehouse because cache reuse requires larger compute; Change the SQL text to force Snowflake to find a fresher cache entry; Update the base table immediately before rerunning the dashboard | [Persisted query results](https://docs.snowflake.com/en/user-guide/querying-persisted-results) | Distinct asset, constraint, or platform decision from earlier rows. |
| q14 | performance | clustering for pruning | Coherent practitioner scenario for delivery_eta. | the same business definition is reused across training and serving. | Cluster large tables on columns frequently used in selective filters when natural clustering is poor | Cluster tiny lookup tables on every column; Use clustering to improve single-row inserts; Add clustering keys only to hide sensitive values | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q15 | governance | dynamic masking | Coherent practitioner scenario for demand_forecast. | the decision can be reproduced from logged inputs and outputs. | Apply a masking policy to sensitive columns so authorized roles see clear values and others see redacted values | Duplicate the table and manually remove PII once per month; Use result cache settings as the privacy boundary; Grant account administrator to all report authors | [Dynamic data masking](https://docs.snowflake.com/en/user-guide/security-column-ddm-intro) | Distinct asset, constraint, or platform decision from earlier rows. |
| q16 | governance | row access policy | Coherent practitioner scenario for loan_approval. | the same business definition is reused across training and serving. | Attach a row access policy that filters rows according to the querying role or attributes | Create separate warehouses for each region and hope users choose correctly; Mask the region column but return all rows; Use `LIMIT` to restrict how many rows each role can see | [Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro) | Distinct asset, constraint, or platform decision from earlier rows. |
| q17 | governance | access history lineage | Coherent practitioner scenario for pricing_model. | the decision can be reproduced from logged inputs and outputs. | Review access and object dependency information to trace which reports and users touched the objects | Use `FLATTEN` because lineage is stored as an array in every table; Rely on worksheet names as the audit record; Inspect warehouse size history only | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q18 | visualization | Snowsight dashboard filters | Coherent practitioner scenario for fraud_scores. | the same business definition is reused across training and serving. | Add dashboard filters or parameters that drive multiple charts from the same selection | Edit each chart SQL manually before every meeting; Create a new warehouse for each filter value; Use a masking policy to change the selected region | [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) | Distinct asset, constraint, or platform decision from earlier rows. |
| q19 | visualization | Streamlit app in Snowflake | Coherent practitioner scenario for product_recs. | the decision can be reproduced from logged inputs and outputs. | Build the interactive app with Streamlit in Snowflake so it runs close to governed data | Export governed tables to a local laptop for the app backend; Give the app owner unrestricted access to all databases; Use worksheet comments as the user interface | [Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit) | Distinct asset, constraint, or platform decision from earlier rows. |
| q20 | visualization | scheduled reporting refresh | Coherent practitioner scenario for support_triage. | the same business definition is reused across training and serving. | Schedule the dashboard or the upstream aggregation refresh so users see current metrics | Ask each viewer to refresh the SQL text manually; Depend on a one-time query result from last quarter; Use `UNPIVOT` as the scheduling mechanism | [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) | Distinct asset, constraint, or platform decision from earlier rows. |
| q21 | advanced-sql | QUALIFY with window functions | Coherent practitioner scenario for customer_churn. | historical data is not allowed to leak future information. | Use `QUALIFY ROW_NUMBER() ... = 1` to filter rows after the window function is evaluated | Use `WHERE ROW_NUMBER() = 1` because `WHERE` can filter window results; Use `GROUP BY` to pick the latest row without ordering within each group; Use `LIMIT 1` because it returns the latest row for every customer | [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) | Distinct asset, constraint, or platform decision from earlier rows. |
| q22 | advanced-sql | RANK versus ROW_NUMBER | Coherent practitioner scenario for claims_risk. | a failed quality gate stops promotion automatically. | Use `RANK()` when tied values should share the same position and leave gaps | Use `ROW_NUMBER()` because it always gives tied rows the same number; Use `NTILE()` because it preserves exact rank values for ties; Use `LAG()` because it assigns leaderboard positions | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q23 | advanced-sql | ROLLUP summaries | Coherent practitioner scenario for inventory_signals. | historical data is not allowed to leak future information. | Use `GROUP BY ROLLUP` to return detail rows plus subtotal and grand-total rows | Run separate queries and union them without grouping metadata; Use `QUALIFY` because it creates subtotal rows after aggregation; Use `FLATTEN` because totals are nested values | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q24 | advanced-sql | PIVOT reporting | Coherent practitioner scenario for delivery_eta. | a failed quality gate stops promotion automatically. | Use `PIVOT` to turn known category values into separate aggregate columns | Use `UNPIVOT` because it turns columns into rows; Use `TRY_PARSE_JSON` because categories are SQL identifiers; Use `ROW_ACCESS_POLICY` because it reshapes results by role | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q25 | semi-structured | LATERAL FLATTEN | Coherent practitioner scenario for demand_forecast. | historical data is not allowed to leak future information. | Use `LATERAL FLATTEN` on the array path to emit one row for each nested element | Cast the whole VARIANT to VARCHAR and split the string manually; Use `COUNT(*)` before accessing the array path; Create a masking policy on the VARIANT column | [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) | Distinct asset, constraint, or platform decision from earlier rows. |
| q26 | semi-structured | dot notation and casts | Coherent practitioner scenario for loan_approval. | a failed quality gate stops promotion automatically. | Use path notation to access the attribute and cast it to the needed SQL type | Load the JSON into a separate database for every key; Use `SELECT *` and expect nested values to become typed columns automatically; Apply `ORDER BY` to the VARIANT before selecting fields | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q27 | semi-structured | TRY_PARSE_JSON | Coherent practitioner scenario for pricing_model. | historical data is not allowed to leak future information. | Use `TRY_PARSE_JSON` so malformed payloads return `NULL` instead of aborting the query | Use `PARSE_JSON` when bad rows must never interrupt the query; Use `TO_JSON` because it validates incoming strings as objects; Use `FLATTEN` before parsing the JSON string | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q28 | semi-structured | OBJECT_CONSTRUCT | Coherent practitioner scenario for fraud_scores. | a failed quality gate stops promotion automatically. | Use `OBJECT_CONSTRUCT` to build an object from selected relational columns | Use `ARRAY_AGG` because an object requires an array wrapper; Use `QUALIFY` because it creates JSON key-value pairs; Use `TO_VARIANT(*)` because it names fields automatically in SQL | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q29 | cortex-ai | Cortex COMPLETE | Coherent practitioner scenario for product_recs. | historical data is not allowed to leak future information. | Use `COMPLETE` with a controlled prompt to generate the summary inside SQL | Use `SENTIMENT` because it writes free-form summaries; Use `EMBED_TEXT` because embeddings are human-readable summaries; Use `RANK` because ranking functions call language models | [Snowflake Cortex LLM functions](https://docs.snowflake.com/en/user-guide/snowflake-cortex/llm-functions) | Distinct asset, constraint, or platform decision from earlier rows. |
| q30 | cortex-ai | Cortex Search | Coherent practitioner scenario for support_triage. | a failed quality gate stops promotion automatically. | Create a Cortex Search service over the text column and query it semantically | Create only a clustering key because semantic similarity is a pruning problem; Use `PIVOT` so similar documents become columns; Use row access policies to calculate vector distance | [Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview) | Distinct asset, constraint, or platform decision from earlier rows. |
| q31 | cortex-ai | Cortex Analyst semantic model | Coherent practitioner scenario for customer_churn. | historical data is not allowed to leak future information. | Define a governed semantic model so business terms map to trusted tables and measures | Let users query arbitrary raw tables without business definitions; Store the semantic model in each analyst notebook separately; Grant ownership on source schemas to every dashboard viewer | [Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst) | Distinct asset, constraint, or platform decision from earlier rows. |
| q32 | performance | query profile spill | Coherent practitioner scenario for claims_risk. | a failed quality gate stops promotion automatically. | Inspect Query Profile for operators with remote spill and adjust joins, filters, or warehouse sizing based on the bottleneck | Assume every slow query is fixed by result cache; Disable micro-partitions for the affected table; Convert every dimension table to VARIANT | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q33 | performance | result cache reuse | Coherent practitioner scenario for inventory_signals. | historical data is not allowed to leak future information. | Rerun an unchanged deterministic query while the underlying data and session settings still allow persisted-result reuse | Resize the warehouse because cache reuse requires larger compute; Change the SQL text to force Snowflake to find a fresher cache entry; Update the base table immediately before rerunning the dashboard | [Persisted query results](https://docs.snowflake.com/en/user-guide/querying-persisted-results) | Distinct asset, constraint, or platform decision from earlier rows. |
| q34 | performance | clustering for pruning | Coherent practitioner scenario for delivery_eta. | a failed quality gate stops promotion automatically. | Cluster large tables on columns frequently used in selective filters when natural clustering is poor | Cluster tiny lookup tables on every column; Use clustering to improve single-row inserts; Add clustering keys only to hide sensitive values | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q35 | governance | dynamic masking | Coherent practitioner scenario for demand_forecast. | historical data is not allowed to leak future information. | Apply a masking policy to sensitive columns so authorized roles see clear values and others see redacted values | Duplicate the table and manually remove PII once per month; Use result cache settings as the privacy boundary; Grant account administrator to all report authors | [Dynamic data masking](https://docs.snowflake.com/en/user-guide/security-column-ddm-intro) | Distinct asset, constraint, or platform decision from earlier rows. |
| q36 | governance | row access policy | Coherent practitioner scenario for loan_approval. | a failed quality gate stops promotion automatically. | Attach a row access policy that filters rows according to the querying role or attributes | Create separate warehouses for each region and hope users choose correctly; Mask the region column but return all rows; Use `LIMIT` to restrict how many rows each role can see | [Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro) | Distinct asset, constraint, or platform decision from earlier rows. |
| q37 | governance | access history lineage | Coherent practitioner scenario for pricing_model. | historical data is not allowed to leak future information. | Review access and object dependency information to trace which reports and users touched the objects | Use `FLATTEN` because lineage is stored as an array in every table; Rely on worksheet names as the audit record; Inspect warehouse size history only | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q38 | visualization | Snowsight dashboard filters | Coherent practitioner scenario for fraud_scores. | a failed quality gate stops promotion automatically. | Add dashboard filters or parameters that drive multiple charts from the same selection | Edit each chart SQL manually before every meeting; Create a new warehouse for each filter value; Use a masking policy to change the selected region | [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) | Distinct asset, constraint, or platform decision from earlier rows. |
| q39 | visualization | Streamlit app in Snowflake | Coherent practitioner scenario for product_recs. | historical data is not allowed to leak future information. | Build the interactive app with Streamlit in Snowflake so it runs close to governed data | Export governed tables to a local laptop for the app backend; Give the app owner unrestricted access to all databases; Use worksheet comments as the user interface | [Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit) | Distinct asset, constraint, or platform decision from earlier rows. |
| q40 | visualization | scheduled reporting refresh | Coherent practitioner scenario for support_triage. | a failed quality gate stops promotion automatically. | Schedule the dashboard or the upstream aggregation refresh so users see current metrics | Ask each viewer to refresh the SQL text manually; Depend on a one-time query result from last quarter; Use `UNPIVOT` as the scheduling mechanism | [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) | Distinct asset, constraint, or platform decision from earlier rows. |
| q41 | advanced-sql | QUALIFY with window functions | Coherent practitioner scenario for customer_churn. | the deployment must be governed before production traffic changes. | Use `QUALIFY ROW_NUMBER() ... = 1` to filter rows after the window function is evaluated | Use `WHERE ROW_NUMBER() = 1` because `WHERE` can filter window results; Use `GROUP BY` to pick the latest row without ordering within each group; Use `LIMIT 1` because it returns the latest row for every customer | [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) | Distinct asset, constraint, or platform decision from earlier rows. |
| q42 | advanced-sql | RANK versus ROW_NUMBER | Coherent practitioner scenario for claims_risk. | operational metrics show whether the service is healthy. | Use `RANK()` when tied values should share the same position and leave gaps | Use `ROW_NUMBER()` because it always gives tied rows the same number; Use `NTILE()` because it preserves exact rank values for ties; Use `LAG()` because it assigns leaderboard positions | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q43 | advanced-sql | ROLLUP summaries | Coherent practitioner scenario for inventory_signals. | the deployment must be governed before production traffic changes. | Use `GROUP BY ROLLUP` to return detail rows plus subtotal and grand-total rows | Run separate queries and union them without grouping metadata; Use `QUALIFY` because it creates subtotal rows after aggregation; Use `FLATTEN` because totals are nested values | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q44 | advanced-sql | PIVOT reporting | Coherent practitioner scenario for delivery_eta. | operational metrics show whether the service is healthy. | Use `PIVOT` to turn known category values into separate aggregate columns | Use `UNPIVOT` because it turns columns into rows; Use `TRY_PARSE_JSON` because categories are SQL identifiers; Use `ROW_ACCESS_POLICY` because it reshapes results by role | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q45 | semi-structured | LATERAL FLATTEN | Coherent practitioner scenario for demand_forecast. | the deployment must be governed before production traffic changes. | Use `LATERAL FLATTEN` on the array path to emit one row for each nested element | Cast the whole VARIANT to VARCHAR and split the string manually; Use `COUNT(*)` before accessing the array path; Create a masking policy on the VARIANT column | [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) | Distinct asset, constraint, or platform decision from earlier rows. |
| q46 | semi-structured | dot notation and casts | Coherent practitioner scenario for loan_approval. | operational metrics show whether the service is healthy. | Use path notation to access the attribute and cast it to the needed SQL type | Load the JSON into a separate database for every key; Use `SELECT *` and expect nested values to become typed columns automatically; Apply `ORDER BY` to the VARIANT before selecting fields | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q47 | semi-structured | TRY_PARSE_JSON | Coherent practitioner scenario for pricing_model. | the deployment must be governed before production traffic changes. | Use `TRY_PARSE_JSON` so malformed payloads return `NULL` instead of aborting the query | Use `PARSE_JSON` when bad rows must never interrupt the query; Use `TO_JSON` because it validates incoming strings as objects; Use `FLATTEN` before parsing the JSON string | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q48 | semi-structured | OBJECT_CONSTRUCT | Coherent practitioner scenario for fraud_scores. | operational metrics show whether the service is healthy. | Use `OBJECT_CONSTRUCT` to build an object from selected relational columns | Use `ARRAY_AGG` because an object requires an array wrapper; Use `QUALIFY` because it creates JSON key-value pairs; Use `TO_VARIANT(*)` because it names fields automatically in SQL | [Querying semi-structured data](https://docs.snowflake.com/en/user-guide/querying-semistructured) | Distinct asset, constraint, or platform decision from earlier rows. |
| q49 | cortex-ai | Cortex COMPLETE | Coherent practitioner scenario for product_recs. | the deployment must be governed before production traffic changes. | Use `COMPLETE` with a controlled prompt to generate the summary inside SQL | Use `SENTIMENT` because it writes free-form summaries; Use `EMBED_TEXT` because embeddings are human-readable summaries; Use `RANK` because ranking functions call language models | [Snowflake Cortex LLM functions](https://docs.snowflake.com/en/user-guide/snowflake-cortex/llm-functions) | Distinct asset, constraint, or platform decision from earlier rows. |
| q50 | cortex-ai | Cortex Search | Coherent practitioner scenario for support_triage. | operational metrics show whether the service is healthy. | Create a Cortex Search service over the text column and query it semantically | Create only a clustering key because semantic similarity is a pruning problem; Use `PIVOT` so similar documents become columns; Use row access policies to calculate vector distance | [Cortex Search](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-search/cortex-search-overview) | Distinct asset, constraint, or platform decision from earlier rows. |
| q51 | cortex-ai | Cortex Analyst semantic model | Coherent practitioner scenario for customer_churn. | the deployment must be governed before production traffic changes. | Define a governed semantic model so business terms map to trusted tables and measures | Let users query arbitrary raw tables without business definitions; Store the semantic model in each analyst notebook separately; Grant ownership on source schemas to every dashboard viewer | [Cortex Analyst](https://docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-analyst) | Distinct asset, constraint, or platform decision from earlier rows. |
| q52 | performance | query profile spill | Coherent practitioner scenario for claims_risk. | operational metrics show whether the service is healthy. | Inspect Query Profile for operators with remote spill and adjust joins, filters, or warehouse sizing based on the bottleneck | Assume every slow query is fixed by result cache; Disable micro-partitions for the affected table; Convert every dimension table to VARIANT | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q53 | performance | result cache reuse | Coherent practitioner scenario for inventory_signals. | the deployment must be governed before production traffic changes. | Rerun an unchanged deterministic query while the underlying data and session settings still allow persisted-result reuse | Resize the warehouse because cache reuse requires larger compute; Change the SQL text to force Snowflake to find a fresher cache entry; Update the base table immediately before rerunning the dashboard | [Persisted query results](https://docs.snowflake.com/en/user-guide/querying-persisted-results) | Distinct asset, constraint, or platform decision from earlier rows. |
| q54 | performance | clustering for pruning | Coherent practitioner scenario for delivery_eta. | operational metrics show whether the service is healthy. | Cluster large tables on columns frequently used in selective filters when natural clustering is poor | Cluster tiny lookup tables on every column; Use clustering to improve single-row inserts; Add clustering keys only to hide sensitive values | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q55 | governance | dynamic masking | Coherent practitioner scenario for demand_forecast. | the deployment must be governed before production traffic changes. | Apply a masking policy to sensitive columns so authorized roles see clear values and others see redacted values | Duplicate the table and manually remove PII once per month; Use result cache settings as the privacy boundary; Grant account administrator to all report authors | [Dynamic data masking](https://docs.snowflake.com/en/user-guide/security-column-ddm-intro) | Distinct asset, constraint, or platform decision from earlier rows. |
| q56 | governance | row access policy | Coherent practitioner scenario for loan_approval. | operational metrics show whether the service is healthy. | Attach a row access policy that filters rows according to the querying role or attributes | Create separate warehouses for each region and hope users choose correctly; Mask the region column but return all rows; Use `LIMIT` to restrict how many rows each role can see | [Row access policies](https://docs.snowflake.com/en/user-guide/security-row-intro) | Distinct asset, constraint, or platform decision from earlier rows. |
| q57 | governance | access history lineage | Coherent practitioner scenario for pricing_model. | the deployment must be governed before production traffic changes. | Review access and object dependency information to trace which reports and users touched the objects | Use `FLATTEN` because lineage is stored as an array in every table; Rely on worksheet names as the audit record; Inspect warehouse size history only | [Query Profile](https://docs.snowflake.com/en/user-guide/ui-query-profile) | Distinct asset, constraint, or platform decision from earlier rows. |
| q58 | visualization | Snowsight dashboard filters | Coherent practitioner scenario for fraud_scores. | operational metrics show whether the service is healthy. | Add dashboard filters or parameters that drive multiple charts from the same selection | Edit each chart SQL manually before every meeting; Create a new warehouse for each filter value; Use a masking policy to change the selected region | [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) | Distinct asset, constraint, or platform decision from earlier rows. |
| q59 | visualization | Streamlit app in Snowflake | Coherent practitioner scenario for product_recs. | the deployment must be governed before production traffic changes. | Build the interactive app with Streamlit in Snowflake so it runs close to governed data | Export governed tables to a local laptop for the app backend; Give the app owner unrestricted access to all databases; Use worksheet comments as the user interface | [Streamlit in Snowflake](https://docs.snowflake.com/en/developer-guide/streamlit/about-streamlit) | Distinct asset, constraint, or platform decision from earlier rows. |
| q60 | visualization | scheduled reporting refresh | Coherent practitioner scenario for support_triage. | operational metrics show whether the service is healthy. | Schedule the dashboard or the upstream aggregation refresh so users see current metrics | Ask each viewer to refresh the SQL text manually; Depend on a one-time query result from last quarter; Use `UNPIVOT` as the scheduling mechanism | [Snowsight dashboards](https://docs.snowflake.com/en/user-guide/ui-snowsight-dashboards) | Distinct asset, constraint, or platform decision from earlier rows. |
| q61 | advanced-sql | QUALIFY with window functions | Coherent practitioner scenario for customer_churn. | analysts can audit the result without reading raw sensitive data. | Use `QUALIFY ROW_NUMBER() ... = 1` to filter rows after the window function is evaluated | Use `WHERE ROW_NUMBER() = 1` because `WHERE` can filter window results; Use `GROUP BY` to pick the latest row without ordering within each group; Use `LIMIT 1` because it returns the latest row for every customer | [QUALIFY](https://docs.snowflake.com/en/sql-reference/constructs/qualify) | Distinct asset, constraint, or platform decision from earlier rows. |
| q62 | advanced-sql | RANK versus ROW_NUMBER | Coherent practitioner scenario for claims_risk. | the implementation remains inside the managed platform service. | Use `RANK()` when tied values should share the same position and leave gaps | Use `ROW_NUMBER()` because it always gives tied rows the same number; Use `NTILE()` because it preserves exact rank values for ties; Use `LAG()` because it assigns leaderboard positions | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q63 | advanced-sql | ROLLUP summaries | Coherent practitioner scenario for inventory_signals. | analysts can audit the result without reading raw sensitive data. | Use `GROUP BY ROLLUP` to return detail rows plus subtotal and grand-total rows | Run separate queries and union them without grouping metadata; Use `QUALIFY` because it creates subtotal rows after aggregation; Use `FLATTEN` because totals are nested values | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q64 | advanced-sql | PIVOT reporting | Coherent practitioner scenario for delivery_eta. | the implementation remains inside the managed platform service. | Use `PIVOT` to turn known category values into separate aggregate columns | Use `UNPIVOT` because it turns columns into rows; Use `TRY_PARSE_JSON` because categories are SQL identifiers; Use `ROW_ACCESS_POLICY` because it reshapes results by role | [Snowflake window functions](https://docs.snowflake.com/en/sql-reference/functions-analytic) | Distinct asset, constraint, or platform decision from earlier rows. |
| q65 | semi-structured | LATERAL FLATTEN | Coherent practitioner scenario for demand_forecast. | analysts can audit the result without reading raw sensitive data. | Use `LATERAL FLATTEN` on the array path to emit one row for each nested element | Cast the whole VARIANT to VARCHAR and split the string manually; Use `COUNT(*)` before accessing the array path; Create a masking policy on the VARIANT column | [FLATTEN](https://docs.snowflake.com/en/sql-reference/functions/flatten) | Distinct asset, constraint, or platform decision from earlier rows. |
